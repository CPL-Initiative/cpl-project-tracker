---
title: cpl-project-tracker docs — Index
created: 2026-05-27
updated: 2026-09-07
tags: [meta, index, obsidian-target]
kb-status: internal
obsidian-folder: cpl-project-tracker
related:
  - "[[CLAUDE]]"
  - "[[docs/kb-notes/README]]"
---

# cpl-project-tracker — Docs Index

Landing page for the project's documentation surface, intended as the
**Obsidian vault entry-point** when browsing this repo from the vault-side clone
at `CPLBrain/COG-second-brain/cpl-project-tracker/`.

The per-lane catalogs below are **generated** by `kb/_build_docs_index.py` from
each doc's own frontmatter — rebuild rather than hand-append. The prose on this
page is hand-written and is preserved across rebuilds; only the block between
the `generated:corpus` markers is replaced.

> Previously this page listed every document inline and reached **273,616 B,
> 6.8× the 40,000 B index budget** — a landing page you must scroll is not a
> landing page. The catalogs are where the full listings live now.

## The three lanes

| Lane | What | Where |
|---|---|---|
| **KB notes** | Durable, distilled, reusable knowledge | [`docs/kb-notes/`](kb-notes/) |
| **Lessons (WIP)** | Workstream scratchpads, append-only | `docs/<workstream>_lessons.md` |
| **Session handoffs** | "Fattyfat" capsules for the next session | `docs/session_<N>_handoff.md` |

See [`docs/kb-notes/README.md`](kb-notes/README.md) for the lane contract.

## CLAUDE.md reference offloads (`docs/reference/`) — added 2026-07-10 (Session 111, the pare-down)

Always-current project memory moved out of `CLAUDE.md` (2,514 → ~590 lines);
**Rule 9 checkpoints update these files now**, and `CLAUDE.md` keeps read-before
stubs pointing here.

| Doc | Was | Read before |
|---|---|---|
| [Pipeline Reference](reference/pipeline_reference.md) | `CLAUDE.md` §Pipeline Reference (1,087 lines) | generator/workflow/tabs/Supabase/EACR/C-ID work |
| [KB Build Status](reference/kb_build_status.md) | `CLAUDE.md` §KB & Unified Courses (421 lines) | KB/CCR curation work, build-phase history |
| [M-ID Lifecycle & CID/CIDx](reference/mid_lifecycle.md) | `CLAUDE.md` §11 prose + strategic roadmap (449 lines) | re-mints, MC/TMC calls, auditor, pathway decisions |
| [Branch policy](reference/branch_policy.md) | `CLAUDE.md` §Branch policy evidence (2026-08-28) | why a merge rule says what it says |
| [Engineering & UI practices](reference/engineering_ui_practices.md) | `CLAUDE.md` §Engineering & UI evidence (2026-08-28) | a UI rework, a First Light artifact, a table layout |
| [Obsidian vault wiring](reference/obsidian_vault_wiring.md) | `CLAUDE.md` §Obsidian vault wiring (2026-08-28) | vault-sync, exclusion, the sparse-checkout fix |
| [**`reference/lanes/` — one file per §11 roadmap lane**](catalog/reference.md) | `CLAUDE.md` §11 roadmap cells, 88 KB (2026-08-28) | **working any lane — and REFRESHING it at checkpoint** |

⚠️ **The lane files are the usual checkpoint edit now.** §11's table is a
pointer index: it carries each lane's state, and the lane file carries what you
learned. A checkpoint that updates only the row leaves 30 files to go stale.

---

## The catalogs

Every document in `docs/`, by lane. Rebuild with `python3 kb/_build_docs_index.py`
(`--check` fails when a rebuild would change anything, so CI catches a stale index).

<!-- generated:corpus -->
| Lane | Docs | Catalog |
|---|---:|---|
| Doctrine (behavior-shaping) | 4 | [`catalog/doctrine.md`](catalog/doctrine.md) |
| KB notes | 405 | [`catalog/kb-notes.md`](catalog/kb-notes.md) |
| Lessons docs | 76 | [`catalog/lessons.md`](catalog/lessons.md) |
| Workstream docs | 80 | [`catalog/workstream-docs.md`](catalog/workstream-docs.md) |
| Reference (pull-side) | 42 | [`catalog/reference.md`](catalog/reference.md) |
| Session handoffs | 216 | [`catalog/session-handoffs.md`](catalog/session-handoffs.md) |
| **total** | **823** | |
<!-- /generated:corpus -->

Not covered by a lane catalog:

- [`visuals/README.md`](visuals/README.md) — **Visuals**: decision briefs and
  mock-ups worth returning to, committed as HTML with a dated filename.
- [`catalog/`](catalog/) — the generated catalogs themselves.

## Top-level orientation docs

- [`../CLAUDE.md`](../CLAUDE.md) — project memory, Critical Rules, M-ID lifecycle (§11)
- [`co_platform_strategy.md`](co_platform_strategy.md) — **the long-term "plan of attack"** (Session 83): scaling COBI + the CPL KB into a governed, team-based, CO-wide platform — operating model, account migration off personal logins, knowledge lanes, real APIs vs scraping, governance/security/accessibility/HUMANS, decisions only humans make, pushback, and a scorecard against all ~14 asks
- [`roadmap_archive.md`](roadmap_archive.md) — museum annex to CLAUDE.md: completed roadmap rows + Session 26-31 narratives (moved out Session 33 to keep CLAUDE.md to live, steering content)
- [`../README.md`](../README.md) — first-time visitor entry
- [`../kb/README.md`](../kb/README.md) — knowledge-base schemas + generators

## Sierra integration docs (vendor-facing, added 2026-07-02)

Commissioned by Sam for integrating Sierra into a vendor-built platform:

- [`sierra_technical_reference.md`](sierra_technical_reference.md) — how
  Sierra is built: architecture, the full `cpl-chat` API contract (request /
  SSE protocol / errors), the six-lookup answer pipeline, behavior rules,
  data layer, client surfaces, security model, ops, v13→v26 timeline.
- [`sierra_integration_analysis.md`](sierra_integration_analysis.md) —
  benefits / risks / challenges of embedding Sierra on another site, the
  pre-launch preconditions checklist, and the decision points for Sam.
- [`sierra_integration_guide.md`](sierra_integration_guide.md) — the vendor
  implementation plan: link / iframe / native-API / server-proxy paths, a
  minimal reference client, non-negotiable client requirements, launch
  checklist, ongoing-operations expectations.
- [`sierra_iframe_implementation_guide.md`](sierra_iframe_implementation_guide.md)
  — the **day-one iframe recipe** (2026-07-03): the exact `?ctx=external`
  URL, annotated markup/CSP/sandbox, sizing, QA checklist (incl. the
  contacts-gate check), launch coordination, rollback.
- [`sierra_maturity_roadmap.md`](sierra_maturity_roadmap.md) — **Malone's
  scope-and-sequence to end-state** (2026-07-03): Phases 1–6 (guardrails →
  contract hardening → content maturity → integration graduation →
  recommender depth → platform ops), efforts, dependencies, the three
  human-only decisions, the critical path. The iframe is explicitly interim.
- [`sierra_vendor_lane_handoff.md`](sierra_vendor_lane_handoff.md) — the
  lane handoff (2026-07-03): what shipped (#654/#657 + v27 LIVE), locked
  decisions, verified access facts, the priority queue, safety rails.

## Reference materials

Authoritative external sources we've cached:
- [`reference/`](reference/) — ASCCC / COCI / CCN-CID source documents

## Update history
- **2026-09-08 (SkyTrue, S242 — the frame budget, and a bill that moved instead of leaving)** — Two levers on the Sky's frame, and Sam's question about the no-discipline pile answered mid-run. Back-to-back on the served page, same machine: a median frame **81 ms → 46 ms**, about **12.3 → 21.7 fps**. ⭐ **A memo keyed on a value that drifts is not a memo**: `textW` cached on `ctx.font` + the string, and an island label is sized off its drawn radius — `18.0263px`, `18.2506px`, `18.1185px` as the sky turns — so it never hit once, and each miss handed Chromium a font size it had to build (`measureText` **11.2%** of the profile against `textW`'s own 0.5%, on 15 calls a frame). ⚠️ **Fixing it moved the bill rather than removing it**: calls fell 15.4 → 0.17 a frame and **`strokeText` went 0.6% → 9.6%** — a font is built at its first USE, and with the measure no longer asking, the stroke was next in line. Only rounding the drawn size stopped it (`labelSize`, whole pixels with a **0.6px dead band**, because a bare round is a threshold on a drifting value — `NODE_ZOOM_KEEP` again) ([`methodology-fixing-a-cost-can-move-it-rather-than-remove-it`](kb-notes/methodology-fixing-a-cost-can-move-it-rather-than-remove-it.md)). ⭐ **An island on screen is not an island whose points are on screen**: S241 settled which islands the window shows, but a discipline wider than the window still spilled — **5,755 of 27,931 batched dots a frame (20.6%) lay wholly outside the canvas**; tested inside the fast branch only, where the dot has no halo, light or ring. ⚠️ **The first draft of the guard was a decoration** — both fixture islands sat at the label-size clamp and all three points near the middle, so it passed with every fix reverted; fixed with `__ccrTextStep(0)` and a point 4,000 units out, one check deleted as unfailable, and each remaining one verified by reverting its fix. ⚠️ The revert harness overwrote its own backup and tested a state missing the fix for two rounds — the suite caught it. **Sam's question**: `PSYC C1000` *does* carry Psychology — `kb/reference/coci_courses.json`, classified 2026-05-20 — while the payload says `disc:null`. Two minting paths, one inference pipeline: all five passes read `coci_minted_courses.json`, **19,568 records all M-ID**, and `excel_to_dashboard.py` reads only `description` from the C-ID/CCN reference. Blank rate **0.4% M-ID vs 47.7% official**; **199 of the 326 recoverable today** ([`methodology-a-discipline-can-exist-in-the-repo-and-never-reach-the-payload`](kb-notes/methodology-a-discipline-can-exist-in-the-repo-and-never-reach-the-payload.md)). 9 new checks, `npm run a11y skyview` 11/11.
- **2026-09-08 (SkyClear, S241 — the asteroid field, and a fix that made the picture worse)** — S240 shipped two flicker fixes and Sam still saw it; his own words found the cause: *"an enlarged grouping that is crossing over all the others — like a loose asteroid field spiraling around"* (#1516). ⭐ **The sky is stereographic, so scale grows as `sec²(ang/2)` off the view direction** — 1.3× at 60°, 4× at 120°, **131× at 170°** — and `projectDir` refuses only past 174.8°, so an island almost directly BEHIND the reader still projected, at a hundredfold scale. The screen cull was a bounding box built from `isl.r * k`, so that inflated radius covered the window and **the cull passed**: the island drew as a sprawl of its courses over every other discipline. ⚠️ **It was in S240's own output** — its probe logged Music at `k = 71.5` beside islands at 0.2–2.4 and read it as a normal cull. The fix is ANGULAR (`acos(cz) - S.th > angMax`): largest scale **88.4× → 1.5×**, frames with one over 8× **149/150 → 0/150**, and because **89 of 159 islands at the default zoom were drawn having never been visible**, **11.4 → 17.4 fps**. ⚠️ **S240's `dt` fix was right and made the picture worse and did not measure that**: time-true motion at 8 fps steps further than broken half-speed motion, 18.59 → 21.87 — a correctness fix and an appearance fix are different claims. The remainder is spread over 877 of 1008 cells (uniform motion plus 27,000 one-pixel stars aliasing), not a defect; a rate sweep that would have hidden it was measured and reverted. Sam's two asks settled by measurement: **188° across** (the widest where all 99 islands keep their stars; 240° drops four) and the **Map button** leaving the row — his second reversal of a committed ruling in two days, both named in the code and beside the new assertion. 309 suites, `npm run a11y skyview` 11/11.
- **2026-09-07 (SkyDome, S240 — three recordings, six bugs, and a profile that moved the lever)** — The first run driven entirely by **screen recordings**. Sam sent three, each a sentence long, and **not one named its own cause** (#1515). ⭐ **The flicker had two causes, and the two obvious suspects were both innocent** — rebuilding the page without the twinkle moved the per-frame difference 18.26 → 19.46, and without the 8-step star-alpha bucketing 18.26 → 18.20, while stopping the turn gave **exactly 0.000**. The real ones: `dt` was clamped at 0.1 s, *below* the measured 133 ms frame, so `sph.spin` advanced a **fixed 7.20e-3 rad while the interval swung 192–319 ms** — and the sky had been turning at **0.0393 rad/s against an intended 0.0720**, 55% speed, since it shipped; and `showNodes = k > NODE_ZOOM`, a flat-map rule where `k` is one global number, was being asked per island on the sphere where it is `sec²(ang/2)` × the center's — **18 of 159 islands within ±3% of the threshold**, 11 flipping inside 120 frames, each blinking a whole discipline's dot field while its disc and name stayed put. ⚠️ **A third variant looked like a 7× cure and was an artifact**: it had switched every dot off, and an empty canvas is very stable. The purple sky was `--sky-island-sel` #2E2A44 — a selection tint is a figure-ground relationship, and past the zoom where the disc's edge leaves the window there is no ground left. A carry released only on the canvas paths meant that after one move from the panel **every Drag button was a silent no-op for the rest of the session**. Sam's four asks shipped with them: the outline as a **sheet over the map** (his own better idea — nothing to restore because nothing is lost), a **Back** that parks and restores the camera, a sidebar that closes by dragging its border, COBI in the ⋮ menu, and *Move instead…* on a staged move — which **reversed an S239 test assertion**, changed with the old reasoning named in a comment. ⚠️ 309 green jsdom suites saw none of it; two needed Chromium and one needed `npm run a11y skyview`, which failed all 11 routes on a 5px overhang. ⭐ **S239's frame-budget lever was backwards** — the canvas is 5.9 ms of a 133 ms frame; the cost is `measureText` (12.3%) and per-point JS. 1 KB note ([`methodology-a-bug-report-is-evidence-not-diagnosis`](kb-notes/methodology-a-bug-report-is-evidence-not-diagnosis.md)).
- **2026-09-07 (SkyGlobe, S239, evening — the Sky goes in)** — Sam answered the sheet in one sitting (**eight of eight `yes`**, no notes; *"decisions done!"*) and the Sky shipped the same evening (#1514): `#skyview` opens the inside window onto the night sky, **Sky · Globe · Map** are three words in the row and three places to stand on ONE canvas, Night by default with Day keeping the rim, a slow turn that stops at the first touch (twinkle only while turning, none of either under reduced motion), drag and drop by angle, By kind with its two region names, silver M-IDs on every dark canvas, the prototype's controls as tabled. ⭐ **The sphere is the map through a projection, not a second renderer**: `prepSphere()` projects each island's center and Jacobian once a frame, `w2s()` is one multiply-add from it and `pick()` reads the same cache — so the S237 drop fixture, the label placer and the keyboard path passed on the curve unchanged ([`methodology-the-sphere-is-the-map-through-a-projection`](kb-notes/methodology-the-sphere-is-the-map-through-a-projection.md)). Measured on the served page: 117 → 42.5 ms a draw once the star pass was batched (13.5 fps in software rendering; his machine is the open question). Wrong on the way: the globe came out mirrored (the window's right is the globe's left), opening the Sky broke seven flat-map suites until the harness declared what opens (`CPL_SKYVIEW_OPENS`), and the last failing check reported a state bug that was a missing `clip()` stub. 50 new checks, eight SkyView suites green, `npm run a11y skyview` 11 of 11.
- **2026-09-07 (SkyGlobe, S239 — eight calls before the sky goes in)** — Sam had ruled the globe into SkyView; per his decision-sheet practice the port's design calls went on ONE sheet with reply chips before any code (`docs/visuals/2026-09-07-eight-calls-before-the-sky-goes-in.html`). Seven from the handoff and an eighth the measurements raised: **the M-ID color on the dark ground** — his *silver, like stars* (globe) and his *match our legend* violet (map) meet there, and **49,355 of 49,896 points (99%)** wear it. Measured, not quoted: the day sky's dots exist only by their rim (silver 1.24:1, the light 1.08:1); By kind keeps **29%** of five nearest committed neighbors, **49%** the ceiling; the kind reading rests on 24% of points; a Canvas 2D redraw of 49,896 points is **18–35 ms**, so the port needs no three.js. Shipped without his answers (#1513): **the sphere placement as a daily artifact** (`kb/_build_ccr_sky.py` → `prototype/ccr_sky.json`, fingerprinted so the 90-second relaxation runs only on a change and the check takes half a second; a side held across a threshold by a margin; the prototype's generator imports the relaxation and the globe page rebuilds byte for byte) and **the staged-to-move mark** (v4 item 7 — the governing review is fully shipped): on the model, every view asks it — *staged here — not saved* at the destination, a dashed ghost star and *Put back* at the origin, counts on labels, hovers and the outline's band. ⚠️ The docked panel narrows the canvas — measure a screen position after the open. 2 KB notes ([`methodology-a-staged-state-lives-on-the-model-and-every-view-asks-it`](kb-notes/methodology-a-staged-state-lives-on-the-model-and-every-view-asks-it.md), [`methodology-a-slow-build-fingerprints-its-inputs-so-the-check-stays-cheap`](kb-notes/methodology-a-slow-build-fingerprints-its-inputs-so-the-check-stays-cheap.md)).
- **2026-09-07 (SkyFacet III, S238 — the CPL face and the articulations light)** — Sam's eight rulings of the morning became a build. SkyView's control row has two new words next to Show: **Courses | CPL** names each point by its course or by the credential that reaches it — curated name, then the issuing agency and the training agency where they differ, then what it earns, then the colleges holding it — and **Articulations** lights what carries one and leaves the rest drawn as it is. A point nothing reaches is **unlabeled**; search switches to the credential vocabulary; `#skyview/cpl` is a link; the course outline of record gained its CPL layer with both agencies. The payload (`kb/_build_ccr_cpl.py` → `prototype/ccr_cpl.json`, on demand) joins the articulation crosswalk — the same join the `ar` badge counts, so the lit set and the face agree by construction — to the curated CER artifact for agencies and the statewide feed for the exhibit universe; the daily run rebuilds it and CI fails on a stale file. ⭐ **The ruled coverage line carried two wrong numbers**: 1,490 was the count of *identities* (the exhibits are 1,924) and 6,388 was the credit funnel, a universe sharing only **570** ids with what reaches the map — ACE-keyed against MAP-keyed. The surface computes **1,924 of 5,497 articulated exhibits** from the data, and the fixture's *4 of 777* makes a literal fail ([`methodology-a-coverage-line-takes-both-numbers-from-one-universe`](kb-notes/methodology-a-coverage-line-takes-both-numbers-from-one-universe.md)). ⚠️ The crosswalk's inlined issuer is a May snapshot, wrong on 1,743 of 4,592 records; 55 crosswalk exhibits are absent from today's feed and are flagged in words, never dropped. Verified in Chromium and by `npm run a11y skyview` (9 routes); 62 new checks, six mutations each caught. Afternoon: the globe prototype, three rounds on Sam's reactions (#1509–#1511), its generators in `prototype/globe/`, two KB notes (the hemisphere lesson, the switch-not-version method).
- **2026-09-07 (SkyFacet II, S237 — a rule that is right for reading is wrong for moving)** — Sam's three reports (#1505), all on the curation path. ⭐ **"Courses no longer responsive after 2nd drag and drop … no go" named the wrong variable**: repeating the gesture eight times in a row all landed, at his zoom and above it. What varies is the DESTINATION. `pick()` gives an open identity's member stars absolute priority — S236's measured fix, still right for READING — but the ring **spreads**, so with *Introduction to Welding* open at 296% **six identity circles inside the viewport sat under one of its own stars**; a drop on each of the first three resolved to the identity the course was already in and `applyMove()` refused it in a hint at the foot of the window. The verb decides the hit test: `pick(px,py,forDrop)` resolves circles only while carrying, and reading is untouched (24 of 24 drawn stars still open the college course). ⚠️ **A refusal nobody can see is a dead control** — the carry now rings its destination and names it under the pointer, and the receipt stopped pointing at a pane `body.u-solo` never paints. ⭐ **Duplicated skills, folded where the counting happens**: `olWords()` keeps a hyphen inside a token, so `flux-cored arc welding` and `flux cored arc welding` were two skills; over all 46,317 identities carrying a description, **209 rows differed by a hyphen and 835 by a plural, 762 identities (1.6%)** — none now. Folding the KEY rather than merging finished rows is what keeps the college count honest, and `sses` had to be in the `-es` family or *processes* stems to *processe*. A reviewer may now **add or remove a skill**, staged, with removals recorded rather than derived. ⚠️ Two defects found on the way, both worse than reported: an outline opened by its own `#outline/<id>` link carried **zero college courses** (`buildMemberIndex()` ran only inside the map view), and `npm run a11y`'s *"10 focusable with no ring"* was **a closed `<details>` still measuring** — Chromium hides its content with content-visibility, so the rect is real while `focus()` is a no-op. 1 KB note ([`methodology-a-rule-that-is-right-for-reading-can-be-wrong-for-writing`](kb-notes/methodology-a-rule-that-is-right-for-reading-can-be-wrong-for-writing.md)).
- **2026-09-07 (SkyFacet, S236 — a local session's deferred commit, landed)** — Sam ran a **local desktop session** against a screen recording, pushed `claude/video-project-2-frames`, and it sat with no PR while `main` moved two PRs ahead — so two runs had rewritten the **same search dropdown** independently from two different reviews of it. Merged as a synthesis (#1503): main's masthead fix (the branch would have put `prototype v1` back into the aria-label), `runSearch()` and `markSug()`'s id-addressing; the branch's whole **deferred-commit model** — ticking collects, Enter applies, Escape abandons, ticks survive refining the term. ⚠️ `skyview.html` is **generated**, not hand-maintained in three files as the branch's note claimed. ⭐ **THE OPEN BUG WAS NEITHER OF ITS NAMED CAUSES, AND IT WAS WORSE THAN REPORTED.** `.filter(Boolean)` drops nothing and `__ccrTokenKey` **is** `tokenFromSuggestion(s).key`, so the suspects could not disagree; one `console.error` showed both items present. Removals were **derived** from a once-per-session snapshot, so a token committed outside that seed read as an untick — **Enter destroyed an already-committed pick nobody had touched**. Intent is now recorded (`pendOff`/`pendItem`, one `pendingEdit()` feeding both the footer and the commit), guarded by a check that names the destroyed key rather than a count, mutation-tested ([`methodology-a-snapshot-cannot-be-the-authority-on-intent`](kb-notes/methodology-a-snapshot-cannot-be-the-authority-on-intent.md)). **Verified in Chromium**, since jsdom returns zeroes for every rectangle: ticking a visible row holds `scrollTop` at 800; deep in a revealed page, 120 rows before and after at 847; four consecutive ticks leave `#u-bar` at 30 and `#sug` top at 75.0 — the chip-row wrap **cannot fire while the reader is picking**, because no chip exists until Enter. ⭐ **Then the governing review turned out to be a different recording.** Sam: *"the local session had a new video v4, not the other"* — its output had been written to a directory nobody could find, because `video-context` writes **relative to the shell's working directory** (`SKILL.md` now says so). He recovered it and supplied the transcript **and all 21 frames**: six of v4's eight items had already shipped, and ⚠️ **frame 17 overturned the main open one** — the move confirmation he asked for was already on screen, so the real defect is that the moved course's own mark never says *staged, not saved* ([`skyview_video4_findings`](skyview_video4_findings.md)). ⚠️ Also caught by CI: `.video-context/` is gitignored **and main deliberately keeps two files inside it** (#1501's per-file call), so removing the directory wholesale deleted a file main holds.
- **2026-09-06 (SkyOutline II, S235 — the course outline of record, and three rulings)** — ⭐ **THE OUTLINE IS BUILT** — planned three times, cleared three times, built zero (#1502). `#outline/<id>`, reachable by double-click or a panel button, six layers, and the layers we cannot fill yet (MAP exhibits, military CRs) are **present and empty** because an outline that omits them reads as finished. **The description is chosen, never written**: the *medoid* member catalog description — the one that says what the rest say — quoted and attributed to the college that wrote it, since composing prose out of several catalogs would read as authoritative while belonging to nobody. Sam's sentence prints verbatim. **Skills are imputed** from the colleges' own words, because we hold **zero** agency skill statements; confidence is agreement BETWEEN colleges and thin skills stay, chipped. **Two level axes, neither derived** — measured both ways: `WELD M1012` is course-level *Advanced* with its skills mostly "not stated", `WELD M1073` has no course level and carries a skill reading *Beginning*. ⚠️ **Two extraction defects invisible to jsdom and to reading the code**: every n-gram length counted per position, so fragments outscored the names containing them ("shielded metal arc" beside "shielded metal arc welding"), and n-grams crossed the commas catalog prose is full of ("pain tissue integrity gas"). Sam's three rulings all shipped: Enter closes the panel (sort control to the list's top right, an Enter button at the bottom; `markSug` now addresses rows by id, since a header child would desync every arrow key), double-click opens the outline, and the chip row reserves its space. The stranding is fixed — `#work/<discipline>`, a named crumb back to SkyView, and the selection parked and re-rung on return. ⚠️ **Both inherited diagnoses named the wrong place**: the 36px drop is `.sugwrap`, not `#u-bar` (which never moves, so a fix there would have shipped and changed nothing), and the picks were destroyed leaving the map rather than returning to it — 1 KB note ([`methodology-a-correct-measurement-can-name-the-wrong-place`](kb-notes/methodology-a-correct-measurement-can-name-the-wrong-place.md)). 23 new checks, both key guards mutation-tested; five existing assertions rewritten to the reversed rulings rather than deleted. Sam's four new asks captured verbatim.
