---
title: cpl-project-tracker docs — Index
created: 2026-05-27
updated: 2026-08-19
tags: [meta, index, obsidian-target]
kb-status: internal
obsidian-folder: cpl-project-tracker
related:
  - "[[CLAUDE]]"
  - "[[docs/kb-notes/README]]"
---

# cpl-project-tracker — Docs Index

Auto-maintained landing page for the project's documentation surface, intended
as the **Obsidian vault entry-point** when browsing this repo from the
vault-side clone at `CPLBrain/COG-second-brain/cpl-project-tracker/`.

Refreshed at every checkpoint (per `CLAUDE.md` Rule 8).

## The three lanes

| Lane | What | Where |
|---|---|---|
| **KB notes** | Durable, distilled, reusable knowledge | [`docs/kb-notes/`](kb-notes/) |
| **Lessons (WIP)** | Workstream scratchpads, append-only | `docs/<workstream>_lessons.md` |
| **Session handoffs** | "Fattyfat" capsules for the next session | `docs/session_<N>_handoff.md` |

See [`docs/kb-notes/README.md`](kb-notes/README.md) for the lane contract.

## CLAUDE.md reference offloads (`docs/reference/`) — added 2026-07-10 (Session 111, the pare-down)

Always-current project memory moved out of `CLAUDE.md` (2,514 → ~590 lines);
**Rule 8 checkpoints update these files now**, and `CLAUDE.md` keeps read-before
stubs pointing here.

| Doc | Was | Read before |
|---|---|---|
| [Pipeline Reference](reference/pipeline_reference.md) | `CLAUDE.md` §Pipeline Reference (1,087 lines) | generator/workflow/tabs/Supabase/EACR/C-ID work |
| [KB Build Status](reference/kb_build_status.md) | `CLAUDE.md` §KB & Unified Courses (421 lines) | KB/CCR curation work, build-phase history |
| [M-ID Lifecycle & CID/CIDx](reference/mid_lifecycle.md) | `CLAUDE.md` §11 prose + strategic roadmap (449 lines) | re-mints, MC/TMC calls, auditor, pathway decisions |

---

## KB notes (`docs/kb-notes/`)

Lane established 2026-05-27, Session 11 (Bruh El). See
[`kb-notes/README.md`](kb-notes/README.md) for the contract.

| Title | Type | Status | Created | Updated |
|---|---|---|---|---|
| [A normalisation and the screens that judge it must see the same text](kb-notes/methodology-a-normalisation-and-its-screens-must-see-the-same-text.md) | methodology | published | 2026-08-13 | 2026-08-13 |
| [Tell a parser defect from a people defect before you build a curation queue](kb-notes/methodology-tell-a-parser-defect-from-a-people-defect.md) | methodology | published | 2026-08-14 | 2026-08-14 |
| [Exhibit CRs Catalog: all 27 fields, measured](kb-notes/reference-exhibit-crs-catalog-field-census.md) | reference | published | 2026-08-14 | 2026-08-14 |
| [An instruction that names another surface is a dependency nothing enforces](kb-notes/methodology-an-instruction-naming-another-surface-is-an-unenforced-dependency.md) | methodology | published | 2026-08-14 | 2026-08-14 |
| [A copy detector must read the rendered string, not the lines the author typed](kb-notes/methodology-a-copy-detector-must-read-the-rendered-string.md) | methodology | published | 2026-08-14 | 2026-08-14 |
| [Before repairing a field, check whether the record already holds a better signal](kb-notes/methodology-the-record-may-already-hold-a-better-signal-than-the-field-you-are-repairing.md) | methodology | published | 2026-08-14 | 2026-08-14 |
| [ADR — the side menu as a curator overlay over code defaults (the COBI nav becomes `cobi_nav`, edited by drag and drop, and the overlay NEVER gates the menu: a rules table that fails closed gives an untuned bot, a nav that fails closed gives a site with no navigation. Lockout is prevented in CODE at three points because the table is the thing being guarded; two protection lists whose axis is RECOVERABILITY — `hidden` is irrecoverable from the browser, an audience rule is not)](kb-notes/adr-the-side-menu-as-an-overlay-over-code-defaults.md) | adr | published | 2026-08-14 | 2026-08-14 (SkyGate #1195/#1196) |
| [Migrate the display, not just the data (a faithful migration — every field copied, counts reconciled, original kept as rollback — still lost the caveat saying the citations were unverified, the briefing's actual recommendation, and an entire blast-radius layer; the schema was fine, so no schema diff could see it. Losing content makes a product smaller; losing a disclaimer makes it dishonest)](kb-notes/methodology-migrate-the-display-not-just-the-data.md) | methodology | published | 2026-08-19 | 2026-08-19 (Sky168) |
| [A filter needs a field — prose is not a facet (a "Title 5 / Ed. Code section dropdown" could not be built because §55050 existed only inside sentences; *filter · facet · group-by · sort-by* are one request — make the attribute ADDRESSABLE — and the classifier is the hard half: an `else` bucket would have filed Government Code §11342.2 under Ed. Code, a fabricated citation shown to lawyers)](kb-notes/methodology-a-filter-needs-a-field.md) | methodology | published | 2026-08-18 | 2026-08-18 (Sky168) |
| [A manager must show everything it manages, or say what it cannot see (the COBI Admin tab administered the side menu while two real menu items — the Fact Sheet and Ask Sierra launchers — were invisible to every one of its queries, because they are anchors and the selector asked for `data-tab`. Nothing failed: a partial inventory is indistinguishable from a complete one. And widening the set made three rules answer confidently and wrongly, including a "not checked" count that would have RISEN by two the day the omission was fixed)](kb-notes/methodology-a-manager-must-show-everything-it-manages.md) | methodology | published | 2026-08-15 | 2026-08-15 (Sky160 #1213) |
| [A live rename must be order-proof, because the database and the deploy cannot be simultaneous (renaming `team_access.id` from `raci` to `team` left a window where the code and the table disagreed — and the mismatch renders as a BLANK phrase card, which invites someone to type into it and save, creating a SECOND row that `team_pass_check()` accepts. Two live shared phrases, one invisible to whoever rotated the other. A `legacy` alias makes the order irrelevant; test the two DATABASE states, not the two code branches)](kb-notes/methodology-a-live-rename-must-be-order-proof.md) | methodology | published | 2026-08-15 | 2026-08-15 (Sky160 #1214) |
| [A check that never registers can never fail (a test harness that summarises on a TIMER, or lets an async block die silently, reports a total that is partly a measurement of how busy the machine was — `admin_tab.test.js` gave 116, 122 and 123 checks on identical source. A missing check is an absence, not a failure, and it subtracts from BOTH sides of the ratio, so every run still reads "all passed". Await every block; turn a rejected one into a named failed check; watch the DENOMINATOR)](kb-notes/methodology-a-check-that-never-registers-can-never-fail.md) | methodology | published | 2026-08-15 | 2026-08-15 (Sky159 #1209) |
| [A rotating credential cannot be cached (refreshing CONSUMES a Supabase refresh token, so a module renewing from a copy it captured earlier may be re-spending one another module already used — which reads as theft, fails, and in three of six modules DROPS the session mid-edit. Resolve from storage at the moment of use; and only a definitive 400/401 may end a session, never a network blip)](kb-notes/methodology-a-rotating-credential-cannot-be-cached.md) | methodology | published | 2026-08-15 | 2026-08-15 (SkyKey #1205) |
| [An assertion pinned to a mutable value stops being a guard (a negative assertion written against a live value — a person's name in synced data — silently stops testing anything once that value leaves, while still printing `[assert ok]`. The smoke test's external-context privacy guard went vacuous when a college changed coordinator. Anchor on the INVARIANT and derive the negative from its paired positive)](kb-notes/methodology-an-assertion-pinned-to-a-mutable-value-stops-being-a-guard.md) | methodology | published | 2026-08-15 | 2026-08-15 (SkyKey #1206) |
| [A bulk POST is one statement over the union of its keys (PostgREST builds ONE INSERT whose column list unions every object's keys, so a key present on some rows arrives NULL on the rest — a NOT NULL column then 400s the whole batch. The defect exists only ACROSS the array, so row-level tests and a permissive mock both pass. Assert key-set UNIFORMITY, and never blame a 400 on the sign-in)](kb-notes/methodology-a-bulk-post-is-one-statement-over-the-union-of-its-keys.md) | methodology | published | 2026-08-15 | 2026-08-15 (SkyKey #1203) |
| [An empty read is only evidence if the set cannot legitimately be empty (PostgREST answers an RLS-filtered read with `200 + []`, byte-identical to an empty table; only prior knowledge distinguishes them. `team_access` is known non-empty so `[]` proves "not a reviewer"; `sierra_rules` is seeded empty ON PURPOSE, so the same inference would tell a reviewer they are locked out AND tell a locked-out person Sierra has no rules. Four states, not two — and a FAILED probe is never an accusation)](kb-notes/methodology-an-empty-read-is-only-evidence-if-the-set-cannot-be-empty.md) | methodology | published | 2026-08-14 | 2026-08-14 (SkyGate #1190/#1193) |
| [`methodology-a-tag-scan-cannot-see-a-fetch`](kb-notes/methodology-a-tag-scan-cannot-see-a-fetch.md) | methodology | Folder portability is a runtime property; `src=`/`href=` cannot see a `fetch()` or a `../` | 2026-08-19 |
| [`adr-pull-from-the-source-rather-than-accept-a-push`](kb-notes/adr-pull-from-the-source-rather-than-accept-a-push.md) | adr | When a vendor offers to write into your database, ask for the interface instead | 2026-08-19 |
| [`methodology-a-summary-field-is-not-the-record.md`](kb-notes/methodology-a-summary-field-is-not-the-record.md) | methodology | A single scalar standing in for a set will be quoted as the set — and the answer looks complete, so nobody files a bug. POST: `ccc_rec` held 1 of 10 recs. |
| [`methodology-a-conditional-key-breaks-a-bulk-upsert.md`](kb-notes/methodology-a-conditional-key-breaks-a-bulk-upsert.md) | methodology | PostgREST needs every object in a bulk payload to carry the same keys; an optional field fails POSITIONALLY and leaves the table partly loaded, which looks populated. |
| [`methodology-a-settled-ruling-does-not-enforce-itself.md`](kb-notes/methodology-a-settled-ruling-does-not-enforce-itself.md) | methodology | `cpl_memory` said "use the adoption file" for weeks while the sync used the other one. Writing a decision down changes no existing consumer — grep for readers the same day. |
| [Methodology — a shared credential can only scope to a surface that is exclusive to its group (before building per-group phrases, ask which surfaces belong to exactly ONE group; every surface belonging to two must accept *either* credential, so the scopable set is the exclusive set — measured across COBI's 34 tabs, exactly two qualified, and C&I/CIP turned out to have zero gated tables of their own)](kb-notes/methodology-a-shared-credential-can-only-scope-to-an-exclusive-surface.md) | methodology | published | 2026-08-12 | 2026-08-12 (SkyFund) |
| [Methodology — a limit nobody can see eats work, and a limit enforced twice drifts (`maxlength` stops keystrokes with no message, so truncation is indistinguishable from a stuck keyboard; several independent inputs landing on an identical length is a FINGERPRINT of truncation; and a cap enforced on both a client and a server must be asserted as a PAIR, or raising one side just relocates the silent cut)](kb-notes/methodology-a-silent-cap-eats-work-and-a-paired-cap-drifts.md) | methodology | published | 2026-08-12 | 2026-08-12 (SkyFund) |
| [Methodology — a status lane must link to the remedy lane, or "done" measures attention (when one control changes the *state of the record* and a different control changes *the thing the record is about*, the first must lead to the second; otherwise the queue reports itself clear while nothing changed — the same shape as the disposition rate and the cadence that was decided in June and never ran)](kb-notes/methodology-a-status-lane-must-link-to-the-remedy-lane.md) | methodology | published | 2026-08-12 | 2026-08-12 (SkyFund) |
| [Methodology — validate a supplied code column by its STRUCTURAL INVARIANT, not by spot-checking values (a column of identifiers can be entirely real, entirely plausible and attached to the WRONG ROWS: a curator-supplied roster's LocationID had valid 3-digit codes and its first row matched the authority exactly, yet only 3 of 106 agreed — spot-checking cannot catch a shifted column because every individual value is still genuine; what catches it is a property the codes must satisfy AS A SET, here that CCCCO MIS college codes are DISTRICT-PREFIXED — Appendix A satisfies it for 25 of 26 multi-college districts, the roster for 3 of 23, scattering Los Angeles CCD across 121/234/312/422/471/571/721/748/862; useful invariants: hierarchical prefixes, density, cardinality both ways, cross-source agreement, format; a column that fails its own invariant is STRUCTURALLY untrustworthy rather than mostly-fine, and the right response is to mark it unusable INSIDE the file and keep the file for what it is good at — the same spreadsheet's name columns took a hand-curated bridge table from 30 entries to zero; corollary: a column that contradicts its own data needs no external authority to condemn, and anything derivable should be DERIVED)](kb-notes/methodology-validate-a-code-column-by-its-structural-invariant.md) | methodology | published | 2026-08-12 | 2026-08-12 (SkyLink) |
| [Methodology — a source file that abbreviates titles fakes an absence](kb-notes/methodology-a-source-file-that-abbreviates-titles-fakes-an-absence.md) | methodology | published | — | — |
| [Methodology — assert what the reader sees, not what the source says (a test that greps the SOURCE file for a phrase is unsound in BOTH directions: it goes red on a correct page when copy is reworded or merely REFLOWED — concatenated string literals mean `/batch-upload already-posted credit/` never matches a source that splits the phrase across a `+`, and a test failing for a reason the reader cannot see teaches the next person to edit the assertion rather than investigate it — and it goes GREEN on a broken page, because a self-contradiction has no fixed string to grep for: 163 assertions passed on copy printing "100%" directly above a row showing 0.2% of something else; so assert `root.textContent` after a real render with whitespace normalised, reserve source-greps for genuine source invariants like "this function body must never reference state.viewSlot", and READ THE RENDER ONCE FOR EVERY BRANCH — empty, all-of-it, singular, withheld — which took 30 seconds and found two copy defects no assertion would ever have named)](kb-notes/methodology-assert-what-the-reader-sees.md) | methodology | published | 2026-08-11 | 2026-08-12 (SkyPro — + never pin an assertion to a daily-refreshed value) |
| [Methodology — normalise both sides of a join, or the resolver is decoration (putting one side through a name resolver and comparing it to the RAW other side works for every row whose two spellings already agreed — most of them — and silently drops the rest: five colleges were shown no implementation funding because `fundingFor()` normalised MAP's name while `baseCollege()` compared the roster's raw string; the existing join test asserted `S(roster)` vs `S(roster)` and reported "0 orphans" while five were orphaned in production)](kb-notes/methodology-normalise-both-sides-of-a-join.md) | methodology | published | 2026-08-12 | 2026-08-12 (SkyPro) |
| [Methodology — a collapsed section must still inform, or it is hidden rather than minimal ("make it collapsible" asks for a shorter page, not a page with less on it; a closed drawer showing only its title forces the reader to open all of them, which is worse than the scroll it replaced — so every closed header carries that college's own figure, every branch including the unhappy ones gets a summary, and open state lives in application state because render() rewrites innerHTML)](kb-notes/methodology-a-collapsed-section-must-still-inform.md) | methodology | published | 2026-08-12 | 2026-08-12 (SkyPro) |
| [Methodology — an affordance gated on a problem is invisible when there isn't one](kb-notes/methodology-an-affordance-gated-on-a-problem-is-invisible-when-there-isnt-one.md) | methodology | published | — | — |
| [ADR — judgment goes in curatable tables; mechanism stays in code (Sam's COBI design principle: move instructional variables to curatable tables, *unless* doing so is less efficient or less reliable for AI procedures — the caveat is what makes it implementable, forcing four tests instead of "move everything". Forced by Sierra obeying a hard-coded `STATEWIDE_RULE` the team could neither see nor edit, while the prompt merely *promised* team guidance wins. Load-bearing consequence: a curatable table must OVERLAY code defaults, never replace them, so a failed read costs the edits and never the governance. Sierra's rules deliberately do NOT become `cpl_memory` rows — three collisions, one of them a safety hole, since sessions auto-write memory with no approval gate)](kb-notes/adr-judgment-in-tables-mechanism-in-code.md) | adr | published | 2026-08-14 | 2026-08-14 (SkyHaul; implemented Sky155 #1186) |
| [ADR — the unified `cpl_memory` table](kb-notes/adr-unified-memory-table.md) | adr | published | — | — |
| [Methodology — conflicting source tabs: use the certified value](kb-notes/methodology-conflicting-source-tabs-use-certified-value.md) | methodology | published | — | — |
| [Methodology — crowd consensus beats a single-item signal](kb-notes/methodology-crowd-consensus-beats-single-item-signal.md) | methodology | published | — | — |
| [Methodology — fix the root, not a counter-rule](kb-notes/methodology-fix-the-root-not-a-counter-rule.md) | methodology | published | — | — |
| [Methodology — grounded lexical CIP confidence](kb-notes/methodology-grounded-lexical-cip-confidence.md) | methodology | published | — | — |
| [Methodology — lead with the steps, not the rationale](kb-notes/methodology-lead-with-the-steps-not-the-rationale.md) | methodology | published | — | — |
| [Methodology — MAP API value-signature probe](kb-notes/methodology-map-api-value-signature-probe.md) | methodology | published | — | — |
| [Playbook — the NC integration run](kb-notes/playbook-nc-integration-run.md) | playbook | published | — | — |
| [Playbook — the trail-crew method (magic audit)](kb-notes/playbook-trail-crew-method-magic-audit.md) | playbook | published | — | — |
| [Reference — funding-metrics measurability](kb-notes/reference-funding-metrics-measurability.md) | reference | published | — | — |
| [Reference — humans, principles](kb-notes/reference-humans-principles.md) | reference | published | — | — |
| [Reference — the articulated-and-waiting backlog is almost entirely basic military service credit (98.8% of all 64,074 units at Needs Action with an articulated exhibit behind them is Credit for Basic Military Service — 87.7% landing on a GE or graduation area rather than a named course; 65 of the 73 colleges with any are at 100% and the whole backlog is 592 rows, so it is close to ONE decision applied repeatedly, not hundreds of judgment calls; 33 of 106 colleges have none waiting at all, which is a FINISHED QUEUE and must never render as missing data; group on `course_type`, never on the 298 freehand `credit_rec` variants)](kb-notes/reference-the-waiting-credit-backlog-is-basic-military-service.md) | reference | published | 2026-08-11 | 2026-08-11 (SkyLink) |
| [Methodology — a materialized view cannot carry RLS, so its suppression has no backstop (Postgres does not implement row-level security for matviews at all and `security_invoker` does not apply, so a matview granted to `anon` is public no matter how tightly its SOURCE tables are policed — the copy left the policy behind; where such a view applies k-anonymity that suppression is enforced solely by the build script, with no second line beneath it, so the invariant must be ASSERTED AGAINST THE BUILT OBJECT rather than merely implemented in the builder; found safe-but-unbacked on `map_credential_student_rollup` — 0 published cells under k=10 and all 420 suppressed rows nulling every measure including the unit columns)](kb-notes/methodology-a-materialized-view-cannot-carry-rls.md) | methodology | published | 2026-08-11 | 2026-08-11 (SkyLink) |
| [Methodology — a percentage must never round UP into a claim it cannot support (`Math.round()` turns 99.76% into "100%", and 100% is not a rounded number but a CLAIM OF TOTALITY that the page may be visibly contradicting three lines above; round down whenever the top of the range is qualitative rather than quantitative, give the genuine-totality case its own branch of prose since hedges written for the approximate case are wrong when it IS all of it, and note the inbound twin — a published rate rounded to one decimal disagrees with any threshold test run on the unrounded ratio; shipped live and caught not by 163 assertions but by RENDERING THE PAGE AND READING IT)](kb-notes/methodology-a-percentage-must-not-round-up-into-a-claim.md) | methodology | published | 2026-08-11 | 2026-08-11 (SkyLink) |
| [Playbook — loading a Microsoft Access export into Supabase: design the table against REAL ROWS rather than a spec (the first run's brief was wrong in four places and none would have thrown an error — a PK colliding on 8%, a 3-value vocabulary that was really 11 across two vocabularies, a null-sentinel premise true for only one of three variants, and a ratio whose four denominator columns are all 0 on exactly the population it measures); Access has no COUNT(DISTINCT) so use a distinct sub-query, and an equi-join on a NULL column SILENTLY RETURNS NOTHING so normalise with Nz() and join on a composite key; collapse all THREE no-value spellings (NULL, '', and a literal "-"); route the unrecognised to a visible bucket so "did we cover every case" becomes a countable query; make SELECT DISTINCT match the PK exactly and duplicate-key failure becomes impossible by construction; land in an all-text nullable no-PK STAGING table (which inherits the sensitivity of its contents), ⭐ RECONCILE THE ROW COUNT AGAINST THE SOURCE BEFORE ANYTHING ELSE — Supabase Studio landed 222,646 rows from a 220,588-row file and reported success — then transform with explicit casts and assert the count matches EXACTLY, which simultaneously proves no rows lost, only artifacts deduped, and that trim() merged nothing)](kb-notes/playbook-access-export-to-supabase.md) | playbook | published | 2026-08-08 | 2026-08-08 (SkyNaut) |
| [ADR — student-detail aggregates: k=10, published grain, and the reviewer/published split (Sam proposed skipping a shielded table entirely — publish aggregates granular enough for Sierra to read directly, show cells under 10 as `<10`, keep the real numbers for totals; the direction is right and 10 improves on the funding surfaces' 5, but that exact combination is the failure documented two days earlier — a published total plus every sibling makes the hidden cell recoverable by subtraction, and suppression applied at DISPLAY time over real rows is decoration. Sierra compounds it: per-answer suppression does not survive a sequence of answers re-cutting the same data. RATIFIED: k=10; two objects, a reviewer-only base plus a separately-built published aggregate; suppression at WRITE time in a build step, never a SQL view, because complementary suppression is iterative; thin cells show existence with `<10` and no breakdown (Sam's option b); complementary suppression at every level publishing a total; suppression driven by DISTINCT-STUDENT count and taking the credit sums with it, since a one-student cell publishing PotentialCredits discloses that student's exact total; no rate at a grain where its inputs are suppressed; test the PROPERTY — that subtraction cannot pin the value — not the flag)](kb-notes/adr-student-detail-aggregate-disclosure-control.md) | adr | published | 2026-08-08 | 2026-08-08 (SkyNaut) |
| [Methodology — a successful import is not a correct one: a loader that reports success has told you it finished, not that it moved your data (Supabase Studio's CSV importer re-sent a committed batch and silently landed 222,646 rows from a 220,588-row file — 2,058 groups each duplicated exactly twice, arithmetic closing exactly; Access wrote 220,588 and re-read 220,588, isolating the fault to the transfer). ⭐ BOTH ENDPOINTS CAN BE RIGHT WHILE THE TRANSFER IS WRONG — reconciliation is a property of the boundary, not of the things it connects. Over-counting is more dangerous than under-counting: 0.93% inflation sits inside the range where a figure still looks plausible and nobody investigates, and duplicates arrive in contiguous batches so the error lands entirely on whichever colleges sat in that window — right for most, wrong for a handful, the hardest shape to notice. Pattern: land in a permissive all-text nullable no-PK staging table, reconcile counts, then `INSERT SELECT DISTINCT` with explicit casts; dedupe only when the source guarantees uniqueness. An EXACT match is a stronger signal than it looks — landing on 220,588 simultaneously proved no rows lost, the dedup removed only artifacts, and `trim()` had not merged two rows differing by whitespace)](kb-notes/methodology-a-successful-import-is-not-a-correct-one.md) | methodology | published | 2026-08-08 | 2026-08-08 (SkyNaut) |
| [Methodology — an unordered LIMIT is a correctness bug, not a style nit, and downstream of an LLM it reads as model flake (SQL guarantees no row order without ORDER BY, so `ilike '%angeles%' limit 3` returned {East LA, LA City, LA Harbor} on one live call and {LA Mission, LA Southwest, West LA} on the next — a college-detection lookup that resolved its answer only when the right row happened to fall inside an arbitrary window; two compounding hiders — the consequence surfaced one layer away as a *ranking* bug in a rank function that was innocent and unreachable, and an LLM downstream meant the intermittency got charged to generation, with a prior handoff documenting it as inherent prose flake; rules — every LIMIT gets an ORDER BY, a LIMIT narrower than the match set is a silent filter with no predicate, never `return` out of a candidate loop on a partial answer, and when a symptom is intermittent suspect the data path before the model; the tell that found it — the code you were pointed at could not arithmetically produce the symptom (`min(courses,39) < 40`), so stop fixing it and go up a layer; plus: strip comments before source-level regex assertions, since the first limit-check matched `.limit(3)` inside the comment describing the old bug)](kb-notes/methodology-an-unordered-limit-is-a-correctness-bug.md) | methodology | published | 2026-08-07 | 2026-08-07 (SkyHero) |
| [Methodology — the feedback queue already knew: collection is not the bottleneck, triage is (a defect that cost an engineering session and an automated battery to find had been reported in writing by a domain expert in the product's own feedback table **five weeks earlier** — 43 rows, **zero ever advanced past `status='new'`**, and **19 of the 43 were the smoke test writing into the queue it was meant to fill**; three failure modes visible in one table — nobody owns the queue (a schema is not a process), the harness pollutes the queue it fills, and the invitation outpaces the capacity, because an unread queue that GROWS teaches people their feedback goes nowhere and that lesson is expensive to reverse; triage by CLASS not by vote — retrieval gap (a real bug) / data gap (corpus, not bug) / **expectation gap (the system was RIGHT and the person expected otherwise — a map of what the field misunderstands, often the most valuable category, and also the answer to "what if feedback is misinformed?")**; ask for QUESTIONS not ratings, since every conversational aside a domain expert offered found a real defect while bare ratings found none; and never let feedback auto-tune the system — but then STAFF the gate, because an ungated-unread queue and a gated-unread queue are the same queue)](kb-notes/methodology-the-feedback-queue-already-knew.md) | methodology | published | 2026-08-07 | 2026-08-07 (SkyHero) |
| [Methodology — a guardrail that only forbids disables the feature: restrain salesmanship, not facts (when a tool serves two parties whose interests diverge, a rule written purely as a prohibition destroys the value for the party it wasn't protecting — "never volunteer a comparison", meant to stop an assistant embedded on a college's own page from poaching its students, instructs a compliant model to WITHHOLD, so a learner asking about an unarticulated credential gets a polite dead end, which fails the learner AND the college that never learns there was demand; the resolving distinction — never withhold anything that materially changes the person's outcome, never volunteer editorial comparison — and the honest version usually serves both because THE GAP IS THE PRODUCT: framed as an adoption opportunity, the same sentence is a service to the learner and market intelligence for the host; ship every protective rule as a PAIR (prohibition + permission + a stated tie-break) and **test the permission harder**, because a violated prohibition is loud (a stakeholder complains) while a violated permission is silent (the person just isn't helped and files nothing); plus — Yes/And beats both/and, a behavioural rule needs a phrasing pattern not just a policy, and a test that goes red after a deliberate policy change is a QUESTION for the product owner, never something to green by editing the assertion)](kb-notes/methodology-a-guardrail-that-only-forbids-disables-the-feature.md) | methodology | published | 2026-08-07 | 2026-08-07 (SkyMiner — **Resolution** section added: both halves SHIPPED; the tie-break must also be repeated where the conflict BITES, not only where it is defined) |
| [Methodology — a test that writes to the queue it monitors: CI noise is indistinguishable from user signal (a smoke test that verifies a feedback write path the honest way — through the real anon RPC — leaves one row per run in the same table a human reads for signal, and after five weeks 28 of 53 rows were CI, every one rated down, so the review tab's headline read 38 negatives when 10 were real; the test CANNOT clean up after itself and that is correct, because the write goes through an anon credential that is deliberately write-only on that table — a test able to delete its row would run with more privilege than the path it verifies and would silently stop testing it, while a fixed turn_id would leave one row forever but only ever exercise the UPDATE branch of the upsert; so the fix belongs at the READING surface — stamp CI writes with a field before you need it, exclude them by default from the list AND every count derived from it (a headline that disagrees with the rows below it is worse than no filter), disclose rather than silently drop by showing the withheld count next to a toggle, and budget for undeletable residue against any row limit; generalises to synthetic transactions, health-check requests, canary users and monitoring probes, and the synthetic share only ever goes UP)](kb-notes/methodology-a-test-that-writes-to-the-queue-it-monitors.md) | methodology | published | 2026-08-07 | 2026-08-07 (SkyMiner) |
| [Methodology — a failed read is not an empty result, and an optimistic write must precede its repaint (three defects in one small editor, all the same shape: the interface asserting something it had no basis for while the DATA WAS FINE — `r.ok ? r.json() : []` turning a 401 into "nobody has an owner" (indistinguishable from someone deleting the assignments); an "optimistic repaint" that ran BEFORE the optimistic write, leaving the UI always exactly ONE WRITE BEHIND (reported as "the save button needs two clicks", which it only resembles because the dialog pre-fills from the same stale state); and a cache keyed on whether a load happened rather than on what that load was ALLOWED to see, so signing in never re-read the gated data; a UI that is wrong while the data is right is the hardest defect class to report, because the reporter accurately describes a symptom that points away from the cause — and all three shipped because every existing test set state directly and called render(), never driving the button→dialog→Save path, so they could not catch a bug in HOW the end state is reached)](kb-notes/methodology-a-failed-read-is-not-an-empty-result.md) | methodology | published | 2026-08-07 | 2026-08-07 (SkyMiner) |
| [Methodology — RLS is not a gate in front of a service-role edge function: a tight policy protects nothing on a code path that reads with the SERVICE-ROLE key, and if that function is also reachable unauthenticated then whatever it is willing to say is public (the policies here were genuinely correct AND completely irrelevant to the path being added — which is why it is easy to miss: the policy is right there, it is tight, and reading it produces a feeling of safety that does not correspond to the code being written); identify the PRINCIPAL not the policy, and note that a client-supplied `audience`/`ctx`/`Origin` is never an authorisation — curl sets them freely and Origin protects users from other sites, not servers from clients; includes the trap found INSIDE the guard, where an assertion treating any non-`[]` response as a leak reported a statement timeout as STUDENT GRAIN LEAKED, plus the positive-control rule)](kb-notes/methodology-rls-is-not-a-gate-in-front-of-a-service-role-function.md) | methodology | published | 2026-08-09 | 2026-08-09 (SkyMind) |
| [Methodology — a written backlog decays silently: a list of outstanding work is wrong from the moment it is written and never announces it, because a stale list and a current one are typographically identical; measured instance — an inherited six-item backlog had two items wrong within TWO DAYS, including a governance register whose own OQ-01 ('who owns each row?') had been ANSWERED by people in the system while the document still listed it as the outstanding work; so: compute every item at load, put the unmeasurable in an explicit lane showing 'last confirmed N days ago', show the derivation next to the number, treat a failed read as unknown-and-ranked-first rather than zero, mark cleared items clear rather than deleting them, and delete a hand-tracked item the day it becomes measurable](kb-notes/methodology-a-written-backlog-decays-silently.md) | methodology | published | 2026-08-09 | 2026-08-09 (SkyDesk) |
| [Methodology — a sweep scoped by a proxy leaves a shadow: scoping remediation by an attribute that CORRELATES with the need rather than being it makes everything outside the scope invisible — never examined, yet afterwards indistinguishable from examined-and-fine, and worse than an ordinary gap because the completed sweep reads as coverage; measured instance — a contact sweep scoped to 'colleges without a CPL Assistant' (a proxy) missed 7 of the 25 colleges that actually could not route a student, 28% divergence, and they were the CHEAPEST items on the backlog; so: write the need as a predicate over the full population first, compute population−scope explicitly, give 'never examined' its own state distinct from 'examined, nothing found', and report coverage against the need's denominator not the batch's](kb-notes/methodology-a-sweep-scoped-by-a-proxy-leaves-a-shadow.md) | methodology | published | 2026-08-09 | 2026-08-09 (SkyDesk) |
| [Methodology — a provenance tier must encode what you could NOT check: a tier is a claim about METHOD, not confidence, so weaker evidence must never be filed under a tier whose meaning includes a check that did not happen; measured instance — sessions became egress-blocked from college domains, so candidate contact addresses were reachable via search but their PAGES were not, and the curator's sourcing rules are rules about what a page shows (department inbox vs a list of counsellors vs a mental-health inbox — 2 of the previous 71 published only the last, invisible from a snippet and the one outcome that actively harms a student); so: add a tier naming the missing check, carry the artefact a human must open, make the consuming code refuse it BY CONSTRUCTION rather than by convention, and record the resulting capability loss where the work is PLANNED, since no test catches an environment that moved](kb-notes/methodology-a-tier-must-encode-what-you-could-not-check.md) | methodology | published | 2026-08-09 | 2026-08-09 (SkyHigh) |
| [Methodology — verify the last hop of a resolution chain: a documented resolution order decays one hop at a time, and 'the overlay is empty' is the least durable sentence you can write about a surface designed to be edited by people who will never touch your docs; measured instance — the $35M priorities were documented as baked-defaults ⊕ an overlay that was 'currently EMPTY so defaults stand', but the overlay held two live scenarios whose active one used 50/30/20 rather than the documented 30/42/28, different descriptions AND metrics, plus the 23 hand-typed strategies that the roadmap had listed as the hard part still to be built; so: read every hop before quoting any, never document a hop as empty (document how to CHECK it), suspect the overlay first when docs and screen disagree, and note that specificity reads as verification but is not — the sentence to distrust is the confident aside inside an otherwise rigorous description](kb-notes/methodology-verify-the-last-hop-of-a-resolution-chain.md) | methodology | published | 2026-08-09 | 2026-08-09 (SkyHigh) |
| [Methodology — a guard that fails on truth gets muted: an assertion that fires on CORRECT behaviour is worse than no assertion, because it trains everyone to discount that check, and the cost is asymmetric on security-shaped guards whose whole value is being believed the one time they are right; two instances hours apart from the same author, the second written AFTER diagnosing the first (an error body read as a leak; then a regex widening that would have failed the system for being right — measured: of 17 institutions absent from the aggregate, exactly ONE had any activity, so 'zero' is approximately TRUE for an absent one); both encoded a PROXY instead of the property, so: enumerate the response space, say what failure looks like positively, query which real cases a widening would newly catch BEFORE shipping it, add a positive control to every returns-nothing assertion, and record in the file what the check cannot prove)](kb-notes/methodology-a-guard-that-fails-on-truth-gets-muted.md) | methodology | published | 2026-08-09 | 2026-08-09 (SkyMind) |
| [Methodology — a knowledge base needs a lint pass, not just an ingest and a query (a corpus with a write path and a read path but no compaction path does not decay by losing things; it decays by keeping every version and letting a future reader find the wrong one — 115 of 116 handoffs superseded with nothing machine-readable saying so, after a session already cited handoff 105 when 111 was authoritative; measure LANES not files, since the only lane inside budget was the one with a promotion gate and a template; and build the lint against the corpus you HAVE — v1 flagged 55 KB notes as malformed when only 3 were, the other 52 being valid sibling dialects, then the very next rule proposed excluding docs/ itself, so a new lint's first report is data about the LINT, not the corpus)](kb-notes/methodology-a-knowledge-base-needs-a-lint-pass.md) | methodology | published | 2026-08-09 | 2026-08-09 (Sky) |
| [Playbook — keep build artifacts out of the Obsidian vault (the vault clone of this repo carries ~1.07 GB of generated files it will never use; a docs-only sparse checkout drops it to ~11 MB without changing the sync workflow, and UNLIKE an exclusion list it actually removes them from disk — Obsidian's Excluded files is a RELEVANCE filter that never stops the watcher, the metadata cache or Sync; and do not scope it by file extension, because kb/row_audit is 418 MB of MARKDOWN so a 'materialise **/*.md' rule would keep 423 MB and look like it worked — scope by LANE)](kb-notes/playbook-keep-build-artifacts-out-of-the-vault.md) | playbook | published | 2026-08-09 | 2026-08-09 (Sky) |
| [Methodology - the plausible cause is not the measured one (five confident diagnoses in one session, each plausible and each wrong; a named line number beats a good story, prefer the check that can return "no", restate the symptom in the user's words before choosing what to measure, and treat the first run on real hardware as the actual test)](kb-notes/methodology-the-plausible-cause-is-not-the-measured-one.md) | methodology | published | 2026-08-09 | 2026-08-09 (Sky) |
| [Methodology — a wrong column is worse than a missing one: a missing field errors out and somebody investigates, but a WRONGLY-MATCHED field produces a confident, plausible number that gets repeated as a finding (a loose matcher took a column named `Status` — MAP's workflow stage: Needs Action / Implementation / Faculty / Initiator / Articulation Officer — instead of `CPLStatusPlan`, and reported a statewide disposition rate of 0.0% across 525,362 rows; ⭐ it was wrong in the direction that LOOKED RIGHT, because this project already expects a low rate, median 4.7%); the cause was the matcher's SHAPE not its vocabulary — one regex with alternation resolves to the first COLUMN matching ANY branch, so precedence is decided by header order in the file, and patterns must be tried most-specific-first ACROSS ALL COLUMNS; verify the VALUES against the vocabulary the field should hold, excluding any value it SHARES with its likely decoy (`Needs Action` appears in both columns, so accepting it as evidence reproduces the bug), then WITHHOLD the derived metric rather than emit it — return None, never 0.0, and record which column was used and whether it validated; print every header on EVERY run, since the evidence needed to diagnose it was one print away and withheld)](kb-notes/methodology-a-wrong-column-is-worse-than-a-missing-one.md) | methodology | published | 2026-08-07 | 2026-08-07 (SkyGauge) |
| [Methodology — judge a detector by what it prints: filters are only observable in their output (a filter that is subtly too loose and one that silently matches NOTHING both look perfectly reasonable in code; the first flagged every table a file merely read because the write test was per-FILE not per-reference, the second returned zero forever because a tab's nav button and its loadScript sit 1.2M characters apart — and ⚠ a scan returning zero is indistinguishable from a clean result, so it reports good news and nobody investigates good news: for every scan ask what a BROKEN version would print, and if the answer is "the same as a clean one" add a canary; bias to precision because a false positive costs more than a miss; the first complete run's 39 undifferentiated candidates became 15 usable ones through persistent memory of what was already judged, ranking so the first five rows are the five worth acting on, and disclosing the denominator so a short list reads as FILTERED rather than as all-we-could-find; commit the noise budget as a numeric test because filter decay is gradual and no single commit makes a list unreadable)](kb-notes/methodology-judge-a-detector-by-what-it-prints.md) | methodology | published | 2026-08-07 | 2026-08-07 (SkyMiner) |
| [Playbook — deploy a Supabase Edge Function from the runner, not by hand (passing a 66 KB function body inline to the MCP tool means re-emitting every byte into a live surface with no staging tier, where one dropped line inside ~9 KB of prompt text changes ANSWER BEHAVIOUR without breaking syntax; a dispatch-only workflow ships the exact bytes out of git instead. Carries the five distinct failures hit getting there — absent secret, **403 ≠ 401** with the credential-type taxonomy that makes a JWT or project key 403 exactly like an under-privileged role, a GitHub CLI-release rate limit, and a working-directory trap whose check fires AFTER auth and a Docker pull so it reads like a permissions failure — plus why `--no-verify-jwt` belongs pinned IN the workflow)](kb-notes/playbook-deploy-an-edge-function-from-the-runner.md) | playbook | published | 2026-08-07 | 2026-08-07 (SkyHunter) |
| [Methodology — a concatenated haystack penalises your best-curated record (build a search field by joining every alias a record has, rank it with a length-normalised similarity, and the records with the MOST aliases score WORST: POST's 16 curated variants ranked it below a rival with two, so curation effort became a ranking penalty; fix is to score the best SINGLE name and keep the concatenation only for candidate generation)](kb-notes/methodology-a-concatenated-haystack-penalises-your-best-record.md) | methodology | published | 2026-08-10 | 2026-08-10 (SkyLine) |
| [Methodology — emit the threshold with the label it prints (a privacy floor defined as a named constant is half a control: the mask "<5" was hard-coded in EIGHT places, one of them printing the live floor beside a stale label in the same sentence, so raising 5→10 would have understated the protection while overstating precision — silently, since no test compares a label to a constant)](kb-notes/methodology-emit-the-threshold-with-the-label-it-prints.md) | methodology | published | 2026-08-10 | 2026-08-10 (SkyLine) |
| [Methodology — a grain-invariant measure can still be the wrong one (a published share moved when the data grain changed; re-basing it on distinct students was provably invariant — 96 of 96 colleges unchanged — and useless, saturating at exactly 100.0% for 34 of 96. Stability is a property, not a virtue: check the measure still discriminates)](kb-notes/methodology-a-grain-invariant-measure-can-still-be-the-wrong-one.md) | methodology | published | 2026-08-10 | 2026-08-10 (SkyLine) |
| [Reference — batch-uploaded transcribed credit (colleges batch-upload already-transcribed AP/IB/CLEP/Credit-by-Exam lists, which MAP encourages; SDCCD was first, for thousands. So transcribed credit exists at only 24 of 111 colleges and measures recording practice as much as outcomes — never rank on it. Signature: ~1 row per student across few exhibits)](kb-notes/reference-batch-uploaded-transcribed-credit.md) | reference | published | 2026-08-10 | 2026-08-10 (SkyLine) |
| [Methodology — a retrieval miss and a data gap look identical from the answer (Sierra said no statewide CompTIA recommendation existed — MAP holds ten — and blamed retrieval; retrieval was fine, the measure was missing. Probe the accused function before tuning it. The invented cert list was accidentally CORRECT, which is worse than wrong)](kb-notes/methodology-a-retrieval-miss-and-a-data-gap-look-identical.md) | methodology | published | 2026-08-11 | 2026-08-11 (SkyRoute) |
| [Methodology — reuse the model, not its formula (a derivation can be wrong for the cases its special case never touches: the $35M allocation is an iterative floor waterfall, 50 of 115 colleges pinned at the $150K minimum, and the floor's cost comes out of the same pool — so a flat proportional figure misses the UNfloored colleges too. Call the module that owns the math)](kb-notes/methodology-reuse-the-model-not-its-formula.md) | methodology | published | 2026-08-11 | 2026-08-11 (SkyBridge) |
| [Methodology — a safe fallback is safe only for the caller it was written for (returning the input unmatched is right for a display chip and silently wrong for a money join; the fallback did not change, the second caller arrived. `f(f(x)) == f(x)` is the only test that tells "resolved" from "gave up")](kb-notes/methodology-a-safe-fallback-is-caller-specific.md) | methodology | published | 2026-08-11 | 2026-08-11 (SkyBridge) |
| [Methodology — publish the denominator with the number, as a column (only 4.2% of student rows can be named, so every per-credential count is a floor; ship coverage as a field beside it, because a caveat in prose is separated from the number the first time anyone quotes it. "Suppressed" and "no data" must stay distinct)](kb-notes/methodology-publish-the-denominator-with-the-number.md) | methodology | published | 2026-08-11 | 2026-08-11 (SkyRoute) |
| [Methodology — a static guard should assert the contract, not the argument order (two CI checks pinned exact argument position and template adjacency; a later route inserted a section between them and both went red, unnoticed for weeks because the check is non-required. Confirm an inherited failure is inherited)](kb-notes/methodology-assert-the-contract-not-the-argument-order.md) | methodology | published | 2026-08-11 | 2026-08-11 (SkyRoute) |
| [Methodology — an auto-triggered smoke test validates the version it is replacing (smoke fires on push, deploy is a manual dispatch, so the green run tested v37 while v38 landed twelve minutes later; only the timestamps show it. A post-deploy check must be ordered after the deploy)](kb-notes/methodology-order-the-post-deploy-check-after-the-deploy.md) | methodology | published | 2026-08-11 | 2026-08-11 (SkyRoute) |
| [Methodology — assert what retrieval RETURNS, not that the answer reads better (a RAG fix verified by reading the model's prose is not verified: handed 200 irrelevant rows a good model writes a fluent hedged answer naming the two that matched, which reads correct; the same CPR question broke twice in five weeks with different root causes and the SECOND was introduced by the FIRST fix; the committed keyword-set assertion went red on a third unrelated defect — plurals losing the whole synonym bridge — within a minute of existing)](kb-notes/methodology-assert-what-retrieval-returns.md) | methodology | published | 2026-08-06 | 2026-08-06 (SkyHunter) |
| [Reference — Postgres full-text search pitfalls on freehand credential titles (four production traps: the English stemmer strips the "-ed" from `aed` so `aed:*` becomes a prefix match on the letter "a"; the default parser glues `Aid/CPR/AED` into ONE file token so `cpr` cannot match a title about CPR; one generic term OR'd in swallows the result cap (`cert` = 445 of 2,397); and trigram fuzz cannot bridge a vocabulary gap — `word_similarity('cardiopulminary','Adult CPR…')` = 0.069, so fuzz the synonym KEY not the corpus. Plus the CREATE-OR-REPLACE overload trap that breaks PostgREST with 42725)](kb-notes/reference-postgres-fts-pitfalls-for-credential-titles.md) | reference | published | 2026-08-06 | 2026-08-06 (SkyHunter) |
| [Methodology — small-cell suppression must survive subtraction (hiding a count below k protects nobody when the same record publishes the total and every sibling: 47 − (6+30+10) = 1 recovers the hidden cell exactly, and `acted` recovers it a second way; hide a SECOND cell so two unknowns face one equation, and note that a rate published against a total IS its numerator — some cells cannot be protected cell-by-cell at all, which is a thing to state in the code rather than paper over with a null that implies protection you are not providing)](kb-notes/methodology-small-cell-suppression-must-survive-subtraction.md) | methodology | published | 2026-08-06 | 2026-08-06 (SkyHunter) |
| [Methodology — validate a derived metric against the expert's ranking before publishing it (when a domain expert can already name the top performers, that list is a FREE TEST SET, and a metric that disagrees with it is measuring something else however defensible its arithmetic; four metrics were tried on the same data and three ranked a known-adept college 24th-29th — only the disposition rate put all three of Sam's named colleges in the top thirteen of 106 against a 4.7% median; the disagreement is also DIAGNOSTIC, since transcribe-completion crowning a college at 99% is what exposed that its figure was a batch AP/IB upload auto-transcribed by construction rather than lifecycle mastery; the cost of asking for the ground-truth list is one sentence, the cost of publishing a metric that ranks known-good performers mid-pack is the credibility of the whole instrument)](kb-notes/methodology-validate-a-derived-metric-against-expert-ranking.md) | methodology | published | 2026-08-06 | 2026-08-06 (SkyPlan) |
| [Methodology — an incentive teaches where the finish line is (a metric does not merely observe behaviour, it DEFINES where the work is understood to end: the Veteran Star is achievable without performing the per-CR articulation step even once, because a JST upload creates the plan and Basic Training credit auto-applies against an already-articulated exhibit — so colleges max the reward and genuinely believe they are done, and the data carries the fingerprint at applied-students ≈ JSTs-uploaded, ratio 1.00 for the median college; consequences — whatever you make green becomes the new finish line so collapsing six checkboxes to three achieves nothing if the three are still checkboxes, express each step as a FRACTION which cannot be satisfied by doing it once, name the grain in the label or it will be read at the wrong grain, and pair a reward already in flight with what it produced so the redirect lands exactly where the misunderstanding forms)](kb-notes/methodology-an-incentive-teaches-where-the-finish-line-is.md) | methodology | published | 2026-08-06 | 2026-08-06 (SkyPlan) |
| [Methodology — a label that decides behaviour is a policy switch, not a label (if code infers behaviour by pattern-matching a human-editable string then editing that string silently changes the maths and the editor has no way to know: `wantsUnits()` string-matched "headcount" in a metric LABEL to choose between CPL-FTES and student scoring, so a curator retitling a metric in the live config moved that priority onto `sizeOf(c) × target_rate` where target_rate is a headcount-era PERCENTAGE applied to credit FTES — a category error reachable by a typo-level edit, with no surface displaying the choice; the fix is an explicit `unit` field, but making it explicit is not enough — the unit is a property of the METRIC so it must resolve from the same LAYER, since a naive SCENARIO ?? SHARED ?? BASE lookup inverts the bug and lets a layer that retitles a metric without declaring a unit inherit the baked one; the test is simple — if changing the words changes a number, it is a switch)](kb-notes/methodology-a-label-that-decides-behaviour-is-a-policy-switch.md) | methodology | published | 2026-08-06 | 2026-08-06 (SkyPlan) |
| [Playbook — diagnose a stalled GitHub Actions repo by checking `runner_id` before anything else (a uniform failure duration across unrelated workflows is not a code signature, it is a TIMER: `runner_id: 0` with `runner_name: ""` and `started_at == created_at` means no runner was ever assigned and the job was cancelled at the ~15-minute allocation timeout, which is also why `get_job_logs` returns "No failed jobs found" on a run whose conclusion is `failure`; diagnostic order — runner_id first, then whether non-runner checks like secret scanning still pass, then compare against a commit predating your changes, and only THEN consider billing or environment rules; two wrong turns named — a wedged run can report an approval gate that does not exist in the environment config, and public repos get free unlimited hosted runners so billing cannot be the cause regardless of the billing page; the one repo-side lever is renaming the `concurrency` group to route around an uncancellable wedged run, which does nothing for starvation — establish which you have first; confirmed after the fact by GitHub's own incident, published three hours after the symptoms began)](kb-notes/playbook-diagnose-a-starved-actions-runner.md) | playbook | published | 2026-08-06 | 2026-08-06 (SkyPlan) |
| [Methodology — crosswalking a partner's occupation list to CPL: curate the JUDGMENT, not the run (a workforce partner's list carries job/apprenticeship titles while MAP is keyed by CREDENTIAL titles, with no mechanical join between the vocabularies — so persist the expensive occupation→credential reasoning in a shared normalized-key map and let each run be disposable, with every run's `unmapped.json` serving as the curator worklist so coverage COMPOUNDS across partners instead of restarting; keep "no CPL exists" (curated finding, a build-it opportunity) structurally distinct from "nobody has looked", or unreviewed rows launder into confident absence claims in a partner-facing deliverable; tier every match Direct vs Related because a crosswalk that flattens the two over-promises to students; take statewide from the ADOPTION file — `statewide_data.js` has 138 CCC-Collaborative titles vs the credential reference's 84, a strict SUBSET whose 54-title delta is exactly the contractor-licence/apprenticeship/NCCER cohort trades lists are full of; union adopters across statewide AND local records because the question is "where can my student get credit", which means counts here legally exceed the statewide tab's; and split each nearby college's portfolio into academic credit-by-exam vs career/technical, because a partner assumes their in-county college is the referral target and the raw count hides that it isn't — SJCOE run 1: San Joaquin Delta carries 69 credentials of which ONE is career/technical, while Modesto Junior has 265)](kb-notes/methodology-partner-occupation-crosswalk.md) | methodology | published | 2026-08-05 | 2026-08-06 (SkyWalker) |
| [Methodology — a source file that abbreviates titles fakes an absence (operational data files are written to a column width, not to your regex: the statewide MIS course file writes `INTERCULTURAL COMM`, `Interpersonal Commun` and `BIO-ETHICS`, so matching the full word `communicat` returned not "fewer rows" but six confident FALSE ABSENCES — colleges reported as having no receiving course while the canonical course sat in their catalog; fold punctuation once at the boundary and match the stem the file actually writes, and apply it to the RANKING regexes too because fixing only the candidate-generation patterns reproduces the identical symptom from a second location; never ship a "none found" you have not opened by hand, and write the absence as a sentence carrying its own provenance — a false negative is silent and produces a clean, plausible, shorter list nobody questions, where a false positive is loud)](kb-notes/methodology-a-source-file-that-abbreviates-titles-fakes-an-absence.md) | methodology | published | 2026-08-12 | 2026-08-12 (SkyTouch) |
| [Reference — for "which colleges offer X", start at the course file, not the program file (the same TOP code finds 32 colleges in the statewide COCI PROGRAM export and 61 in the MIS COURSE file, because a COCI program record exists only where a college built a certificate or degree around the training and many deliver state-regulated occupational courses as noncredit COURSES with no award wrapped around them — COCI answers "what credential can a student earn here", `cb_course_basic` answers "what is actually taught here", and a partner asking where their people can train is asking the second; report the award level as a column, never as the filter that decides who is on the list, or 29 colleges drop out silently and a shorter list looks like a cleaner one — with the caveats that `cb_course_basic` is a TERM SNAPSHOT and that CNA providers are approved by CDPH, not the Chancellor's Office)](kb-notes/reference-course-level-mis-beats-program-level-coci.md) | reference | published | 2026-08-12 | 2026-08-12 (SkyTouch) |
| [Methodology — a governance artifact must measure itself, and render what it lacks (governance documents fail predictably: they describe intentions, drift silently, and have NO mechanism by which they could become wrong OUT LOUD; so split the artifact — STORE the reasoning and allocations, but COMPUTE at render time anything checkable against the system, with the test being "could this be false without anyone editing the file?"; worked example — a register recorded a contact-refresh cadence as "decided each semester" while the page read the nudge log, found ZERO rows, and printed "never run", which was the most actionable fact on it and existed only because it was measured rather than asserted, the corollary being that to audit governance you don't read the process, you measure whether it RAN; second rule — render the ABSENCE as loudly as the presence: ship unfilled owner fields visible, styled as gaps and counted in the headline, because blank cells make a page look finished while marked gaps make it a meeting agenda; preserve those markers on EXPORT or the artifact is honest on screen and flattering in the room where decisions get made; and test the honesty properties directly, since they decay first under later edits — every future contributor feels mild pressure to make the page look better)](kb-notes/methodology-a-governance-artifact-must-measure-itself.md) | methodology | published | 2026-08-05 | 2026-08-05 (SkyMail) |
| [Methodology — when you can't decide for someone, route to a determination they already made (filling an empty field for an AUTONOMOUS population — locally-governed colleges, independent teams — is a decision you lack standing to make, and "pick a sensible default and tell them" is what draws the objection; instead find a choice they ALREADY made sitting unused in some system and propagate it, which reframes the act from deciding to ROUTING; order the cascade by how close each source sits to the actual job (not by how well-populated it is), stop the cascade exactly where the source stops being their choice about THIS question — a cascade that covers 100% has become a guess with extra steps — and state the provenance in the message, because "you already named this person" IS the warrant; the check for drift: did they choose this person for something reasonably like what I'm using it for?; an empty cell with a reason beats a filled cell that's wrong, because it can be worked while the wrong one just looks finished; worked example — 25 of 123 colleges had no student-facing CPL contact, 17 resolved from their own MAP designations, and the 8 left over split into two genuinely different problems a forced default would have hidden)](kb-notes/methodology-route-to-a-determination-they-already-made.md) | methodology | published | 2026-08-05 | 2026-08-05 (SkyMail) |
| [Methodology — recompute a document's figures from the live engine, don't copy them (a number in a human-authored document that your system also derives is a claim to VERIFY, not a source to copy: the Sep-BOG workbook's $74M total double-counted $3M via a cross-cut subtotal summed on top of the pot it's drawn from — real total $71M — and its "Max Award $665,971" was ~$144K stale vs the live model's $522,239 because it predated the $1M NC + $1M rural carve-outs and the credit-FTES basis; recompute from the authoritative engine not the document's own arithmetic, check the denominator (a 119-recipient average paired with a 115-recipient max), and reconcile the delta OUT LOUD — the "why it diverges" is what stops it recurring)](kb-notes/methodology-recompute-a-documents-figures-from-the-live-engine.md) | methodology | published | 2026-08-05 | 2026-08-05 (SkyOptIn) |
| [Methodology — provenance is a field, not a footnote, and a two-state detector is wrong about the middle (when one column is assembled from sources of different quality — system of record, a colleague's knowledge, a web lookup — rendering them identically forces the consumer to trust all or none; put the SOURCE on every row next to the value, which then lets different tiers carry different PERMISSIONS: a web lookup may only yield a department inbox, a curator may name an individual because they know who actually answers; record WHO and WHEN so a value stays questionable later instead of only believable-or-deletable; the same idea as a detector corollary — a REAL/FAKE gate meets something real-but-weak-signalled and reports it as fake, so when the cost of a false negative is "we tell someone their data doesn't exist," add a middle state that escalates to a human rather than deciding; sparse is not fake, a fake column returns NOTHING)](kb-notes/methodology-provenance-is-a-field.md) | methodology | published | 2026-08-05 | 2026-08-05 (SkyMail) |
| [Methodology — a published asset with zero uptake is an outreach worklist, not a build backlog (systems measure things BUILT and volume FLOWED, so the built-and-unused fall invisibly between the two metrics everyone already watches; query the join filtered to zero, set a floor so mid-rollout isn't mistaken for dormant, rank by REACH not age, and compute it live because a hand-copied list sends people chasing finished work; a dormant published asset is the cheapest volume in the system because the expensive part — the faculty determination, the approval, the sign-on — is already done, which inverts the reflex from "build more" to "light up what exists"; worked example — 49 dormant statewide CPL exhibits across 252 college-slots, and only 30 of 84 statewide exhibits had EVER converted a unit)](kb-notes/methodology-dormant-asset-worklist.md) | methodology | published | 2026-08-05 | 2026-08-05 (SkyPartner) |
| [Methodology — split prose from state: the register is the spine, the narrative cites it (when a long analysis doc also has to be an operational surface, do NOT render the doc in the tool — that's two copies with two edit paths and guaranteed drift; keep REASONING in markdown, STATE in a structured register, and DERIVED facts computed at render time, with the test being "does it change without the argument changing?"; then make the narrative cite the register by `[[ITEM-ID]]` so a claim can only point at a status rather than restate one — and the property worth having falls out third: an UNCITED claim becomes visibly unbacked, detectable by looking instead of by remembering to check; enforce with a test that every ref resolves, or the traceability quietly rots; pull report prose from the RENDERED DOM so the export can't disagree with the screen)](kb-notes/methodology-register-is-the-spine-narrative-cites-it.md) | methodology | published | 2026-08-05 | 2026-08-05 (SkyPartner) |
| [ADR — live notes sit alongside the curated register, with a promotion path (a list of items labelled "Needs Input" that you can't answer IN PLACE is a to-do list that can't be completed — but letting the UI write into the curated JSON puts a live-edited file in the PR flow and the cron's path and destroys the audit trail, while a live store with no path OUT becomes a graveyard; so: both, with an explicit promotion gate; keyed by ITEM ID so one affordance covers every section with no schema change when a section is added; "answering never closes, just revises" enforced structurally — a supersede RPC that links the pair in ONE round trip, and NO delete policy at all, because a ruling living only in a code comment gets violated by the next contributor; gate READS too when the surface is private, and verify by seeding a row and confirming anon reads zero rather than by reading the policy)](kb-notes/adr-notes-alongside-the-curated-register.md) | adr | published | 2026-08-05 | 2026-08-05 (SkyPartner) |
| [Methodology — answering "which programs at &lt;college&gt; have CPL for apprentices?" — the three-artifact join (the Common Exhibit Reference is keyed by EXHIBIT, not college/program, so a college-program roster is a join: `credential_reference_data.js` exhibits→college courses ⋈ `coci_programs_data.js` degrees ⋈ `coci_program_course_file.csv.gz` membership; the load-bearing caveat — the program→course join is a LOWER BOUND because the COCI program-course file is partially populated (ARC's Scaffold Erector & Hardwood Floor Layer degrees were absent despite having CPL courses), so corroborate degree counts vs the by-subject sponsor rollup + COCI program titles; the ARC/NorCAL-Carpenters slide seed — 149 exhibits · 5 sponsors · 13 A.A./A.S. programs; `_college_apprenticeship_cpl_roster.js`)](kb-notes/methodology-college-apprenticeship-cpl-roster.md) | methodology | published | 2026-08-03 | 2026-08-03 (SkyCobi) |
| [Methodology — populate-on-file-drop seam for grounded reference data (when the only reachable source is the wrong edition / lossy / a summarizer, do NOT fabricate to avoid an empty deliverable: builder reads an authoritative file from a known path IF present + validates against data you trust + emits only when non-empty so a no-source build is byte-identical; consumer shows the value when present, else the prior grounded display; report the blocker and name the exact file — the NCES 4-digit CIP title `sub4` seam)](kb-notes/methodology-populate-on-file-drop-seam-for-grounded-reference-data.md) | methodology | published | 2026-07-29 | 2026-07-29 (SkyMark) |
| [Reference — CDCP, course-level, and the CIP-count rule (credit=1 / noncredit=1 / noncredit-CDCP=2; CDCP earns higher apportionment as "Special Populations"; CDCP is a COURSE-level property read from `CreditType` "Enhanced Funding" — NOT inherited from the program; the CTE/Non-CTE "Both" choice)](kb-notes/reference-cdcp-and-the-cip-count-rule.md) | reference | published | 2026-07-28 | 2026-07-28 (SkyLark) |
| [Methodology — single source of truth flows via the regenerated snapshot; de-hardcode consumers, and verify the join key (one Supabase field is authoritative → every surface must DERIVE it from `CPL_Data.js`; the work is deleting the hardcoded copies that drifted; a de-hardcode that reads the right SOURCE but joins on the wrong KEY is silently dead — the report `ACTIVITY_DESC` bug; adversarial-review the diff + guard with a rename-flow test)](kb-notes/methodology-single-source-of-truth-flows-via-snapshot.md) | methodology | published | 2026-07-27 | 2026-07-27 (SkyElemental — +Path A: two tables one tree, elect the entity table + demote the other to a by-id overlay) |
| [Reference — the workplan Activity↔Project linking model (`projects.workplan_activity` = the single HOME Activity + grouping-by-NUMBER, so its name-part may drift harmlessly; vs `workplan_activity_associations` = the N-to-N "Contributes to" cross-links; everything DISPLAYS off the first, so the second can be silently incomplete — 10/33 projects had no association row; the backfill SQL; deleting an Activity is 6 tables deep)](kb-notes/reference-workplan-activity-project-linking-model.md) | reference | published | 2026-07-27 | 2026-07-27 (SkyElemental) |
| [Methodology — in a parent/child ledger, totals sum PARENT rows only (the moment a financial table gains hierarchy it gains a silent, plausible double-count: a child row is DETAIL, never an addend, so the only correct total filters to rows with no parent; the same shape appeared THREE times in one day across three independently-authored artifacts — a board budget amendment's $74M grand total, a mockup built to explain that error, and the seeded database — so knowing the trap does not protect you, only an enforced invariant does; enforce in read path + renderer + tests, and assert BOTH the right total and that the naive sum is larger; companion rule — a computed total = Σ years is a defect DETECTOR, and a source row carries the appropriation once, never the appropriation plus its own spend plan)](kb-notes/methodology-parent-child-ledger-totals.md) | methodology | published | 2026-07-30 | 2026-07-30 (SkyReconcile) |
| [Methodology — a summary surface must share the unit of the detail it summarises (when a value and its target are computed at DIFFERENT sites, a test that asserts each side independently passes while the two disagree by a whole conversion factor — assert the RELATIONSHIP instead, recomputing the stated percentage from the surface's own two printed numbers so it must be internally consistent regardless of the data; a total must be in the units of the column it tops, and you assert the WRONG unit is absent rather than that some total is present; count the sites before fixing one, because a seam that covers the model but not the surfaces describing it is a half-seam; worked example — three summary surfaces read raw UNITS against a CPL-FTES target and called them "students", a 30× error that survived review three times because a big wrong number reads as "a big number")](kb-notes/methodology-a-summary-must-share-the-unit-of-its-detail.md) | methodology | published | 2026-08-01 | 2026-08-01 (SkyUnit) |
| [Methodology — omit, don't zero, a measure your source didn't supply (a zero and an absence are DIFFERENT claims and consumers act on them differently: a present `0` says "we looked and there were none", an absent key can be handled as "we didn't look" — so when a producer can't compute a metric because the pull lacked the column, emit no key at all and say so on stdout; the mirror image of the default-payout note (same root cause — a default standing in for "we don't know" — opposite direction), so whenever you add a metric ask what its ABSENCE pays in BOTH directions; the guard belongs in the suite as a NEGATIVE assertion, because the failure is invisible in review and at runtime; worked example — zeroing an unrequested column would have made every college in the state read as having posted nothing, i.e. earn $0)](kb-notes/methodology-omit-dont-zero-an-absent-measure.md) | methodology | published | 2026-08-01 | 2026-08-12 (SkyPro — + the render-side inverse: absence as achievement) |
| [Methodology — move one rung down the funnel to route around an upstream defect you can't fix (when a source metric is inflated by a defect the upstream owner cannot practically repair, don't apply a correction factor (it encodes today's defect as tomorrow's magic number) and don't wait on the owner — enumerate the pipeline stages downstream and measure at the EARLIEST one whose GRAIN the defect cannot reach; verify immunity explicitly, since "later" doesn't imply "immune", and stop before the stage so sparse you've traded inflation for absence; worked example — MAP's eligible units are inflated by ACE/JST skill-level duplication, our own arithmetic cross-checked at 1.0054 against the source's published totals, so the fix was to score APPLIED units (18% of eligible) rather than to correct eligible)](kb-notes/methodology-move-down-the-funnel-to-route-around-an-upstream-defect.md) | methodology | published | 2026-08-01 | 2026-08-01 (SkyUnit) |
| [Methodology — when you can't verify an assumption, ship the oracle beside it (if a computation rests on an assumption you cannot check from anything in the repo — someone else's data shape, a row grain, a population definition — do NOT pick the likelier reading and move on: take the reducer that can only fail in ONE direction, find an independent measurement already in reach, emit the comparison as DATA not a log line, and write down what each outcome would MEAN before the data arrives so a future reader with none of your context can act on it; never let the oracle overwrite, since the populations differ and silently correcting to it destroys the signal; worked example settled a sum-vs-take-first question that was unanswerable offline — ratio came back 1.0054, not 2.0, and it re-checks daily forever)](kb-notes/methodology-ship-the-oracle-with-the-assumption.md) | methodology | published | 2026-07-31 | 2026-07-31 (SkyQueue cont. 2) |
| [Methodology — a default payout masks the data gap beneath it (any rule of the form "if we can't measure it, pay/pass/allow it anyway" is simultaneously a POLICY and a BLINDFOLD: it sits on the same code path as every upstream failure that produces "no value" — a broken name join, a skipped builder branch — so it reads as correct right up until you remove it, which is exactly when it turns into a false accusation against whoever the gap belonged to; enumerate what reaches the default BEFORE removing it, fix the exposed defects in the SAME batch (the window between removing the mask and fixing what it hid is a window where the product lies about real institutions), make the remaining honest gap read differently from a measured zero, and add the assertion for the CLASS not the fix; includes the instrument — a COLLISION-CHECKED fuzzy entity join, which drops any key reachable from two entities rather than picking one, so it can add matches but never merge institutions)](kb-notes/methodology-a-default-payout-masks-the-gap-beneath-it.md) | methodology | published | 2026-07-31 | 2026-07-31 (SkyQueue cont.) |
| [Methodology — retire a mode toggle by making both modes coexist (when a user says a toggle "reads wrong," the bug is usually not in either mode but in a SCOPE MISMATCH between the two things the toggle separates — and the toggle is what makes that mismatch structurally invisible, since the user can never see both numbers at once; the signature is a VAGUE bug report plus two modes that are each individually defensible in isolation; fix by deleting the toggle and putting both figures in one cell, which is self-policing where a scope fix is not; the related shape — a toggle that swaps the UNIT OF ANALYSIS deletes the detail rows the user came to compare, so group with subtotal headers instead of switching; plus: name any sub-part carrying different confidence, since an aggregate of two confidence levels silently reports the stronger one, and rewrite the toggle's tests into the property the retirement was FOR)](kb-notes/methodology-retire-a-mode-toggle-by-coexistence.md) | methodology | published | 2026-07-30 | 2026-07-30 (SkyQueue) |
| [Methodology — recompute a source's own summary statistics from its line items (an authoritative document that states BOTH line items AND totals/averages/min-max: recompute every summary before adopting any of it — where they agree you have verified rather than trusted, where they disagree the PATTERN localizes the error; the two failure shapes (a grand total that double-counts a subtotal derived from its own slices; summaries computed over mismatched denominators) with the diagnostics for each; validate the recomputation against figures the source already publishes BEFORE using it to judge the source; and the trap — a reconciliation that ties out differently-scoped quantities assumes its own conclusion)](kb-notes/methodology-recompute-a-sources-own-summary-statistics.md) | methodology | published | 2026-07-30 | 2026-07-30 (SkyReconcile) |
| [Methodology — change the input, not the consumers: store the new source-of-truth field and derive the old field at the single seam (a curator's "% of headcount" input became a "$/student" rate; storing `per_student` and deriving `target_rate` in the one function that builds the objects left every downstream consumer reading `target_rate` unchanged — no consumer re-wired, no schema change, legacy rows self-migrate, tests assert the inverse while dollars stay put)](kb-notes/methodology-invert-an-input-derive-at-the-single-seam.md) | methodology | published | 2026-07-27 | 2026-07-27 (SkyMoney) |
| [Methodology — a team-curated Supabase table needs `team_pass_ok()` on UPDATE too, not just SELECT + INSERT (a reviewer-only UPDATE under a phrase-gated curate pane makes every team edit hit the PostgREST RLS zero-row trap — `200 []` → `checkWrite` reads a 403 → the phrase is cleared → lockout; grant team on SELECT+INSERT+UPDATE, keep DELETE reviewer-only, model destructive as a status change)](kb-notes/methodology-team-curated-table-needs-update-rls.md) | methodology | published | 2026-07-26 | 2026-07-26 (Sky10Men) |
| [Playbook — auto-write cpl_memory at every checkpoint (Phase 3 of the memory loop; own-writes land `proposed` until corroborated by a committed KB-note/PR source, a 2nd session, or Sam; supersede don't delete; keep the truth table lean; log every write; NOT a corpus sweep)](kb-notes/playbook-cpl-memory-auto-write-at-checkpoint.md) | playbook | published | 2026-07-24 | 2026-07-24 (SkyKnow) |
| [Methodology — achievement-based funding: the cap-and-earn model (allocation = a CAP; `earned = cap × min(1, actual ÷ target)`, proportional + capped, unearned rolls forward; the projection % becomes the achievement target; the measurability phase-in where the default for an *unmeasured* cell depends on WHY — metric-gap/feed-pending → advance, college-posted-nothing → $0 incentive, suppressed → $0 flagged; the invariants to assert)](kb-notes/methodology-achievement-based-funding-cap-and-earn.md) | methodology | published | 2026-07-24 | 2026-07-24 (SkyFriend) |
| [Methodology — recover a music bed from a mixed video by subtracting the clean voice stem (`music = mix − g·voice`, sample-aligned; g from least-squares, lag from cross-correlation; verify via consistent-g + ≥15 dB voice cancellation; leaves music & voice as separate stems to rebalance — no AI stem model, no re-encode of the music)](kb-notes/methodology-recover-music-bed-by-voice-stem-subtraction.md) | methodology | published | 2026-07-23 | 2026-07-23 (SkyVid) |
| [Methodology — warm a tinny voiceover with measured EQ; diagnose before you boost (measure band energy + spectrogram first; restore 80–320 Hz body, tame the presence edge, darken the top; two-pass loudnorm to −16 LUFS; the codec's lost top octave can't be recovered — don't fake "air"; A/B on the same sentence)](kb-notes/methodology-warm-a-tinny-voiceover-measured-eq.md) | methodology | published | 2026-07-23 | 2026-07-23 (SkyVid) |
| [Methodology — a hide affordance must suppress the item in the report too; reuse the class the export already strips, never the `hidden` attribute (a DOM-cloning export un-hides `[hidden]` to flatten collapsibles → the item reappears; mark hidden things `.fs-ov-hidden`/`.fs-withheld` so page-hide + print-hide + export-strip all come free; the whole per-section Hide toggle = 1 class + 1 reserved override key)](kb-notes/methodology-hide-must-suppress-the-export.md) | methodology | published | 2026-07-23 | 2026-07-23 (SkyVeil) |
| [Methodology — adversarially verify an id crosswalk before a live PK renumber (3 lenses: fresh ground-truth + blind classify + adversarial validation; catches a survivor-that's-also-a-dissolve-target + a false-"empty"-container that a paper plan misses; re-run the dry-run if you edit the map after verifying)](kb-notes/methodology-adversarial-verify-crosswalk-before-live-rekey.md) | methodology | published | 2026-07-21 | 2026-07-21 (SkyPlan) |
| [Methodology — rebuild a flattened diagram as a built slide (a "too busy" slide is often a full-bleed PNG carrying reference data; split tables to an appendix, build the narrative in 3 clicks — the road → who does what → what it's worth; the final state must read with no animation; ⭐ re-key the graphic and check its arithmetic against its own stated totals — a flattened diagram will publish a table it clipped at the slide edge; the `<p:timing>` click-build recipe)](kb-notes/methodology-rebuild-a-flattened-diagram-as-a-built-slide.md) | methodology | published | 2026-08-10 | 2026-08-10 (SkyDeck) **NEW** |
| [Reference — CPL at the pre-apprenticeship stage (⟨Sam, 2026-08-10⟩ three mechanisms: noncredit coursework · industry certifications · **clearing admission requirements for the apprenticeship itself** — the one nobody names, and it speeds *entry* not completion; ⚠ CSU does not recognise HS-CTE *articulated* credit for transfer but does recognise credit by exam; the four standalone NC institutions are at ZERO; ⚠ the 44% DAS withdrawal figure is not ours — attribute, don't assert)](kb-notes/reference-cpl-at-the-pre-apprenticeship-stage.md) | reference | published | 2026-08-10 | 2026-08-10 (SkyDeck) **NEW** |
| [Playbook — build a CPL executive/board deck, and fill an existing brand PPTX template (the tracker data map: live_metrics / fact_sheet / cpl_funding_data + the COBI 3 priorities; SB 111 / SB 135 legislative facts; python-pptx from scratch; the template-fill technique — read theme1.xml for palette+fonts, set the native title, delete the empty body placeholder, draw in the body band, reorder sldIds; install libreoffice-impress+poppler or fall back to pptx→HTML+Chromium)](kb-notes/playbook-building-cpl-executive-presentations.md) | playbook | published | 2026-07-20 | 2026-07-20 (StarBOG side-lane) |

| [Methodology — answer "eligible students & credits by program area × statewide/local" from the Credential Reference data (join the curated statewide category map to `credential_reference_data.js` on the credential title; read `students_served` + `eligible_credits`; use the `statewide` flag as the tier — but that flag = "has a CCC-Collaborative articulation," which diverges from "on the statewide CPL page," e.g. Paramedic License; exclude firearm/firestop false positives)](kb-notes/methodology-area-eligibility-rollup-from-cer.md) | methodology | published | 2026-07-20 | 2026-07-20 (StarEmber) |
| [Playbook — manually re-deploy GitHub Pages: dispatch a fresh `pages.yml` run, never `rerun_failed_jobs` (a transient Pages 503 fails only the deploy step so a green merge can leave the site stale; re-running the job dupes the `github-pages` artifact → "Multiple artifacts" error; dispatch a fresh run — own id, single artifact — and poll to success)](kb-notes/playbook-github-pages-manual-redeploy.md) | playbook | published | 2026-07-20 | 2026-07-20 (SkyCIP) |
| [Methodology — infer required-core coursework from embedded-certificate structure when the catalog flag is missing (COCI carries no required/elective designation; a course inside an embedded Certificate of Achievement (cert.courses ⊆ AS/AA) is a required-core proxy, ranked by field-family frequency; label it inferred, catalog authoritative — the CPL Pathways "method & magic" ordering)](kb-notes/methodology-embedded-cert-required-core-inference.md) | methodology | published | 2026-07-16 | 2026-07-16 (StarMora) |
| [Methodology — build a packaging dry-run: title-primary carve-outs, safe-default under-claim, review-bucket when no authoritative flag (reconcile the population count first; the theme must be the SUBJECT not incidental vocabulary; gate the under-serving direction behind human review; emit signal + confidence per row; the ESL 2,364→3 worked instance and its traps — rung codes masquerading as signals, short numeric ladders; the template for the Music/Dance/KINE passes)](kb-notes/methodology-packaging-dryrun-classification.md) | methodology | published | 2026-07-15 | 2026-07-15 (Session 117 — StarMarcus) |
| [Methodology — elicit curation doctrine with small, grounded, curated scenario batches, not the firehose (Sam bounced off the thousands-strong 🧠 panel; ≤3 forks/batch each a distinct open question; profile the discipline before bringing the edges; frame against the nearest settled precedent; capture the REASONING as named levers; the gate-as-interview corollary — a blind calibration gate's divergences ARE the unsettled forks)](kb-notes/methodology-curated-scenario-batches-doctrine-elicitation.md) | methodology | published | 2026-07-14 | 2026-07-14 (Session 115 — StarMagna) |
| [Methodology — filter live-derived counts against a current-catalog snapshot to drop retired identifiers (the CPL Pathways "sus count" root cause: MAP keeps retired course numbers alive; intersect with the current course catalog, key precisely college-scoped, fail-open per entity so an active record can never be dropped)](kb-notes/methodology-filter-live-counts-against-current-catalog.md) | methodology | published | 2026-07-14 | 2026-07-14 (StarMarathon side-lane) |
| [ADR — COBI org layer: one platform, "site" as a view dimension (not a repo/site per org) — the C&I subsite pilot: a masthead site-switcher + per-cohort curation phrase, cosmetic filtering, no data isolation yet](kb-notes/adr-cobi-org-layer.md) | adr | published | 2026-07-14 | 2026-07-14 (SkyFlyer side-lane) |
| [Methodology — TOP is a last-in-line signal, never a gatekeeper (faculty-entered in COCI with no data-entry gatekeeper → unreliable; the three-role rule (never gatekeeper / last-in-line corroborator / fuzzy search-filter), the discipline signal ladder, the "gate identity, keep display" ruling holding TOP-inferred disciplines out of the SUBJ4 identity vote, the 24%/17,059-row blast radius, and the CTE/CIP-crosswalk exceptions)](kb-notes/methodology-top-is-a-last-in-line-signal.md) | methodology | published | 2026-07-16 | 2026-07-16 (StarBoard) |
| [Methodology — detect mis-disciplined minted identities via subject-code cohort outliers + a two-signals-agree gate (a course whose assigned discipline is a small minority of its LOCAL subject-code cohort AND TOP/lexicon corroborate the SAME fix; covers the singletons the corroboration-gated rules skip; the DANCFOLK/MCOM/OT failures that motivate the 2nd signal; reuse for both correcting mis-mints and seeding blanks)](kb-notes/methodology-subject-cohort-discipline-outlier.md) | methodology | published | 2026-07-13 | 2026-07-13 (Session 113 — SkyTeleo) |
| [The CPL merge lens — the student-repeat test (would you make them repeat it? — anchored to Title 5 §55050's "similar" standard; learning-equivalence over subject-codes; the one test every merge/mint rule serves, ratified as Doctrine v0.6 P-1)](kb-notes/glossary-cpl-merge-lens-student-repeat-test.md) | glossary | published | 2026-07-13 | 2026-07-13 (Session 112 — SkyEmpyrean) |
| [Methodology — parse PDF column grids positionally (x/y anchors), never from linearized text (the MQ Index: a lone X loses its column → HUM/PE mis-binned + 8 disciplines silently dropped behind artifact keys; validate the full row census against an authoritative count)](kb-notes/methodology-positional-pdf-column-grids.md) | methodology | published | 2026-07-11 | 2026-07-11 (Session 112 — SkyEmpyrean) |
| [Playbook — resume a long multi-agent workflow across task death, model swaps, and spend caps (`resumeFromRunId` cache-replay; assemble from the journal not the task result; flag capped sub-findings)](kb-notes/playbook-resume-long-workflow-across-failures.md) | playbook | published | 2026-07-12 | 2026-07-12 (Session 112 — CCR wave 3) |
| [Methodology — derive pathway check-offs from live articulation data, never bake them (the 🎓 CPL Pathways pattern: curated requirements + live-derived ✓ coverage, no_count alt-tracks, published-default-vs-view-override stages, the print-window token block)](kb-notes/methodology-live-derived-pathway-checkoffs.md) | methodology | published | 2026-07-10 | 2026-07-10 (SkyIron side-lane) |
| [Methodology — re-key every DERIVED identity map when a re-mint permutes slots, or make consumers prefer the canonical store (the S110 "Finish Carpentry" phantom: coci_articulations `identities` never re-keyed by the fold — 681/693 stale)](kb-notes/methodology-rekey-derived-identity-maps.md) | methodology | published | 2026-07-09 | 2026-07-09 (Session 110) |
| [Methodology — a soft-delete overlay on dual-rendered rows must be scoped to ONE render layer (the Activity⇄Project mixup: immune id set at every consumer + render dedup + the structural-vs-semantic classifier lesson)](kb-notes/methodology-layer-scoped-soft-delete-dual-rendered-rows.md) | methodology | published | 2026-07-02 | 2026-07-02 (Session 95) |
| [Methodology — a click-outside-closes overlay must test the click TARGET, never walk ancestors (the Archive-radio bug; the precomputed-dispatch-path self-concealment: saves "work" with default form state)](kb-notes/methodology-overlay-close-on-backdrop-target-only.md) | methodology | published | 2026-07-02 | 2026-07-02 (Session 95) |
| [Reference — TMC confidence score & near-auto-approval: what data we hold vs still need (the 5 CO checks scored; contact hours = the one true gap; fresh-extract cadence; the mid-July 200+-backlog build order)](kb-notes/reference-tmc-confidence-data-requirements.md) | reference | published | 2026-07-01 | 2026-07-01 (Session 92 — StarFab) |
| [Methodology — a capped retrieval must rank by relevance; popularity is only a tiebreaker (the CPR miss: `ORDER BY rec_count` made 76% of the exhibit catalog unfindable; category columns out of the searched tsvector; meta stop-words)](kb-notes/methodology-capped-retrieval-ranks-by-relevance.md) | methodology | published | 2026-07-02 | 2026-07-02 (Session 93 — SkyReach) |
| [Methodology — every live database function needs a committed schema-of-record file (live-only SQL is unreviewable; update the file in the same commit as the migration; fetch live definitions before editing)](kb-notes/methodology-live-db-functions-need-committed-schema.md) | methodology | published | 2026-07-02 | 2026-07-02 (Session 93 — SkyReach) |
| [Methodology — Visual PDF read (not text parse) for layout-encoded facts (columns/OR-adjacency that get_text scrambles; fan one reader per doc + adversarial verify; land as a curated overlay, not inline regex; guard the apply with anchor/overlap/all-or-nothing + zero-drift diff)](kb-notes/methodology-visual-pdf-read-for-layout-encoded-facts.md) | methodology | published | 2026-07-01 | 2026-07-01 (Session 91 — SkyGOAT, the TMC "X OR Y" overlay) |
| [Reference — TMC / ADT data model & the C-ID auto-match (UPDATED S91: c-id.net authority union doubles coverage; slot.alts[] "OR" fold)](kb-notes/reference-tmc-adt-data-model.md) | reference | published | 2026-06-16 | 2026-07-01 (Session 91 — SkyGOAT) |
| [Methodology — Hybrid live-vs-manual value column (per-row source flag; live cells render verbatim + carry NO edit attr → read-only by construction; the live sync is a post-pass after the merges; single-store titles by repointing the render)](kb-notes/methodology-live-vs-manual-hybrid-column.md) | methodology | published | 2026-06-30 | 2026-06-30 (Session 85 — SkyLight, the Annual Workplan "Current") |
| [Methodology — Dark→light recolor mapping (COBI tokens; chips/trendlines contrast; guard the SOURCE when CSS is generator-injected)](kb-notes/methodology-dark-to-light-recolor-mapping.md) | methodology | published | 2026-06-30 | 2026-06-30 (Session 87 — SkyGuy, the light/glass theme) |
| [ADR — Obsidian sync via vault-side clone (not edge function)](kb-notes/adr-obsidian-vault-via-clone.md) | adr | published | 2026-05-27 | 2026-05-27 |
| [ADR — Supersede, don't mutate, at the synthetic identity layer](kb-notes/adr-supersede-dont-mutate-synthetic-layer.md) | adr | published | 2026-05-27 | 2026-05-27 |
| [Methodology — Two-mode sync (safe Mode A vs identity-touching Mode B)](kb-notes/methodology-two-mode-sync.md) | methodology | published | 2026-05-27 | 2026-05-27 |
| [Methodology — Derive whitelists from rendered DOM, not hardcoded lists](kb-notes/methodology-derive-from-dom.md) | methodology | published | 2026-05-27 | 2026-05-27 |
| [Methodology — Snapshot-with-stamp fallback for live-data dependencies](kb-notes/methodology-snapshot-with-stamp-fallback.md) | methodology | published | 2026-05-28 | 2026-05-28 |
| [Methodology — XSS audit when a previously-trusted field becomes curator-editable](kb-notes/methodology-xss-audit-on-curator-editable-fields.md) | methodology | published | 2026-05-28 | 2026-05-29 (#192 JSON-in-script class) |
| [Methodology — Parity test as the proof for a data-source cutover](kb-notes/methodology-parity-test-cutover-proof.md) | methodology | published | 2026-05-29 | 2026-05-31 (PR-1 closed the blank-vs-0 gap) |
| [Methodology — Re-mint split invariants (id↔SUBJ4, control-number atomicity, dry-run↔apply cross-check)](kb-notes/methodology-remint-split-invariants.md) | methodology | published | 2026-05-29 | 2026-05-29 |
| [Methodology — Retiring an auto-seeded data layer (check for curator decisions riding on it)](kb-notes/methodology-retiring-an-auto-seeded-layer.md) | methodology | published | 2026-05-30 | 2026-05-30 |
| [Methodology — Cosmetic relabel via a display-label map, not a stored-value rename](kb-notes/methodology-display-label-map-vs-data-rename.md) | methodology | published | 2026-05-31 | 2026-05-31 |
| [Methodology — Verify the consumer graph before migrating a data class (a dead reader means delete)](kb-notes/methodology-verify-consumer-before-migrating.md) | methodology | published | 2026-05-31 | 2026-05-31 |
| [Playbook — Auto-sync vault-side repo clones via Windows Task Scheduler](kb-notes/playbook-vault-sync-setup.md) | playbook | published | 2026-05-27 | 2026-05-29 (consolidation + plugins follow-up) |
| [Playbook — Measure-first Supabase migration (snapshot → validate → dry-run → workflow_dispatch apply → cutover)](kb-notes/playbook-measure-first-supabase-migration.md) | playbook | published | 2026-05-28 | 2026-05-28 (Phase 2) |
| [Playbook — Phase 2 projects migration scope (column map, KPI ladder contract, 6 forks for Sam)](kb-notes/phase-2-projects-migration-scope.md) | playbook | published | 2026-05-28 | 2026-05-28 |
| [Playbook — Cross-discipline over-merge re-mint scope (split algorithm, 60% de-corroboration, 6 forks for Sam)](kb-notes/over-merge-remint-scope.md) | playbook | published | 2026-05-29 | 2026-05-29 |
| [Playbook — Moving a generator-managed dashboard section to its own tab (end-anchor classify + sentinel marker)](kb-notes/playbook-move-generated-section-to-tab.md) | playbook | published | 2026-05-30 | 2026-05-30 |
| [Playbook — GitHub scheduled-workflow reliability (diagnose a missed cron + backstop-cron fix)](kb-notes/playbook-github-scheduled-workflow-reliability.md) | playbook | published | 2026-06-01 | 2026-06-22 (S68 — 3-cron ladder + transient-failure resilience) |
| [Playbook — Soft-delete a daily-generated entity via an overlay table + collapsed section](kb-notes/playbook-soft-delete-generated-entity-via-overlay.md) | playbook | published | 2026-06-29 | 2026-06-29 (S84 — project Table/Archive) |
| [Playbook — Lean custom GitHub Pages deploy for a large static repo (git archive + assert + workflow_run trigger)](kb-notes/playbook-lean-custom-github-pages-deploy.md) | playbook | published | 2026-06-29 | 2026-06-29 (S84 — 553→192 MB) |
| [Playbook — Full Excel retirement final scope (KPI ladder blank-vs-0 crux, D.* helpers, read_projects sunset, 5 forks)](kb-notes/excel-retirement-final-scope.md) | playbook | published | 2026-05-31 | 2026-05-31 |
| [Playbook — CPL Chatbox → dashboard integration + cpl-knowledge-base re-point (shared-backend finding, 6 forks)](kb-notes/cpl-chatbox-integration-scope.md) | playbook | published | 2026-06-01 | 2026-06-01 (Phase 1 SHIPPED + LIVE v14) |
| [Playbook — Redeploying a shared, live Supabase Edge Function safely (capture-before-redeploy, preserve verify_jwt, fail-closed)](kb-notes/playbook-deploy-shared-supabase-edge-function.md) | playbook | published | 2026-06-01 | 2026-06-01 |
| [Reference — Excel dependency audit + fix queue (every remaining .xlsx touchpoint; the "Update→Excel" button)](kb-notes/excel-dependency-audit.md) | reference | published | 2026-06-01 | 2026-06-01 (P1+P2+P4 done) |
| [Reference — Daily dashboard data pipeline (all 7 sources, every KPI's lineage, the committed daily dataset)](kb-notes/reference-daily-dashboard-data-pipeline.md) | reference | published | 2026-06-01 | 2026-06-01 |
| [Reference — Windows PowerShell scripting gotchas (PS 5.1 + Task Scheduler)](kb-notes/reference-windows-powershell-gotchas.md) | reference | published | 2026-05-27 | 2026-05-27 |
| [Reference — Session 26 codebase audit findings catalog (51 findings, ranked fix queue)](kb-notes/reference-codebase-audit-2026-06-01.md) | reference | published | 2026-06-01 | 2026-06-01 |
| [Reference — College short-name dataset + resolver (compact chips across CCR/EACR/CER)](kb-notes/reference-college-short-names.md) | reference | published | 2026-06-02 | 2026-06-02 |
| [Reference — TMC / ADT data model & the C-ID auto-match (the transfer chain, the two in-repo sources, the exact-match rule)](kb-notes/reference-tmc-adt-data-model.md) | reference | published | 2026-06-16 | 2026-06-16 |
| [Playbook — Purge committed PII from git history (CustomReport_latest.json; Rule-5-override force-push)](kb-notes/playbook-pii-history-purge.md) | playbook | published | 2026-06-01 | 2026-06-01 |
| [Playbook — EACR consolidation + master-detail gallery scope (Local+CCC merge, versioned gallery, 3 audience views, 4-phase ladder, backlog)](kb-notes/eacr-consolidation-scope.md) | playbook | published | 2026-06-01 | 2026-06-01 (Session 28: PR-4 prescriptive layer SHIPPED) |
| [Methodology — Versioned prototype gallery (preserve v1, stack v2, graduate the winner)](kb-notes/methodology-versioned-prototype-gallery.md) | methodology | published | 2026-06-01 | 2026-06-01 |
| [Methodology — Styling a native &lt;details&gt;: keep a visible affordance, drive the toggle in JS](kb-notes/methodology-styling-native-details-toggle.md) | methodology | published | 2026-06-01 | 2026-06-01 |
| [Playbook — Pre-stage optional auth before an external API enforces it (no-op until secret set)](kb-notes/playbook-prestage-optional-external-auth.md) | playbook | published | 2026-06-01 | 2026-06-01 |
| [Methodology — A self-contained injected component must paint its own canvas, not just its text](kb-notes/methodology-self-contained-injected-component-styling.md) | methodology | published | 2026-06-02 | 2026-06-02 |
| [Playbook — Resuming a frozen session: check `main` before rebuilding its in-flight work](kb-notes/playbook-resume-frozen-session-check-main-first.md) | playbook | published | 2026-06-02 | 2026-06-02 |
| [Playbook — Runner-as-proxy for an external API the sandbox can't reach (source/verify on a CI runner, commit back)](kb-notes/playbook-runner-as-external-api-proxy.md) | playbook | published | 2026-06-19 | 2026-06-28 (Session 81 — headless-browser escalation for a bot-protected host) |
| [Methodology — Ship generator changes live-on-merge when the artifact regenerates from committed inputs](kb-notes/methodology-ship-generator-changes-live-on-merge.md) | methodology | published | 2026-06-02 | 2026-06-02 |
| [Methodology — Adding a curation surface with a synthesized kb_curation namespace (zero schema migration)](kb-notes/methodology-kb-curation-synthesized-namespace.md) | methodology | published | 2026-06-02 | 2026-06-02 |
| [Methodology — Resolving the V4 articulation-ripple gate when folding a CER unclassified title](kb-notes/methodology-cer-fold-articulation-ripple-sync.md) | methodology | published | 2026-06-03 | 2026-06-03 |
| [Methodology — A consumer must guard fields the baked payload omits but the fallback fills](kb-notes/methodology-consumer-tolerate-omitted-baked-fields.md) | methodology | published | 2026-06-04 | 2026-06-04 |
| [Playbook — Merging two existing CER credentials (existing→existing fold)](kb-notes/playbook-cer-credential-merge.md) | playbook | published | 2026-06-04 | 2026-06-04 |
| [Reference — Dashboard UI design system (tokens + canonical components)](kb-notes/reference-ui-design-system.md) | reference | published | 2026-06-04 | 2026-06-04 |
| [Methodology — Commit the test harness; don't let verification evaporate](kb-notes/methodology-commit-the-test-harness.md) | methodology | published | 2026-06-04 | 2026-06-04 |
| [Reference — AP credit is a GE-Area mapping (the canonical anchor for AP/standardized-exam credentials)](kb-notes/reference-ap-credit-ge-area-canonicalization.md) | reference | published | 2026-06-04 | 2026-06-04 |
| [ADR — Student-impact counts in the public CER (aggregate + small-cell suppression)](kb-notes/adr-cer-student-impact-counts-privacy.md) | adr | published | 2026-06-04 | 2026-06-04 (Session 34 — per-college <2 + minimization + guard) |
| [Methodology — Turn a one-time PII audit into a standing guard (committed test over the public artifacts)](kb-notes/methodology-standing-pii-guard.md) | methodology | published | 2026-06-04 | 2026-06-04 |
| [Methodology — Consolidating near-duplicate course identities within one credential (the ordinal rule)](kb-notes/methodology-within-credential-identity-consolidation.md) | methodology | published | 2026-06-04 | 2026-06-04 (#310 worklist side: co-articulation + SUBJ4 gates) |
| [Reference — Recovering screen/computer-use reach on Claude Code (web)](kb-notes/reference-claude-code-web-environment-reach.md) | reference | published | 2026-06-04 | 2026-06-04 |
| [Methodology — Lazy-load heavy per-tab data behind tab activation](kb-notes/methodology-lazy-load-heavy-tab-data.md) | methodology | published | 2026-06-09 | 2026-06-09 |
| [Reference — CPL eligibility (military vs non-military) + the Exhibit CRs Catalog rollup](kb-notes/reference-cpl-eligibility-and-exhibit-cr-catalog.md) | reference | published | 2026-06-09 | 2026-06-09 |
| [Methodology — Cron-as-window (reach egress-blocked data via a workflow + run logs)](kb-notes/methodology-cron-as-discovery-window.md) | methodology | published | 2026-06-09 | 2026-06-09 |
| [Methodology — Triaging credential-dedup candidates (merge vs leave-split; scope-of-competency)](kb-notes/methodology-credential-dedup-triage.md) | methodology | published | 2026-06-09 | 2026-06-09 |
| [ADR — Official C-ID/CCN ids are the common course reference; M-IDs only where none exists (merge-into-official, honors rule, descriptor-catalog targets)](kb-notes/adr-official-ids-as-common-course-reference.md) | adr | published | 2026-06-10 | 2026-06-10 |
| [Methodology — Umbrella-discipline SUBJ4 split (one MQ discipline, many subjects)](kb-notes/methodology-umbrella-discipline-subj4-split.md) | methodology | published | 2026-06-09 | 2026-06-09 |
| [Methodology — Fan-in discipline convergence (fold alternate names to a canonical)](kb-notes/methodology-fan-in-discipline-convergence.md) | methodology | published | 2026-06-10 | 2026-06-11 (guard 7) |
| [Methodology — Re-key every id-keyed artifact (the severed promotions index; registry + conservation gates + drift detector)](kb-notes/methodology-rekey-every-id-keyed-artifact.md) | methodology | published | 2026-06-11 | 2026-07-27 (SkyElemental — 2nd instance: #872 left `workplan_goals` + associations behind; + the `information_schema` discovery step) |
| [Methodology — Auto-layout tables silently park columns off-pane (fixed layout + the inspector rung)](kb-notes/methodology-fixed-table-layout-off-pane-columns.md) | methodology | published | 2026-06-11 | 2026-06-11 |
| [`methodology-user-vocabulary-category-maps.md`](kb-notes/methodology-user-vocabulary-category-maps.md) | methodology | Rollup labels owned by the user = curated JSON (`titles{}` source of truth) + `^`-anchored ordered patterns as drift insurance + visible fallback bucket; merge-preserving seeder | 2026-06-11 |
| [`methodology-college-homonym-subject-codes.md`](kb-notes/methodology-college-homonym-subject-codes.md) | methodology | Subject codes are college-local vocabulary: detect homonym lexicon entries (TOP-division votes + minority-title evidence), SCOPE them per college instead of removing, and make inference passes RETRACT their own stale fills (the CRIM M1003 case) | 2026-06-11 |
| [Methodology — Title-similarity merge candidates: the guard suite and the licensure-spec lesson](kb-notes/methodology-title-similarity-merge-guards.md) | methodology | published | 2026-06-12 | 2026-06-12 |
| [Methodology — SUBJ4-consumer semantics: umbrella allowances + alias families mirrored everywhere](kb-notes/methodology-subj4-consumer-semantics.md) | methodology | published | 2026-06-12 | 2026-06-12 |
| [Methodology — re-mint applies: recompute through the dry-run's own allocator, gate on byte-fidelity to the reviewed plan](kb-notes/methodology-apply-equals-spec-via-shared-allocator.md) | methodology | published | 2026-06-12 | 2026-06-12 |
| [Methodology — Witness-kinship gate (historical receipts need a present-tense validity check)](kb-notes/methodology-witness-kinship-gate.md) | methodology | published | 2026-06-11 | 2026-06-11 |
| [Methodology — Alias maps are permutations, not graphs (slot-reuse resolution semantics)](kb-notes/methodology-alias-map-resolution-semantics.md) | methodology | published | 2026-06-11 | 2026-06-11 |
| [`kb-notes/methodology-fanin-alias-lexicon-contamination.md`](kb-notes/methodology-fanin-alias-lexicon-contamination.md) | Fan-in folds must re-point inference lexicons (the PEDU incident) | Session 51 |
| [`kb-notes/playbook-gated-bulk-autocuration.md`](kb-notes/playbook-gated-bulk-autocuration.md) | Gated bulk auto-curation: dry-run receipt → human skim → md5-pinned server-side apply (auto-merge pass 1) | Session 53 |
| [Methodology — Promoted-record ghosts in candidate worklists (exclude promoted orphans at join time)](kb-notes/methodology-promoted-record-ghosts-in-worklists.md) | methodology | published | 2026-06-15 | 2026-06-15 |
| [Methodology — A similarity threshold can't bridge a zero-overlap synonym; use a curated synonym map](kb-notes/methodology-synonym-map-vs-similarity-threshold.md) | methodology | published | 2026-06-16 | 2026-06-18 (Session 62 — candidate validator) |
| [Playbook — Re-keying a shared DB from a committed re-mint alias map (service-key script in CI + md5 set-equality)](kb-notes/playbook-rekey-shared-db-from-alias-map.md) | playbook | published | 2026-06-15 | 2026-06-15 (Session 56 — the UC-CUR→Z re-mint) |
| [Methodology — Rank a cleanup queue by downstream impact, not structural leverage](kb-notes/methodology-rank-cleanup-by-downstream-impact.md) | methodology | published | 2026-06-09 | 2026-06-09 |
| [Methodology — Coarse TOP-division discipline fallback (make the orphan tail visible)](kb-notes/methodology-coarse-top-division-discipline-fallback.md) | methodology | published | 2026-06-09 | 2026-06-09 |
| [Methodology — Surface a finer derived layer on a single-grain reference tab](kb-notes/methodology-surface-derived-layer-on-single-grain-tab.md) | methodology | published | 2026-06-09 | 2026-06-09 |
| [ADR — Per-college funding-priority metric counts (aggregate + suppression)](kb-notes/adr-funding-priority-metrics-privacy.md) | adr | **RATIFIED** | 2026-06-11 | 2026-06-11 (Sam: "Yes on forks"; shipped #364) |
| [Reference — The P1 completion-data gap (why completions aren't in MAP; the strategy ladder out)](kb-notes/reference-p1-completion-data-gap.md) | reference | published | 2026-06-11 | 2026-06-11 |
| [Methodology — Maintaining a committed-workbook model (one-shot revisions, input-driven builder)](kb-notes/methodology-committed-workbook-models.md) | methodology | published | 2026-06-11 | 2026-06-11 |
| [Methodology — Derive theme tokens from brand seeds with a contrast script (the mock is the spec)](kb-notes/methodology-derived-aa-token-palette.md) | methodology | published | 2026-06-12 | 2026-06-12 |
| [Methodology — Retheming a generator-owned HTML monolith via token value-swap](kb-notes/methodology-token-retheme-on-generated-html.md) | methodology | published | 2026-06-12 | 2026-06-12 |
| [Reference — Public-domain art sourcing for the dashboard (Adams 79-AA, CA Impressionists, the four traps)](kb-notes/reference-public-domain-art-sourcing.md) | reference | published | 2026-06-12 | 2026-06-12 |
| [ADR — Level-collapsing consolidation: over-merge beats under-merge for CPL (Title 5 §55050; suggestions-only so safe to be aggressive)](kb-notes/adr-level-collapsing-consolidation.md) | adr | published | 2026-06-16 | 2026-06-16 |
| [ADR — Reference-data home: committed JSON by default, Supabase only for live curation](kb-notes/adr-reference-data-committed-json-vs-supabase.md) | adr | published | 2026-06-18 | 2026-06-18 |
| [Methodology — Join loose institutional datasets on the coded key, not freehand text](kb-notes/methodology-coded-key-over-freehand-text-join.md) | methodology | published | 2026-06-18 | 2026-06-18 |
| [Playbook — Digest an anonymous write-only table into a private vault (service-role in CI, output never touches the public repo)](kb-notes/playbook-write-only-table-private-vault-digest.md) | playbook | published | 2026-06-18 | 2026-06-18 |
| [Playbook — Embed a self-contained auth-gated bundle as a dashboard tab (iframe; directory-`src` + `target="_top"`)](kb-notes/playbook-embed-auth-gated-bundle-as-dashboard-tab.md) | playbook | published | 2026-06-19 | 2026-06-19 |
| [Methodology — In-browser doc capture → Claude → tokenless GitHub write (extract-text-to-dodge-proxy-cap, vision blocks, create-new-file deep-link)](kb-notes/methodology-browser-doc-capture-to-claude-and-github.md) | methodology | published | 2026-06-19 | 2026-06-19 |
| [Playbook — A live Edge Function 502 is often a RETIRED model id (diagnose via logs, fix via model swap)](kb-notes/playbook-edge-function-502-retired-model.md) | playbook | published | 2026-06-19 | 2026-06-19 |
| [Scope — CPL Assistant CCR/CER-grounded recommendations + real-time benchmark + landing-site demand signal (D1–D5, build ladder, Custom Reports opportunity)](kb-notes/cpl-assistant-ccr-cer-recommendation-scope.md) | scope | published | 2026-06-19 | 2026-06-19 |
| [Scope — TMC tab → CO-staff ADT review/processing tool (validated PCF↔COCI joins, CO-first phased plan; S92: C-ID coverage limiter CLOSED, honest-limits repointed at the confidence-data-requirements note)](kb-notes/tmc-co-review-scope.md) | scope | published | 2026-06-20 | 2026-07-01 (Session 92 — StarFab) |
| [Reference — ADT acceptance & course-substitution rules (ASCCC C-ID guidance + STAR Act; the tiered-acceptance engine, structural checklist, per-TMC flexibility)](kb-notes/reference-adt-acceptance-rules.md) | reference | published | 2026-06-20 | 2026-06-20 |
| [Playbook — Unattended news aggregation into a live dashboard tab (free adapters, capability-probe auth, closed-social manual queue, curation boundary)](kb-notes/playbook-cpl-news-aggregation.md) | playbook | published | 2026-06-21 | 2026-06-21 |
| [Methodology — Reworking a generator-managed section without touching the generator (anchor-park + CSS-from-JS + run-twice idempotency)](kb-notes/methodology-regen-safe-section-rework.md) | methodology | published | 2026-06-22 | 2026-06-22 |
| [Scope — TMC ADT supporting-document (COR) upload (private Storage bucket + tmc_submission_docs index; submitting-college uploads; two-feeds-one-model; COCI-embed/SSO graduation path)](kb-notes/tmc-adt-document-upload-scope.md) | scope | published | 2026-06-22 | 2026-06-22 |
| [Reference — CCR curation sync & live-merge durability (instant fold + Supabase replay vs the cron's downstream regen; per-click regen is infeasible)](kb-notes/reference-ccr-curation-sync-and-live-merge.md) | reference | published | 2026-06-23 | 2026-06-23 |
| [Reference — Common SUBJ vs Local SUBJ, and the real discipline↔subject invariant (one discipline → one canonical SUBJ4; C-ID/CCN verbatim; the cardinality table)](kb-notes/reference-common-vs-local-subj-and-discipline-cardinality.md) | reference | published | 2026-06-23 | 2026-06-23 |
| [Methodology — A stacked PR can squash to an empty commit; verify `main` has the diff (branch fresh per PR; the receipt is not the outcome)](kb-notes/methodology-stacked-pr-empty-squash.md) | methodology | published | 2026-06-23 | 2026-06-23 |
| [Methodology — Forward-looking display for a curate-now / re-key-later split (show the destination + a pending marker; don't let the deferred backlog skew the derived map)](kb-notes/methodology-forward-looking-display-curate-now-rekey-later.md) | methodology | published | 2026-06-24 | 2026-06-24 |
| [Reference — Common-Course level convention (Beg/Int/Adv; the §55050 classifier — ranges/words/ordinals win, bare numbers are a curator-overridable hint; the Levels filter + chip)](kb-notes/reference-course-level-convention.md) | reference | published | 2026-06-25 | 2026-06-25 |
| [Reference — Statewide Collaborative (CCC) credit recommendations are not housed at one college (system-wide standards; access via your OWN college's landing page)](kb-notes/reference-statewide-credit-recommendations.md) | reference | published | 2026-06-25 | 2026-06-25 |
| [Methodology — Build a hierarchy from dotted ids without migrating stored keys (id-prefix parenting; re-tier visually, keep the persisted key stable)](kb-notes/methodology-tree-from-dotted-ids-stable-keys.md) | methodology | published | 2026-06-26 | 2026-06-26 |
| [Methodology — Refresh the access token before every write (don't trust a format-valid JWT; the silent-401 phantom-save trap)](kb-notes/methodology-refresh-token-before-write.md) | methodology | published | 2026-06-26 | 2026-06-26 (Session 77 — StarPort) |
| [Methodology — Overlay live data onto generated cards via a stamped data-key hook (generator stamps the hook, a static read-only JS overlay fills it)](kb-notes/methodology-live-overlay-onto-generated-cards.md) | methodology | published | 2026-06-26 | 2026-06-26 (Session 78 — SkyMap) |
| [Reference — Authority-anchored credential naming: CareerOneStop Certification/License Finder + O*NET + Credential Engine as the CER's established naming layer (strategy, terms, decision queue)](kb-notes/reference-authority-anchored-credential-naming.md) | reference | published | 2026-07-07 | 2026-07-07 (Session 100 — SkyVault) |
| [Reference — Issuing-agency authority sources for CER exhibit canonicalization (DIR DAS apprenticeship registry, NCCER catalog, CSLB queue, house issuer names)](kb-notes/reference-issuing-agency-authority-sources.md) | reference | published | 2026-07-07 | 2026-07-07 (Session 103) |
| methodology | Bulk-writing beside a live curator — snapshot decay, conflict-yielding writes, audit-the-live-overlay, presence-conditional spots (Session 108) | [`methodology-live-curation-concurrency.md`](kb-notes/methodology-live-curation-concurrency.md) |
| [Methodology — Paginate every PostgREST read that can outgrow 1,000 rows (the silent-truncation trap: arbitrary-order 1,000-row cap made saved work render as unsaved)](kb-notes/methodology-paginate-postgrest-reads.md) | methodology | published | 2026-07-08 | 2026-07-08 (Session 105 — SkyClose) |
| [Methodology — Re-arm a success-disabled Save button the moment its inputs change (the dead-button trap on saved-then-re-edited rows)](kb-notes/methodology-rearm-disabled-save-on-edit.md) | methodology | published | 2026-07-08 | 2026-07-08 (Session 106 — SkySeal) |
| [Methodology — Confirmed merges via a decision row: never infer, never block the clean set (a colliding rename waits in a non-blocking queue until the curator's exact-target confirm; the apply folds with dedupe + drift guards)](kb-notes/methodology-confirmed-merge-via-decision-row.md) | methodology | published | 2026-07-08 | 2026-07-08 (Session 107 — SkyKey) |
| [Methodology — CER ⇄ CCR crossover: course-identity integrations for exhibit-title curation (Rule 5c wire, college-scoped joins, ranked next integrations)](kb-notes/cer-ccr-crossover-integrations.md) | methodology | published | 2026-07-07 | 2026-07-07 (Session 104 — Bruh SkyTime) |
| [Reference — The authoritative statewide MAP exhibit is the raw row with `Collaborative Type == "CCC"` (adopt/adapt copies tag CCC too → unified grouping over-counts; filter the raw column, don't dedup)](kb-notes/reference-authoritative-statewide-exhibit-signal.md) | reference | published | 2026-06-28 | 2026-06-28 (Session 79 — StarBender) |
| [Playbook — A standalone public page on the COBI Pages site (sits-alone subdir, live data via `../live_metrics.json`, non-tab nav anchor, print-to-PDF)](kb-notes/playbook-standalone-public-page.md) | playbook | published | 2026-06-25 | 2026-06-25 (Session 74 — the Fact Sheet) |
| [Playbook — Curate-editable overlay for a standalone static page (content-agnostic Supabase overlay + DOM-walked stable keys → tiny HTML diff; reviewer magic-link; allowlist sanitizer)](kb-notes/playbook-curate-editable-standalone-page.md) | playbook | published | 2026-06-28 | 2026-06-28 (Session 81 — boxes/images extension) |
| [Methodology — Separate affordance visibility from action eligibility (show the action everywhere; enforce opt-out / no-target in the data layer; test eligibility via the recipient/href, not button presence)](kb-notes/methodology-affordance-visibility-vs-action-eligibility.md) | methodology | published | 2026-06-28 | 2026-06-28 (Session 81 — StarFarout, the per-row + per-card nudge) |
| [Methodology — Reserved-key namespaces extend a key→override table with new block types (no schema migration; match vs. materialize vs. parse; two delete semantics; Storage bucket for image bytes)](kb-notes/methodology-reserved-key-namespaces-on-overrides-table.md) | methodology | published | 2026-06-28 | 2026-06-28 (Session 81 — StarFarout, Fact Sheet boxes + images) |
| [Methodology — Stable DOM keys must exclude live (data-bound) text (a text-slug key over live metrics churns daily and orphans saved edits; key off the non-`[data-bind]` text)](kb-notes/methodology-stable-dom-keys-exclude-live-text.md) | methodology | published | 2026-06-28 | 2026-06-28 (Session 82 — SkyFlyer, Fact Sheet KPI/Vet-Sprint boxes) |
| [Playbook — Export a standalone page to Word (.doc) from the live DOM, no library (clone `<main>` → strip chrome → grid→table → mso-HTML Blob; reflects live data + Curate edits)](kb-notes/playbook-standalone-dom-to-word-export.md) | playbook | published | 2026-06-28 | 2026-06-28 (Session 82 — SkyFlyer, Fact Sheet ⬇ Word) |
| [Methodology — Server-enforced shared-password edit gate (no per-user accounts; header-checked RLS via `team_pass_ok()` + a not-anon-readable secret table; pseudo-session client; widen-with-OR so identity still works; **empty-Bearer 401 pitfall · validate-via-gate-RPC · reviewer-manage admin**)](kb-notes/methodology-server-enforced-shared-password-gate.md) | methodology | published | 2026-06-29 | 2026-06-29 later (Session 83 — the hardening: 401 fix + validation + admin) |
| [ADR — Surface, don't edit, a read-only system of record (no write API → deep-link into the source + own the nudge/accountability; don't build a shadow editor that drifts)](kb-notes/adr-surface-dont-edit-readonly-system-of-record.md) | adr | published | 2026-06-30 | 2026-06-30 (Session 87 — StarMax, the MAP Users nudge) |
| [Methodology — Three-layer scenario config `SCENARIO ?? SHARED ?? BASE` (baked data defaults ⊕ team-phrase-editable Supabase config ⊕ anonymous per-browser what-if; compute derived values live, promote scenario on unlock, layer-scoped reset, rollback on RLS no-op)](kb-notes/methodology-three-layer-scenario-config.md) | methodology | published | 2026-07-03 | 2026-07-03 (Session 98 — the Implementation Funding rework) |
| [Methodology — Model-driven house-format document generator (memo/letter/report/brief from the live model, editable `contenteditable`, export to PDF + Word via a DOM→docx walker over `docx.min.js`; no LLM/backend)](kb-notes/methodology-model-driven-document-generator.md) | methodology | published | 2026-07-23 | 2026-07-23 (SkyFunder — the ESS-25-82 Report sub-tab) |
| [Methodology — Generalize a hardcoded calc by reducing it to the old form, then assert conservation (classify terms by kind so the default config is exactly the old formula; pin a conservation test against the baked data, not the code; test the new knobs separately; real chars not HTML entities in input values)](kb-notes/methodology-generalize-a-calc-conserve-the-baseline.md) | methodology | published | 2026-07-23 | 2026-07-23 (SkyFunder — the add/delete pool boxes) |
| [Methodology — Retire a global dial into per-item dials without moving any numbers (pick the per-item neutral value to reproduce the old global exactly; fold any structural role it secretly did into the formula so neutral is a true identity; split the behavior-neutral merge from the post-deploy config activation; confirm factor direction — stringency vs price — first)](kb-notes/methodology-retire-a-global-dial-into-per-item-dials.md) | methodology | published | 2026-08-04 | 2026-08-04 (SkyUnit — the per-priority price factor, #971) |

---

## Lessons docs (`docs/*_lessons.md`)

Workstream-anchored scratchpads. Append a dated section every checkpoint.

| File | Workstream | Last touched |
|---|---|---|
| [`session_credentials_lessons.md`](session_credentials_lessons.md) | Session credentials — what "signed in" means across 26 modules, token expiry, rotation and browser-tab scope | 2026-08-15 (SkyKey) |
| [`team_phrase_lessons.md`](team_phrase_lessons.md) | Team phrase + reviewer sign-in — where a credential is entered | 2026-08-14 (Sky157) |
| [`cip_crosswalk_lessons_archive.md`](cip_crosswalk_lessons_archive.md) | CIP crosswalk — archived 2026-07-14 → 07-17 build-out | 2026-08-14 (compaction) |
| [`admin_tab_lessons.md`](admin_tab_lessons.md) | Admin tab · the side menu as data · the governance-red resolution | 2026-08-14 (SkyGate, Session 156) |
| [`sierra_credit_recs_lessons.md`](sierra_credit_recs_lessons.md) | Sierra credit recommendations & false absences — why POST listed one course of ten, why Cerritos ironworker CPL read as zero, and the statewide flag that came from the wrong file | 2026-08-13 |
| [`team_phrase_lessons.md`](team_phrase_lessons.md) | **Team phrase & site access** (SkyFund): which tabs offer a phrase box, which phrase a tab answers to, and why only an *exclusive* tab can carry a site phrase. Carries the Contracts swap that is written but deliberately unapplied, and the superset question that outranks it. | 2026-08-12 (SkyFund) |
| [`college_identity_lessons.md`](college_identity_lessons.md) | **College & district identity** (SkyLink): one taxonomy resolving every college/district name variant to MAP's `college_id` + the CCCCO MIS district code. Carries the scrambled-code-column finding and the two Appendix A parse defects. | 2026-08-12 (SkyLink) |
| [`college_action_page_lessons.md`](college_action_page_lessons.md) | **College action page & MAP-team queue** (SkyDesk → SkyLink): one page, not 123 — a college's own standing, funding, tier and next steps, plus the same engine pointed inward at the MAP team's backlog. Carries the waiting-credit composition finding and the RLS/matview blockers. | 2026-08-11 (SkyLink) |
| [`student_detail_load_lessons.md`](student_detail_load_lessons.md) | **Student-detail load** (SkyNaut): MAP's per-student export AND the credit funnel into Supabase — the four places the spec was wrong, the importer that duplicates silently, and the dormant-credit figure (published 1,051,870 / 63,991 at Needs Action and already-articulated; unsuppressed 1,052,531 / 64,074, with 13 of 111 colleges withheld under k=10) | 2026-08-08 (part 2) |
| [`partner_crosswalk_lessons.md`](partner_crosswalk_lessons.md) | **Partner occupation → CPL crosswalk** (SkyWalker): a training partner's occupation list → the CPL credit recommendations that already exist and the colleges honoring them. The reusable engine + the shared, growable occupation→credential map; SJCOE run 1 (139 occupations — 51 statewide / 53 local-only / 35 no-CPL-anywhere), the utility-line-work desert, and the in-county-college capacity trap. | 2026-08-06 (SkyWalker) |
| [`futuro_hth_crosswalk_lessons.md`](futuro_hth_crosswalk_lessons.md) | **Futuro Health HTH → CCC CNA crosswalk** (SkyTouch, Ashley's ask): the 6-week/80-hour Human Touch Healthcare soft-skills course mapped to every CCC teaching CNA. Why HTH points at the course BESIDE the CDPH-regulated 160-hour CNA course rather than at it; course-level MIS finding 61 colleges where program-level COCI finds 32; the abbreviated-title false absences; and Futuro Health sitting in MAP as entity 133 with zero exhibits. | 2026-08-12 (SkyTouch) |
| [Noncredit & Learning-Partner CPL](noncredit_cpl_lessons.md) | noncredit · not-for-credit · adult school · ROP · HS-Cx · apprenticeship — the six modes, the scalability ladder, the register/tab build | 2026-08-05 (SkyPartner) |
| [`cobi_summary_tools_lessons.md`](cobi_summary_tools_lessons.md) | **COBI summary-data tools** (SkyCobi side-lane): Sam's data ask → the ARC / NorCAL Carpenters apprenticeship-CPL slides. Pulled straight from source artifacts (EACR tab filters were broken): **ARC = 5 sponsors · 149 exhibits · ~13 A.A./A.S. apprenticeship degrees**; NorCAL Carpenters/CTCNC flagship = **120 exhibits → 8 degrees**. Built a 2-slide CCCCO-branded PPTX (delivered). Committed the reusable [`kb/_college_apprenticeship_cpl_roster.js`](../kb/_college_apprenticeship_cpl_roster.js) (any-college roster) — the seed of the COBI "College CPL Summary" tool Sam wants next. Recipe: [`methodology-college-apprenticeship-cpl-roster`](kb-notes/methodology-college-apprenticeship-cpl-roster.md). Read-only/deliverable lane — left `cpl_todos.json` + the numbered handoff to the CCR mainline | **SkyCobi side-lane (2026-08-03) NEW** |
| [`workplan_single_source_editor_lessons.md`](workplan_single_source_editor_lessons.md) | **Workplan single-source title/description editor + linking cleanup** (SkyElemental side-lane): the **Annual Workplan Goals tab is now the ONE place** to edit workplan titles + brief descriptions — `workplan_goals.js` gained project-description + Activity-title + Activity-description editors (→ `projects.description` / `workplan_goals.name` / new `workplan_goals.description`); generator emits `activity_desc` store→`CPL_Data.js`; **de-hardcoded** the drifted Activity copies in `raci.js`/`master_report.js`/`generate_reports.js` (adversarial review caught a dead de-hardcode via a join-key mismatch, fixed + rename-flow test). Also the **Element Map refresh** (#900, Excel→Supabase) + a **workplan data cleanup** (backfill 10 links, purge Activity 5 + 5.1/5.5 ghosts, delete tabled 5.1, retitle Activity 4). Continuation: [`workplan_single_source_editor_handoff.md`](workplan_single_source_editor_handoff.md); KB notes [`methodology-single-source-of-truth-flows-via-snapshot`](kb-notes/methodology-single-source-of-truth-flows-via-snapshot.md), [`reference-workplan-activity-project-linking-model`](kb-notes/reference-workplan-activity-project-linking-model.md) | **SkyElemental side-lane (2026-07-27, #900/#902 merged) NEW** |
| [`cobi_memory_tab_lessons.md`](cobi_memory_tab_lessons.md) | **COBI Memory tab + MAP Data Quality register** (Sky10Men side-lane): the 🧠 Memory 📄 Report → non-techie **prose** + short item **titles** + reader **`plain`** column + **✨ Autogenerate** (Add & Edit, cpl-chat RAG, prefill-only) — #894/#895; the Memory **curate lockout fix** (UPDATE RLS widened reviewer→reviewer-OR-team, the RLS zero-row trap) — #896; the new **🩺 MAP Data Quality register** (Supabase `map_data_quality` + team-gated tab, seeded with Sam's 4 issues incl. the **USMC JST skill-level CR duplication** `f8`/`o3`) — #897; and the **license** correction (vestigial MIT © 2019 Zachary Rice → CCCCO all-rights-reserved) — #898. Continuation: [`cobi_memory_tab_handoff.md`](cobi_memory_tab_handoff.md); KB note [`methodology-team-curated-table-needs-update-rls`](kb-notes/methodology-team-curated-table-needs-update-rls.md) | **Sky10Men side-lane (2026-07-26, #894–#898 merged) NEW** |
| [`cpl_portal_video_lessons.md`](cpl_portal_video_lessons.md) | **CPL Student Portal "Credit for Being You" intro video** (SkyVid side-lane): audio restoration + accessibility, all via `ffmpeg`/`numpy` (no repo code; deliverables handed to Sam directly). Corrected the auto-captions (CLAP→CLEP, CDL→CPL, 11 run-ons → 22 WCAG cues + `.srt` + transcript); de-tinnied the voice (measured EQ → −16 LUFS, "Broadcast"→"warmer"); **recovered the music bed by subtracting the clean VO stem** (`music = mix − 1.30·voice`; YouTube + Demucs both proxy-blocked) → warm voice + original music, +7 dB ducked, 2 s end-card freeze + fade. Continuation: [`cpl_portal_video_handoff.md`](cpl_portal_video_handoff.md); KB notes [`methodology-recover-music-bed-by-voice-stem-subtraction`](kb-notes/methodology-recover-music-bed-by-voice-stem-subtraction.md), [`methodology-warm-a-tinny-voiceover-measured-eq`](kb-notes/methodology-warm-a-tinny-voiceover-measured-eq.md) | **SkyVid side-lane (2026-07-23) NEW** |
| [`activity_reorg_lessons.md`](activity_reorg_lessons.md) | **COBI Activities tab reorg** (SkyPlan side-lane): realign the tab to the CPL Workplan (**4 Activities, not 5** — "Activity 5" is an invented fallback), fold the separate **Projects** category + the duplicative **Veteran/Apprenticeship Sprints** into the Activities. Locked (Sam): 4 Activities · **drop the 3 Goal chips, keep the "Activities" label** (Path A) · **Option-B render** (nest projects under each Activity, delete the Projects grid + Workplan-Goals Projects table) · sprints as slim Activity-4 nodes + a cross-cutting `sprint_tag` · **clean renumber** (32 items, verified by a 3-lens workflow). Shipped (PR #872 draft): spec ([`activity_reorg_scope.md`](activity_reorg_scope.md) + [`kb/activity_reorg_alias_map.json`](../kb/activity_reorg_alias_map.json)), 📄 Workplan link, KPI-map lockstep, Activity-3 retitle. **Pending:** generator Option-B render + consumers + tests + the live re-key. Continuation: [`activity_reorg_handoff.md`](activity_reorg_handoff.md); KB note [`methodology-adversarial-verify-crosswalk-before-live-rekey`](kb-notes/methodology-adversarial-verify-crosswalk-before-live-rekey.md) | **SkyPlan side-lane (2026-07-21, PR #872 draft) NEW** |
| [`cpl_presentations_lessons.md`](cpl_presentations_lessons.md) | **CPL executive presentations** (StarBOG side-lane): a 12-slide **Board of Governors** update deck (the 2026–27 funding win — $7M ongoing + $35M one-time; COBI 3 priorities; portal soft-launch; Moreno Valley EM B.S. pathway; Fire/CSTI hand-off to Miramar; speaker script per slide) + 3 **CBO budget-workshop** slides filled *natively* into the CO "2026 Annual Budget Workshop" template's CPL section (theme-matched: Source Sans Pro, navy/blue/gold, watermark, footer). Reusable how-to: [`playbook-building-cpl-executive-presentations`](kb-notes/playbook-building-cpl-executive-presentations.md). Continuation: [`cpl_presentations_handoff.md`](cpl_presentations_handoff.md) Latest: **CAC apprenticeship-pathway slides (2026-08-10)** — 3 flattened-PNG slides → 6 native click-built ones, speaker notes for all 15, and ⭐ two source unit totals that do not reconcile because the originals clipped their own tables. | **SkyDeck (2026-08-10) NEW** |

| [`fire_ems_eligibility_lessons.md`](fire_ems_eligibility_lessons.md) | **Fire & EMS CPL eligibility — statewide vs local** (StarEmber side-lane): Sam's data ask — students with eligible credit + credit volume for the statewide (and then local) credit recommendations in **Fire / EMT / EMS / Wildlands / Paramedic**, from the committed CER rollup. Answer (2026-07-20 pull): statewide **27 creds · ~238 students · ~1,946 credits**; local **142 · ~251 · ~2,567**. Findings: the locals mirror the statewide series (colleges built on them); **"statewide" has two divergent definitions** (CER `statewide` flag = has-a-CCC-Collaborative-articulation ≠ on-the-statewide-page → Paramedic License reads local). Deliverable: a private **filtered-view artifact**. Recipe: [`methodology-area-eligibility-rollup-from-cer`](kb-notes/methodology-area-eligibility-rollup-from-cer.md). Continuation: [`fire_ems_eligibility_handoff.md`](fire_ems_eligibility_handoff.md). Read-only lane — left `cpl_todos.json` + the numbered handoff to the CCR mainline | **StarEmber side-lane (2026-07-20) NEW** |
| [`cip_crosswalk_lessons.md`](cip_crosswalk_lessons.md) | **The CIP tab → CIP Coder (Beta)** (CIP side-lane): the TOP→CIP transition tool — Browse the CIP-2020 reference, Find-my-course's-code, and **Review my catalog** (the whole-catalog triage sheet). Arc through StarCIP/SkyLoft/SkyLiftoff (reference + fit-check + course-first recommend), then **SkyCoco/SkyCIP**: the Review-tab legibility pass (#842), program-coherence default (#843), inline multi-CIP (#844), Phase-A baseline counts (#846), the Claude-for-Chrome fix loop (#849), the confidence-scoring rework (#850), and the **UI redesign PORT (#851)** — sticky College+Subject+tiles, white row gutters, expanded-course package, "COCI Sync'd" destination tile, "CIP Coder (Beta)" rename. Continuation: [`cip_crosswalk_handoff.md`](cip_crosswalk_handoff.md). Open: the %-in-box follow-up; Phase B (progress store → access → COCI push); WCAG pre-field gate | **SkyCIP side-lane (2026-07-20, #850/#851 merged) NEW** |
| [`top_dethroning_lessons.md`](top_dethroning_lessons.md) | **Dethroning TOP — from gatekeeper to last-in-line signal** (StarBoard side-lane): a 4-agent audit of all 3 repos found the doctrine ~80% built (confidence/source ladder, TOP weighted 0.10) but two real leaks — the canonical-SUBJ4 seed let a 0.4–0.5 TOP guess vote on + fold into the M-ID identity (24%/17,059 rows), and `_row_audit.py` scored TOP-sourced `inferred-high`. **PR A #799** (doctrine + trust demotion) & **PR B #800** (`kb/_top_gate.py` predicate + seed vote-gate + dry-run; **0 canonical values changed** — provably non-disruptive). Anchor: [`methodology-top-is-a-last-in-line-signal`](kb-notes/methodology-top-is-a-last-in-line-signal.md). Continuation: [`top_dethroning_handoff.md`](top_dethroning_handoff.md). Open: fold-apply enforcement (Rule-7 dispatched), the `excel_to_dashboard.py` fallback label, a curation-gated public-KB caveat | **StarBoard side-lane (2026-07-16, #799/#800 merged) NEW** |
| [`co_platform_orglayer_lessons.md`](co_platform_orglayer_lessons.md) | **COBI org layer — the C&I subsite pilot** (SkyFlyer side-lane): Sam's CO talk → the C&I team's own COBI site. The masthead **site-switcher** (`cobi_orgs.js` — CPL/C&I, per-site tag, nav-filtered, `?org=ci`, no gating, #766), the **"Our Process" viz tab** (#765), masthead polish (#767), and the **`ci-team-2026`** curation phrase (#768). Strategy: one platform, org as a view dimension. Open: 4 tuning calls + per-area data isolation (Phase 1b, deferred). ADR: [`adr-cobi-org-layer`](kb-notes/adr-cobi-org-layer.md) | **SkyFlyer side-lane (2026-07-14, #765–#768 merged) NEW** |
| [Session 119](session_119_handoff.md) | **AUTHORITATIVE** — NC/Learning-Partner instrument is live; two lanes open (NC + the CCR mainline at 118) | 2026-08-05 |
| [`subject_discipline_cleanup_lessons.md`](subject_discipline_cleanup_lessons.md) | **Mis-mint detection + blank-discipline cleanup** — Sam's `HVAC M10FR` (really Diesel) catch → the `subject_discipline_outlier` auditor rule (subject-code cohort outlier + two-signals-agree; covers singletons), the 41-row discipline apply (`mismint-s113@bot`, `kb/mismint_out/2026-07-13/`), the Triage-filter + blank-Common-SUBJ display fixes, and the blank-discipline pre-seed (5 homonym-checked codes → live lexicon). Next: [`session_114_handoff.md`](session_114_handoff.md) | **Session 113 (2026-07-13, SkyTeleo) NEW** |
| [`cpl_pathways_lessons.md`](cpl_pathways_lessons.md) | **🎓 CPL Pathways tab** — apprenticeship-to-baccalaureate course maps with live-derived CPL ✓ check-offs; first pathway = **Cerritos Field Ironworker Supervisor BS** (CAC presentation 2026-08-13): 15 IWAP courses ✓ 31.5u from live MAP articulations, 15u CLEP-eligible GE (ESLEI 24-35), the Structural "articulations in progress" card, "The billboard" callout. **#733: status stages (Discussion Draft default) + big banner + ⬇ PDF extract; link out to colleagues for feedback.** Continuation: [`cpl_pathways_handoff.md`](cpl_pathways_handoff.md). Open: fall-2027 confirm, real upper-div course list (regionalcte COURSES is JS-rendered — browser-only) | **SkyIron side-lane (2026-07-10, #732/#733 merged) NEW** |
| [`ccr_convergence_lessons.md`](ccr_convergence_lessons.md) | **CCR Convergence** — the doctrine-driven merge/mint campaign toward the ≤2,500-course CPL crosswalk. Kickoff 2026-07-03: strategy of record [`ccr_convergence_strategy.md`](ccr_convergence_strategy.md) · Merge/Mint Doctrine v0 `kb/merge_doctrine.md` + question bank · the 🧠 Mind-meld voice panel. **Session 111 (SkyMighty): Doctrine v0.2 distilled (P-3 ratified, NEW P-11) + waves 1–2 adjudicated (2,144 articulated ids).** **Session 112 (SkyEmpyrean, 2026-07-12): the vocational wire-up (`cte`+`mq_list` into `_ccr_trail.py`) + CCR wave 3 — 2,000 corroborated multi-college ids adjudicated (539 agents, nothing fired; top discipline fix "Accounting" ×23 validating the same-day MQ restore); survived a spend-cap + task-death via `resumeFromRunId` on Opus.** **Session 115 (StarMagna, 2026-07-14): the doctrine GRADUATION GATE (v0.6 re-seed, 92%/94.7% blind) → a scenario cascade to v0.11 (#784–#789) — P-6/P-1b/P-7, the 3-rung cap, the ESL 3-comprehensive collapse (2,364→3), FL numeric rungs, Music/Dance activity doctrine + the permutation-pressure lever; 7/11 open Qs closed; ~6,600 identities policied; batch-apply authorized.** Method: [`kb-notes/methodology-curated-scenario-batches-doctrine-elicitation.md`](kb-notes/methodology-curated-scenario-batches-doctrine-elicitation.md). **Session 117 (StarMarcus, 2026-07-15): closed the LAST open forks → Doctrine v0.13 (#791 — P-13 honors/variant, P-5 whole-vs-part units, D-8 cross-discipline; every named Part IV fork now RESOLVED) + shipped the ESL packaging DRY-RUN (#792 — 2,364 → Beginning 1,305/Intermediate 548/Advanced 296 + carve-outs Citizenship 38/VESL 155/Transfer-review 22; measurement-only, `kb/esl_package_out/2026-07-15/`).** Method: [`kb-notes/methodology-packaging-dryrun-classification.md`](kb-notes/methodology-packaging-dryrun-classification.md). Next session: [`session_118_handoff.md`](session_118_handoff.md) | **CCR doctrine v0.13 + ESL dry-run (2026-07-15, StarMarcus) NEW** |
| [`annual_workplan_authoritative_lessons.md`](annual_workplan_authoritative_lessons.md) | Annual Workplan tab = authoritative source — **Session 85 (SkyLight):** the "Current" column LIVE/manual HYBRID (5 KPI-mapped sub-activities pull the live headline value read-only; the rest editable via the new `workplan_goals.current` column; `PID_TO_KPI_KEY` module const + 3.2→`transcripted_units` fix; live sync as a post-pass after the KPI merges) + authoritative editable titles (`projects.name`, single store). Pattern: [`kb-notes/methodology-live-vs-manual-hybrid-column.md`](kb-notes/methodology-live-vs-manual-hybrid-column.md) | **Session 85 (SkyLight, 2026-06-30) NEW** |
| [`ccr_cluster_cleanup_lessons.md`](ccr_cluster_cleanup_lessons.md) | CCR cleanup — cluster dissolution; Session 37: impact columns + FL SUBJ4 re-mint; Session 38: CCR refinements #333 + the KIN/PE #334 & Drama/Theater #335 fan-in convergences; Session 39: cron verify + the Supabase-mirror fix #337 + the KINE/FLSP strict twin-merge (74 folds) + [`cis_cs_convergence_scope.md`](cis_cs_convergence_scope.md) (GATED); Session 39 cont.: the live-curation loop #339–#342 — merge ≠ verify, UC-CUR retired, official-id merge targets, Spanish → SPAN 100/110/220/230; **Session 40: the severed evidence index — [`official_id_fold_scope.md`](official_id_fold_scope.md) APPROVED + BUILT #344/#345 (promotions re-key applied, plurality rule, 🧾 evidence lane, Phase B 455→1,155 folds, M-ID SPAN 10x anchors retired)**; Sessions 41/42: kinship gate + slot-fix; Session 43: the off-pane-columns bug (#372/#373) + cron no-op verified; **Session 45: rules day — C-ID router statewide #379 (8,377 members, 454 descriptors, dual-approval honesty fix), the CADM homonym machinery #381 (scoped lexicon entries + retraction propagation), the description-evidence worklist lane #382 (474 dark-M-ID groups)**; **Session 46: the AUTO/smog over-mint case — the 🏷 title-evidence lane #385 (IDF title cosine over dark M-IDs + 54k stand-alones, shared guard suite, NO units gate) + the STATEWIDE strict twin-merge #386 (589 token-identical twins absorbed, guard-clique gated, Sam-confirmed smog families → AUTO M1001 / M1007)**; **Session 53: auto-merge pass 1 APPLIED (2,272 dependable groups / 5,838 rows, 0 conflicts, band-purity gate, cohort automerge-v1@bot) + the mojibake repair + COCI correction queue + Sam's KPI/popup batch (#418–#424)**; **Session 55: Suggested-merges clarity — ★ merge-target badge #434, self-merge ghosts fixed + Discipline picker clarity #435, "⌕ merge into a different existing course" search picker #436, + the UC-CUR→Z re-mint SCOPE #437**; **Session 56: the UC-CUR → Z-scheme re-mint APPLIED #439 (4,053 ids → SUBJ Z<band><seq>, dry-run 7/7 → Sam's go → atomic land, md5-verified live; reusable Supabase re-key tool + workflow)**; **Session 57: worklist popup polish + the "(NC)" title cleanup #441 (110 singleton + 13 bot curated titles, Supabase + snapshot) + the CONSOLIDATION LOOSENING #442 (_sug_sig level-SAFE → level-COLLAPSING, worklist 229→2,665 anchored / 217→2,519 singleton, suggestions-only); **Session 62: synonym-map growth #461 (ECE/EMT/CNA/HVAC/LVN) + the candidate ambiguity validator**; **Session 69: the CCR UI polish sweep — filter-bar #492, flag chips #493, Disciplines legend #495, worklist search #496 + the live-merge durability note**; **Session 70: the CCR merge-workspace arc — Pending-merges panel + mint→Common SUBJ preview #500; re-discipline-on-merge + forward-looking Common SUBJ #503; search-add fix #504; band filters #505; Conservative↔Aggressive slider #506; opt-in checkboxes #507; morphological-variant fold + cross-discipline flag #508/#509** | **Session 70 (PaintSky, 2026-06-24)** |
| [Scope — Similar-course family consolidation (level-collapsing, the measure-first dry-run + the over-merge decision; loosening shipped #442)](similar_course_family_scope.md) | 2026-06-16 | 2026-06-16 |
| [Scope — UC-CUR → Z-scheme re-mint (full re-key of 4,053 synthetic ids → SUBJ Z<band><seq>; Rule 7, **APPLIED Session 56**, PR #439)](uc_cur_zscheme_remint_scope.md) | 2026-06-15 | 2026-06-15 |
| [Scope — Unverified-M-ID renumber re-mint (close gaps + re-sort; full Rule-7, unverified-only, ONE pass after the merge wave; the grounded re-key chain; #494)](unverified_mid_renumber_scope.md) | 2026-06-23 | 2026-06-23 |
| [Scope — CCR merge-workspace EPIC (consolidate the two merge popups into one shared MergeEditor + dock the worklist panel; divergence table, two-feeders design, 4-PR ladder, Session 71)](ccr_merge_workspace_epic_scope.md) | 2026-06-24 | 2026-06-24 |
| [`ccr_merge_workspace_lessons.md`](ccr_merge_workspace_lessons.md) — Session 71: the shared-editor extraction (#511–#518); two-feeders/one-editor design, the seed-`k` precedence bug, deterministic-splice method. **Session 72 (StarLander): the post-consolidation polish pass — 14 PRs #520–#534 (Wave 1/2 six asks + candidate slider; Wave 3 nine refinements #527–#531; Wave 4 Beg/Int/Adv relabel #532; Wave 5 #534 — decouple-from-CCR-filters + keyword-surfaces-all + single-course-rename→Save + header Prev/Next)** | 2026-06-24 | 2026-06-25 |
| [Scope — C-ID articulation authority (the math cleanup)](cid_articulation_authority_scope.md) | 2026-06-11 | 2026-06-11 |
| [`cobi_raci_nudge_lessons.md`](cobi_raci_nudge_lessons.md) | Team & RACI tab + the update-nudge loop. **Session 77 (StarPort):** the full nudge → braindump → CC-writes-it-up → card loop + the save-persistence token-refresh fix (#556–#562). **Session 78 (SkyMap):** posted `item_updates` surface ON the Activity/sub-activity/project card face + 📝/👥 deep-links on sub-activity cards (#564, `card_updates.js`). **Session 79 (StarBender):** card Lead from the RACI Responsible + hover roster (`card_raci.js`), lead-seeding, nudge opt-out fix, sortable columns (#567–#570). **Session 81 (StarFarout):** per-row 📣 on every matrix row + a 📣 Nudge button on every Activity/sub-activity/project card + bulk button → "Nudge All" (#574; affordance-visibility-vs-eligibility). **Session 83 (StarNova):** card 📝/📣 popups open IN PLACE on Activities & Projects, no #raci redirect (`card_actions.js`, #596) + the team-phrase 401 fix / validation note (#595/#598) | **Session 83 (StarNova, 2026-06-29)** |
| [`mission_control_lessons.md`](mission_control_lessons.md) | Mission Control + CO-platform strategy — **Session 83 (StarNova):** the strategy doc #586/#588, the Lift Off program tracker (`liftoff_plan.json` task+decision-fork model) #588/#592, the `mission_control.js` collapsible overlay below the RACI matrix #590/#592, the server-enforced shared team-phrase gate #593; **+ the team-phrase HARDENING (#595–#598): the empty-Bearer 401 fix, popups-in-place, Mission Control parity, validate-on-entry + the reviewer ⚙ Manage-team-phrase admin**. Related: [`co_platform_strategy.md`](co_platform_strategy.md) + [`kb-notes/methodology-server-enforced-shared-password-gate.md`](kb-notes/methodology-server-enforced-shared-password-gate.md) | **Session 83 (StarNova, 2026-06-29) NEW** |
| [`project_lifecycle_lessons.md`](project_lifecycle_lessons.md) | Project soft-delete (Table/Archive) — Session 84 build; **Session 95: the Activity ⇄ Project separation** (the 23-card mixup + restore, activity-layer immunity + grid dedup #652, the Archive-radio capture-walk bug) **+ the poke-around wave** (charts → CPL Analytics, 4.1 phantom row, `project_add.js` Add-project, the AWG Projects section, the +1-blank-line/run accretion fix) | **Session 95 (2026-07-02)** |
| [`dashboard_cleanup_lessons.md`](dashboard_cleanup_lessons.md) | Dashboard cleanup (renames, CER, slim header, SUBJ filters, Exhibit Adoption tab) + cross-disc accounting + 3 rule changes; **Session 23: #2 sidebar sub-links + #3 display-label map** | Session 23 (Bruh 23, 2026-05-31) |
| [`engineering_practices_lessons.md`](engineering_practices_lessons.md) | Dev infra / testing / design system (committed jsdom harness + CI, stop-hook fix, `:root` tokens + `var()` rule, prototype-first) | **Session 32 (Busy Feynman, 2026-06-04) NEW** |
| [`common_subject_code_tab_lessons.md`](common_subject_code_tab_lessons.md) | CSC tab / canonical SUBJ4 / CSC-G; **Session 37: orphan-tail discipline fallback #330 + FL-split CSR search/display #331** | **Session 37 (2026-06-09)** |
| [`cpl_assistant_lessons.md`](cpl_assistant_lessons.md) | CPL Assistant (in-dashboard RAG chatbot tab); **Session 64: the retired-model 502 fix (cpl-chat v15, `claude-sonnet-4-6`) + the CCR/CER recommender + benchmark + landing-site demand kickoff (scope #472)**; **Session 92: the audience selector (required, 5 populations, shared key) + 👍/👎 feedback (`sierra_feedback`) + `cpl-chat` v22 `AUDIENCE_RULES`**; **Session 93: the CPR retrieval miss (relevance re-rank, v24) + the Sierra Training tab Phase 1**; **Session 94: Sierra mark + full markdown on all 3 surfaces (#649), Training P1 (#650), the GUIDANCE layer — `sierra_guidance` + v26 (#651)** ; **Session 123 (SkyHunter, 2026-08-06): the CPR question again — five defects, the 07-01 fix caused this outage, 2 → 5 colleges, `search_exhibits_by_topic_v2` (#1016)**; **Session 129 (SkyMind, 2026-08-09): Sierra reaches the credit-disposition aggregates (v36, #1064–#1066) — ⭐ the topology check refuted the handoff's premise (ONE function, service-role, `--no-verify-jwt`, so per-college disclosure was Sam's explicit decision), Sam's opportunity-not-deficiency framing, and TWO guards that fired on correct behaviour** | **Session 129 (SkyMind, 2026-08-09)** |
| [`sierra_training_tab_scope.md`](sierra_training_tab_scope.md) | Scope — Sierra Training tab recommendation (YES, phased: P1 review-queue + log-informed gap miner over `sierra_feedback`+`chat_interactions` · P2 `sierra_guidance` prompt knob · P3 RAG-corpus artifact ingestion — never the public KB) + the **Malone guardrails lane** (durable rate limit, daily cost breaker, CORS tightening) pre-"Credit for Being Me" | 2026-07-02 (Session 94 — **Phases 1+2 SHIPPED** (#647/#651) + the P1 team-session affordances (#650); Phase 3 + the Malone lane open) |
| [`cpl_funding_lessons.md`](cpl_funding_lessons.md) | CPL Implementation Funding tab — full arc #352–#368: shell → data/renderer → what-if sandbox → shares-first rev2 workbook → P2/P3 actuals (ratified ADR) → roster edits + no-scroll rule. **Session 2 (2026-07-03, #663): the Chancellor-facing rework** — 2-year selectable window, year-specific priorities (all text editable), noncredit-feeder carve-out (the 4 feeder institutions OUT of the college table), 3-layer team-phrase config (`cpl_funding_config`), Excel workbook RETIRED, 2025-26 headcount refresh (74 rows; per-row `hc_vintage`). Scope: [`funding_priority_metrics_scope.md`](funding_priority_metrics_scope.md); own handoff: [`cpl_funding_handoff.md`](cpl_funding_handoff.md) | **2026-08-04 (SkyUnit) — #971**: per-priority **PRICE FACTOR** replaces the global multiplier — price = factor × base rate, target = pot ÷ price, `factor 1.0` = cumulative identity, behavior-neutral merge + post-deploy config activation; live shares .5/.3/.2 · factors .5/1/2 → target 5,759 FTES, earns 35.6%. Prior **2026-08-03 — #967**: P1 → APPLIED units (proxy for the upfront articulation work), shares .50/.45/.05, cumulative targets via multiplier 2.0; three of my own proposals withdrawn on measurement; the eligible→applied gap re-explained as correct filtering, not defect. Prior **2026-08-01 — #964/#965**: headcount out of the cards; the 30× summary-vs-detail unit defect (sites 4-6); Applied CPL units (`pa`/`pa_u`) measured; the CPL FTES rate curator-editable at $5,649.63 |
| [`tmc_builder_lessons.md`](tmc_builder_lessons.md) | TMC Builder tab — interactive ADT submission (fixed C-ID left / per-college COCI-dropdown right, C-ID auto-match, Total Units, Save/Resume + export); Session 60 list-first + GE companion; Session 61: the per-college approved-ADT overlay #458; **Session 66: the CO-review pivot — Active/Approved split #477, scope + ASCCC acceptance ruleset #478, acceptance metadata on the templates #479 (flexible slots + per-TMC flexibility + recovered C-IDs)**. Data model: [`kb-notes/reference-tmc-adt-data-model.md`](kb-notes/reference-tmc-adt-data-model.md); scope: [`kb-notes/tmc-co-review-scope.md`](kb-notes/tmc-co-review-scope.md); rules: [`kb-notes/reference-adt-acceptance-rules.md`](kb-notes/reference-adt-acceptance-rules.md); **Session 69: title-fill recovery for approved + in-progress ADTs #489/#490 + the COR-upload scope #491 ([`kb-notes/tmc-adt-document-upload-scope.md`](kb-notes/tmc-adt-document-upload-scope.md))**; **S91: c-id.net union #639 + OR alternatives #640; S92: the join ladder + graded provenance #642 (every approval lands; the confidence-score data map)** | **Session 92 (StarFab, 2026-07-01)** |
| [`first_light_lessons.md`](first_light_lessons.md) | First Light — daily PD plein air greeting LIVE (#394/#396: gallery reveal, read-aloud, anonymous write-only reflections) + the theme spec (prototype v1.4.2 BLESSED: derived AA tokens, glass=chrome/opaque=data, solid uniform chips); retheme shipped S49; **S62: local-day rotation + the reflections digest #460; S65: gallery 3→89 + the runner-as-Commons-proxy sourcing pipeline #474** | **Session 65 (2026-06-19)** |
| [`working_with_claude_code.md`](working_with_claude_code.md) | **Working with Claude Code — the MAP team guide.** Say who you are (what you tell a session gets stored with your name on it); tell it what you NOTICED, not just what you want (the two prompts that reshaped the contact worklist were observations, not requests); ask to see it; ask for a TOOL not a document (anything handed over in chat is a photograph that starts going stale); a green/amber/red guide to approving what a session asks to do; when a change touches other people's work; checkpoint before you leave; and what a session genuinely cannot do. Plus a recommendation to Sam on agents — not yet, because an agent has to be INVOKED and so fails exactly when a new user forgets | **Session 120 (SkyMail, 2026-08-05) NEW** |
| [`governance_lessons.md`](governance_lessons.md) | Governance & team enablement — the diagnosis that every problem in the MAP Users session was a **governance gap wearing a data-quality costume**; the two design choices that made the register an instrument (store reasoning, MEASURE facts; render the absence); the uncomfortable finding that the contact cadence was decided in June and **never run once**; and why agents were recommended AGAINST for habit enforcement — an agent must be invoked, so it fails exactly when a new user forgets | **Session 120 (SkyMail, 2026-08-05) NEW** |
| [`map_users_lessons.md`](map_users_lessons.md) | MAP Users — **the student-contact worklist**: 25 of 123 colleges had no Primary Contact email (the address MAP routes student CPL requests to), 24 with a live landing page; the shared-inbox design Sam rejected on local-governance grounds and the stronger rule that replaced it (**propose only people the college already designated**); the probe that found the contacts view carried 24 fields while the sync pulled 11; the three provenance tiers Jessica's input produced | **Session 120 (SkyMail, 2026-08-05) NEW** |
| [`cobi_lessons.md`](cobi_lessons.md) | COBI — masthead rename #475; single-row masthead #487; **S86–87: KPI-card shelf, card↔KPI live sync, the MAP Users tab end-to-end (value-signature probe, gated-PII), and the nudge follow-up #623–#626 (recipient picker + CEO + last-nudged log + MAP deep-link + roster-in-email checklist; the surface-don't-edit ADR)** | **Session 87 (StarMax, 2026-06-30) NEW** |
| [`cpl_news_lessons.md`](cpl_news_lessons.md) | CPL News lane — automated harvest (cpl-news-harvest Edge Function: Google News/GDELT/CalMatters/CCCCO/Bluesky + a suggest-a-story queue) → Claude triage → `public.cpl_news` → `#cpl-news` tab (live-read) + CPLBrain vault digest; capability-probe auth; first run 12 CA items (avg rel 0.84) | **Session 67 (Skywatch, 2026-06-21) NEW** |
| [`fact_sheet_lessons.md`](fact_sheet_lessons.md) | Public CPL Fact Sheet — standalone Pages page (`fact-sheet/`) recreating the journalist PDF, live KPIs from `../live_metrics.json` + baked fallback, full COBI KPI grid + Statewide Exhibits section (expandable per-sector lists), "The CPL Bump" CAEL/WICHE citation, the 1,304-vs-1,101 KPI reconciliation, Cambria/Calibri + 0.4in print-to-PDF, non-tab nav-rail launch link; sandbox PDF-extraction + headless-verify notes. PRs #537/#540. **Session 79 (StarBender): statewide credit recs data layer (`statewide_recs.js`, the `Collaborative Type == "CCC"` authoritative signal). Session 80 (StarMan): the Curate-editable overlay — `factsheet_edit.js` + `factsheet_overrides`, sign-in-to-edit any box, JST card removed, #570. Session 81 (StarFarout): the Curate arc — add/✕delete/drag-reorder boxes #576, "My CPL Stories" headless-sourced section #577, add/resize/replace/delete images via the `factsheet-images` Storage bucket #578 (reserved-key namespaces on the unchanged overrides table). Session 82 (SkyFlyer): editable-everywhere + a11y + Word — Vet-Sprint editable / KPI move-only / budget hide-only / per-grid Add box (`gN` grid signature), stable keys exclude `[data-bind]` text, ~15 embedded links, a WCAG 2.1 AA pass, print fixes (navy header no longer white-on-white), new `⬇ Word` export (`factsheet_word.js`), PR #584. **SkyVeil (2026-07-23): the per-section 🙈 Hide section toggle — reserved `<sid>|__hidden` override + `.fs-ov-hidden` on the section + TOC link (suppressed in Print + the Word export for free), #874–#876; the Funding hide→un-hide; KB note [`methodology-hide-must-suppress-the-export`](kb-notes/methodology-hide-must-suppress-the-export.md).** | **SkyVeil (2026-07-23)** · Session 82 (SkyFlyer, 2026-06-28) |
| [`kb_portal_lessons.md`](kb_portal_lessons.md) | KB Portal — transplant #464 + login-gated Knowledge Base tab (iframe) #465 + the New-doc composer (draft → Claude polish → tokenless GitHub deep-link) #466/#467 + attachment upload (in-browser pdf.js/mammoth/SheetJS extraction + image downscale) #468 | **Session 63 (2026-06-19) NEW** |
| [`coursecontrolnumber_remint.md`](coursecontrolnumber_remint.md) | Re-mint playbook (THE reference) | Session 5 |
| [`excel_to_supabase_lessons.md`](excel_to_supabase_lessons.md) | Excel → Supabase migration (Phase 1 + Activity↔Project + Phase 2 + Budget read-path + **Excel-retirement P1/P2/P4 DONE** + the daily-pipeline reference doc) | **Session 25 (Bruh 25, 2026-06-01)** |
| [`exhibit_canonicalization_lessons.md`](exhibit_canonicalization_lessons.md) | Credential identity / EACR / Cred-Ref / CER triage / **merge tool** | **Session 106 (SkySeal — the triage rules day: 5f/5c-mech/5g, four pre-seed lanes, multi-issuer, the Save fix; "continued 9"–close-out)** |
| [`cer_v2_redesign_lessons.md`](cer_v2_redesign_lessons.md) | CER v2 — the triage-first single-surface redesign + ⬇ extracts + the MAP Export feed | **Session 110 (carpentry pass 2 closed — 252 CARP/Carpentry fills + 18 MAP-export titles; the stale identities-map fix; the SUBJ filter + COCI Lookup tab)** |
| [`eacr_consolidation_lessons.md`](eacr_consolidation_lessons.md) | EACR consolidation + gallery (v2) + prescriptive + three-grains + Student view (v3) #301 + the data-unblock loop + **Session 35: CER consolidation EMT 29→18 #308 + the ordinal rule + the CCR worklist co-articulation family merges #310** + **Session 36: perf lazy-load #314 + cross-disc re-mint RSCH/WKEX #315 + the CER Eligible/Students columns from the Exhibit CRs Catalog #318/#319/#320 (the id-namespace Title-bridge)**; **Session 37: CER credential dedup — 21 Signal-B merges #323 + the detector elective-bucket gate #324 (162→77) + "Eligible students" relabel #322** | **Session 37 (2026-06-09)** |
| [`map_api_auth_handoff.md`](map_api_auth_handoff.md) | MAP CustomReport API auth coordination (Teams spec sheet + the no-op pre-stage activation steps) | **Session 27 (2026-06-01) NEW** |
| [`exhibit_unification_vision.md`](exhibit_unification_vision.md) | Credential design doc | retrospective |
| [`letter_curator_handoff.md`](letter_curator_handoff.md) | Letter Curator workstream | Session 10 |
| [`overmerge_remint_lessons.md`](overmerge_remint_lessons.md) | Cross-discipline over-merge re-mint (member_top_divergence → dry-run/apply → split-brain iters) | **Session 18 (2026-05-29)** |
| [`quickstart_chat_lessons.md`](quickstart_chat_lessons.md) | Quick-start chat | Session 10 |
| [`session_26_audit_lessons.md`](session_26_audit_lessons.md) | Session 26 codebase audit + remediation (PII fix, idempotency, security, BUG-1) | **Session 26 (Bruh 26, 2026-06-01)** |
| [`sidebar_lessons.md`](sidebar_lessons.md) | Sidebar / tabs.js router | Session 11 |
| [`statewide_kpi_lessons.md`](statewide_kpi_lessons.md) | Statewide Exhibits KPI card #375 → program-area categories + doublewide #376 + KPI card drag-to-reorder #377 | **Session 44 (2026-06-11) NEW** |
| [`subj4_canonicalization_remint_lessons.md`](subj4_canonicalization_remint_lessons.md) | Phase 1e re-mint | Session 5 |
| [`unified_courses_audit_lessons.md`](unified_courses_audit_lessons.md) | Trust-Card auditor | **Session 18 (`member_top_divergence`)** |
| [`vault_sync_lessons.md`](vault_sync_lessons.md) | Vault auto-sync (Windows Task Scheduler) | Session 11 (NEW) |

---

## Session handoffs (`docs/session_<N>_handoff.md`)

| Session | Moniker | Handoff doc |
|---|---|---|
| 160 → 161 | Sky160 | [`session_161_handoff.md`](session_161_handoff.md) — six questions from Sam, three shipped (#1212/#1213/#1214). 🎉 **The three-session Priority 1 is CLOSED: `cobi_nav` holds 43 rows Sam saved himself** — the answer had been in the database for hours while three handoffs called it unproven. ⭐ **Share was two menu items the manager could not see.** ⭐ **The guard was on the wrong axis** — `plan()` already lifted Admin out of a hidden group, so barring it from every category was a second belt over a sealed door. ⚠️ **The fallback tab was `valid[0]`, so dragging anything to the top of the menu silently changed where broken links land.** ⚠️ **The no-glyphs rule was recorded 08-14 and the tab shipped covered in emoji that week.** **NEXT: the Finance phrase scope (needs Sam), then the org roster as data.** |
| 159 → 160 | Sky159 | [`session_160_handoff.md`](session_160_handoff.md) — closed **all three** Admin items Sam reported on 08-14 and 158 left unbuilt (#1204/#1209/#1210). ⭐ **Hide and audience were one question wearing two controls** — one ladder now, and pressing it ASKS instead of toggling; the merge is at the CONTROL, not the table. ⭐ **Only the alarming case was labelled**: the protection chip covered 1 of 7 rows, now 7 of 7 — and chipping every row would have reported *"Not mapped" ×35* off one failed request without a distinct `unread` state. ⭐ **The test harness was dropping checks** — 116/122/123 on identical source. ⚠️ **The real-browser round trip is STILL unproven, two sessions running.** |
| 158 → 159 | SkyKey | [`session_159_handoff.md`](session_159_handoff.md) — ⭐ **one dead token wearing four costumes**: Admin *save 400*, Sierra *"not signed in"* and CR Reference *"could not read"* were the SAME expired-token bug (13 of 26 modules check only the token's SHAPE); the Admin save had **never worked once** (`cobi_nav` held zero rows — a bulk POST is one INSERT over the UNION of its keys). Fixed with a **keeper**, not a 14th copy. ⚠️ **Shipping that alone would have been worse than the bug** — six modules renew from a cached, already-rotated token and three drop the session on failure. **PRIORITY 1: one real-browser round trip proves the lot.** |
| 157 → 158 | Sky157 | [`session_158_handoff.md`](session_158_handoff.md) — ⭐ **the reviewer sign-in was a pointer at a route that no longer existed**: `raci.js` kept a complete `signIn()` with no caller while Admin told people to use it there. Moved to **ℹ About**; Admin mounts the same control inline. ⭐ Then measured: **43 tables gate on a phrase, 26 on the READ**, and **eight tabs** offered neither an input nor a mention of the header — now one shared `lockedBanner()` plus a CI guard. ⚠️ **The guard was wrong twice**, both caught by reading its output. **SkyCode's noncredit CIP categories** folded in: a blanket `32.0111` rule shipped and was reverted; **the TOP is not load-bearing**. |
| 156 → 157 | SkyGate | [`session_157_handoff.md`](session_157_handoff.md) — ⭐ **the COBI side menu is data**: `cobi_nav`, drag-and-drop from a new Admin tab, seeded EMPTY so today it is exactly what the code ships; the overlay never gates the menu and six failure modes each have a test. Sierra's **ten built-in rules are visible and editable** at last, on Sierra Training. ⭐ **The long-red `governance.test.js` is GREEN** — reading the assertion's own rationale showed it is a NOISE guard, and **19 of its 26 candidates were genuinely resolvable**, including six unrecorded decision rights (**DR-13 the workplan itself had no named owner**). **PRIORITY 1: Sam drags something in a real browser** — nothing here has been exercised outside jsdom. |
| 147 → 148 | SkyPeak | [`session_148_handoff.md`](session_148_handoff.md) — ⭐ **the data is published and Sierra still cannot read it**: POST's real statewide set is TEN credit recs (8 C-IDs + 2 electives) and `ccc_rec` is a single string holding one of them. `chatbox_credential_recs` is LIVE at **2,205 rows**, but `cpl-chat` is not wired — **PRIORITY 1 is one PR + a deploy**. Also fixes in that PR: the **Cerritos ironworker false absence** (corpus says `FIW Orientation`, curated layer has 11 `Ironworker Apprenticeship` rows) and adoption ranking that sorts ready-to-adopt exhibits **last**. ✅ Sierra's statewide flag came from the wrong file — **42 credentials read as local**, Paramedic License among them; now 126 up from 84. ✅ A saved Sierra instruction is finally **editable and testable in place**. |
| 146 → 147 | SkyFund | [`session_147_handoff.md`](session_147_handoff.md) — ⭐ a phrase box in the masthead (7 tabs had none), site-scoped phrases, and 🔑 Team Phrases as a tab. **PRIORITY 1 is the Contracts policy swap, written and deliberately NOT applied** until Sam distributes the Finance phrase. ⚠️ **PRIORITY 2 outranks it**: under "allow either" a site phrase is a SUPERSET of the shared one — decide before Finance's phrase leaves the core team. Also: Sierra Training made usable, and **Sam must retype three instructions the old 500-char cap silently ate**. |
| 131 → 132 | SkyDesk | [`session_132_handoff.md`](session_132_handoff.md) — the 📥 MAP Team Queue is live and **measures every item at load instead of carrying a list**; ⭐ the inherited six-item backlog was two-sixths wrong within two days and 7 colleges that cannot route a student had never been looked up; next is the college-facing briefing on the same pure `buildQueue()` engine, blocked on Malone's view name for its top slot |
| 134 → 136 | SkyDeck | [`session_136_handoff.md`](session_136_handoff.md) — ⚠️ **resequenced: two sessions ran concurrently on 2026-08-10 and both wrote a "135"; SkyLine's landed first, so this one is 136.** the **CAC apprenticeship-pathway deck** is rebuilt and scripted (3 slides → 6, 3-click builds, notes on all 15, 13:00 inside a 15:00 slot); ⚠️ **two unit totals still do not reconcile** (Carpentry 20.0 vs 26 cited, Cerritos 31.5 vs 38 — the originals clipped their own tables; American River is exact at 29.5) and need Santiago Canyon + Cerritos; ⚠️ **the 44% DAS withdrawal figure is not ours** and DAS is in the room on the 13th; ⭐ pre-apprenticeship CPL now has a named mechanism set from Sam, and mechanism 1 is noncredit coursework — which sharpens the "four standalone NC institutions at ZERO" gap |
| 134 → 137 | SkyLine | [`session_137_handoff.md`](session_137_handoff.md) — Sierra names credentials: the canonical credential layer was already curated and simply never reached the DB Sierra queries; k=10 suppression, 1,987 credentials live, route CRED·STD deployed as v37. |
| 137 → 138 | SkyLine | [`session_138_handoff.md`](session_138_handoff.md) — ⭐ the `map_student_credit` re-load landed (537,908 rows from `TblSOURCE`), applied/transcribed computable at last, and the two "applied" measures disagree by 55%. **CRED·ADOPT is next.** |
| 141 → 142 | SkyLink | [`session_142_handoff.md`](session_142_handoff.md) — ⭐ **My College is complete except three MAP links** (#1121: waiting-credit breakdown · funding-pool split · Resources · the tier block). **98.8% of the 64,074 articulated-and-waiting units is Credit for Basic Military Service** — one decision applied repeatedly, not 300 judgment calls. ⚠️ **Both remaining blockers are decisions, not code**: the MAP URL shapes (Sam ← Malone + Pedro) and an RLS call — the access shape is DB-gated, and `map_credential_student_rollup` is a **matview, which cannot carry RLS**. |
| 140 → 141 | SkyBridge | [`session_141_handoff.md`](session_141_handoff.md) — ⭐ **Sierra AI embedded on My College** (#1117/#1118/#1119), plus who MAP has on file. Sam drove the design live: "transcribed" is a **mark** not a posting, Priority 3 routing is standard practice, use the funding **tab names**. **Next: MAP deep links.** |
| 140 → 141 | SkyBridge | [`session_141_handoff.md`](session_141_handoff.md) — ⭐ **My College is 7 of 8 sections**: the funding box, a 72-district picker and Ask Sierra (#1117). The handoff's own worked figure was wrong — an allocation is a **floor waterfall**, never share × pool. **The last section needs a MAP portal feed, not code.** |
| 138 → 139 | SkyRoute | [`session_139_handoff.md`](session_139_handoff.md) — ⭐ Sierra answers per-credential student questions (CRED·VOLUME + COLLEGE·ADOPT, **v38**); a subtraction leak shipped and was caught at checkpoint. **EACR's prescriptive layer is next.** |
| 132 → 133 | SkyHigh | [`session_133_handoff.md`](session_133_handoff.md) — ⭐ **the action library the roadmap called "the hard part" was already written** (23 team-typed strategies in Supabase `cpl_funding_config`), and ⚠️ **the overlay documented as "currently EMPTY" is not** — live Scenario 1 is 50/30/20, not the documented 30/42/28, so a briefing built off the baked file prints wrong priorities while claiming to be sourced; the 7 never-looked-up colleges are looked up (5 candidates, 2 blank) but ⚠️ **sessions are now egress-blocked from college domains**, so the sweep method is gone and the addresses ship as a new `via: "search"` tier that `proposedFillFor()` refuses in code; **two questions open with Sam: Scenario 1 vs 2, and Year 1 vs 2 (Year 2 has no strategies)** |
| 129 → 130 | SkyMind | [`session_130_handoff.md`](session_130_handoff.md) — Sierra is wired to the credit-disposition data and live (cpl-chat v36); ⭐ there is ONE Sierra not two, so per-college disclosure was a real decision Sam made explicitly; next is the proposed-fills column the MAP team can adopt, then the college action page with the MAP-team queue as the same engine pointed inward |
| 128 → 129 | SkyNaut | [`session_129_handoff.md`](session_129_handoff.md) — `map_student_credit` is live at 220,588 verified rows and Sprint goal 2 is measured statewide; next is the per-college split under the k=10 ADR, then loading the second Access table so the analysis speaks in credits rather than row counts |
| 121 → 122 | SkyWalker | [`session_122_handoff.md`](session_122_handoff.md) — the partner-crosswalk engine is live but has seen one list; the second run is the priority |
| CPL Portal video lane (2026-07-23) | SkyVid | [`cpl_portal_video_handoff.md`](cpl_portal_video_handoff.md) — workstream-named side-lane capsule; the numbered lane belongs to the CCR mainline |
| CCR Convergence lane (2026-07-03 →) | MindMeld | [`ccr_convergence_handoff.md`](ccr_convergence_handoff.md) — workstream-named to avoid colliding with the concurrent session's numbering |
| CPL Pathways lane (2026-07-10 →) | SkyIron | [`cpl_pathways_handoff.md`](cpl_pathways_handoff.md) — workstream-named (the numbered lane belongs to the CSR mainline); the 🎓 tab's continuation capsule |
| 5 → 6 | Bruh Hex | [`session_6_handoff.md`](session_6_handoff.md) |
| 6 → 7 | Bruh Hept | [`session_7_handoff.md`](session_7_handoff.md) |
| 7 → 8 | Octaman | [`session_8_handoff.md`](session_8_handoff.md) |
| 9 → 10 | Sexy Dexy | [`session_10_handoff.md`](session_10_handoff.md) |
| 10 → 11 | Bruh El | [`session_11_handoff.md`](session_11_handoff.md) |
| 11 → 12 | Bruh El (handoff) | [`session_12_handoff.md`](session_12_handoff.md) |
| 12 → 13 | Bruh Dec (handoff) | [`session_13_handoff.md`](session_13_handoff.md) |
| 13 → 14 | Bruh Baker (handoff) | [`session_14_handoff.md`](session_14_handoff.md) |
| 14 → 15 | Bruh Sonnet (handoff) | [`session_15_handoff.md`](session_15_handoff.md) |
| 15 → 16 | Bruh Parallax (handoff) | [`session_16_handoff.md`](session_16_handoff.md) |
| 16 → 17 | Bruh Word (handoff) | [`session_17_handoff.md`](session_17_handoff.md) |
| 17 → 18 | Qualitastic / Q (handoff) | [`session_18_handoff.md`](session_18_handoff.md) |
| 18 → 19 | Cascade (handoff) | [`session_19_handoff.md`](session_19_handoff.md) |
| 19 → 20 | Wizardly Turing (handoff) | [`session_20_handoff.md`](session_20_handoff.md) |
| 20 → 21 | Bruh / "Twenty" (handoff) | [`session_21_handoff.md`](session_21_handoff.md) |
| 22 → 23 | Bruh Sentinel (handoff) | [`session_23_handoff.md`](session_23_handoff.md) |
| 23 → 24 | Bruh 23 (handoff) | [`session_24_handoff.md`](session_24_handoff.md) |
| 24 → 25 | Bruh 24 (handoff) | [`session_25_handoff.md`](session_25_handoff.md) |
| 25 → 26 | Bruh 25 (handoff) | [`session_26_handoff.md`](session_26_handoff.md) |
| 26 → 27 | Bruh 26 / Deuce-Six (handoff) | [`session_27_handoff.md`](session_27_handoff.md) |
| 27 → 28 | Bruh 27 (handoff) | [`session_28_handoff.md`](session_28_handoff.md) |
| 28 → 29 | Bruh 28 (handoff) | [`session_29_handoff.md`](session_29_handoff.md) |
| 29 → 30 | Two-Niner (handoff) | [`session_30_handoff.md`](session_30_handoff.md) |
| 30 → 31 | Session 30 (handoff) | [`session_31_handoff.md`](session_31_handoff.md) |
| 31 → 32 | Session 31 (handoff) | [`session_32_handoff.md`](session_32_handoff.md) |
| 32 → 33 | Busy Feynman (handoff) | [`session_33_handoff.md`](session_33_handoff.md) |
| 33 → 34 | Sleepy Goodall (handoff) | [`session_34_handoff.md`](session_34_handoff.md) |
| 34 → 35 | Lucid Wozniak (handoff) | [`session_35_handoff.md`](session_35_handoff.md) |
| 35 → 36 | CER identity consolidation (handoff) | [`session_36_handoff.md`](session_36_handoff.md) |
| 36 → 37 | perf + cross-disc re-mint + CER Eligible/Students (handoff) | [`session_37_handoff.md`](session_37_handoff.md) |
| 37 → 38 | CER Signal-B dedup + CCR impact columns + Foreign-Language SUBJ4 re-mint (handoff) | [`session_38_handoff.md`](session_38_handoff.md) |
| 38 → 39 | CCR refinements #333 + the KIN/PE #334 & Drama/Theater #335 fan-in convergences (handoff) | [`session_39_handoff.md`](session_39_handoff.md) |
| 39 → 40 | Cron verify + Supabase-mirror fix #337 + KINE/FLSP twin-merge + CIS↔CS scope (handoff) | [`session_40_handoff.md`](session_40_handoff.md) |
| 40 → 41 | The severed evidence index #344/#345 — promotions re-key + plurality rule + evidence lane + anchor retirement (handoff) | [`session_41_handoff.md`](session_41_handoff.md) |
| 41 → 42 | Witness-kinship gate #347 + R4 singletons #348 (handoff) | [`session_42_handoff.md`](session_42_handoff.md) |
| 42 → 43 | Bruh Moonshot — the slot-fix #357 + era guard + C-ID router #365/#366 (handoff) | [`session_43_handoff.md`](session_43_handoff.md) |
| 43 → 44 | Bruh Starlord — cron no-op verified; off-pane-columns fix #370–#373 (handoff) | [`session_44_handoff.md`](session_44_handoff.md) |
| 44 → 45 | Statewide Exhibits KPI card #375/#376 + KPI reorder #377 (handoff) | [`session_45_handoff.md`](session_45_handoff.md) |
| 45 → 46 | CCR rules day: statewide router #379 + homonym repair #381 + description lane #382 (handoff) | [`session_46_handoff.md`](session_46_handoff.md) |
| 46 → 47 | The AUTO/smog over-mint case: 🏷 title lane #385 + statewide twin merge #386 + smog consolidations (handoff) | [`session_47_handoff.md`](session_47_handoff.md) |
| 48 → 49 | First Light design sprint #391–#400: daily plein air greeting LIVE + theme spec v1.4.2 BLESSED → the retheme is GO (handoff) | [`session_49_handoff.md`](session_49_handoff.md) |
| 47 → 50 | Data lane (Bruh Supernova): SUBJ ⇄ CCR checker #388/#402 + 📋 To-Do feed #389 + SUBJ4 fold dry-run #405 (5/5 gates) → the receipted APPLY | [`session_50_handoff.md`](session_50_handoff.md) |
| 50 → 51 | Data lane (Bruh Dawnleader): the SUBJ4 canonical fold APPLIED (71,037-alias permutation + Supabase mirror + post-fold twins + the full Rule-7 chain; collision signal 1,206 → 3) → verify regen, CCR Subject grouping | [`session_51_handoff.md`](session_51_handoff.md) |
| 51 → 53 | Data lane (Bruh Photonicus): KIN/PE pass 2 — PEDU dissolved + TOP-aware ATHL carve-out (1,057 re-keys), 19.7k titles normalized, 205 merges (roster/fitness/HS), lost-saves fix + Keep-as-is + Subject optgroups (#412–#415) → verify regen, the flagged family queues | [`session_53_handoff.md`](session_53_handoff.md) |
| 53 → 54 | Data lane: auto-merge pass 1 applied (2,272 groups, cohort automerge-v1@bot) + mojibake repair + KPI batch → ⚙ chip + Triage lane, title-lane pass-2 decision, post-apply regen verify, MilStudents wiring | [`session_54_handoff.md`](session_54_handoff.md) |
| 54 → 55 | Data lane (Bruh Spaceranger): auto-merge cohort made reviewable — ⚙ auto-merged chip + "Auto-merged" Triage lane LIVE (#428, `auto_n` on 2,272 rows) + Pipeline-tab refresh + post-apply verify → title-lane pass-2 DRY-RUN (Sam's go), per-row revert, ceramic-tech curator pick | [`session_55_handoff.md`](session_55_handoff.md) |
| 55 → 56 | Data lane (Bruh Nebula): Suggested-merges clarity — ★ merge-target badge #434, self-merge ghosts fixed + Discipline picker disable/explain #435, "⌕ merge into a different existing course" search picker #436, + the UC-CUR→Z re-mint SCOPE #437 → build the Z-scheme DRY-RUN (Sam's go), title-lane pass-2 | [`session_56_handoff.md`](session_56_handoff.md) |
| 56 → 57 | Data lane (Star Treader): the UC-CUR → Z-scheme re-mint APPLIED #439 (4,053 ids → SUBJ Z<band><seq>, dry-run 7/7 → Sam's "Go now" → atomic land, live md5-verified; reusable Supabase re-key tool + workflow) → the Z future-mint half + auditor re-run, title-lane pass-2 dry-run, per-row auto-merge revert | [`session_57_handoff.md`](session_57_handoff.md) |
| 57 → 58 | Data lane (Bruh Skydriver): worklist popup polish + "(NC)" cleanup #441 + the CONSOLIDATION LOOSENING #442 (_sug_sig level-COLLAPSING, worklist ~10× bigger, suggestions-only) → the member-join Jaccard 0.5→~0.4 (measure member-flips first), work the bigger worklist, title-lane pass-2 | [`session_58_handoff.md`](session_58_handoff.md) |
| 58 → 59 | Data lane (Bruh Skyleader): extend the merge candidate set — override-rename + segment-fold + completion note #445, synonym map + keyword-gather #446, looseness slider #447, AJ synonym #448 (a threshold can't bridge a zero-overlap synonym — use the map) → the member-join Jaccard 0.5→~0.4 (measure member-flips first), grow the synonym map, title-lane pass-2 | [`session_59_handoff.md`](session_59_handoff.md) |
| 49 → 52 | Design lane (Bruh Orbitron): the First Light retheme SHIPPED (#407 palette flip · #408 glass + ghosted painting · #410 glass-quiet chips; --live contrast lint in CI) → Sam's screenshot verdicts, kpi_reorder keyboard path, per-tab polish | [`session_52_handoff.md`](session_52_handoff.md) |
| 59 → 60 | TMC Builder (Bruh Star Navicus → Momentus): the new tab end-to-end #450–#452 (all 45 official TMCs, C-ID auto-match, curator layer + CO-review queue) → the list-first redesign + GE Breadth companion, then CPL-native TMC wiring | [`session_60_handoff.md`](session_60_handoff.md) |
| 61 → 62 | Bruh Skymarker: the per-college approved-ADT overlay from the COCI program export #458 (`tmc_college_adts.js`, 3,238 pairs/115 colleges/99.9% by TOP code, UCTP own instance, the taxonomy ADR) → faculty-verify the drafts + the `college_short_names.json` taxonomy follow-up | [`session_62_handoff.md`](session_62_handoff.md) |
| 62 → 63 | SkyLion: First Light reflections digest #460 + CCR synonym pairings #461 (candidate validator) → morphological-variant CCR pass (Med Assisting/Assistant) + faculty-verify the TMC drafts | [`session_63_handoff.md`](session_63_handoff.md) |
| 63 → 64 | SkyGate: the KB Portal end-to-end — transplant #464 + login-gated Knowledge Base tab #465 + New-doc composer #466/#467 + attachment upload #468 → smoke-test the 5 attachment types (fix any esm.sh path), the bundle-divergence decision, then data/CCR + TMC lanes | [`session_64_handoff.md`](session_64_handoff.md) |
| 64 → 65 | Startripper: the retired-model 502 fix #471 (`cpl-chat` v15, `claude-sonnet-4-6`) + the CCR/CER recommender + real-time benchmark + landing-site demand kickoff (scope #472) → build the CCR/CER/adoption ETL into shared Supabase, then M1 | [`session_65_handoff.md`](session_65_handoff.md) |
| 65 → 66 | Skyloft: First Light gallery 3→89 via the runner-as-Commons-proxy pipeline #474 + the COBI masthead rename #475 (Mamba brand layer) → the standing data/CCR (CPL-Assistant CCR/CER recommender ETL, green-lit) + TMC + KB-portal lanes resume | [`session_66_handoff.md`](session_66_handoff.md) |
| 66 → 67 | Skylander: TMC → a CO-staff ADT review tool — the Active/Approved split #477, the scope + ASCCC acceptance ruleset #478, the template acceptance metadata #479 (119 flexible slots + per-TMC flexibility + 15 recovered C-IDs, AfAm 0→3) → build the Phase-2 acceptance engine (Sam: "Go for A") + the bulk-PCF Playwright extractor | [`session_67_handoff.md`](session_67_handoff.md) |
| 67 → 68 | Skywatch (nick SkyMurrow): the CPL News lane — cpl-news-harvest Edge Function + cron + `#cpl-news` tab #481 + CPLBrain vault digest #9 (12 CA items, avg rel 0.84; capability-probe auth; closed-social manual queue; CA-first) → the TMC acceptance engine (still queued from the S67 handoff) + CPL News tuning + the public-KB decision | [`session_68_handoff.md`](session_68_handoff.md) |
| 68 → 69 | SkyAlizarin: spotty-cron fixes — the 06:17/09:17/12:17 UTC cron ladder #485 + the curation-sync resilience fix #486 (transient Supabase TLS was killing the whole publish) — AND the COBI masthead consolidation #487 (single-row app bar, ready + tested, holding for Sam's seal upload) → finish #487 (sample the seal navy → merge → dispatch), then the TMC acceptance engine | [`session_69_handoff.md`](session_69_handoff.md) |
| 69 → 70 | Stargaze: TMC title-fill (approved + in-progress ADTs) #489/#490 + the COR-upload scope #491 + the CCR polish sweep — filter-bar #492, flag chips +NC/+CR/NC type #493, Disciplines legend #495, worklist search #496 — + the unverified-M-ID renumber re-mint scope #494 → the Pending-merges tracking panel, then the re-mint build (when merges settle) + the TMC acceptance engine | [`session_70_handoff.md`](session_70_handoff.md) |
| 70 → 71 | PaintSky: the CCR merge-workspace arc (9 PRs) — Pending-merges panel #500, re-discipline-on-merge + forward-looking Common SUBJ #503, search-add fix #504, band filters #505, Conservative↔Aggressive slider #506, opt-in checkboxes #507, morphological-variant fold + cross-discipline flag #508/#509 → the merge-workspace EPIC (dock the worklist as a panel + consolidate the two merge popups, scope-first), then the unverified-M-ID renumber re-mint + the TMC acceptance engine; bonus: more First Light CA plein-air landscapes | [`session_71_handoff.md`](session_71_handoff.md) |
| 71 → 72 | (Session 71) the CCR merge-workspace EPIC, completed in full — #511 scope, #512 PR-1 (extract shared `buildMergeEditor`), #513 PR-2a (hoist to init scope), #514 PR-2b (per-row dialog adopts it, in-row ★ model), #515 lessons, #516 PR-3 (dock the worklist as a right-hand panel), #518 PR-4 (live CCR↔worklist re-filter) → the standing lanes: unverified-M-ID renumber re-mint, TMC acceptance engine, CPL-Assistant recommender ETL | [`session_72_handoff.md`](session_72_handoff.md) |
| 72 → 73 | StarLander: the post-consolidation merge-workspace polish pass — 14 PRs #520–#534 (decouple worklist from CCR filters, keyword-surfaces-all, single-course rename→Save, header Prev/Next) → the standing lanes | [`session_73_handoff.md`](session_73_handoff.md) |
| 74 → 75 | SkyBlaster: the public CPL Fact Sheet — standalone live page (`fact-sheet/`) #537 + Statewide Exhibits section & KPI-count reconciliation #540 → Fact-Sheet follow-ups (live-wire the snapshot tier; tech-landscape → live HTML) + the standing lanes | [`session_75_handoff.md`](session_75_handoff.md) |
| 75 → 76 | SkyMaster: the COBI ownership + nudge + report layer — workplan-alignment regroup #545, Team & RACI tab + registry #546, nudge toggle + test modes #547, editable Directory cells #548; Annual Report draft delivered (not a tab yet) → the RACI carry-overs (matrix filter, nudge SEND channel, `allowed_reviewers`, per-card RACI links, `update_log`, Report tab) | [`session_76_handoff.md`](session_76_handoff.md) |
| 76 → 77 | SkyTrek: Team & RACI — matrix **filter** + per-card **`👥 RACI` deep-links** #550, the **CI-poll-via-MCP** learning → CLAUDE.md #552, and the **3-tier matrix** (Activity → sub-activity → project, hierarchical filter) #553; PLUS the **Veterans Sprint + Military Base dp plans** added to the vault (CPLBrain #10/#11) and a scrubbed public-KB mirror (draft PR #15, awaiting Sam) → the decision-gated RACI carry-overs (nudge channel, lead emails, `update_log`) + the Annual Report tab + the standing lanes | [`session_77_handoff.md`](session_77_handoff.md) |
| 77 → 78 | StarPort: the RACI update loop end-to-end (8 PRs #556–#562) — Copy-RACI #556, Annual Report tab #557, check-all/clear-all + manual nudge #558, **the save-persistence token-refresh fix** + nudge accountability #559, the 📝 braindump→CC composer + `item_updates` #560, per-item 📣 nudge #561, 📝 on every card #562 → surface `item_updates` on the card face + Annual Report (self-freshening); the 3 lead emails; standing lanes | [`session_78_handoff.md`](session_78_handoff.md) |
| 78 → 79 | SkyMap: posted updates surface ON the card face (PR #564) — 📝/👥 deep-links on sub-activity cards + the read-only `card_updates.js` overlay (newest `item_updates` per `activity:N`/`project:<id>` key → card, hides the creation-era line); NEW kb-note `methodology-live-overlay-onto-generated-cards` → the Annual Report half of the same carryover (fold `item_updates` into `annual_report.js`); the 3 lead emails; standing lanes | [`session_79_handoff.md`](session_79_handoff.md) |
| 79 → 80 | StarBender: RACI becomes the card's source of truth (PRs #567–#570) — card Lead from the RACI Responsible + hover roster (`card_raci.js`), lead-seeding (Beth Kay dropped), nudge opt-out fix, sortable RACI/directory columns; + statewide Fact Sheet credit recs from our MAP data, authoritative `Collaborative Type == "CCC"` only (PR #571), NEW kb-note `reference-authoritative-statewide-exhibit-signal` → the Fact Sheet **consumer wedge** (now unblocked, #570 merged) + Annual Report self-freshening; the 3 lead emails; standing lanes | [`session_80_handoff.md`](session_80_handoff.md) |
| 80 → 81 | StarMan: the public Fact Sheet becomes Curate-editable (PR #570) — standalone `factsheet_edit.js` overlay (DOM-walked stable keys → tiny HTML diff; reviewer magic-link; allowlist sanitizer) + `factsheet_overrides` table; JST card removed → the Fact Sheet **consumer wedge** + Annual Report self-freshening; the 3 lead emails; standing lanes | [`session_81_handoff.md`](session_81_handoff.md) |
| 81 → 82 | StarFarout: RACI/Activity nudge tweaks (PR #574) — per-row 📣 on every matrix row + a 📣 Nudge button on every Activity/sub-activity/project card + bulk button renamed "Nudge All"; NEW kb-note `methodology-affordance-visibility-vs-action-eligibility` → the Fact Sheet **consumer wedge** (top) + Annual Report self-freshening; the 3 lead emails; standing lanes | [`session_82_handoff.md`](session_82_handoff.md) |
| 82 → 83 | SkyFlyer: Fact Sheet editable-everywhere + WCAG 2.1 AA + Word export (PR #584) — Vet-Sprint editable / KPI move-only / budget hide-only / per-grid Add box, stable keys exclude live text, ~15 links, a11y pass, print fixes, new `⬇ Word` export; NEW kb-notes `methodology-stable-dom-keys-exclude-live-text` + `playbook-standalone-dom-to-word-export` → the standing queue (M-ID renumber, TMC acceptance engine, CPL-Assistant ETL) + the 3 surfaced data items | [`session_83_handoff.md`](session_83_handoff.md) |
| 83 → 84 | StarNova: CO-platform strategy doc #586/#588 + Mission Control tracker (task+decision-fork) #590/#592 + the shared "team phrase" edit gate #593 — **then HARDENED it live with Sam+Malone**: empty-Bearer 401 fix #595, card popups open in place (`card_actions.js`) #596, Mission Control parity #597, validate-on-entry + reviewer ⚙ Manage-team-phrase admin #598 (+ 2 Dependabot bumps). Updated kb-note + `mission_control_lessons`/`cobi_raci_nudge_lessons` → confirm the team phrase + admin work live, gut-check the Lift Off plan with Malone, then the NOW lane (institution-owned accounts, `cobi-auth.js` consolidation) + standing lanes | [`session_84_handoff.md`](session_84_handoff.md) |
| 84 → 85 | SkyScribe: project soft-delete (Table/Archive, reversible) #600/#605 + `.nojekyll` + lean custom Pages deploy (~65% smaller) #601/#602 + computed Goal/Stretch progress bars #604 → scoped the Annual Workplan authoritative-source work | [`session_85_handoff.md`](session_85_handoff.md) |
| 85 → 86 | SkyLight: Annual Workplan tab = authoritative source — the "Current" column LIVE/manual HYBRID (5 KPI-mapped sub-activities pull the live headline value read-only with a `live · as of` badge, the rest editable via the new `workplan_goals.current` column; `PID_TO_KPI_KEY` const + 3.2→`transcripted_units` fix; post-pass after the KPI merges) + authoritative editable titles (`projects.name`). Code-only PR; NEW kb-note `methodology-live-vs-manual-hybrid-column` + `annual_workplan_authoritative_lessons` → confirm it live + the open grid-card-sync follow-on + standing lanes | [`session_86_handoff.md`](session_86_handoff.md) |
| 90 → 91 | SkySherpa: the standalone Sierra page's header rebrand — CPL Initiative logo replaces the 🏔️ emoji #635 + a hand-traced Mt Whitney ridge ghosted behind the "Sierra" wordmark (`sierra/whitney-mark.svg`, +snowcap) #636 + tagline → "Your CPL Sherpa" #636/#637; `docs/cpl_assistant_lessons.md` (S90) → the CCR/CER recommender (M1) + standing lanes | [`session_91_handoff.md`](session_91_handoff.md) |
| 91 → 92 | SkyGOAT: TMC Builder gets both C-ID authorities (c-id.net union → coverage doubled #639) + "X OR Y" alternatives via the visual-PDF-read overlay `tmc_or_groups.json` (77/80 folded #640) → the 3 skipped OR-groups + the standing lanes | [`session_92_handoff.md`](session_92_handoff.md) |
| 92 → 93 | StarFab: every c-id.net approval lands via a join ladder with graded provenance (#642 — 1,195 wrong blanks → 0, Saddleback SOCI 110 fixed; adversarial verify caught a title-stripping sibling-capture blocker pre-merge) + Sam's confidence-score / can't-submit-misaligned reframe mapped in `kb-notes/reference-tmc-confidence-data-requirements.md` → BUILD THE CONFIDENCE ENGINE for the mid-July 200+ backlog | [`session_93_handoff.md`](session_93_handoff.md) |
| 92 → 93 | StarLab (parallel Session 92, same day): Sierra audience selector (required, 5 populations, shared `cplSierraAudience.v1`) + 👍/👎 feedback → `sierra_feedback` (anon-write / reviewer-read) + `cpl-chat` **v22** `AUDIENCE_RULES` (students get zero inside-baseball; production widget untouched) + `chat_interactions` reviewer SELECT for gap mining; Training-tab recommendation ([`sierra_training_tab_scope.md`](sierra_training_tab_scope.md)) → Phase 1 on Sam's go + the Malone guardrails lane | [`session_93_handoff.md`](session_93_handoff.md) |
| 93 → 94 | SkyReach: the CPR retrieval miss fixed (relevance re-rank migration + cpl-chat v24 + smoke mode 13, #646 — found via the day-old 👎 feedback loop) + the Sierra Training tab Phase 1 SHIPPED (#647: feedback queue + `sierra_feedback_set_status` triage + gap miner) → fold the team's tab feedback, Phase 2 `sierra_guidance`, the Malone guardrails lane | [`session_94_handoff.md`](session_94_handoff.md) |
| 94 → 95 | SkySierra: the Sierra mark (Whitney roundel) replaces 🏔️/🎓 everywhere + full markdown rendering on all 3 chat surfaces (#649); Training-tab P1 — 🧪 Test-in-Sierra, date filters, bulk triage, feedback→chat-turn telemetry, window-listener fix (#650); the GUIDANCE layer SHIPPED — `sierra_guidance` + cpl-chat **v26** `fetchTeamGuidance()` + the 🧭 pane, marker-rule-proven (#651; ⚠ deploy tool defaults verify_jwt=true) → the team's first real guidance rules, Phase 3 ingestion, the Malone lane | [`session_95_handoff.md`](session_95_handoff.md) |
| 95 → 96 | Session 95: the Activity ⇄ Project separation — 23 mistaken tablings restored (5.1 kept), sub-activities IMMUNE to Table/Archive at every consumer + Projects-Grid dedup (grid = 4.1.x + 5.x only) + the Archive-radio fix (#652); then Sam's live wave: charts → CPL Analytics, 4.1 phantom-row goal fix, ＋ Add-project (`project_add.js`, projects RLS widened to team-phrase), the AWG Projects section, byte-idempotent regen → watch Sam's first Add-project uses, 5.8 re-table?, the standing Sierra/TMC lanes | [`session_96_handoff.md`](session_96_handoff.md) |
| 97 → 98 | BigSky: Activities tab optimization — Custom Report gains Elevation slider + per-audience titles + progress bar and absorbs the Master Report; slim actions bar to pane top (frozen Lead-dropdown bug fixed); sidebar grouped (`nav_groups.js`) + label → Activities; Where To resets; MAP naming locked everywhere (CPL Initiative / Mapping Articulated Pathways platform; old name history-only); team-phrase expansion plan authored | [`session_98_handoff.md`](session_98_handoff.md) |
| 98 → 99 | Session 98: the Implementation Funding rework (#663) — 2-year selectable window + year-specific priorities (Sam's six metrics, all text editable) + noncredit-feeder carve-out (4 feeder institutions OUT of the college table, real headcounts) + 3-layer team-phrase config (`cpl_funding_config`; SCENARIO ?? SHARED ?? BASE, scenario promotes on unlock); Excel workbook RETIRED; 2025-26 headcount refresh (74 rows, 41 still 2022-23 per-row `hc_vintage`) → Chancellor hand-off; true up remaining headcounts; the CCR MindMeld voice pass stays the priority lane | [`session_99_handoff.md`](session_99_handoff.md) |
| 100 → 101 | SkyVault: CER triage loop unstuck (token-refresh fix, SUPERSEDE/STALE fold lanes, queue → 0, cron fold+audit) + the CareerOneStop lane built (#673/#674/#675) | [`session_101_handoff.md`](session_101_handoff.md) |
| 101 → 102 | SkyAnchor: COS authority LIVE (probe → contract fixes #676 → 6,490 certs, 83 CER matches; Pages serving fix #677) + triage QA (retarget-to-existing-family doctrine) + the AP art fold applied | [`session_102_handoff.md`](session_102_handoff.md) |
| 102 → 103 | SkySeed: the AP/CLEP brand-family PRE-SEED — 158 of 451 triage rows assigned to existing house families in one verified pass (`kb/_preseed_unclassified.py` + 43-check harness + MCP apply, md5-verified) | [`session_103_handoff.md`](session_103_handoff.md) |
| 103 → 104 | The STAGED pre-seed (163 rows prefilled, ready-to-save-not-saved) + the Needs-triage/All worklist toggle + DIR DAS / NCCER authority sources noted | [`session_104_handoff.md`](session_104_handoff.md) |
| 104 → 105 | SkyTime: the statewide-catalog pass — 97 of the last 100 staged (v3 lanes) + originating-college chips + the multi-issuer `issuer_adds` fold lane + Mode A2 issuer promotion + `appendRowSafe` row isolation | [`session_105_handoff.md`](session_105_handoff.md) |
| 105 → 106 | SkyClose: the truncated-read root cause (PostgREST 1,000-row cap — saves were fine, reads dropped a tail; now paginated) + Save-All saves hand-typed rows + the missing-issuer triage lane (1,130 rows, 978 ⚡ staged) + 10-Key seeded + the seal-blue/glass pass | [`session_106_handoff.md`](session_106_handoff.md) |
| 106 → 107 | SkySeal: the triage rules day (6 PRs #690–#695) — Rules 5f / 5c-mechanized / 5g (level-to-end titles), the apprenticeship / statewide-agency (AWS) / cert-family (FAA) / title-style lanes, the save→re-edit fix, ＋ add-issuing-agency (Rule 4 multi-issuer); 1,009 staged / 1,125 queue | [`session_107_handoff.md`](session_107_handoff.md) |
| 124 → 125 | SkyHero (→ **SkySteward** suggested): mode 7's root cause was one layer up — the home college was never DETECTED (`.limit(3)` with no ORDER BY is non-deterministic), so `askedGeo` was null and nothing could rank by distance #1023; 📋 Copy button on both Sierra surfaces #1024; four passes on student routing — both/and → **Yes/And** → the portal's fuller portfolio → **anti-poaching** #1025/#1026/#1027; ⚠️ mode 7 still red on a NEW meaning, and the seeker-vs-college line is drafted but NOT shipped |
| 123 → 124 | SkyHunter (→ **SkyHero**): Sierra's topic retrieval — five defects behind "CPR finds only 2 colleges", including that the 2026-07-01 fix for the same question CAUSED this one (`aed` → `'a':*`); **2 → 5 colleges at 100% precision**; plurals/two-word/misspelling handling; small-cell suppression that actually suppresses (#1016/#1017). ⚠️ `cpl-chat` NOT deployed — production still answers "2"; corpus covers 59 of 123 colleges | [`session_124_handoff.md`](session_124_handoff.md) |
| 122 → 123 | SkyPlan: the $50k tab's real measure — the **disposition rate** (median 4.7%; MVC 3rd, Bakersfield 6th, Cabrillo 13th of 106, the only metric matching Sam's own read) off Malone's student×CR export; **headcount retired as a basis** (explicit layer-aware `unit`, NC split → noncredit FTES, Calbright placeholder); CR-backlog generator (#1007/#1012/#1013/#1014). Rework itself NOT built | [`session_123_handoff.md`](session_123_handoff.md) |
| 116 → 117 | StarMarcus: the interrogation-room finale — closed the merge/mint doctrine's LAST open forks → v0.13 (#791: P-13 honors folds/standalone kept + Lab folds/Refresher-Bridge separate, P-5 whole-vs-part units, D-8 same-subject-code→canonical) via two grounded 3-fork batches (Sam swept all 6 recommended) · then the ESL packaging DRY-RUN (#792: 2,364 → Beginning 1,305/Intermediate 548/Advanced 296 + carve-outs 38/155/22, measurement-only) + a visual artifact | [`session_118_handoff.md`](session_118_handoff.md) |
| 112 → 113 | SkyEmpyrean: the vocational climb — MQ 19th-ed re-validation (HUM/PE/PEDS→masters + 8 dropped disciplines restored, 240→248, #746) · CCR scanner wired with `cte`+`mq_list` + `--stratum multi` (#747) · CCR wave 3 (2,000 corroborated multi-college ids, 539 agents, nothing fired, #749; top fix "Accounting" ×23 validating the same-day restore) · survived a spend-cap + task-death via `resumeFromRunId` on Opus | [`session_113_handoff.md`](session_113_handoff.md) |
| 111 → 112 | SkyMighty: the triple-R day — CER 103 renames LIVE (#726) · CSR pass + POSC→POLS re-mint (#729/#730/#731) · CCR waves 1–2 (2,144 articulated ids, 56 fixes fired #742) · Doctrine v0.2 · MQ Handbook fold (#737) · CLAUDE.md pared + truth-audit + NEW Rule 9 (#727/#728) | [`session_112_handoff.md`](session_112_handoff.md) |
| 108 → 109 | SkyPhilo: the big triage night — COCI code-title resolution + 🔎/✨ title lookup (#707), Sam's **HS rule (#710), the audit that wrote nothing (his split pattern had it), the scoped bulk-CCC close-out (339 rows, #711); queue 1,036 → 233 (200 prefilled) | [`session_109_handoff.md`](session_109_handoff.md) |
| 127 → 128 | SkyGauge: **mode 7 measured and CLOSED** — retrieval was never the problem (LA Trade Tech ranked 2 of 150; the context held all ten LA-county colleges) and v35 had already fixed it, greens on smoke 48/49/50 · the session's own last PR fixed the item its handoff filed as Priority 1 · **the disposition gap found**: `CPLStatusPlan` is in NONE of the nine fetched views, so Sierra can never say what a college has ACTED on · **CPR/AED = 17,904 distinct students at 106 colleges, 42% of all CPL students** · a matcher took the workflow-stage column and reported a 0.0% disposition rate | [`session_128_handoff.md`](session_128_handoff.md) |
| 126 → 127 | SkyMiner pt2: Sierra's edge cases live at v35 (distance is a fact; the true dead end; never invent a college) · the "save needs two clicks" report turned out to be **three** bugs where the data was fine and only the screen lied · **OQ-08 drift detector built + wired to the cron** (39 → 15 candidates, 0 stale rows, noise budget as a test) · governance.test.js 44 → 90 | [`session_127_handoff.md`](session_127_handoff.md) |
| 125 → 126 | SkyMiner: Sam's two queued decisions ASKED and SHIPPED (#1029, deploy 11) — *restraint binds salesmanship, not facts* in `PORTAL_RULE`, and mode 7 = **all three parts in order** (host → precedent → nearest college that TEACHES it); the feedback queue triaged for the first time ever — 53 rows all `new`, **28 of them our own smoke test**, and it had already reported three of the last four sessions' fixes weeks early | [`session_126_handoff.md`](session_126_handoff.md) |
| 110 → 111 | Carpentry close-out live with Sam (252 CARP/Carpentry fills + 18 titles from his MAP export, cohorts `carp-*-s110@bot`; folds await his ✓ in the Merge-confirms lane); his "where does CARP 1203 come from?" exposed the never-re-keyed `identities` map (681/693 stale keys — the CER exporter now prefers the minted catalogs); CER SUBJ filter + the COCI Lookup tab (141,738 rows, M-ID/C-ID/CCN chips, sortable/filterable/resizable, ⬇ CSV) | [`session_111_handoff.md`](session_111_handoff.md) |
| 109 → 110 | SkyBreak: CER v2 LIVE — Sam-blessed mockup → same-day port (in-cell editing, six lanes over one surface, SUBJ column, ⚙ Columns, ⬇ CSV/JSON extracts) + the 🗺 MAP Export tab shaped to Malone's FullExhibitJSON | [`session_110_handoff.md`](session_110_handoff.md) |
| 107 → 108 | SkyKey: the PR-5b re-key LIVE (6 PRs #697–#702) — 49 renames applied (first production rename apply, receipts + Supabase migration), the PR-5b/2 confirm-merge lane (Sam's 6 collisions await ✓), his four evening asks (unlimited ＋ agencies · hs-generic “Local High School” · ase-align · 🔎/✨ issuer lookup), the daily-run push-race fix; 922 staged / 1,036 queue | [`session_108_handoff.md`](session_108_handoff.md) |
| 96 → 97 | SkyPress: both report generators wired to LIVE card data — Custom Report fetches `item_updates` + RACI leads at Generate time; Master Report = a selection modal (Activities & Projects tree) building the Workplan docx client-side (`master_report.js`); stale-reports root cause fixed (runner never had node `docx` + `reports/` never committed — workflow repaired); pipeline folds `item_updates` into `projects[].update` + `CPL_DATA.live_updates`; composer closes on Save, semicolon mailtos, charts → BOTTOM of CPL Analytics, 📎 attach explainer → the native Supabase-Storage attachments decision (access model) + attachments→KB-md ingest | [`session_97_handoff.md`](session_97_handoff.md) |

---

## Top-level orientation docs

- [`../CLAUDE.md`](../CLAUDE.md) — project memory, Critical Rules, M-ID lifecycle (§11)
- [`co_platform_strategy.md`](co_platform_strategy.md) — **the long-term "plan of attack"** (Session 83): scaling COBI + the CPL KB into a governed, team-based, CO-wide platform — operating model, account migration off personal logins, knowledge lanes, real APIs vs scraping, governance/security/accessibility/HUMANS, decisions only humans make, pushback, and a scorecard against all ~14 asks
- [`roadmap_archive.md`](roadmap_archive.md) — museum annex to CLAUDE.md: completed roadmap rows + Session 26-31 narratives (moved out Session 33 to keep CLAUDE.md to live, steering content)
- [`../README.md`](../README.md) — first-time visitor entry
- [`../kb/README.md`](../kb/README.md) — knowledge-base schemas + generators

## Sierra integration docs (vendor-facing, added 2026-07-02)

Commissioned by Sam for integrating Sierra into a vendor-built platform:

- [`sierra_technical_reference.md`](sierra_technical_reference.md) — how
  Sierra is built: architecture, the full `cpl-chat` API contract (request /
  SSE protocol / errors), the six-lookup answer pipeline, behavior rules,
  data layer, client surfaces, security model, ops, v13→v26 timeline.
- [`sierra_integration_analysis.md`](sierra_integration_analysis.md) —
  benefits / risks / challenges of embedding Sierra on another site, the
  pre-launch preconditions checklist, and the decision points for Sam.
- [`sierra_integration_guide.md`](sierra_integration_guide.md) — the vendor
  implementation plan: link / iframe / native-API / server-proxy paths, a
  minimal reference client, non-negotiable client requirements, launch
  checklist, ongoing-operations expectations.
- [`sierra_iframe_implementation_guide.md`](sierra_iframe_implementation_guide.md)
  — the **day-one iframe recipe** (2026-07-03): the exact `?ctx=external`
  URL, annotated markup/CSP/sandbox, sizing, QA checklist (incl. the
  contacts-gate check), launch coordination, rollback.
- [`sierra_maturity_roadmap.md`](sierra_maturity_roadmap.md) — **Malone's
  scope-and-sequence to end-state** (2026-07-03): Phases 1–6 (guardrails →
  contract hardening → content maturity → integration graduation →
  recommender depth → platform ops), efforts, dependencies, the three
  human-only decisions, the critical path. The iframe is explicitly interim.
- [`sierra_vendor_lane_handoff.md`](sierra_vendor_lane_handoff.md) — the
  lane handoff (2026-07-03): what shipped (#654/#657 + v27 LIVE), locked
  decisions, verified access facts, the priority queue, safety rails.

## Reference materials

Authoritative external sources we've cached:
- [`reference/`](reference/) — ASCCC / COCI / CCN-CID source documents


### Added 2026-08-13 (SkyBridge)

| Doc | Lane | What |
|---|---|---|
| [`local_course_alignment_lessons.md`](local_course_alignment_lessons.md) | lessons | Aligning a college's own courses to a credit recommendation + peer articulations; the two-signal requirement; the unbounded-peers and phantom-recommendation-group defects; the Common CR Reference direction. |
| [`kb-notes/methodology-a-summary-field-can-be-a-retrieval-gate.md`](kb-notes/methodology-a-summary-field-can-be-a-retrieval-gate.md) | kb-note | Filtering on a derived field is a membership test in disguise. |
| [`kb-notes/methodology-two-signals-for-a-judgment-proposal.md`](kb-notes/methodology-two-signals-for-a-judgment-proposal.md) | kb-note | Similarity + peer precedent; neither is a fallback for the other. |
| [`kb-notes/methodology-an-unadopted-item-is-a-shelf-not-an-absence.md`](kb-notes/methodology-an-unadopted-item-is-a-shelf-not-an-absence.md) | kb-note | Zero uptake is a product state; give it its own band with reserved slots. |
| [`kb-notes/methodology-bucket-military-and-non-military-credit-recommendations.md`](kb-notes/methodology-bucket-military-and-non-military-credit-recommendations.md) | kb-note | A JST lands scores of CRs, a non-military exhibit lands 1–2 — so an undifferentiated "awaiting action" total is 98.8% military and hides the tractable part. |
| [`kb-notes/methodology-use-the-identity-key-before-you-score-strings.md`](kb-notes/methodology-use-the-identity-key-before-you-score-strings.md) | kb-note | If the domain already has an identity standard in a column (C-ID, ISBN, SOC…), it belongs ABOVE your similarity scorer, not inside it. |
| [`kb-notes/methodology-a-false-positive-costs-more-than-a-miss.md`](kb-notes/methodology-a-false-positive-costs-more-than-a-miss.md) | kb-note | On a trust-building surface one obviously-wrong row discredits the whole page; set the threshold for credibility, not recall. |
| [`session_149_handoff.md`](session_149_handoff.md) | handoff | Next session: build the alignment layer. |
| [`kb-notes/methodology-bound-both-sides-of-a-union.md`](kb-notes/methodology-bound-both-sides-of-a-union.md) | kb-note | A limit naming the function's grain must bound EVERY branch of a union, and a capped list must ship its own total. |
| [`kb-notes/methodology-a-grouping-key-must-come-from-the-authoritative-set.md`](kb-notes/methodology-a-grouping-key-must-come-from-the-authoritative-set.md) | kb-note | Grouping rows by a free-text field each row supplies for itself manufactures phantom groups indistinguishable from real empty ones. |
| [`kb-notes/methodology-search-the-awarding-body-not-just-the-name.md`](kb-notes/methodology-search-the-awarding-body-not-just-the-name.md) | kb-note | 90% of credentials carry an issuer word absent from title and variants; a false zero is the worst answer a retrieval system gives. |
| [`session_150_handoff.md`](session_150_handoff.md) | handoff | Next session: read Sierra's actual prose; scope the Common CR Reference. |
| [`kb-notes/methodology-a-copy-with-no-refresh-path-is-a-fossil.md`](kb-notes/methodology-a-copy-with-no-refresh-path-is-a-fossil.md) | kb-note | A denormalised copy nothing refreshes is a snapshot of the day it was seeded; measure the divergence, then change the consumer, not the copy. |
| [`kb-notes/methodology-a-one-shot-handoff-must-not-consume-what-it-cannot-deliver.md`](kb-notes/methodology-a-one-shot-handoff-must-not-consume-what-it-cannot-deliver.md) | kb-note | Clear a hand-off slot only after the value lands, and resolve the destination from the DOM — a module-level element ref goes stale the moment a widget mounts twice. |
| [`kb-notes/methodology-migrate-the-display-not-just-the-data.md`](kb-notes/methodology-migrate-the-display-not-just-the-data.md) | kb-note | Enumerate what the old surface DISPLAYED and confirm each item is shown or deliberately dropped; "it's still in the database" is not an answer. |
| [`kb-notes/methodology-a-filter-needs-a-field.md`](kb-notes/methodology-a-filter-needs-a-field.md) | kb-note | A filter request is a schema request: assign parsed values by explicit membership, never by `else`, and label what was derived. |
| [`kb-notes/methodology-a-provenance-label-must-say-why-not-what.md`](kb-notes/methodology-a-provenance-label-must-say-why-not-what.md) | kb-note | A chip naming the source of a derived value reads as a category of the thing unless it states what the source implies. |
| [`session_151_handoff.md`](session_151_handoff.md) | handoff | Next session: plan + build the Common CR Reference tab, then debug the EACR filters. |
| [`eacr_scope_lessons.md`](eacr_scope_lessons.md) | lessons | EACR college scope, the CER fold, the two-sub-tab rework and the accessibility pass. |
| [`sierra_surface_alignment_lessons.md`](sierra_surface_alignment_lessons.md) | lessons | Sierra is mounted by three separate files; keeping them saying the same thing, and the cross-file test that makes "aligned" mechanical. |
| [`kb-notes/methodology-a-filter-and-what-justifies-it-must-share-one-source.md`](kb-notes/methodology-a-filter-and-what-justifies-it-must-share-one-source.md) | kb-note | A filter, the column that justifies it and the export must derive from one scope — and the export must state that scope in the artifact. |
| [`kb-notes/methodology-an-opportunity-figure-must-be-what-peers-achieved.md`](kb-notes/methodology-an-opportunity-figure-must-be-what-peers-achieved.md) | kb-note | An "opportunity" number must be what comparable institutions ACHIEVED, never the maximum the record allows — 83% of adoptions are partial and nobody ever reaches the total. |
| [`kb-notes/methodology-a-fold-at-the-label-layer-is-not-a-fold.md`](kb-notes/methodology-a-fold-at-the-label-layer-is-not-a-fold.md) | kb-note | When a display resolver normalises two spellings to one label, the duplicate stops being visible without ceasing to exist — and every count taken through that resolver looks right while the data under it is still split. |
| [`kb-notes/methodology-revoking-from-anon-does-not-remove-the-public-grant.md`](kb-notes/methodology-revoking-from-anon-does-not-remove-the-public-grant.md) | kb-note | `revoke ... on function f() from anon, authenticated` reads like it closes a function to the API roles and closes nothing — PUBLIC still holds EXECUTE. Six security-definer functions that truncate live tables were reachable with the published anon key. |
| [`kb-notes/methodology-a-gate-cannot-protect-the-step-that-fills-it.md`](kb-notes/methodology-a-gate-cannot-protect-the-step-that-fills-it.md) | kb-note | Nine fail-closed gates sat between staging and live, and the first thing that broke was the step that fills staging — which no gate is downstream of. Also: the manual runs that passed met a state the schedule never sees again. |
| [`kb-notes/methodology-a-guard-test-must-not-be-able-to-fire-the-guarded-action.md`](kb-notes/methodology-a-guard-test-must-not-be-able-to-fire-the-guarded-action.md) | kb-note | The obvious way to test that code refuses to delete the wrong table is to ask it to delete the wrong table — a test that is correct only while the guard works, and performs the damage in exactly the case it exists to catch. |
| [`kb-notes/methodology-minimisation-happens-twice.md`](kb-notes/methodology-minimisation-happens-twice.md) | kb-note | A pipeline that fetches and then stores has two independent minimisation boundaries; treating the fetch as the only one quietly makes every fetched column a stored column, at the looser standard. |
| [`kb-notes/methodology-an-rls-filtered-read-is-not-an-error.md`](kb-notes/methodology-an-rls-filtered-read-is-not-an-error.md) | kb-note | PostgREST answers a policy-filtered SELECT with 200 and an empty array, so a missing credential and an honest zero are byte-identical — assert the credential on the wire, because no rendered state will tell you which happened. |
| [`kb-notes/methodology-a-report-must-read-the-screen-not-recompute-it.md`](kb-notes/methodology-a-report-must-read-the-screen-not-recompute-it.md) | kb-note | An export that derives its own figures is a second implementation, and the copy that leaves the building is the one that drifts; read the rendered view and the document inherits every guard the view enforces. |
| [`kb-notes/methodology-one-assistant-three-files-is-a-drift-machine.md`](kb-notes/methodology-one-assistant-three-files-is-a-drift-machine.md) | kb-note | When one surface is implemented in several standalone files, only a test that reads both and compares them keeps them saying the same thing — a rule applied once holds on the file the author had open. |
| [`kb-notes/methodology-a-partial-aria-pattern-is-worse-than-none.md`](kb-notes/methodology-a-partial-aria-pattern-is-worse-than-none.md) | kb-note | An ARIA role promises an interaction contract; half a pattern misinforms. Where a native element carries the semantics, take it. |
| [`session_163_handoff.md`](session_163_handoff.md) | handoff | Next session: Sam confirms the EACR default; work the 4 unclassified-only titles and sweep the stale test bounds. |
| [`session_164_handoff.md`](session_164_handoff.md) | handoff | Next session: build `buildMatrixView()` — the CER Adoption Matrix sub-tab. Sam's four design rulings are locked; the generator half shipped in #1226. |
| [`session_166_handoff.md`](session_166_handoff.md) | handoff | Next session: the matrix CSV export, and show Sam the partial-adopter variant (349 cells / 172 credentials / ~1,106 units) that Session 164 found and nobody has shipped. |
| [`session_168_handoff.md`](session_168_handoff.md) | handoff | Next session: Sam opens the redesigned My College tab and says whether it reads. Then the two region lists, the briefing filename convention, and the optional glyph sweep. ⚠️ `session_167_handoff.md` was never written — Sky166 named it and did not leave one. |
| [`delta_college_crosswalk_lessons.md`](delta_college_crosswalk_lessons.md) | lessons | SJCOE ↔ San Joaquin Delta: the college-scoped crosswalk. Why it is a different instrument from the statewide engine, the 42 adopt-now rows, and the absence-claim guard. *(2026-08-19)* |
| [`kb-notes/methodology-a-scoped-question-may-need-a-different-instrument.md`](kb-notes/methodology-a-scoped-question-may-need-a-different-instrument.md) | kb-note | When a follow-up looks like the last request "but narrower", state the best outcome for each; if the two are not the same sentence, a filter on the existing tool produces a well-formatted wrong deliverable. |
| [`kb-notes/methodology-shared-prose-over-claims-on-rows-it-was-not-written-for.md`](kb-notes/methodology-shared-prose-over-claims-on-rows-it-was-not-written-for.md) | kb-note | Prose authored per cluster inherits to rows it is false about. Any authored claim restating a computed column is checkable — assert it and fail the build. Manual review found 2 of 6. |
| [`session_170_handoff.md`](session_170_handoff.md) | handoff | Next session: Ashley's Delta meeting outcome feeds the offering map; the statewide engine's 2nd occupation list is still outstanding. |
| [`map_cleanup_worklist.md`](map_cleanup_worklist.md) | worklist | The prioritised CPL clean-up list: what to fix, in what order, and who fixes it — ranked by DECISIONS, not rows, because the biggest class by row count turned out to be two phone calls. |
| [`map_custom_report_load.md`](map_custom_report_load.md) | runbook | Loading the two new MAP Custom Report views: dispatch, the per-college reconciliation gate, the swap, and why the two tables' RLS policies must be restored SEPARATELY. |
| [`map_custom_reports_lessons.md`](map_custom_reports_lessons.md) | lessons | The three new MAP Custom Reports: served by the API we already pull, two wired, both loaded to staging and reconciled. The value is two dimensions no table we held carries, not freshness. |
| [`kb-notes/methodology-a-negative-result-needs-a-positive-control.md`](kb-notes/methodology-a-negative-result-needs-a-positive-control.md) | kb-note | "We looked and it isn't there" is only as good as proof, gathered in the same run, that the instrument could have found it. Plus: a response differing in KIND is a lead, not a tally entry. |
| [`kb-notes/methodology-falsify-a-claim-when-falsification-is-cheap.md`](kb-notes/methodology-falsify-a-claim-when-falsification-is-cheap.md) | kb-note | Cost of falsification, not degree of trust, decides whether to test a claim. Turn the caveat you already wrote into the experiment; name the residual; prefer a detector to a repeat question. |
| [`session_171_handoff.md`](session_171_handoff.md) | handoff | Superseded by 172. Load the two new MAP views and reconcile per-college. |
| [`session_172_handoff.md`](session_172_handoff.md) | handoff | Next session: staging is loaded and the reconciliation PASSED — the swap is the job, and it moves a public headline. Ashley's Delta lane still open. |

## Update history

- **2026-08-19 (Session 170, SkyFetch)** — **the reports were already there, and the probe lied about it** (#1246/#1247/#1248). Sam built three MAP Custom Reports and asked whether a session could fetch them. ⭐ **All three are served by the API the daily cron already pulls**, `dataCount` matching the builder exactly — so wiring was config (`REQUEST_PAYLOAD` 8 → 10 datasets) and ITPI's daily-push offer is declinable on the merits. ⭐ **Two of the three are OUR OWN SPEC coming back**: the 13-column report IS Dataset A of `map_dataset_sql_for_malone.md`, already held as `map_college_cr_unit`, so the job was a **reconciliation** — found by re-reading a committed doc, not by generating anything. All three deltas run one way (+1.46% · +3.07% · +10.02%) = our staleness resolving, exactly as `cpl_memory` predicted. ⚠️ **A standing §11 claim was wrong** — "catalog year: nothing we hold carries it" — `map_student_credit.catalog_year` is **100% filled**, 9 values. ⚠️ **THE PROBE REPORTED "NONE EXPOSED" AND WAS WRONG**: `columnName: []` had silently stopped enumerating and now 500s on known-good views, so its success condition could not fire for **any** view — only a positive control caught it. It still leaked the truth (valid names 500, invalid 400; the single 500 was the real student view, printed as ✗ and summarised away). ⭐ **Pedro's salt-hash assurance was corroborated, not merely accepted** — the spec's own warning run as an experiment: 5M decimals + 8 variants ≠ a sampled hash. ⚠️ **The payload IS the PII boundary** (minimisation is the *absence* of entries), now pinned by a test verified to FAIL, not just pass. 2 new KB notes; Sky167's narrative archived out of `CLAUDE.md`.
- **2026-08-19 (Session 169, Sky169)** — **what the college already teaches, and nobody claimed** (#1243). Ashley continued her August SJCOE crosswalk, scoped to one college for a meeting with San Joaquin Delta. ⭐ **A college-scoped ask is a different instrument, not a flag** — the statewide engine deliberately does not privilege the in-county college, and the two disagree about what a good answer *is*: engine best outcome *"some college offers this"* (a fact) vs *"this college teaches it AND the exhibit exists AND nobody joined them up"* (a task). Filtering the engine would have reported Delta as thin and pointed at Modesto — correct for a referral, useless with the college across the table. ⭐ **Keeping "teaches it" and "exhibit exists" in SEPARATE columns IS the deliverable**: crossed, **42 adopt-now** rows need neither curriculum nor exhibit, against **0 of 139 articulated at Delta today** (its lone career CPL is POST Basic Academy; the other 68 are AP/CLEP). ⭐ Delta holds curriculum for the utility/hydro cluster run 1 found had **no CPL anywhere in California** — 6 first-in-state rows — but **not lineworker** (substation/plant electrical ≠ line work). ⚠️ **A capability can be invisible to a program search**: `A CON 87A–90D` is a 10-course plumbing apprenticeship under no plumbing-named COCI program, prefix reading as *construction*, and MAP lists Delta on neither side of the statewide C-36 exhibit. ⚠️ **Shared prose over-claims on rows it was not written for** — the guard caught **6** ("no CPL anywhere" against exhibits that exist); manual review had found 2. Narrative copy moved out of the generator into the offering map, since hardcoding findings would make the reusable-engine docstring a lie. 2 new KB notes; §11 EACR cell compacted 5,629 → 2,862 (`stacked_roadmap_cell` cleared).
- **2026-08-19 (Sky169 · SkyRegister)** — auth model + repo posture measured and recommended; public/private split scoped (#1242); reviewer roster 5→10; 2 KB notes; handoff 170.
- **2026-08-17 (Session 167, Sky167)** — **the key was a ghost** (#1232/#1233/#1234). Sam: *"all the colleges are coming up blank on this."* `getSession()` on My College read `localStorage.cpl_team_session` — **a string occurring exactly once in the repo, as that read**. Nothing has ever written it, so neither the reviewer session (`cpl_sb`, kept fresh by the #1205 keeper) nor the team phrase reached the server, and **an RLS-filtered SELECT answers 200 + `[]`** — four gated tables returned empty arrays indistinguishable from *"this college has nothing"*, on every college, while the public reads beside them kept the tab looking healthy. **109 of 120 non-test colleges had a summary row the whole time.** ⚠️ **The 232-check suite signed in via the broken path and stubbed `fetch`** — it exercised the defect every run and asserted nothing about it; the new guard asserts HEADERS, because no rendered state distinguishes the bug from the truth. Then Sam's seven asks: the tab opens on a **scope question**, Sierra is a **collapsible section expanded by default**, Collapse all closes her too (his ruling), and the briefing is a **docx that reads the rendered DOM** so it cannot drift from the screen and inherits the k=10 suppression by construction. ⭐ **A roll-up must not leak a withheld college** — district and statewide sum unsuppressed rows only, or `total − visible` hands back the withheld figure. ⚠️ **Two of five scopes ship disabled with their reason**: SWP region and ASCCC region exist nowhere here, and the region data we DO hold (`college_geo`) is a ~10-way *proximity* map — SWP has 8 consortia, ASCCC has 4 areas — so a test pins that it stays unwired. ⚠️ **Two bugs the tests caught, both introduced by this work**: `finish()` hoisted the first `.cb-bar` in document order (a positional selector is a bound on authoring order), and the briefing extractor was **empty for two of the three scopes**. Sierra also aligned across all three surfaces she is mounted on. 3 new KB notes; `CLAUDE.md` net **−8.6 KB** (two finished rows + Sky165's narrative moved out).
- **2026-08-17 (Sky165)** — CER Adoption Matrix VIEW shipped (#1229): 434 credentials x 118 colleges, 17.0% inked, Sam's four rulings each carried by a check. Found and folded a college that was two columns (the `CaÃ±ada`/`Cañada` mojibake pair). Two neighbouring test bounds rescoped to properties. New KB note `methodology-a-fold-at-the-label-layer-is-not-a-fold`; new handoff `session_166_handoff.md`; Sky162's narrative archived out of `CLAUDE.md`. Session 164's work was lost unpushed — its `cpl_memory` rows survived and two are now corroborated.
- **2026-08-17 (Sky163)** — CER Adoption Matrix scoped with Sam and the generator half shipped (#1226): sandbox colleges dropped, duplicate spellings folded, per-college CR units emitted. New KB note `methodology-an-opportunity-figure-must-be-what-peers-achieved`; new handoff `session_164_handoff.md`; Sky160's narrative archived out of `CLAUDE.md`.
- **2026-08-16 (Session 162, Sky162)** — **the filter was answering a different question** (#1221–#1223, plus sibling #1224). Sam suspected the EACR College filter matched *adopted or could adopt*; he was right, and the ratio is the argument: **93.6% of College-filter hits were not adoptions** — Pasadena City College returned **1,790 cards against 44 adopted**, and the median card carries **1 adopter and 41 potentials**, because `potential` is *program-of-study under the same TOP code* (Rule 7, as a primary determination). ⭐ **The strong signal already existed and drove nothing** — the prescriptive M-ID layer (739 titles / 4,972 pairs, 25× tighter) **names the local course** and no filter could reach it, which is why the answer is **three scopes rather than Sam's two-position toggle**: a binary would pool it with the TOP guesses, the exact conflation being fixed. ⭐ **The exports were outside the loop I had just built** — found by re-reading against Sam's *goal* rather than my diff; CSV/JSON/Word still emitted the 41-college list, and **a spreadsheet outlives the screen that produced it**. ✅ CER wiring sound (**1,745/1,745** titles resolve); 8 credentials had been rendering twice under a **blank-issuer** twin (*unknown is not different*), and **5,135 `exhibit_ids` were in the payload and rendered nowhere**. ⭐ **The a11y pass found more defects in day-old work than in inherited code** — a half-declared ARIA tab pattern, a scope control exposing no selected state, a **colour-only** WCAG 1.4.1 failure. ⚠️ **`val()` guards the check; the driver is the other half** — handoff 161 predicted the harness trap and prescribed `val()`, the harness used it, and it still printed **zero checks** pre-fix: the throw was in a driver *between* checks. The prescription had been recorded as a mechanism, not a principle. ⚠️ The one red file on main was a **stale `{0,900}` window** from Sky160's own rename, not a defect. **All 224 test files pass.** 2 new KB notes, 1 updated.
- **2026-08-15 (Session 158, SkyKey)** — **one dead token wearing four costumes.** Sam reported six things live; four were real and **three were the same defect**. ⭐ **The Admin Save had NEVER worked** — `cobi_nav` held **zero rows**: a bulk POST is one INSERT over the **union** of the array's keys, so `audience` (NOT NULL, on tab rows only) arrived NULL on every group row → 400. The defect exists only ACROSS the array, so every row-level test and the jsdom mock passed, and an overlay **seeded empty on purpose** gives no signal that writes are failing. ⭐ **Sierra "not signed in", CR Reference "could not read" and the 400's misleading advice were one expired token** — a Supabase access token lives ~1h and **13 of 26 modules check only its SHAPE**; re-signing in "fixes" it, which is what hides the cause. Fixed with a **keeper** (`cpl_session.js`) rather than a fourteenth copy, so the 13 benefit untouched. ⚠️ **Shipping that alone would have been worse than the bug**: refresh tokens rotate, six modules renew from a cached session and three drop it on any failure = silent sign-out mid-edit; `credential_reference.js` already carried the fix *and the reason* and nine files did not. ⚠️ **A privacy guard had silently stopped guarding** (a negative pinned to a coordinator's name that left the data). ⚠️ **Every detector written this run was wrong on first writing**, all three caught by printing what they found. Sam diagnosed the last one himself: `sessionStorage` is **per browser tab**, so the magic link's new tab left his working tab signed out. 3 new KB notes.

