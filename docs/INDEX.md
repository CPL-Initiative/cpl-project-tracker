---
title: cpl-project-tracker docs — Index
created: 2026-05-27
updated: 2026-09-09
tags: [meta, index, obsidian-target]
kb-status: internal
obsidian-folder: cpl-project-tracker
related:
  - "[[CLAUDE]]"
  - "[[docs/kb-notes/README]]"
---

# cpl-project-tracker — Docs Index

Landing page for the project's documentation surface, intended as the
**Obsidian vault entry-point** when browsing this repo from the vault-side clone
at `CPLBrain/COG-second-brain/cpl-project-tracker/`.

The per-lane catalogs below are **generated** by `kb/_build_docs_index.py` from
each doc's own frontmatter — rebuild rather than hand-append. The prose on this
page is hand-written and is preserved across rebuilds; only the block between
the `generated:corpus` markers is replaced.

> Previously this page listed every document inline and reached **273,616 B,
> 6.8× the 40,000 B index budget** — a landing page you must scroll is not a
> landing page. The catalogs are where the full listings live now.

## The three lanes

| Lane | What | Where |
|---|---|---|
| **KB notes** | Durable, distilled, reusable knowledge | [`docs/kb-notes/`](kb-notes/) |
| **Lessons (WIP)** | Workstream scratchpads, append-only | `docs/<workstream>_lessons.md` |
| **Session handoffs** | "Fattyfat" capsules for the next session | `docs/session_<N>_handoff.md` |

See [`docs/kb-notes/README.md`](kb-notes/README.md) for the lane contract.

## CLAUDE.md reference offloads (`docs/reference/`) — added 2026-07-10 (Session 111, the pare-down)

Always-current project memory moved out of `CLAUDE.md` (2,514 → ~590 lines);
**Rule 9 checkpoints update these files now**, and `CLAUDE.md` keeps read-before
stubs pointing here.

| Doc | Was | Read before |
|---|---|---|
| [Pipeline Reference](reference/pipeline_reference.md) | `CLAUDE.md` §Pipeline Reference (1,087 lines) | generator/workflow/tabs/Supabase/EACR/C-ID work |
| [KB Build Status](reference/kb_build_status.md) | `CLAUDE.md` §KB & Unified Courses (421 lines) | KB/CCR curation work, build-phase history |
| [M-ID Lifecycle & CID/CIDx](reference/mid_lifecycle.md) | `CLAUDE.md` §11 prose + strategic roadmap (449 lines) | re-mints, MC/TMC calls, auditor, pathway decisions |
| [Branch policy](reference/branch_policy.md) | `CLAUDE.md` §Branch policy evidence (2026-08-28) | why a merge rule says what it says |
| [Engineering & UI practices](reference/engineering_ui_practices.md) | `CLAUDE.md` §Engineering & UI evidence (2026-08-28) | a UI rework, a First Light artifact, a table layout |
| [Obsidian vault wiring](reference/obsidian_vault_wiring.md) | `CLAUDE.md` §Obsidian vault wiring (2026-08-28) | vault-sync, exclusion, the sparse-checkout fix |
| [**`reference/lanes/` — one file per §11 roadmap lane**](catalog/reference.md) | `CLAUDE.md` §11 roadmap cells, 88 KB (2026-08-28) | **working any lane — and REFRESHING it at checkpoint** |

⚠️ **The lane files are the usual checkpoint edit now.** §11's table is a
pointer index: it carries each lane's state, and the lane file carries what you
learned. A checkpoint that updates only the row leaves 30 files to go stale.

---

## The catalogs

Every document in `docs/`, by lane. Rebuild with `python3 kb/_build_docs_index.py`
(`--check` fails when a rebuild would change anything, so CI catches a stale index).

<!-- generated:corpus -->
| Lane | Docs | Catalog |
|---|---:|---|
| Doctrine (behavior-shaping) | 5 | [`catalog/doctrine.md`](catalog/doctrine.md) |
| KB notes | 420 | [`catalog/kb-notes.md`](catalog/kb-notes.md) |
| Lessons docs | 76 | [`catalog/lessons.md`](catalog/lessons.md) |
| Workstream docs | 80 | [`catalog/workstream-docs.md`](catalog/workstream-docs.md) |
| Reference (pull-side) | 46 | [`catalog/reference.md`](catalog/reference.md) |
| Session handoffs | 222 | [`catalog/session-handoffs.md`](catalog/session-handoffs.md) |
| **total** | **849** | |
<!-- /generated:corpus -->

Not covered by a lane catalog:

- [`visuals/README.md`](visuals/README.md) — **Visuals**: decision briefs and
  mock-ups worth returning to, committed as HTML with a dated filename.
- [`catalog/`](catalog/) — the generated catalogs themselves.

## Top-level orientation docs

- [`../CLAUDE.md`](../CLAUDE.md) — project memory, Critical Rules, M-ID lifecycle (§11)
- [`co_platform_strategy.md`](co_platform_strategy.md) — **the long-term "plan of attack"** (Session 83): scaling COBI + the CPL KB into a governed, team-based, CO-wide platform — operating model, account migration off personal logins, knowledge lanes, real APIs vs scraping, governance/security/accessibility/HUMANS, decisions only humans make, pushback, and a scorecard against all ~14 asks
- [`roadmap_archive.md`](roadmap_archive.md) — museum annex to CLAUDE.md: completed roadmap rows + Session 26-31 narratives (moved out Session 33 to keep CLAUDE.md to live, steering content)
- [`../README.md`](../README.md) — first-time visitor entry
- [`../kb/README.md`](../kb/README.md) — knowledge-base schemas + generators

## Sierra integration docs (vendor-facing, added 2026-07-02)

Commissioned by Sam for integrating Sierra into a vendor-built platform:

- [`sierra_technical_reference.md`](sierra_technical_reference.md) — how
  Sierra is built: architecture, the full `cpl-chat` API contract (request /
  SSE protocol / errors), the six-lookup answer pipeline, behavior rules,
  data layer, client surfaces, security model, ops, v13→v26 timeline.
- [`sierra_integration_analysis.md`](sierra_integration_analysis.md) —
  benefits / risks / challenges of embedding Sierra on another site, the
  pre-launch preconditions checklist, and the decision points for Sam.
- [`sierra_integration_guide.md`](sierra_integration_guide.md) — the vendor
  implementation plan: link / iframe / native-API / server-proxy paths, a
  minimal reference client, non-negotiable client requirements, launch
  checklist, ongoing-operations expectations.
- [`sierra_iframe_implementation_guide.md`](sierra_iframe_implementation_guide.md)
  — the **day-one iframe recipe** (2026-07-03): the exact `?ctx=external`
  URL, annotated markup/CSP/sandbox, sizing, QA checklist (incl. the
  contacts-gate check), launch coordination, rollback.
- [`sierra_maturity_roadmap.md`](sierra_maturity_roadmap.md) — **Malone's
  scope-and-sequence to end-state** (2026-07-03): Phases 1–6 (guardrails →
  contract hardening → content maturity → integration graduation →
  recommender depth → platform ops), efforts, dependencies, the three
  human-only decisions, the critical path. The iframe is explicitly interim.
- [`sierra_vendor_lane_handoff.md`](sierra_vendor_lane_handoff.md) — the
  lane handoff (2026-07-03): what shipped (#654/#657 + v27 LIVE), locked
  decisions, verified access facts, the priority queue, safety rails.

## Reference materials

Authoritative external sources we've cached:
- [`reference/`](reference/) — ASCCC / COCI / CCN-CID source documents
| [`statewide_fire_electrical_crosswalk_lessons.md`](statewide_fire_electrical_crosswalk_lessons.md) | lessons | Statewide Fire / Wildland / Cal-JAC / Electrical CPL crosswalk: the chain completes for fire, half-completes for wildland, and breaks for electrical. *(2026-09-09)* |
| [`kb-notes/methodology-follow-the-recommendation-to-the-course-that-receives-it.md`](kb-notes/methodology-follow-the-recommendation-to-the-course-that-receives-it.md) | kb-note | "A credit recommendation exists" is not the end of the chain — count how many colleges teach the course it names. A recommendation pointing at a course nobody offers looks identical to a healthy one. |

## Update history
- **2026-09-09 (SkyLedger, S247 — the phone, the pinch, and a question that moves the map)** — Sam's three SkyView asks in one message: the header toolbar *"now takes up half the screen"*, pinch does not zoom on mobile, and the search box should take questions *"like Sierra handles"*. All three shipped ([#1530](https://github.com/CPL-Initiative/cpl-project-tracker/pull/1530), `6148982`). ⭐ **The number was not the number**: measured at 390×844, `#u-top` was **254px over four wrapped rows (30%)** and `.u-foot` another **430px** — more than the header — so **81% of a screenful was not the map**, and his "half" was low. ⚠️ **And the header was only half the cause**: `fitCanvas()` tested `window.innerWidth<700` BEFORE the solo test, pinning the canvas at **0.62 × the viewport** — 523px at 844, exactly the canvas that was there — so shrinking the row would have freed 140 pixels with nowhere to go. ⚠️ **And with both fixed it still opened at 451px**, because `fitCanvas` ran before the row settled and a phone's window never resizes; a `ResizeObserver` on the chrome is the fix ([`methodology-one-symptom-two-causes-and-the-obvious-fix-changes-nothing`](kb-notes/methodology-one-symptom-two-causes-and-the-obvious-fix-changes-nothing.md)). Final: header **114px**, map **730px (86%)**, desktop untouched. ⭐ **The rail he proposed was refused on geometry** — 48px of a 390px canvas cannot hold *Rotate · Pan · Move · Articulations · Isolate*, so a rail settles the plain-words rule by shape before anyone argues it; the breakpoint then moved **900 → 1100 mid-build** on a measurement (an iPad landscape at 1024 was in the wrapped zone, a three-line 186px header). ⭐ **Two fingers were worse than none**: `touch-action:none` turns the browser's pinch off and `pointerdown` set `drag` unconditionally, so the second finger replaced the first one's grab and both fed the same pan — a 5× spread changed the zoom by **nothing**. ⭐ **The ask translates, it does not answer**, and that is correctness: cpl-chat retrieves from the KB, which does **not** contain SkyView's payload, so prose about it would be composed from a corpus that cannot hold the answer; the model returns a selection in the map's own token grammar and the page counts ([`methodology-ask-a-model-to-translate-not-to-answer`](kb-notes/methodology-ask-a-model-to-translate-not-to-answer.md)). ⚠️ **A surface must be declared in FIVE places and three suites found every one I missed**, in turn. ⭐ **Three defects the tests found and a reading would not have**: the ask affordance was invisible exactly when it became available (a question matches no course title, so the list hid itself and took the "Enter asks" footer with it); the footer's condition disagreed with the key it described; and a guard of mine was unreachable-false ([`methodology-a-check-that-cannot-fire-on-what-it-names`](kb-notes/methodology-a-check-that-cannot-fire-on-what-it-names.md)). ⚠️ **Two harness gaps wider than this lane**: `scripts/a11y.js` reported every **disabled** control as "focusable with no ring" on every view (`focus()` is a no-op there — confirmed pre-existing by sweeping the stashed prototype), and the **dependency map records line numbers**, so +180 lines turned CI red on a locally-green suite. **This checkpoint also compacted the SkyView lane from 33,041 B (2.75×) to 8,647** by splitting ~40 invariants into `docs/reference/skyview_invariants.md` — they answer a question you arrive with, which is the PULL side of the assignment rule. 321/321 suites, `npm run a11y skyview` 11/11. 3 KB notes.
- **2026-09-09 (SkyText — the funding text surfaces, three curator affordances, and two dead guards)** — Sam prepared the Implementation Funding tab for debut, editing the introduction live, and asked for language revised against the draft Title 5 §55050. One sheet, nine calls, all nine answered and executed ([#1528](https://github.com/CPL-Initiative/cpl-project-tracker/pull/1528), `1570fa6`). ⭐ **His rulings:** the banking sense of **draw** is out (*"a business term tied to banking"*) — 14 rendered sites became *earn*, one *receive*; **unspent** → *unearned*; and ⚠️ **expended and allocated are not interchangeable** — *"expended should be kept if I am referring to the colleges spending the funds"* — which narrowed a sweep that would have run too wide. Plus active voice naming the actor, *model* not *engine*, and ⭐ **the outcomes are Ed. Code's, not the model's** (*"they are drawn from Ed Code"*). ⭐ **A `>` blockquote for the statute he quotes**, all-or-nothing so *3 > 2* stays literal — and its reverse mapping turned load-bearing when his introduction became the DEFAULT: `setText()` compares typed text against `htmlToPlain(default)`, so a lossy round trip would make Edit-then-Save store an override rendering the statute as body prose. ⭐ **Rename and Hide-on-the-public-page ride `sectionShell()`**, the one function every section passes through, reaching four surfaces at once; the explainer's seven sections read the tab's OWN resolver rather than a second lookup. ⚠️ **Which is why the redirect stopped being tidiness** — hiding rode only the old public page, so a section the CO held back stayed visible on the explainer. ⚠️ **Two guards were dead and read as clean passes**: `detail_trim` counted a phrase the sweep deleted (*at most one* is satisfied by zero — third occurrence of the wording-coupled pattern, first found by accident), and `cobi_prose_measure` broke on the redirect after I predicted it would pass. ⭐ **And `npm test | tail` reports TAIL's exit status** — I called the suite clean twice off it, once over a run whose failure was printed in the text I was tailing. 2 KB notes.
- **2026-09-09 (SkyPlain, S245 — a triage keyed on the wrong thing, and three checks that could not fail)** — Sam's four asks: the CC session banner he could not see, keep sweeping COBI for dark mode, fix AA / mobile / glyphs along the way, and **leave Implementation Funding alone** (a parallel session had it). Dark contrast findings **184 → 120 (−35%)**, light **18 across eight passes**, glyph control-class ours **26**, `npm test` 316/316. ⭐ **THE TRIAGE WAS KEYED ON THE SELECTOR AND THE CAUSE IS THE COLOR PAIR**: the largest dark fault — 25 findings, 11 routes — wore **12 different selectors at one route each**, so `a11y_triage.js` printed it as twelve `one route — that tab's own CSS` lines at the BOTTOM, below faults a tenth its size. Its own header already said *"a ratio repeated exactly is ONE color, not many"*; it applied that on the route axis only. And `a11y.js` **computed the composited background and threw it away**, so a report named `1.21:1` on `h3` and left you to guess which of forty greys. Recording `fg`/`bg` collapsed **193 "causes" into a handful** ([`methodology-group-an-audit-by-the-decision-not-the-symptom`](kb-notes/methodology-group-an-audit-by-the-decision-not-the-symptom.md)). ⭐ **THREE CHECKS COULD NOT FAIL, IN THREE TOOLS, ALL QUIETLY GREEN**: `prose_only()` looped every mask pattern with a hardcoded `re.S`, so the line-anchored indented-code rule's `.*` ran past the newline and masked **to end of file** — on one lane file, bytes **1,996 of 11,749**, everything below line 44 exempt from `american_spelling`, `house_voice` and `self_corrected_word_pair`; the glyph report counted the 348 findings `--apply` correctly refuses (generator-owned, Rule 1), so "401 control-class" was **26** actionable; and the glyph patterns matched literal emoji and entities only, missing **13 written `"\u{1F512}"`** — a padlock on screen, seven ASCII characters to a scanner. The first surfaced only because I wrote `colour` twelve times and the rule whose word table *begins* with that pair said nothing ([`methodology-a-check-that-cannot-fail-reads-as-a-clean-result`](kb-notes/methodology-a-check-that-cannot-fail-reads-as-a-clean-result.md)). ⭐ **THOSE 13 WERE A USABILITY BUG, NOT A GLYPH ONE**: found by chasing a 1.38:1 contrast finding on *"You are not signed in."* and reading the rest of the sentence — *"the 🔒 button in the header"*, a control moved into the About pane months ago, `cobi_identity.js` saying so in the past tense. Six files sent locked-out readers to something that does not exist. ⭐ **A FILL THAT DOES NOT FLIP NEEDS INK THAT DOES NOT EITHER**: `--gold-accent` is `#E3B341` in **both** themes, and badges painted `--navy-primary`, which flips — 8.77:1 light, **1.61:1 dark**. `--on-accent` is the obvious repair and is `#FFFFFF` in light, so it paints white on gold at **1.95:1 and regresses light**; hence `--on-mustard`, never redefined ([`methodology-a-fill-that-does-not-flip-needs-ink-that-does-not-either`](kb-notes/methodology-a-fill-that-does-not-flip-needs-ink-that-does-not-either.md)). ⚠️ **EVERY SWEEP CONTAINED A SITE THAT MUST NOT MOVE** — course-description **data** in `coci_lookup_desc_*.js`, two archived decision sheets, `project_lifecycle`'s badge where `#6b7280` is a **background**, one `#4b5563` on an explicit fill, and **64 of 69 `#555` uses inside the regenerated section**, fixed in the generator's 12 emission sites instead. A match count larger than the fault you set out to fix is a signal, never a windfall. ⚠️ **AND A GLYPH THE TESTS GUARD IS SOMEBODY'S DECISION**: clearing `cip_crosswalk`'s `✓`/`⇄` turned CI red, and the guarding comment named it as **Sam's own 2026-07-18 call** — reverted the code, left the test; the count is 26, not 24. **The banner was not broken** — the row was `active` but **expired 2026-09-08 20:29 UTC**, and session visibility governs whether a teammate can OPEN the link, never whether the bar RENDERS; fixed alongside, its expiry read `getTime() <= Date.now()` and `NaN <= anything` is **false**, so an unparseable date skipped the check entirely. ⚠️ **Sam then asked why sessions had stopped offering the checkpoint**, and the rule was not demoted: `checkpoint_overdue` is computed ONLY by the lint, and the only instruction to run the lint is step 0 of `/checkpoint` — **the signal fired only once you were already checkpointing**. Replaced with two git commands a session can run unprompted; `CLAUDE.md` is **under its 60,000 budget for the first time in weeks** (59,772) despite the addition. Eight new guards, each verified by reverting its fix. 3 KB notes.
- **2026-09-09 (SkyExit, S244 — one theme control where there were four, five SkyView items, and a remediation pass)** — Sam asked for a dark mode selector in the COBI header that *"sets all tabs and windows using that one control"*. ⭐ **The ask named a real split: COBI had FOUR independent answers to "is it dark"** — `cpl_memory.js` shipped its own button that wrote `data-theme` and **persisted nothing** (a reload lost it, and it disagreed with every other tab meanwhile); `map_cleanup_views.js` and `map_data_quality.js` were already on the right contract; `our_process.js` keyed on `@media (prefers-color-scheme:dark)` **alone**, so it followed the OS and could not be told otherwise. A user-visible inconsistency was four mechanisms, not one bug. `cpl_theme.js` owns it now — System · Light · Dark, `localStorage`, `data-theme` on `<html>`, a `storage` listener for other windows, loaded in `<head>` and render-blocking so a dark reader gets no white flash. Three states because forcing light on an OS-dark reader would regress the four tabs that already honored the OS. ⭐ **A TOKEN WITH TWO JOBS CANNOT BE THEMED**: the invisible wordmark invites flipping `--seal-blue`, but it is **266 uses, mostly background fills carrying white text, against 48 text uses** — flipping fixes one visible element and turns 200+ fills into white-on-`#7DA1D4` at 2.3:1. Split `--seal-blue-text` instead; a test asserts the token is NOT redefined dark, because the symptom invites the wrong fix ([`methodology-a-token-with-two-jobs-cannot-be-themed`](kb-notes/methodology-a-token-with-two-jobs-cannot-be-themed.md)). ⭐ **The sweep found what reading the palette could not**: a new `cobi-dark` a11y target opened at **38/38 routes, 511 findings**, six shared-chrome selectors being ~227 of them — `--navy-primary`/`--navy-secondary` are LITERALS not `var()` aliases (1.07:1 on every h2/h3), two buttons paired a themed `--cobalt` fill with a hard-coded `#fff` (2.65:1), the Unlock gate painted `#f5f5f5` and let its text inherit (1.09:1). **Dark 38 → 26 routes; light 19 → 17**, improved not regressed, and the new control introduces no fault in either theme. ⚠️ **Fixing the rule you found is not fixing the rule that applies** — `.cpl-tab {color:#666}` was corrected and the sweep was unchanged because `.cpl-sidebar .cpl-tab {color:#444}` paints the rail. ⚠️ **And the token split's own regex had two bugs, both caught by re-reading the diff**: `color:` also ends `border-color:` (20 border declarations rewritten), and text on an explicit fill is not text on the ground (1.9–2.4:1). **Then Sam's five SkyView items, all shipped**: rotation `SPIN` 0.045→0.018; ⭐ **a dropped course PARKS** — it snapped back because a member has **no position of its own** (drawn on a spoke at an angle from its index), which also rules out a screen coordinate, so it lives in the world frame islands use for `dx`/`dy`; **level proximity** measured first (only **12%** of points carry a level word) so level orders *within* the ring score already chose, same-level pairs **19.9% closer** over the same 1,990 pairs; **CTE vs academic** from the one TOP use this repo trusts (25,857 · 16,470 · **7,569 with no verdict**, which got its own switch rather than being filed under Academic); and an **Isolate** toggle — ⭐ **whose real bug was a memo**: `islandPass` caches on the Show-switch signature and isolation changes what passes without touching a switch, so the map would not have changed at all. **Finally a standing pass**, [`/a11y-pass`](../.claude/commands/a11y-pass.md): check → **triage** → remediate → re-measure → record, with `scripts/a11y_triage.js` (ranks a saved sweep by blast radius; reproduces those six root causes in a second against two manual re-reads) and `kb/_glyph_sweep.py` (**baseline 1,549 findings across 139 files — 516 control · 225 status · 808 decoration**; a comment line is never a finding, and only the control class is mechanical). ⚠️ Sam's *"remove all emoji glyphs"* supersedes the 📋/🧭/⚖️ exceptions. 314/314 suites; every guard verified by reverting its fix. **Then Sam answered the sheet — all eleven, the same night** (*"decisions done!"*), and the run executed them. ⭐ **The clean-up ran and the authorized number was not the executed one**: 516 is the count of control-class FINDINGS, while the tool's mechanical rule (a *leading* glyph, space, letter, in a quoted string, on a non-comment control line) matched **117** — final **115 stripped across 23 files** plus **16 in the generator**, control-class **516 → 401**; the other 399 need a reworded sentence, not a deletion. ⚠️ **13 of the 16 rewritable sites in the dashboard HTML belonged to the generator**, which replaces those sections daily — so the sweep would have passed every test, been reported as done, and been reverted overnight with nobody told. ⭐ **And the guard against that had to be written twice**: the first asked whether the glyph+label is a literal in the generator source, held 13 of 16 — then the generator was fixed first and the same guard on the same HTML held **2**, silently, because the fragment it looked for was gone. **A guard whose protection depends on the order you work in is worse than none, because it reports a plausible number either way.** Replaced by the UNION of a positional test and the fragment test (the region list is only as complete as its markers; `render_algo_details()` sits inside none of them), pinned by an order-independence test and a marker-drift test verified by renaming a marker ([`methodology-a-guard-that-depends-on-order-is-worse-than-none`](kb-notes/methodology-a-guard-that-depends-on-order-is-worse-than-none.md)). Two suites failed and were right to — `✓ Saved` → `Saved` — so five test files moved with the label, names and comments included. **Two of his answers closed questions rather than opening work**: *"Looks good now:)"* settled the Sky's frame rate that two sessions of headless measurement could not, because every number was taken on a machine that is not his; and the live-session banner is not broken — it has **two switches**, the claude.ai toggle deciding whether the link works and a COBI row deciding whether the banner shows, and the row was pointing at an earlier session with an expiry six hours past. ⚠️ **One sheet claim was wrong**: "an emptied discipline stays drawn" is a BUILDER change, not a one-line renderer tweak — `build_islands()` walks `by_disc | sa_by_disc`, so an emptied discipline is absent from the payload and never reaches the renderer at all. 3 KB notes.
- **2026-09-08 (SkyGate, S243 — a note that never printed, and one CSS line failing 38 tabs)** — Two defects of one family: a value computed correctly that **never reached the reader**. ⭐ **SkyView's edge-vs-vote disagreement note printed on NONE of the nine subjects.** S242 built the line that fires when the subject map overrules what a subject's courses say and recorded **four** cases; measured against `prototype/ccr_universe.json` and the map file `EDGE_URLS` actually fetches it is **nine** — ATHL (Physical Education vs Kinesiology, **1,101 of 1,101**), THTR (1,093/1,109), ESCI (465/476), ELEC (342/353), MUSC (127/134), ETHN (34 under Chicano Studies), PHTO (3/12), ESLN (a malformed name carrying a TOP code) and ENVS. ⚠️ **And zero of them printed**: `standingHtml()` built the note and appended it to ONE of its four exits — the branch for a subject that IS its home's Common SUBJ — and a disagreeing subject is by construction usually not that, so it left by the umbrella or not-its-code exit, both of which dropped it; ATHL and THTR returned earlier still, at the no-seed-entry guard. The lane file's *"the row prints both"* was false, and the count being wrong in the **safe** direction is why it survived — four does not look like a bug. The guard has **one check per EXIT**, not per message, because the wording was never wrong; reverting the fix fails exactly four of nine ([`methodology-a-message-must-ride-every-exit`](kb-notes/methodology-a-message-must-ride-every-exit.md)). ⭐ **The 93 remaining discipline blanks are NOT mechanically fillable, and the payload says so**: they span 50 subject codes, **37 of which have no other identity carrying that prefix at all**, and 6 more read "unanimous" off a **single** row — HUMN's one voter is *Music for Video Games and Film* while its three blanks are popular-culture titles, so that reading files three humanities courses under Music. Only **five** codes carry weight (BSOT 369 at 99% Office Technologies, HUMA 112 at 96%, GRAF 72, BCST 54, BARB 5), covering **7 of the 93 rows**; DR-25 owns the map, so they went on a decision sheet rather than into the file. ⭐ **One CSS declaration was failing every COBI tab**: `input#cplfl-optout` at **182.1 × 19.9** against SC 2.5.8's 24px floor — the sole fault on 17 routes and one fault line on all 38, since `first_light.js` paints on every tab. ⚠️ The floor belongs on the **label**, not the input: the engine measures the hit area and a wrapping label replaces its control's box. With `p.tphx-intro` off the decorative `--text-faint` (3.24:1), measured back-to-back: **38 routes failing → 19**. The remaining 19 are sized, not fixed — 71 distinct failing selectors, 5,332 sub-24px targets, 22 keyboard-unreachable scrollers. ⚠️ A background sweep and a foreground edit of the same file cannot both be trusted: my revert-verification `sed` ran mid-sweep and made the 390px pass read the reverted file. 313/313 suites. **Then Sam ruled all nine, and his one note was the most valuable thing on the sheet.** All `yes`: five codes into the subject map (blanks **93 → 86**), HOSP kept out as genuinely split, and five map values moved to what the courses say (disagreements **9 → 4**) — ⭐ every one of the five moving the map INTO agreement with the CSR, three of them having named a discipline the CSR does not carry at all. ⭐ **Against ATHL he wrote *"the only one to double check … differentiated from KINE which doesn't have the restrictions athletic PE or KIN course"*, and it named a real hazard**: the CSR lists ATHL as a Kinesiology VARIANT with **1,468 M-IDs**, and the CSR is consumed by the Phase 1e re-mint **to fold variants to the canonical** — Session 47 caught that fold doing exactly this. ATHL was safe, but only by a **literal** in `_subj4_dryrun.py`: the one umbrella of four not declared as data. Declared in the CSR now, so every reader sees it and SkyView's row says *an umbrella code under Kinesiology* rather than *not Kinesiology's code*. ⚠️ **And my own sheet's item 8 had its premise wrong** — ESLN's name is not malformed by us: **eight MQ names carry a Title 5 section number** (53412 / 53414) from the 19th-edition index, the tell being `Speech Language Pathology: Disabled Student Programs and 53414 Services` where the number splits the phrase. 45 live rows but **1,183 occurrences across 33 files**, so the repair is an id-keyed rename with a dry-run and a receipt, held for its own PR. **A sheet item can be ruled right and still have described the wrong size.** The remaining 86 blanks are a named curator list (`kb/discipline_blanks_worklist.json`, rebuilt by the daily run).
- **2026-09-08 (SkyTrue, S242 — the frame budget, and a bill that moved instead of leaving)** — Two levers on the Sky's frame, and Sam's question about the no-discipline pile answered mid-run. Back-to-back on the served page, same machine: a median frame **81 ms → 46 ms**, about **12.3 → 21.7 fps**. ⭐ **A memo keyed on a value that drifts is not a memo**: `textW` cached on `ctx.font` + the string, and an island label is sized off its drawn radius — `18.0263px`, `18.2506px`, `18.1185px` as the sky turns — so it never hit once, and each miss handed Chromium a font size it had to build (`measureText` **11.2%** of the profile against `textW`'s own 0.5%, on 15 calls a frame). ⚠️ **Fixing it moved the bill rather than removing it**: calls fell 15.4 → 0.17 a frame and **`strokeText` went 0.6% → 9.6%** — a font is built at its first USE, and with the measure no longer asking, the stroke was next in line. Only rounding the drawn size stopped it (`labelSize`, whole pixels with a **0.6px dead band**, because a bare round is a threshold on a drifting value — `NODE_ZOOM_KEEP` again) ([`methodology-fixing-a-cost-can-move-it-rather-than-remove-it`](kb-notes/methodology-fixing-a-cost-can-move-it-rather-than-remove-it.md)). ⭐ **An island on screen is not an island whose points are on screen**: S241 settled which islands the window shows, but a discipline wider than the window still spilled — **5,755 of 27,931 batched dots a frame (20.6%) lay wholly outside the canvas**; tested inside the fast branch only, where the dot has no halo, light or ring. ⚠️ **The first draft of the guard was a decoration** — both fixture islands sat at the label-size clamp and all three points near the middle, so it passed with every fix reverted; fixed with `__ccrTextStep(0)` and a point 4,000 units out, one check deleted as unfailable, and each remaining one verified by reverting its fix. ⚠️ The revert harness overwrote its own backup and tested a state missing the fix for two rounds — the suite caught it. **Sam's question**: `PSYC C1000` *does* carry Psychology — `kb/reference/coci_courses.json`, classified 2026-05-20 — while the payload says `disc:null`. Two minting paths, one inference pipeline: all five passes read `coci_minted_courses.json`, **19,568 records all M-ID**, and `excel_to_dashboard.py` reads only `description` from the C-ID/CCN reference. Blank rate **0.4% M-ID vs 47.7% official**; **199 of the 326 recoverable today** ([`methodology-a-discipline-can-exist-in-the-repo-and-never-reach-the-payload`](kb-notes/methodology-a-discipline-can-exist-in-the-repo-and-never-reach-the-payload.md)). 9 new checks, `npm run a11y skyview` 11/11.
- **2026-09-08 (SkyClear, S241 — the asteroid field, and a fix that made the picture worse)** — S240 shipped two flicker fixes and Sam still saw it; his own words found the cause: *"an enlarged grouping that is crossing over all the others — like a loose asteroid field spiraling around"* (#1516). ⭐ **The sky is stereographic, so scale grows as `sec²(ang/2)` off the view direction** — 1.3× at 60°, 4× at 120°, **131× at 170°** — and `projectDir` refuses only past 174.8°, so an island almost directly BEHIND the reader still projected, at a hundredfold scale. The screen cull was a bounding box built from `isl.r * k`, so that inflated radius covered the window and **the cull passed**: the island drew as a sprawl of its courses over every other discipline. ⚠️ **It was in S240's own output** — its probe logged Music at `k = 71.5` beside islands at 0.2–2.4 and read it as a normal cull. The fix is ANGULAR (`acos(cz) - S.th > angMax`): largest scale **88.4× → 1.5×**, frames with one over 8× **149/150 → 0/150**, and because **89 of 159 islands at the default zoom were drawn having never been visible**, **11.4 → 17.4 fps**. ⚠️ **S240's `dt` fix was right and made the picture worse and did not measure that**: time-true motion at 8 fps steps further than broken half-speed motion, 18.59 → 21.87 — a correctness fix and an appearance fix are different claims. The remainder is spread over 877 of 1008 cells (uniform motion plus 27,000 one-pixel stars aliasing), not a defect; a rate sweep that would have hidden it was measured and reverted. Sam's two asks settled by measurement: **188° across** (the widest where all 99 islands keep their stars; 240° drops four) and the **Map button** leaving the row — his second reversal of a committed ruling in two days, both named in the code and beside the new assertion. 309 suites, `npm run a11y skyview` 11/11.
- **2026-09-07 (SkyDome, S240 — three recordings, six bugs, and a profile that moved the lever)** — The first run driven entirely by **screen recordings**. Sam sent three, each a sentence long, and **not one named its own cause** (#1515). ⭐ **The flicker had two causes, and the two obvious suspects were both innocent** — rebuilding the page without the twinkle moved the per-frame difference 18.26 → 19.46, and without the 8-step star-alpha bucketing 18.26 → 18.20, while stopping the turn gave **exactly 0.000**. The real ones: `dt` was clamped at 0.1 s, *below* the measured 133 ms frame, so `sph.spin` advanced a **fixed 7.20e-3 rad while the interval swung 192–319 ms** — and the sky had been turning at **0.0393 rad/s against an intended 0.0720**, 55% speed, since it shipped; and `showNodes = k > NODE_ZOOM`, a flat-map rule where `k` is one global number, was being asked per island on the sphere where it is `sec²(ang/2)` × the center's — **18 of 159 islands within ±3% of the threshold**, 11 flipping inside 120 frames, each blinking a whole discipline's dot field while its disc and name stayed put. ⚠️ **A third variant looked like a 7× cure and was an artifact**: it had switched every dot off, and an empty canvas is very stable. The purple sky was `--sky-island-sel` #2E2A44 — a selection tint is a figure-ground relationship, and past the zoom where the disc's edge leaves the window there is no ground left. A carry released only on the canvas paths meant that after one move from the panel **every Drag button was a silent no-op for the rest of the session**. Sam's four asks shipped with them: the outline as a **sheet over the map** (his own better idea — nothing to restore because nothing is lost), a **Back** that parks and restores the camera, a sidebar that closes by dragging its border, COBI in the ⋮ menu, and *Move instead…* on a staged move — which **reversed an S239 test assertion**, changed with the old reasoning named in a comment. ⚠️ 309 green jsdom suites saw none of it; two needed Chromium and one needed `npm run a11y skyview`, which failed all 11 routes on a 5px overhang. ⭐ **S239's frame-budget lever was backwards** — the canvas is 5.9 ms of a 133 ms frame; the cost is `measureText` (12.3%) and per-point JS. 1 KB note ([`methodology-a-bug-report-is-evidence-not-diagnosis`](kb-notes/methodology-a-bug-report-is-evidence-not-diagnosis.md)).
