---
title: Session 210 handoff — from SkyGov (Session 209, the map and its projections)
created: 2026-08-30
updated: 2026-08-30
tags: [handoff, session-210, dependency-map, doctrine, governance, memory-consolidation]
kb-status: internal
obsidian-folder: cpl-project-tracker
---

# You are Session 210

SkyGov here. Four PRs, all merged:
[#1396](https://github.com/CPL-Initiative/cpl-project-tracker/pull/1396) (the
dependency map, remediation A),
[#1397](https://github.com/CPL-Initiative/cpl-project-tracker/pull/1397)
(governance candidates project from it),
[#1398](https://github.com/CPL-Initiative/cpl-project-tracker/pull/1398)
(Admin surface projects from it), plus the checkpoint commit carrying this file.

## Your assignment (Sam, 2026-08-30, closing the session)

> "I'll hand this off to the next session to complete our claude.md and memory
> consolidation work."

That is the doctrine/consolidation lane. Read IN ORDER before touching anything:

1. `cpl_memory` (Rule 8 — query FIRST): tags `doctrine`, `dependency-map`;
   slugs `sam-approved-five-doctrine-remediations-2026-08-29`,
   `sam-ruled-js-tests-required-but-conditional-fast`,
   `dependency-map-built-remediation-a-2026-08-30`,
   `governance-and-admin-tabs-project-from-dependency-map`.
2. `docs/doctrine_enforcement_lessons.md` — the 2026-08-30 section is this
   session's full story.
3. `CPLBrain/04-projects/cpl-initiative/doctrine-probes/` — the probe lane's
   state, criteria, and queue live in the VAULT, not here.

## The consolidation queue, in priority order

1. **`cpl_memory.scope` — STILL Sam's call, still do not write it.** Unchanged
   since 207: 68 of 652+ rows carry `scope`, uncontrolled vocabulary, 25 of the
   68 duplicated in the row's own `tags`. Standing recommendation: a two-value
   axis (`global` vs `lane-local`), `tags` keeps topic. Put it to Sam as a
   decision, then execute whichever he picks.
2. **The two uncaught doctrine scenarios** (`kb/_doctrine_scenarios.py` scores
   10 of 12 honestly): a `cpl_memory` row that contradicts doctrine, and a
   conditional checkpoint item the auditor cannot see. Both need a checker
   spanning two stores — an architecture step, not another `CLAUDE.md` rule.
3. **Remediations B, C, D, F — approved 2026-08-29, none built.** B widen Rule
   10 beyond `kb_curation` to any shared write · C a rollback path for DATA
   writes (`git revert` covers code only) · D route a new write surface through
   Governance + privacy ADRs · F fix rule-number drift across citations. A is
   DONE (this session). E (js-tests required-but-conditional-fast) is ruled
   ("option 2!") and not built — one `if:` on the `npm test` step, then Sam
   flips branch protection (a GitHub setting sessions cannot make).
4. **Carryovers from 205/207/209, untouched:** the 5 British-form *filenames*;
   `docs/roadmap_archive.md` at 3.48× (decide whether `other` is the wrong
   budget for an archive lane); `vault_heavy_path` (45 — a Windows-side
   sparse-checkout action for Sam); the two public-KB recommendations awaiting
   Sam's go; whether the context-pressure hook actually installed on Sam's
   Windows machine (reviewed, never verified — ask him).

## What S209 left OTHERS waiting on (not yours unless Sam redirects)

- **NEEDS SAM: judge the 15 new governance table candidates** — the Governance
  tab strip now proposes `cpl_memory`, `item_raci`/`item_updates`/
  `team_members`, `liftoff_state`, `map_data_quality`, `factsheet_overrides`,
  `gr_areas`/`gr_revisions`/`gr_artifacts`, `cpl_funding_config`/`_notes`/
  `_participation`, `cpl_adoption_interest`, `cpl_reflections`. Map or dismiss
  in `kb/governance_surface_map.json`; the reason is the point. The noise
  guard sits at <30 with a comment saying to tighten it back after this
  backlog clears.
- **NEEDS SAM: react to the "Blast Radius" Admin-pane mock** (artifact link in
  the session log; dataset → consumers, writers, direct-to-main and stale-copy
  strips). Prototype-first: do NOT port into `admin.js` before he reacts.
- **Two stale-copy fixes, small and mechanical when someone wants them:**
  `cpl_pathways_ccr_data.js` is rebuilt daily but absent from
  daily-dashboard.yml's commit list; `kb/credentials.json` is rebuilt by
  `kb/_match_cos_authority.py` under cos-authority-sync.yml and absent from
  ITS list. Both pinned in the map's stale-risk section.

## The new instrument you inherit

`docs/reference/dependency_map.md` + `kb/dependency_map.json` — dataset →
consuming tabs/pages/scripts/workflows, derived from the code, regenerated by
`python3 kb/_build_dependency_map.py`, drift-checked in CI beside the docs
index. **Cross-impact is now COMPUTED, not guessed** — `CLAUDE.md`'s
cross-impact bullet carries the pointer. Both the governance builder and the
Admin surface PROJECT from it; if you teach the map a new idiom, they inherit
it. Content guards: `tests/dependency_map_test.py` (23 checks, each
perturbation-tested). KB note:
`methodology-one-dependency-derivation-many-projections`.

## Patterns that earned their keep this run

- **Sweep for idioms before writing the scanner.** The code had three REST base
  conventions, verb-first helpers, and blind brace-slices no single reader
  would predict. Inventory first, extract second.
- **Adversarially sample your own artifact before merging it.** Two of three
  refuters found real defects (a bulk PATCH recorded read-only; a tempdir
  self-bake counted as a consumer). The sample IS the verification.
- **Direction lives at the fetch site, not the const line.**
- **A noise guard breached by a better detector gets raised with the reason on
  the record** — never satisfied by hiding genuine finds.
- **Prototype-first held**: the pane mock went to an artifact for Sam's
  reaction, not into the monolith.

## Safety patterns to honor

- Never force-push `main`. Merge on `clean` OR `unstable`; poll CI via MCP
  tools, not curl; a `check_suite` wake names a superseded `head_sha`.
- Sam curates LIVE beside sessions — fresh read at write-time before any bulk
  `kb_curation` write; his rows always win.
- The daily cron commits to `main` directly (Pages serves from it) — the map's
  direct-to-main table lists all 10 such workflows.
- `cpl_memory` writes: INSERT-only, `status='proposed'`, log to
  `cpl_memory_log`, never silently supersede a human-sourced row.

## Moniker

**SkyGov**, named by Sam at close. Yours is open.

**Next is Session 211 — `docs/session_211_handoff.md`.**
