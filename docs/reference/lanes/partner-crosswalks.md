---
title: "Partner crosswalks — lane state"
created: 2026-08-28
updated: 2026-08-28
tags: [reference, roadmap-lane]
kb-status: internal
obsidian-folder: cpl-project-tracker/reference/lanes
related:
  - "[[CLAUDE]]"
---

# Partner crosswalks

> **Always-current lane state, not an archive** — refresh it at every checkpoint
> that moves this lane. `CLAUDE.md` §11 keeps the one-line pointer.

**What this lane is:** "Which occupations we train for can our students already get college credit for, and where?" — plus "what can THIS college carry?"

## Status

✅ **FIVE INSTRUMENTS LIVE.** ① **Statewide engine** (#995) — `kb/_build_partner_crosswalk.py` + the shared `kb/occupation_credential_map.json` (139 occupations / 406 rulings). ② **College-scoped crosswalk** (#1243) — `kb/_build_college_offering_crosswalk.py` + `kb/delta_offering_map.json`. ③ **Occupation-anchored** (#1576). ④ **Regional opportunity** (#1576). ⑤ **The COBI register** (#1591). ⭐ **A COLLEGE-SCOPED ASK IS A DIFFERENT INSTRUMENT, NOT A FLAG.** The statewide engine deliberately does not privilege the in-county college — right for a referral, wrong for a meeting where the whole question is what ONE college can do; and the two disagree about what a good answer IS (engine: *"some college offers this"*, a fact; college tool: *"this college teaches it AND the exhibit exists AND nobody joined them up"*, a task). Third time this call has come up — see also Futuro/HTH (#1134), where **one course × one program type** had no vocabulary to reconcile and a simpler generator was right. **Match the instrument to the question's shape, not to the word "crosswalk".** ⭐ **KEEP "does the college teach it" and "does an exhibit exist" in SEPARATE COLUMNS** — crossed, they ARE the deliverable: Delta **42 adopt-now** (teaches it, exhibit exists, MAP already flags it potential — no curriculum and no exhibit to build) · 6 build-first-in-state · **0 of 139 articulated today** (its lone career CPL is POST Basic Academy; the other 68 are AP/CLEP). Collapsing them into one score destroys the only distinction the meeting needs. ⚠️ **A CAPABILITY CAN BE INVISIBLE TO A PROGRAM SEARCH.** Delta's 10-course plumbing apprenticeship sits under no plumbing-named COCI program and its prefix reads as *construction*. **Search the COURSE catalog, not just the program inventory.** Delta also holds curriculum for 6 occupations with no CPL anywhere in California (lineworker is NOT among them). Detail: [`delta_college_crosswalk_lessons`](../../delta_college_crosswalk_lessons.md). ⚠️ **Narrative copy is a FINDING, so it lives with the rulings** (`_narrative` in the offering map), never in the generator — hardcoding it would make the reusable-engine docstring a lie; `check_absence_claims()` now hard-fails any row claiming CPL exists nowhere while its own exhibit list is non-empty (**caught 6 rows**: 2 utility, 4 masonry). ⭐ **A FIFTH INSTRUMENT IS LIVE — `kb/_build_regional_cpl_opportunity.py` (2026-09-16, Sam + Ashley + Sigrid).** A REGION's occupation list crossed against one or many colleges, for the CPL Initiative's regional college meetings. It generalizes the Delta cross off the hand-curated offering map, so it runs for any college by changing one argument. `--college` and `--district` are repeatable and compose; a district expands through `kb/college_identity/`. Output is one row per OCCUPATION with the colleges listed against it — a meeting has several colleges in the room, so N separate reports is the wrong unit. Three artifacts per run: workbook, screen page, and a **printable handout** in the Cal-JAC Credit Opportunities format, self-contained so an emailed copy carries its own spreadsheet.

⭐ **SAM REVERSED THE COBI-TAB PARK (2026-09-16); IT SHIPPED 2026-09-17.** mechanical facts now, curated judgment as a queue. Home is the **My College tab**, extending its existing scope picker rather than a new tab — that tab already answers "a college picks itself and sees its opportunities", and a second copy of the college/district lists is a stale copy waiting. **Single pick stays the default**; multi-select rides along for meetings.

⚠️ **THE MATCHER IS THE OPEN PROBLEM AND IT IS NOT SHIPPABLE YET** — now with a number on it (below). Fixed and measured earlier: SOC titles use *"Except Police, Fire"* as a NEGATION, and a shared token alone is not a match (coverage ≥ 0.5 cut Santa Rosa from **233** "adopt now" to 24). The survivors live in the single-shared-token path — *Commercial Pilots* against Commercial Music, *Gas Plant Operators* against Agriculture Plant Science. **Hand-listing bad words rebuilds the curated map this tool exists to avoid.** Story: [`docs/regional_cpl_opportunity_lessons.md`](../../regional_cpl_opportunity_lessons.md).

⭐ **THE OCCUPATION SIDE IS SOLVED.** COE demand 2024-2029, **nine regions, 4,869 rows, SOC-coded**: `kb/reference/coe_occupation_demand_2024_2029.json`. ⚠️ **The Bay's CPL shape is NOT San Joaquin's** — only 17 of the 139 occupation rulings match the Bay list by title and all 17 are electrical/mechanical, an artifact of SJCOE's IBEW roster. The Bay's demand is health care, early childhood and transportation, so it needs its own rulings. ⚠️ **Raw openings is the wrong sort key** — it ranks Fast Food first; the CPL band is 104 of 541.

⭐ **AN EXTERNAL CREDENTIAL REGISTRY LANDED (2026-09-16).** Sam's framing: *"We've never had a table of active certificates and licenses... other than what we've catalogued in MAP."* [`california_occupational_licenses.json`](../../../kb/reference/california_occupational_licenses.json) — **369 California licenses, COMPLETE**, with issuing agency — and [`credential_registry_national_sample.json`](../../../kb/reference/credential_registry_national_sample.json) — **974 of 6,738** national certifications. ⚠️ **NO OCCUPATION CODE AT ANY LEVEL**, so a name join or a curator ruling is the only connection. ⚠️ **The network refuses both Credential Engine domains**; **the API is Sam and Malone's to pursue.** ⭐ A detail print carries **73 CTID'd task statements**. **51 of the 974 are trades**; C-10, C-46 and NCCER Commercial Electrician are corroborated.

⭐ **THE AGENT-SUFFIX OVER-STEMMING IS FIXED** (`stem()` stripped `-er`/`-or` past four characters: **engineer → engine**, **actors → act**). `NOT_AGENT_ROOTS` names forbidden landing points; both measured collisions are gone and `tests/occupation_matcher_stemming_test.py` pins it in CI. Mechanism: [`regional_cpl_opportunity_lessons`](../../regional_cpl_opportunity_lessons.md).
⚠️ **TWO PLAUSIBLE FIXES WERE MEASURED AND REJECTED — do not retry blind; both are recorded in the code.** ① *Protect the agent strip when the bare word is also in play* runs backwards — for a true agent noun that is exactly when the merge is right (cost Roofers/Roof, Floral Designers/Floral Design, Data Entry Keyers/10-Key; all three are test cases now). ② *Gate the single-token path on `rare()`* — **rarity runs BACKWARDS here**: a college with several welding programs serves welders MORE. Delta F1 fell 0.653 → 0.630. Story: [`regional_cpl_opportunity_lessons`](../../regional_cpl_opportunity_lessons.md).
⭐ **THE SCORE EXISTS: `kb/_score_occupation_matcher.py`**, against Delta's 139 rulings (52 confirmed / 38 potential / 49 none). **Decision level — precision 0.907, recall 0.51, accuracy 0.626.** ⭐ **When the tool says a college has something, it is right about nine times in ten; it finds about half of what a human finds.** That is the right failure shape for a page shown to a college. Pair level (0.47 / 0.124) is harsh by construction. Before the fix: 0.94 / 0.49 / 0.626.
⚠️ **THE STEM FIX EXPOSED A HOMOGRAPH THE BUG WAS MASKING.** Santa Rosa gained five rows pairing *Locomotive / Ship / Rail Yard / Stationary Engineers* and *Operating Engineers* with its **Engineering** program. None of them is an engineer in the academic sense. The collapse onto `engine` had been hiding these by mangling the token. Santa Rosa moved 58 → 63 adopt.
⭐ **SAM ACCEPTED THE GAPS AND ASKED FOR A CAVEAT (2026-09-16):** *"just add a simple caveat to the views where needed."* `ACCURACY_HEAD`/`ACCURACY_BODY` carry the numbers to all three generator surfaces from ONE constant; the COBI register carries them inside its own data file. ⚠️ **Re-score and update them together** — a caveat quoting a stale score is worse than none.

⚠️ **THE RECALL CEILING IS VOCABULARY, NOT THRESHOLDS.** The misses are structural: *application developer* → Computer Programming, *ambulatory coder* → Medical Office Assistant. Those titles share no token at all, so no threshold reaches them. Closing that gap needs a synonym layer or the curated `kb/occupation_credential_map.json` — which is the argument for keeping the curated map rather than replacing it.

⭐ **THE COLLEGE-TO-REGION ROSTER IS RESOLVED AND NOW WIRED.** SWP region derives from `college_geo.county`; the Bay's 28 across 12 counties match the Bay Region COE's own description. Derivation in `kb/reference/swp_region_map.json`; the RESOLVED roster, which that file never carried, is `kb/reference/swp_region_roster.json` (117 colleges, 9 regions), read at build time since the sandbox cannot reach Supabase. ⚠️ **`--swp-region` IS THE CONSORTIUM; `--region` IS PROXIMITY, AND THEY DISAGREE** — `--region "Bay Area"` returns **23**, silently dropping five member colleges. ⚠️ **ASCCC areas are a SEPARATE taxonomy and are still missing** — no Supabase column carries them. **NEEDS SAM.**

⭐ **THE REGISTER IS IN THE MY COLLEGE TAB (#1591, 2026-09-17).** Built for Sigrid's Bay Area Strong Workforce consortium: pick any college, read what it could already give credit for, flip to the next. Ashley's Cal-JAC layout ported behind the tab's existing **My college** scope, which already picks any college. `kb/_emit_regional_opps_data.py` precomputes `regional_cpl_opportunity_data.js` (**28 colleges, 6,903 rows, 1,223 adopt-now**, 2.8MB) — the matcher runs ~14s per college and a room cannot wait on it. Lazy on first OPEN. ⚠️ **P6 ships as a NAME LIST, not rows** — 343 of 541 at a typical college; recall near half makes an absence unconfirmed, so the person in the room can say "we do teach that." ⚠️ **The caveat says when a score was measured OUTSIDE the region it is quoted in**, derived from the data so it appears on a Bay page and vanishes on a Central Valley one.

⚠️ **SAM'S THREE REGISTER TWEAKS ARE THE LIVE WORK (2026-09-17), one of three done.** ① drop the rows where the college has no aligned course or program — the **P5** tier, 57% of rows (6,903 → 2,978 across the Bay 28); ② the **CER name above its credit recommendations**, statewide marked — his reference is the Drive copy of Ashley's Delta page, newer than ours (id in the handoff); ③ a **CIP Sector filter** replacing the P chips. ⚠️ **"CIP Sector" is the 2-DIGIT CIP level** (`cip_crosswalk.js:1782`), never `statewide_data.js`'s ten industry sectors nor the ten noncredit CIP categories — three different tens. Data for ② and ③ is in the generator (`exhibit_detail`, `cip_sectors`); the emitter and the tab are untouched. Detail: [`session_269_handoff`](../../session_269_handoff.md).

**Next:** ⓪ Sam's three tweaks above; ① close the VOCABULARY gap — a synonym layer or the curated map; thresholds are exhausted and the stemming guard already landed; ② the Engineering homograph (4 wrong Santa Rosa rows); ③ the `cpl_occupation_match` verdict queue (a new shared human-write table — Rule 10(a3) Governance first); ④ put the California licenses on the handout as a third column beside adopt-now and build-new. **Gap backlog:** the 35 no-CPL occupations. Story: [`docs/delta_college_crosswalk_lessons.md`](docs/delta_college_crosswalk_lessons.md) · [`docs/partner_crosswalk_lessons.md`](docs/partner_crosswalk_lessons.md).
