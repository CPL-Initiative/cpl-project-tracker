---
title: "Partner crosswalks — lane state"
created: 2026-08-28
updated: 2026-08-28
tags: [reference, roadmap-lane]
kb-status: internal
obsidian-folder: cpl-project-tracker/reference/lanes
related:
  - "[[CLAUDE]]"
---

# Partner crosswalks

> **Relocated verbatim from `CLAUDE.md` §11 on 2026-08-28** (Session 206, the
> consolidation). This is **always-current lane state, not an archive** —
> update it at every checkpoint that moves this lane, exactly as you used to
> update the §11 cell. `CLAUDE.md` keeps the one-line pointer; the detail is
> here.

**What this lane is:** "Which of the occupations we train for can our students already get college credit for, and where?" — plus the college-facing half: "and what can THIS college carry?"

## Status

✅ **TWO INSTRUMENTS LIVE.** ① **Statewide engine** (SkyWalker, #995) — `kb/_build_partner_crosswalk.py` + the shared `kb/occupation_credential_map.json` (139 occupations / 406 rulings / 35 curated no-CPL findings) + region presets + 32-check test. SJCOE run 1: 51 statewide / 53 local-only / 35 no-CPL. ② **College-scoped crosswalk** (Sky169, #1243) — `kb/_build_college_offering_crosswalk.py` + `kb/delta_offering_map.json`, for the SJCOE ↔ San Joaquin Delta meeting. ⭐ **A COLLEGE-SCOPED ASK IS A DIFFERENT INSTRUMENT, NOT A FLAG.** The statewide engine deliberately does not privilege the in-county college — right for a referral, wrong for a meeting where the whole question is what ONE college can do; and the two disagree about what a good answer IS (engine: *"some college offers this"*, a fact; college tool: *"this college teaches it AND the exhibit exists AND nobody joined them up"*, a task). Third time this call has come up — see also Futuro/HTH (#1134), where **one course × one program type** had no vocabulary to reconcile and a simpler generator was right. **Match the instrument to the question's shape, not to the word "crosswalk".** ⭐ **KEEP "does the college teach it" and "does an exhibit exist" in SEPARATE COLUMNS** — crossed, they ARE the deliverable: Delta **42 adopt-now** (teaches it, exhibit exists, MAP already flags it potential — no curriculum and no exhibit to build) · 6 build-first-in-state · **0 of 139 articulated today** (its lone career CPL is POST Basic Academy; the other 68 are AP/CLEP). Collapsing them into one score destroys the only distinction the meeting needs. ⭐ **Delta holds curriculum for the statewide gap run 1 found** — a utility/hydroelectric apprenticeship (`A IND 77A–77N`, `A ELE 75A–75F`) covering 6 occupations with **no CPL anywhere in California**. ⚠️ **Lineworker is NOT among them** (substation/plant electrical ≠ line work). ⚠️ **A capability can be INVISIBLE to a program search** — Delta's 10-course plumbing apprenticeship (`A CON 87A–90D`) sits under no plumbing-named COCI program and the prefix reads as *construction*; MAP lists Delta on neither side of the statewide C-36 exhibit. **Search the COURSE catalog, not just the program inventory.** ⚠️ **Narrative copy is a FINDING, so it lives with the rulings** (`_narrative` in the offering map), never in the generator — hardcoding it would make the reusable-engine docstring a lie; `check_absence_claims()` now hard-fails any row claiming CPL exists nowhere while its own exhibit list is non-empty (**caught 6 rows**: 2 utility, 4 masonry). ⭐ **A FIFTH INSTRUMENT IS LIVE — `kb/_build_regional_cpl_opportunity.py` (2026-09-16, Sam + Ashley + Sigrid).** A REGION's occupation list crossed against one or many colleges, for the CPL Initiative's regional college meetings. It generalizes the Delta cross off the hand-curated offering map, so it runs for any college by changing one argument. `--college` and `--district` are repeatable and compose; a district expands through `kb/college_identity/`. Output is one row per OCCUPATION with the colleges listed against it — a meeting has several colleges in the room, so N separate reports is the wrong unit. Three artifacts per run: workbook, screen page, and a **printable handout** in the Cal-JAC Credit Opportunities format, self-contained so an emailed copy carries its own spreadsheet.

⭐ **SAM REVERSED THE COBI-TAB PARK (2026-09-16), AND CHOSE THE SHAPE:** mechanical facts now, curated judgment as a queue. Home is the **My College tab**, extending its existing scope picker rather than a new tab — that tab already answers "a college picks itself and sees its opportunities", and a second copy of the college/district lists is a stale copy waiting. **Single pick stays the default**; multi-select rides along for meetings.

⚠️ **THE MATCHER IS THE OPEN PROBLEM AND IT IS NOT SHIPPABLE YET** — now with a number on it (below). Fixed and measured earlier: SOC titles use *"Except Police, Fire"* as a NEGATION, and a shared token alone is not a match (coverage ≥ 0.5 cut Santa Rosa from **233** "adopt now" to 24). The survivors live in the single-shared-token path — *Commercial Pilots* against Commercial Music, *Gas Plant Operators* against Agriculture Plant Science. **Hand-listing bad words rebuilds the curated map this tool exists to avoid.** Story: [`docs/regional_cpl_opportunity_lessons.md`](../../regional_cpl_opportunity_lessons.md).

⭐ **THE OCCUPATION SIDE IS SOLVED.** COE occupational demand 2024-2029, **all nine regions, 4,869 rows, SOC-coded**, committed at `kb/reference/coe_occupation_demand_2024_2029.json`. This is the O\*NET SOC spine this lane has wanted. ⚠️ **The Bay's CPL shape is NOT San Joaquin's** — only 17 of the 139 occupation rulings match the Bay list by title and all 17 are electrical/mechanical, an artifact of SJCOE's IBEW roster. The Bay's demand is health care, early childhood and transportation, so it needs its own rulings. ⚠️ **Raw openings is the wrong sort key** — it ranks Fast Food first; the CPL band is 104 of 541.

⭐ **AN EXTERNAL CREDENTIAL REGISTRY LANDED (2026-09-16, Sam capturing by hand).** Sam's framing is the point — *"We've never had a table of active certificates and licenses... other than what we've catalogued in MAP."* MAP's 2,948 exhibits record what colleges have written down; a license registry records what a student already holds, and it grows whether or not a college does the work. Two files:
[`kb/reference/california_occupational_licenses.json`](../../../kb/reference/california_occupational_licenses.json) — **369 California occupational and professional licenses, COMPLETE**, with the issuing state agency (224 Consumer Affairs incl. the CSLB contractor classifications, 33 Industrial Relations, 31 Public Health) — and
[`kb/reference/credential_registry_national_sample.json`](../../../kb/reference/credential_registry_national_sample.json) — **974 of 6,738** national industry certifications (14.5%), the union of two captures that overlapped on only 15 rows.
⚠️ **NO OCCUPATION CODE AT ANY LEVEL** — list, collection print and detail print all omit `ceterms:occupationType`, SOC and CIP. A name join or a curator ruling is the connection. ⚠️ **The network refuses both Credential Engine domains** (connect_rejected, measured) — a human capture is the channel, and the methods are ranked in the national file's own header. **Credential Engine API access is Sam and Malone's to pursue.** ⭐ **A detail-page print carries 73 CTID'd TASK STATEMENTS** — the substrate Phase 4 (SLO ingestion) wants.
⭐ **Corroborated from the registry** (unverified until now in `kb/fire_electrical_external_credentials.py`): **C-10**, **C-46**, and **NCCER Commercial Electrician**, itself one of MAP's 12 statewide electrical credentials. **51 of the 974 are trades.**

⭐ **THE AGENT-SUFFIX OVER-STEMMING IS FIXED, AND THE MATCHER NOW HAS A SCORE.**
`stem()` stripped `-er`/`-or` past four characters, so **engineer → engine** and **actors → act**, which put Diesel Engine Specialists against *Engineer In Training* and Actors against the *Residential Mortgage Lending Act*. The guard names **forbidden landing points** (`NOT_AGENT_ROOTS`), so a strip onto one falls through to the plural strip: `engineers → engineer`, `actors → actor`. Both collisions are gone from the license join (8 pairs and 1 pair, now 0 and 0). Pinned by `tests/occupation_matcher_stemming_test.py` in CI.
⚠️ **TWO PLAUSIBLE FIXES WERE MEASURED AND REJECTED — do not retry blind, both are recorded in the code.** ① *"Protect the strip when the bare word is also in play"* is backwards: for a true agent noun that is exactly when the merge is right, and it cost *Roofers/Roof*, *Floral Designers/Floral Design*, *Data Entry Keyers/10-Key*. Those three are test cases now. ② *Gate the single-token path on `rare()`* — **rarity runs BACKWARDS here**: a college with several welding programs serves welders MORE. It failed to fix its target and cost **Welders**, **Automotive Body Repairers**, **Nursing Assistants**; Delta F1 fell 0.653 → 0.630. Story: [`regional_cpl_opportunity_lessons`](../../regional_cpl_opportunity_lessons.md).
⭐ **THE SCORE EXISTS: `kb/_score_occupation_matcher.py`**, against Delta's 139 rulings (52 confirmed / 38 potential / 49 none). **Decision level — precision 0.907, recall 0.51, accuracy 0.626.** ⭐ **When the tool says a college has something, it is right about nine times in ten; it finds about half of what a human finds.** That is the right failure shape for a page shown to a college. Pair level (0.47 / 0.124) is harsh by construction. Before the fix: 0.94 / 0.49 / 0.626.
⚠️ **THE STEM FIX EXPOSED A HOMOGRAPH THE BUG WAS MASKING.** Santa Rosa gained five rows pairing *Locomotive / Ship / Rail Yard / Stationary Engineers* and *Operating Engineers* with its **Engineering** program. None of them is an engineer in the academic sense. The collapse onto `engine` had been hiding these by mangling the token. Santa Rosa moved 58 → 63 adopt.
⭐ **SAM ACCEPTED THE GAPS AND ASKED FOR A CAVEAT (2026-09-16):** *"just add a simple caveat to the views where needed."* `ACCURACY_HEAD` / `ACCURACY_BODY` in the generator carry the measured numbers to **all three surfaces** — workbook notes sheet, screen page, handout — from ONE constant, so a re-score updates every surface at once. It reads: *about nine in ten of the rows shown hold up, and the list finds roughly half of what the reviewer found. Read a row as a candidate and a gap as unconfirmed.* ⚠️ **Re-score and update the constant together** — a caveat quoting a stale score is worse than none.

⚠️ **THE RECALL CEILING IS VOCABULARY, NOT THRESHOLDS.** The misses are structural: *application developer* → Computer Programming, *ambulatory coder* → Medical Office Assistant. Those titles share no token at all, so no threshold reaches them. Closing that gap needs a synonym layer or the curated `kb/occupation_credential_map.json` — which is the argument for keeping the curated map rather than replacing it.

⭐ **THE COLLEGE-TO-REGION ROSTER IS RESOLVED.** SWP region derives from `college_geo.county`; the Bay's 28 colleges across 12 counties match the Bay Region COE's own description. Renames and the three college moves are in `kb/reference/swp_region_map.json`, **applied**. ⚠️ **ASCCC areas are a SEPARATE taxonomy and are still missing** — no Supabase column carries them. **NEEDS SAM.**

**Next:** ① tune the matcher offline, scored against Delta's 139 human rulings as a labeled test set, and fix the agent-suffix over-stemming in the same run; ② port into the My College tab; ③ the `cpl_occupation_match` verdict queue (a new shared human-write table — Rule 10(a3) Governance first); ④ put the California licenses on the handout as a third column beside adopt-now and build-new. **Gap backlog:** the 35 no-CPL occupations. Story: [`docs/delta_college_crosswalk_lessons.md`](docs/delta_college_crosswalk_lessons.md) · [`docs/partner_crosswalk_lessons.md`](docs/partner_crosswalk_lessons.md).
