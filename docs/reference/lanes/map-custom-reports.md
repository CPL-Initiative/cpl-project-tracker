---
title: "MAP Custom Reports (3 new) / ITPI automation — lane state"
created: 2026-08-28
updated: 2026-09-10
tags: [reference, roadmap-lane]
kb-status: internal
obsidian-folder: cpl-project-tracker/reference/lanes
related:
  - "[[CLAUDE]]"
---

# MAP Custom Reports (3 new) / ITPI automation

> **Relocated verbatim from `CLAUDE.md` §11 on 2026-08-28** (Session 206, the
> consolidation). This is **always-current lane state, not an archive** —
> update it at every checkpoint that moves this lane, exactly as you used to
> update the §11 cell. `CLAUDE.md` keeps the one-line pointer; the detail is
> here.

**What this lane is:** Wire the three new MAP Custom Reports, load them, keep them fresh.

## Status

✅ **LIVE ON THE CRON, NO HUMAN IN THE LOOP** (#1246–#1258). Daily **13:40 UTC**: fetch → staging → `map_promote_custom_reports()` → live, **ONE transaction**, gates that **FAIL CLOSED** (G1–G6 truncated pull/broken surrogate · **G7/G8 refuse to PUBLISH a recoverable suppression** · **G9 refuses if a rebuilt team table lost its gate**). ⭐ **THE RLS TRAP DOES NOT EXIST AS A STEP** — contents are replaced, never the table, so the reviewer-only policy on `map_student_credit` is never dropped. Both published aggregates + the clean-up worklist + the transcribed-gap detail rebuild in the SAME transaction. ⭐ **THE VALUE WAS TWO NEW DIMENSIONS, NOT FRESHNESS** — `Status` (articulation approval STAGE) and `CPLPlanStatus` (lifecycle CHECKS) were carried by no table we held. ⚠️ **`Status` is 91.2% BLANK**, top value `Implementation` — **it cannot facet the backlog**. ⚠️ **`CPLPlanStatus` holds SIX checks / 41 combinations**, delimiting inconsistent — split-and-strip, stored verbatim. ✅ **TRANSCRIBED = UNITS, NEVER THE TICK** (Sam, 2026-08-19; no code change needed — every published figure already sums units). ✅ **THE SIX LIFECYCLE BOOLEANS ARE BACK, AND THE FETCH REQUESTS ONE (2026-09-10).** Pedro restored all six (probe run 34493245398: 100% fill, constant within a student): `CPL_Docs_Verified` · `Ed_Plan_Created` · `Analysis_Completed` · `Counselor_Verified` · `Student_Verified` · `Transcribed`, `'0'`/`'1'` strings. `fetch_custom_report.py` requests **`Counselor_Verified` alone** (Sam, 2026-09-10: *"only counselor verified that we need now, not student -- so just 1"*); the other five stay WATCHED by `kb/_probe_lifecycle_checks.py`, never requested. ⚠️ **ASKING FOR A COLUMN A VIEW DOES NOT HAVE 400s THE WHOLE VIEW** — MAP removed the six once (2026-09-08), the 400 relabeled a neighbour, and the cron died nine consecutive times; so a column nothing reads stays out of the request. **`Counselor_Verified` is the counselor stage** (Sam, 2026-09-02: *"signifies the student met with a counselor and accepted their CPL"*); on 2026-09-23 the funding feed carried it for **2,856 students, 25,037 applied units, 23 colleges**. The CR-row view is unchanged at 30 columns, so `CPLPlanStatus` stays the only lifecycle signal at CR grain. ⚠️ **`Transcribed` is a CHECK there too** — 17,342 flagged vs 14,455 with units > 0. ⚠️ **`columnName: []` now answers HTTP 500 on EVERY view, the control included; `["*"]` enumerates** — `kb/_probe_lifecycle_checks.py` tries both and is the instrument for the next unknown column. The four identity columns the fetcher holds by name are no longer on the live aggregated view (25 = our 19 + the 6). The catalog-year view gained `RecCreatedOn`, one refresh stamp repeated on every row — freshness, not a record date. ✅ **The 55% applied fork is RETIRED** (Sam, 2026-08-19) — the ruling *publish both and name the gap* stands, the figure does not: `applied_credits` is **identical to `articulated_credits` on all 462,355 Needs Action rows**, and scoped properly the measures agree to **0.1%**. ⚠️ **CATALOG YEAR ROLLS FORWARD, so the axis is MUTABLE.** ⚠️ **A LOAD MUST REPRODUCE ITS SOURCE, NOT IMPROVE IT** — the gate caught `""`→NULL on ~200k rows, then a NOT NULL mismatch; **zero-fill is PER TABLE**. ⭐ **MINIMIZATION HAPPENS TWICE** — 12 fetched columns with no consumer dropped and **listed**; `StudentMAPID` derives a surrogate and is discarded; rotation detector is a **min-hash sketch, not a student map**. ⚠️ **THE CLEAR IS NOT A GATED STEP** — it runs BEFORE the promotion, and its mass `DELETE` timed out the first time it met a FULL staging table (would have failed nightly). Now `map_clear_custom_report_staging()`, a no-argument TRUNCATE, **5.3 s**. ⚠️ **MAP BROKE THE STUDENT VIEW AND WE READ THE WRONG FIELD FOR THREE NIGHTS** (Sky197, #1358). `View_StudentDetailsCredits_APIDataset` answered **400 "is not Valid"** from 2026-08-24; runs 12/13/14 reported a *duplicate/missing viewName* instead. ⚠️ **One invalid view in a batch RELABELS A NEIGHBOUR's dataset**, so the name that vanished belonged to a HEALTHY view — never trust `viewName` when a duplicate is present. ⚠️ **`dataCount` is MAP's CLAIM, not a row count** (an empty dataset advertised "204,491 rows"). Now `summarize_response()` reads `responseCode`/`responseMessage` per dataset (**ABSENT = healthy; only a PRESENT non-2xx fails**) and counts `columnValue`. ⚠️ **PRINTING IS UNCONDITIONAL; FAILING IS OPT-IN (`--strict`) EXCEPT ON A REPEATED viewName, WHICH IS NOW FATAL EITHER WAY (2026-09-10).** The old split rested on *"daily-dashboard.yml consumes none of these views"* — a claim about WHICH view MAP mislabels, which is not ours to control. On 2026-09-08 it landed on one the dashboard reads. A 400 leaves the other views readable; a duplicate means the payload cannot be keyed by name at all, which is the only thing any consumer does with it. `read_exhibit_metrics()` drops BOTH copies as a second line of defense. Live was never touched. ✅ **Pedro has both asks (2026-08-26)**: the view fixed overnight, the **origination LocID** longer. ⭐ **The LocID ask is the IDENTITY of a route MAP already records the TYPE of** — same namespace as `CollegeID`, **NULL when unknown, NEVER defaulted to the enrolling college**, and on the catalog-year view too. ⚠️ **Open, ITPI only: can a COLLEGE have a second noncredit landing page with its own LocID**, or only standalone entities (NOCE/SDCCE)? That decides ~108 colleges vs a handful. ⚠️ **`course_type` cannot substitute — all ten values are credit types.** **NEXT:** ⓪ **SUPABASE GRANTS BEFORE 2026-10-30 — APPLIED LIVE 2026-09-23** on Sam's verdict ("apply", item 1 of [his sheet](https://claude.ai/artifact/2xc8Hik6di18kSqmP7vkCV)); receipt `kb/receipts/supabase_rebuild_grants_2026-09-23.sql` (before and after hashes, rollback). ✅ Re-checked 2026-09-24 19:06 UTC: all three API roles read all five tables; counts 173/113/506/279/232 unchanged. ⚠️ **THE 2026-09-24 PROMOTION WAS REFUSED, and the grants are not why:** run 36037739222 (18:01 UTC) hit `map_student_credit_key_range_ck` — `student_key` 1..50,000, created with the table on 2026-08-10 and in no repo file — because the pull carried 50,027 students against 49,965 live (27 keys over, 338 rows); live rolled back whole. Widened live to 250,000 (migration `widen_map_student_credit_key_range_to_250k`; receipt `kb/receipts/map_student_credit_key_range_2026-09-24_s287.sql`; runbook G5 note); G5's density check is unchanged. ✅ The re-dispatched load (run 36047213316) promoted at 19:21 UTC: 224,230 cr_unit rows, 635,527 student rows / 50,027 students (max key 50,027), all five tables rebuilt (transcribed gap 279 → 280), grants intact. Notice forwarded 2026-09-23. FIVE functions DROP and CREATE their table in every nightly promotion and granted nothing: `rebuild_map_college_goal2`, `rebuild_map_college_credit_summary`, `rebuild_map_cleanup_worklist`, `rebuild_map_transcribed_gap`, `rebuild_map_cx_exhibit_guidance`. The project's default privileges cover them today; from 2026-10-30 Supabase stops granting on new `public` tables, so the first promotion after that leaves all five unreadable to COBI and to the service-key publishers (`_publish_college_briefing.py` reads goal 2 and the credit summary). ✅ The repo carries the grants (each body grants SELECT to anon, authenticated and service_role) and `tests/supabase_table_grants_test.py` guards every table the SQL creates at run time, recreates, or adds new (49 live tables baselined). The apply sent ONLY the five `create or replace function` statements, never whole files (several open by dropping a live table). ⚠️ **The repo copies differ from live in dashes only** (measured 2026-09-23, comment-stripped statement hashes): the worklist action text and the transcribed-gap ask read an em dash where live reads a hyphen; `map_promote_custom_reports` itself differs in its error and log strings and needs no re-apply. Every other table keeps its grants. ⭐ The measures on `Counselor_Verified` are `pac`/`pac_u` (applied) and `ptc`/`ptc_u` (transcribed, Sam 2026-09-23); if the column goes missing they are OMITTED, not zeroed, the designed degradation. The funding artifact masks every count under 10 (ADR `adr-funding-counts-mask-under-10-units-carry-the-money`); the fetch and the promotion are untouched by it. ① **`CollegeID2` — Pedro's spelling for the origination LocID (Sam, 2026-09-02: *"to identify where the student originated from when they came to the CR college. We'll use that one to calculate the NC FTES"*) — has NOT landed on any of the four views**; Sam, later 2026-09-02: origin is *one Yes/No field for now* (a public upload) and CollegeID2 *soon*, NC in particular — that interim Yes/No IS `Potential Student` (Sam, 2026-09-02). Re-run the probe (Actions → Discover MAP datasets, first step) the day Pedro says CollegeID2 has landed. ② Store the booleans in Supabase only when a consumer beyond the funding feed needs them (minimization happens twice). Runbook [`docs/map_custom_report_load.md`](docs/map_custom_report_load.md) · story [`docs/map_custom_reports_lessons.md`](docs/map_custom_reports_lessons.md).

- 2026-09-18: the daily run went red with HTTP 504 on the promotion call and printed "rolled back, live unchanged", while `map_data_loads` 35 shows the promotion committed at 17:16:52Z with the day's rows. A gateway timeout does not end the transaction; the loader needs a read-back of `map_data_loads` before it declares a rollback (handoff 277, item 9).
