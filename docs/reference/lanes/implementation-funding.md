---
title: "Implementation Funding tab / the $35M model — lane state"
created: 2026-08-28
updated: 2026-09-03
tags: [reference, roadmap-lane]
kb-status: internal
obsidian-folder: cpl-project-tracker/reference/lanes
related:
  - "[[CLAUDE]]"
---

# Implementation Funding tab / the $35M model

> **Always-current lane state, not an archive** (compacted to budget 2026-09-03; the shipped-session narratives are told once in the lessons doc). `CLAUDE.md` keeps the one-line pointer.

**What this lane is:** The three priorities, their shares and factors, what each earns against, and the college-by-college allocation.

## Status

✅ **THE ONE-POOL MODEL IS LIVE** (adopted 2026-08-31, shipped S216 PR #1427): `cpl_funding.js` runs one solve over 118 institutions (115 colleges + NOCE / SD Cont. Ed / Calbright as ordinary rows; Mt. SAC NC rides the Mt. San Antonio row) · $25,240,308 · **$150,000 base / $400,000 cap on the COMBINED award** · every award decomposes into credit and noncredit shares by FTES share, the NC share restricted to the noncredit measures · the noncredit trio earn by **ORIGINATION, no advances** (N2 b) · targets ride the pre-bounds entitlement's lane slice · statewide reads the full share (Total Possible = CR+NC). ⭐ **NEVER READ THE CONFIG — CALL `_effective()` / `_alloc()` / `_prios()` / `_ncPrios()` / `_ncAward()`**; dump live dials with `scripts/funding_effective.js --config <file>`. Baked defaults are stale by design; `mirrorYears` makes the stored Year-2 block inert (`cpl_funding_effective.test.js` guards the retired fields).

✅ **THE TAB (S218–S221, all live):** introduction (Summary inside it, house voice, no figures) → institution table (footnote under it) → window → breakdown → formula → eligibility → outcomes → timing; every section closed on open except the first two, **per visit** (no fold store). Three statutory bands — (A) Access · (B)+(C) Success · (D) Opportunities — with **membership DERIVED from the metric's milestone** (`prioGoals()`; an unresolved goal lands in a visible orphan band); the (d)(2) goal spine is a fold one click down, per-goal evidence lines from ONE function. Flat ledger with **every dial still editable** (`cpl_funding_ledger_editable.test.js`); priority cards a fixed two-column pair, metric on the surface, strategies behind a fold; low-key rows (nothing bold; the bound word reads **(at base) / (at cap)** beside both award figures); the calm pass — every glyph a word or gone, the sort mark the one ghosted survivor; **five prose blocks editable in curate** (`TEXT_BLOCKS`, `text.<key>` overrides; prose is not a dial — no Edit control signed out). Vocabulary doctrine lives in `CLAUDE.md` (Naming).

✅ **THE EXPLAINER IS THE PUBLIC VIEW** (`funding-model/index.html`, S221 PR #1436): it hosts the tab's own college section in embed mode (`window.CPL_FUNDING_EMBED = "college"`) — same rows, drill-in, search, grouping, columns, Excel export, editable introduction — and paints the timing milestones and each priority's strategies from the engine (`_timing()`, `_prios().strategies`); its basis and factors paint from the payload (`D.basis`; `_prios()` emits `factor`). ⚠️ `cpl_funding_public.html` (the tab in public mode) is still live; the recommendation is a redirect to `funding-model/` (NEEDS SAM ④). Verification: anchor suite `tests/cpl_funding_one_pool.test.js`, `cpl_funding_calm` (56), `cpl_funding_lead_with_the_table` (29), `funding_model_page` (37 — boots the engine and asserts every `P.<key>` the painter reads still exists, after the public `$NaN` of 2026-09-01), `cpl_funding_public_dollars` (10); ~2,000 checks across the family; retired mechanisms are absence guards naming their R-ruling (R1–R11).

✅ **THE MEASURES.** Credit: `pe` eligible units · `pa` applied units · `p3` transcribed units (the documented cohort, `and not is_potential`). Access: `ppe`/`ppe_u` eligible units and `ppa`/`ppa_u` applied units among portal-origin students — DISJOINT from `pe`/`pa`, riding `Potential Student` (the public-upload flag) until CollegeID2 lands. Accepted: **`pac`/`pac_u` = applied units on a counselor-verified plan — LIVE and PUBLISHED since the 2026-09-03 run** (`Counselor_Verified` alone leads the builder's sweep; Sam, 2026-09-02: *"met with a counselor and discussed their options"*; 2,820 students · 24,699 units · 18 colleges; `Student_Verified` unread; no `is_potential` condition — named for the credit's state, not who attested). ⭐ **The counselor step is a policy attestation, not a technical guarantee** (Sam, 2026-09-01): it can be batch-loaded legitimately; its integrity rests on the CO instruction (stop auto-awarding, confirm with the student, then check) plus the audit trail of who attested and when; either the student or the counselor/coordinator/initiator may check it. Noncredit: F1 feeder eligible headcount listed from day one, $0 earned until the origination feed reports (`srcDelivered()` reads an undelivered key as $0, never a cap); the builder already emits `payload["origination"]` (district scope for NOCE/SDCCE, statewide for Calbright; absent-not-zeroed; NULL-never-defaulted; `funding_origination_test.py`) and the tab needs no consumer edit when the feed lands. **The earn diagnostic** (2026-09-01: the credit slice paid 34.0% of its cap and 84% of everything earned came from Access: Outreach, which 97 of 115 colleges max out) is the lane's best single health check — **re-run it after the dials move**.

✅ **COUNTS MASK UNDER 10; UNITS CARRY THE MONEY; PUBLIC DOLLARS COARSEN** (Sam, 2026-09-03, PR #1439 — ADR [`adr-funding-counts-mask-under-10-units-carry-the-money`](docs/kb-notes/adr-funding-counts-mask-under-10-units-carry-the-money.md)). The floor rose 5 → 10 (*"to conform with ferpa practices often used"*), every metric alike — the `pp`/`ppa`/`ppe` raw carve-outs are retired; per-college UNIT sums are never masked, so `earnFraction()` prices the true numbers; a lone masked college gets a complementary mask; on the public page a college's EARNED figures read "<$1,000" or the nearest $1,000 (`earnedMoney` / `earnedCsv`, labels built from `PUBLIC_MONEY_FLOOR`), caps and dials exact, the curator view exact; the rate and the explanations stay public. **Measured on the re-baked artifact (2026-09-03):** no count of 1–9 anywhere, no unit key masked, and **57 small-portal colleges earn on Access** where 54 had earned nothing (their masked `ppe` scored as f=0 and read as "applied but no eligible" — zero such rows exist at either grain). ⚠️ The artifact is public, so "<10" shows on the internal tab too until the public/private split lands a gated copy.

**Durable rulings that carry over unchanged:** ⭐ **SUNSHINE RULE (Sam, 2026-09-01, verbatim): "I don't want to get into specifics on the new funding, just the general principles… New funding is still in draft form that I need to confirm with CO leadership before sunshining details."** Outward materials carry the model's shape only — one funding total, a base and a cap, sized by combined credit + noncredit teaching, outcomes-based draw-down, three draft priorities, noncredit carried within every award — and NO dollar figures, weights, institution counts, or the explainer link, until he confirms with CO leadership. **FUNDED and MEASURED are two axes** — goal (C) is funded with no campus measure, honestly; a priority's goal derives from its MEASURE's milestone, never its title. Standing rule (Sam, verbatim in `cpl_memory`): *"you never need to worry about how things look without the needed data feeds. I would never publish anything unless we could measure it with our data."* Unearned funds are anchored to §78093.2(d)(1) as a priority, never an invented mechanism. **N3 a:** Calbright's 1,000-FTES stand-in never publishes or disburses. Curating requires a magic-link reviewer (#1372). The project card's named projects read live from the Budget ledger. Shares / factors / titles / pins / text are curator edits through the tab, never SQL.

**NEEDS SAM:** ⓪ **SET THE DIALS THROUGH THE TAB** — ruled 2026-09-01 (*"I don't want you to fix it; I want the tab to save it"*): **Accepted at 25% share / factor 1.0**, starting set **Eligible 40% · Accepted 25% · Transcribed 35%**. `prioTarget()` makes factor the price premium; 25%/1.0 asks for a counselor-accepted plan on ~14.1% of applied units. ⚠️ No per-year ramp in this window (`mirrorYears`); expect a large uncommitted balance until the feeds land — the intended incentive picture. ① **Annual-view earning percent** — a cell can read "earning $140,476 · 191%" (window earning against a per-year figure); keep, or compute against the window figure. ② **When does `CollegeID2` land, and on both views?** Pedro's spelling for the origination key (Sam, 2026-09-02: *"to identify where the student originated from when they came to the CR college. We'll use that one to calculate the NC FTES"*); until then origin is `Potential Student` (Yes/No, the public-upload flag — confirmed 2026-09-02). It unlocks the `ppa` cutover and the noncredit trio's first real earn-out. ③ **React to the calm pass's calls by letter** (S220): (a) subtitle gone; (b) the name is the row toggle; (c) the sort mark kept, ghosted; (d) the CPL area word muted beside the picker; (e) an edited scenario reads "(edited)"; (f) the held figure inside the gate sentence and ahead of Total Possible; (g) the eligibility introduction is prose with an Edit word. keep · edit: … · revert. ④ **Confirm the explainer as the one public view, and react to S221's calls by letter:** retire `cpl_funding_public.html` to a redirect to `funding-model/` (recommendation: yes); (a) steps fold closed on open; (b) its own table retired for the tab's rows; (c) footnote under the table; (d) folds not remembered across visits; (e) the embedded table in the explainer's 1000px column, toolbar wrapping to two lines. keep · edit: … · revert. ⑤ **Whether COBI should keep showing "<10"** until the public/private split gives the internal view its own ungated artifact — say if a gated copy is wanted sooner.

**NEXT:** ⓪ **ONE REQUEST TO PEDRO carrying the remaining feed additions:** Origin / CollegeID2 (on none of the four views as of 2026-09-02) and **completions** (a NEW MAP view — the aggregated view has no award column and `View_ProgramsofStudy_APIDataset` is a catalog); completion data would measure (B) in the statute's own word and name each college's near-completers. The lifecycle booleans are delivered (#1437). ① **The `ppa` cutover** the day CollegeID2 lands: re-run the probe (Actions → Discover MAP datasets, first step), confirm the spelling, switch the source, then re-run the earn diagnostic. ② Sweep the college briefing's display names (raw keys; belongs with college-district-identity). ③ A cleanup commit: dead CSS for retired row shapes (`.cplfund-ncrow` / `.cplfund-ncsysrow` / `.cf-lanechip` / `.cplfund-awardrow` / `.cf-prio`, ~15 rules), `pinFrozenRows`'s `:not(.cplfund-ncsysrow)`, and the who-moves caveat if it still names two lanes. ④ Extend the editable prose to any block Sam names (one `TEXT_BLOCKS` entry + one `proseBlockHtml()` call; the memo builder does not read the blocks yet). ⑤ Once NEEDS SAM ④ is ruled: the redirect stub, the ledger note's link, `cpl_funding_public_private.test.js` re-aimed onto the explainer, the Internal · Public preview re-labeled. ⑥ Add the explainer to `scripts/check_public_page_layout.js` (one PAGES block; it measures Sierra alone today). ⑦ Still dead: `prototype/check_funding_explainer.js` (retired id, stale figures, outside `npm test`); the snapshot twin `prototype/funding_model_explainer.html` needs `node prototype/build_funding_model_explainer.js <config.json>` with an md5-verified config. Story: [`docs/cpl_funding_lessons.md`](docs/cpl_funding_lessons.md) ([archive](docs/cpl_funding_lessons_archive.md)).
