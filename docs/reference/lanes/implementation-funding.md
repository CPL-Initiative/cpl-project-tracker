---
title: "Implementation Funding tab / the $35M model — lane state"
created: 2026-08-28
updated: 2026-09-13
tags: [reference, roadmap-lane]
kb-status: internal
obsidian-folder: cpl-project-tracker/reference/lanes
related:
  - "[[CLAUDE]]"
---

# Implementation Funding tab / the $35M model

> **Always-current lane state, not an archive** (compacted 2026-09-03, 09-12, 09-13; narratives live in the lessons doc).

**What this lane is:** The three priorities, their shares and factors, what each earns against, and the college-by-college allocation.

## Status

✅ **THE ONE-POOL MODEL IS LIVE** (adopted 2026-08-31, S216 PR #1427): `cpl_funding.js` runs one solve over 118 institutions (115 colleges + NOCE / SD Cont. Ed / Calbright as ordinary rows; Mt. SAC NC rides the Mt. San Antonio row) · $25,240,308 · **$150,000 base / $400,000 cap on the COMBINED award** · every award decomposes into credit and noncredit shares by FTES share, the NC share restricted to the noncredit measures · the noncredit trio earn by **ORIGINATION, no advances** (N2 b) · targets ride the pre-bounds entitlement's lane slice · statewide reads the full share (Total Possible = CR+NC). ⭐ **NEVER READ THE CONFIG — CALL `_effective()` / `_alloc()` / `_prios()` / `_ncPrios()` / `_ncAward()`**; dump live dials with `scripts/funding_effective.js --config <file>`. Baked defaults are stale by design; `mirrorYears` makes the stored Year-2 block inert (guarded by `cpl_funding_effective.test.js`).

✅ **THE TAB:** introduction leads, then the institution table; sections build into an id-keyed map joined in a stored order (`secOrder`; the public page's own is `pubSecOrder`), with a position picker per section. ⭐ **ID-KEYED, NEVER POSITIONAL** (an unknown id is ignored, an unnamed one appends in house order). Outcome cards with **the goal DERIVED from the metric's milestone by default** (`prioGoals()`; a card whose goal does not resolve says so on its own face). Five prose blocks editable in curate (`TEXT_BLOCKS`); Rename + Hide ride `sectionShell()`. ⚠️ **A REPORTED CARD IS NEVER AN ENTRY IN `priorities(slot)`** — share 0 there still enters every sum, line, column, export and memo. ⚠️ **The register is INLINE in both HTMLs** (32 rows), never loaded from `CPL_Data.js`; the public page has no register, so the designated NAME rides the config and the weekly-changing STATUS deliberately does not. `ABOUT_DEFAULT_HTML` is **Sam's own approved introduction**, quoting Ed. Code §78093.2(d) in full.

✅ **THE EXPLAINER IS THE PUBLIC VIEW** (`funding-model/index.html`, S221 PR #1436) — it hosts the tab's own college section in embed mode (`CPL_FUNDING_EMBED = "college"`), so the outcome section is TAB-SIDE and every curator control is `publicMode()`-gated; `cpl_funding_public.html` REDIRECTS here (Sam, 2026-09-09). ⚠️ **Its seven section ids (`lede` `institutions` `allocation` `qualify` `earning` `timing` `choices`) meet the tab's only at `timing` and are deliberately NOT aliased**; the suite asserts declaration == markup in membership AND order. The public preview curates them under their own ids — never the live explainer, which would be a new public write surface (Rule 10 a3). Verification: the `cpl_funding_*` family, ~2,000 checks, anchor `cpl_funding_one_pool`; retired mechanisms are absence guards naming their ruling.

✅ **THE MEASURES.** Credit: `pe` eligible units · `pa` applied units · `p3` transcribed units (the documented cohort, `and not is_potential`). Access: `ppe`/`ppa` (+`_u`) among portal-origin students, DISJOINT from `pe`/`pa`, riding `Potential Student` until CollegeID2 lands. Accepted: ✅ **`pac`/`pac_u` ARE BACK IN THE BAKE** — measured 2026-09-13 on the artifact's own `as_of`: `pac_u` 24,777.95 statewide across **110 college rows**, `pac` 2,830. ⭐ **THIS IS THE ONLY PATH TO A CAMPUS MEASURE FOR GOAL (C)**: `prioGoals()` resolves the `accepted` milestone to **(B) AND (C)** (Sam, 2026-09-01 — the advising step is the part of career attainment a campus controls). The live **Awards** priority is pinned to `ppa_u`, whose `applied` milestone resolves to (A) alone, so the Counselor step sits in that priority's STRATEGIES as prose while its METRIC points elsewhere. Pointing Awards at the accepted measure is a DIAL — set through the tab, never by a session (his 2026-09-01 ruling). ⭐ **The counselor step is a policy attestation, not a technical guarantee** (Sam, 2026-09-01); no surface may claim otherwise. Noncredit: F1 feeder eligible headcount listed from day one, $0 earned until the origination feed reports (`srcDelivered()` reads an undelivered key as $0, never a cap); the builder emits `payload["origination"]` (guarded by `funding_origination_test.py`) and the tab needs no consumer edit when it lands. **The earn diagnostic** (2026-09-01: the credit slice paid 34.0% of its cap; 84% came from Access: Outreach) is the lane's best health check — **re-run it after the dials move**.

✅ **COUNTS MASK UNDER 10; UNITS CARRY THE MONEY; PUBLIC DOLLARS COARSEN** (Sam, 2026-09-03, PR #1439 — [`adr-funding-counts-mask-under-10-units-carry-the-money`](../../kb-notes/adr-funding-counts-mask-under-10-units-carry-the-money.md), which holds the derivation). Floor 10, every metric alike; per-college UNIT sums never masked (`earnFraction()` prices true numbers); a lone masked college gets a complementary mask; public demonstrated figures coarsen to the nearest $1,000, caps and dials exact. ⚠️ "<10" shows on the internal tab too until the split lands a gated copy.

**Durable rulings:** ⭐ **SUNSHINE RULE (Sam, 2026-09-01)** — outward materials carry the model's SHAPE only, no dollar figures, weights, institution counts or the explainer link, until he confirms with CO leadership (verbatim in `CLAUDE.md` Naming and `cpl_memory`). **FUNDED and MEASURED are two axes** — goal (C) is funded with no campus measure, honestly; a priority's goal derives from its MEASURE's milestone, never its title. Sam, verbatim in `cpl_memory`: *"you never need to worry about how things look without the needed data feeds. I would never publish anything unless we could measure it with our data."* Unearned funds are anchored to §78093.2(d)(1), never an invented mechanism. **N3 a:** Calbright's 1,000-FTES stand-in never publishes or disburses. Curating requires a magic-link reviewer (#1372). Shares, factors, titles, pins and text are curator edits through the tab, never SQL. **The explainer video** (Sam, 2026-09-12) **frames** the institution table, never blurs, and shows only after the model is final.

✅ **POSITIVE-FIRST PROSE (Sam, 2026-09-13):** a rendered sentence never opens with No / Nothing / Not / Never / Cannot / None (`cpl_funding_calm` §5; *Not Applicable* is a MAP status, exempt); `baked` · `scored` · `falling back` · `pin it` · `no data yet` are banned stems; an unmeasured metric reads **awaiting measurement**. ✅ **"EARN" IS RETIRED (Sam, 2026-09-13; PR #1570):** *counts toward* · *qualifies for* · *demonstrated* · *remaining*. ⚠️ Award cells keep the present participle ***qualifying***, and **"students earn credit" stays** — check who the SUBJECT is. **TWO guards**, because a mutation proved the rendered-text ban green on a branch the fixture never paints: `cpl_funding_calm` reads the DOM, `cpl_funding_earn_retired` reads the SOURCE.

⭐ **THE OUTCOME SECTION IS ONE FLAT CARD FAMILY (Sam, 2026-09-14; S262, PR #1574).** The band WRAPPER is retired; each card carries key, name, citation, the statute's sentence and a picker, with the measure-derived goal as DEFAULT. ⚠️ **Clearing stores the sentinel `"derived"`, never a deleted key** (a deleted key lets a SHARED value resurface). Every card renders always; the **totals row** names every statutory goal, served or not. ⭐ **The project allocation SPLITS across (C)/(D)**, fixing a DOUBLE CLAIM — [note](../../kb-notes/methodology-a-figure-tagged-to-two-owners-is-claimed-twice.md). Guard: `cpl_funding_outcome_cards.test.js` (77).

⭐ **THE DRILL-IN IS ONE RENDERER, AND THE STATEWIDE ROW EXPANDS (S263, PRs #1577 #1578 #1580).**
`prioDetailTableHtml(scope)` serves BOTH the college expand and the SYSTEM row — one definition, because a copy is how a
statewide surface once disagreed with the per-college cells by 30x. ⚠️ **The scope supplies `earned`; the renderer never
derives it** — statewide Current Total SUMS each institution's own capped earning, not the statewide cap times a statewide
fraction. ⚠️ **The statewide row must NOT carry `.cplfund-row`** — that class means "is an INSTITUTION row" to eleven
selectors and borrowing it turned nine suites red; the toggle keys on `data-id`. The Actual percent is the TRUE ratio, not
`Math.min(1, …)`. Guard: `cpl_funding_statewide_expand.test.js` (37).

⚠️ **THE COLUMNS MENU HID A COLUMN OF THE DRILL-IN TABLE, ON THE SHIPPED DEFAULT.** `colHideStyleHtml()` used DESCENDANT
combinators, so `tr:not(.cplfund-detail) td:nth-child(3)` reached through the detail row into the nested table and hid ITS
third cell. `COL_PREFS` defaults to `{district:true, working_adults:true}`; District is main column 3, NC funding is detail
column 3 — so **every reader**, COBI and explainer alike, lost the noncredit cell, saw later cells slide one column left under
the wrong header, and Total Possible render empty. Reported as "NC funding shows FTES" because the Target cell landed there.
Every combinator is a CHILD combinator now. ⚠️ **No DOM-reading test could catch it** — the markup was always right, the
damage was at paint; `cpl_funding_col_hide_scope.test.js` (14) asserts on `Element.matches()`.

⚠️ **`target_rate` IS DEAD ON BOTH ENDS — LEAVE IT THAT WAY.** `prioTarget()` reads it only on the students path, so on an
all-FTES config it moves nothing (measured: 10x on Awards, 0.50 on all three — no target moved by one FTES); no control
renders it either, though `setPrio()` and the `edit === "target"` handler exist. S263 wired it (#1579); Sam had it REVERTED
(#1580) as redundant with `factor`, and the maths is exact — `rate = k/factor` reproduces every target because
`prioEntitlement` is proportional to size share. One degree of freedom, two parameterizations. **Do not rebuild it.**

**HELD (Sam, 2026-09-12: "Hold, no action"):** the **LWDA partnership** stays undesignated on the (C) card — no register project names it and he chose to leave it; do **not** designate it on inference.

**NEEDS SAM:** ⓪ **SET THE DIALS THROUGH THE TAB** (ruled 2026-09-01: *"I don't want you to fix it; I want the tab to save it"*). ⚠️ **LIVE IS 33 / 33 / 34 AT FACTOR 0.5** — titles **Outreach · Completion · Awards**, displayed `[0, 2, 1]`, Awards pinned to `ppa_u` (read from `cpl_funding_config` 2026-09-14). ⭐ **P1 → APPLIED IS ONE DIAL AND ZERO CODE (Sam, 2026-09-15: "Changing the Eligible calc to using Applied will fix all the confusion").** Set P1's `metric_src` to `pa_u` in the tab. ⚠️ **DO NOT BUILD A COMBINED SOURCE** — `pa` and `ppa` are DISJOINT cohorts and **Awards already carries `ppa_u`**, so P1 on `pa_u` plus Awards on `ppa_u` covers the dashboard figure exactly (Alameda 78 + 6 = 84). A combined source would count portal-origin units in BOTH and re-create [the double claim](../../kb-notes/methodology-a-figure-tagged-to-two-owners-is-claimed-twice.md). S263 proposed one and Sam stopped it twice. Measured impact of the pin: statewide 17.42x → 2.71x, at-or-above 97/115 → 52/115. ⚠️ **A LANE FILE IS A SUMMARY OF A MEASUREMENT, NOT THE MEASUREMENT** — dump live dials with `scripts/funding_effective.js --config <file>`, which refuses to run on baked defaults for exactly this reason ([note](../../kb-notes/methodology-a-lane-file-is-a-summary-of-a-measurement.md)). `prioTarget()` makes factor the price premium; no per-year ramp in this window (`mirrorYears`). ① **Annual-view earning percent** — a cell can read "earning $140,476 · 191%" (window earning against a per-year figure); keep, or compute against the window figure. ② **When does `CollegeID2` land, and on both views?** Until then origin is `Potential Student`; it unlocks the `ppa` cutover and the noncredit trio's first real earn-out. ③ **The calm pass's lettered calls (S220)** — seven, listed in the lessons doc. ⑤ Whether COBI should keep showing "<10" until the split gives the internal view its own ungated artifact. ⑥ **Position + drag on the (C)/(D) cards** — one id-keyed `cardOrder` covering both card kinds (writing `priorityOrder` in the same action so the two cannot disagree), or reported cards ordering among themselves after the priorities. The approved mockup showed the first.

**NEXT:** ⓪ **ONE REQUEST TO PEDRO** for the remaining feed additions: Origin / CollegeID2 (on none of the four views as of 2026-09-03) and **completions** (a NEW MAP view — the aggregated view has no award column); the lifecycle booleans are delivered (#1437). ① **The `ppa` cutover** the day CollegeID2 lands: re-run the probe, confirm the spelling, switch the source, re-run the diagnostic. ② Sweep the college briefing's display names (college-district-identity). ③ Cleanup, all one-liners: ~15 dead CSS rules for retired row shapes (`.cplfund-ncrow` et al.), `pinFrozenRows`'s `:not(.cplfund-ncsysrow)`, the who-moves caveat, the explainer into `scripts/check_public_page_layout.js`, and the dead `prototype/check_funding_explainer.js` (the snapshot twin rebuilds only via `build_funding_model_explainer.js`). ④ Extend the editable prose to any block Sam names (one `TEXT_BLOCKS` entry + one `proseBlockHtml()` call; the memo builder does not read the blocks). ⑧ The explainer's footer split (sources vs the draft disclaimer) is unruled. ⑨ Sam's **expended vs allocated** rule (2026-09-09) is applied at the one memo site found; sweep the rest of the memo/report builder when it next moves. Story: [`docs/cpl_funding_lessons.md`](../../cpl_funding_lessons.md) ([archive](../../cpl_funding_lessons_archive.md)).
