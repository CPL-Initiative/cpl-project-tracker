---
title: "Implementation Funding tab / the $35M model — lane state"
created: 2026-08-28
updated: 2026-08-30
tags: [reference, roadmap-lane]
kb-status: internal
obsidian-folder: cpl-project-tracker/reference/lanes
related:
  - "[[CLAUDE]]"
---

# Implementation Funding tab / the $35M model

> **Relocated verbatim from `CLAUDE.md` §11 on 2026-08-28** (Session 206, the
> consolidation). This is **always-current lane state, not an archive** —
> update it at every checkpoint that moves this lane, exactly as you used to
> update the §11 cell. `CLAUDE.md` keeps the one-line pointer; the detail is
> here.

**What this lane is:** The three priorities, their shares and factors, what each earns against, and the college-by-college allocation.

## Status

✅ **LIVE, TWO LANES ON ONE CARD SET, ONE SOLVER — AND THE CURATOR ROUND TRIP IS PROVEN.** ⭐ **NEVER READ THE CONFIG — CALL `_effective()` / `_alloc()` / `_nc()` / `_prios()`**; dump the live dials with `scripts/funding_effective.js --config <file>`. Baked defaults are **STALE BY DESIGN**, `mirrorYears` makes the stored Year-2 block **INERT**. ✅ **Sam published his three relabels from the tab (2026-08-28)** — the curator round trip is PROVEN (`cpl_memory` slug `the-funding-curator-round-trip-is-proven` holds the md5s and values). **Do not re-derive it.** ✅ **CURATING THIS TAB NOW REQUIRES A MAGIC-LINK REVIEWER, NOT THE TEAM PHRASE (#1372).** Nine policies read `is_allowed_reviewer()` alone, live and in the SQL of record; the client mirrors it; writes stamp `curatorEmail()`, not `(team)`. ⚠️ **Three policies stay OPEN and a blunt narrowing kills them**: `cfc_select` / `cfp_select` public read, and **`cfp_insert_self`, the college self-attestation door** — nobody notices until a college tries to attest. Durable lesson from how it broke: [`a-client-gate-must-mirror-its-own-rls-policy`](docs/kb-notes/methodology-a-client-gate-must-mirror-its-own-rls-policy.md) · [`fixing-who-may-write-does-not-rescue-what-was-already-written`](docs/kb-notes/methodology-fixing-who-may-write-does-not-rescue-what-was-already-written.md). ⭐ **ONE LANE SWITCH above the cards** (Sam: all three together, every year — *"otherwise I'll be a confuseled Pooh"*); a guard forbids a per-card control. NC cards carry their own figures, metric, `$0` earning and **their own strategies** (`ncPriorities`, keyed on the SOURCE index); ⚠️ every other NC field is READ-ONLY. ✅ **Sam's dials:** factor **0.5 both lanes**, one **FTES rate both lanes**, shares **34/33/33**, `Annual funding`/`Combined funding`. ✅ **THE §78093.2(d)(1) SPINE IS LIVE (#1375)** — four goal cards read from the model, superscript ᴬᴮᶜᴰ links from the priority cards and pool boxes. ⭐ **FUNDED and MEASURED are TWO AXES, never merged**: goal **(C)** is funded (the project pool, Sam) and has **no campus measure**, and one status would force that into a green that lies or a red that denies the money. ⭐ **A priority's goal derives from its MEASURE's milestone, never its title** (eligible/applied → A, transcribed → B, via `measureOf()`); the tag drives **no arithmetic**. ⚠️ Renaming a priority must not move its goal ([`when-two-implementations-agree-on-live-data`](docs/kb-notes/methodology-when-two-implementations-agree-on-live-data-your-test-proves-nothing.md) has the story). ⚠️ **"Equitably" is NOT measured**: the floor and ceiling equalize between COLLEGES, not students. ⚠️ **The CPL story corpus evidences the WRONG goal** — **32 of 36** destinations are educational, **3** name a job; the arrow is `role → credential`, so it documents **(B)**. The intake fix is RULED (Sam, 2026-08-30): every story collection now asks **"What changed in your work or career path?"** — his wording, broader than "at work" so promotions, field changes, and hiring all count. ⚠️ **THE PROJECT POOL'S BREAKDOWN IS RULED, NOT YET WIRED (Sam, 2026-08-30, Open Verdicts item 5):** the pool card sources its named projects **from the jointly wired Activities, Annual Targets, and Budget tables** — never a hand-invented split — and the card becomes **hidable with a toggle**. The ledger's `pool`-section rows are the raw material (`CPL Initiative RCCD Projects` $10,556,650 + `Additional CPL Initiative Projects — CO or TBA` $7,443,350 = the $18M project money; the two $35M/$15M shares are $8,959,692 + $9,040,308 = **$18,000,000** exactly). ⭐ **The Workplan register's goals are NOT a rival vocabulary** (Sam) — set before §78093.2, aligned with Vision 2030 and the Workplan, they are the operational plan that DELIVERS these outcomes; a statutory tag is ADDITIVE, never a correction. ✅ **EVERY INSTITUTION IS A CR/NC PAIR, SYSTEM INCLUDED; THE `NC $` COLUMN IS RETIRED (#1378)** — it printed each carve-out twice, and the NC row's own cell rendered `↑ "the same money, summarized"`. Three NC row shapes: in-lane · below-threshold · **`none on record`** (Sam: *"if they disagree and say, Yes, we have NC, we can find the error and fix it"* — 7 colleges). Sam's 2026-08-22 requirement (NC money on the surface, never lumped in) is now carried by the ROW structure. ⚠️ **The CSV KEEPS its noncredit column** — the export has no NC rows, so deleting it would remove the figure, not de-duplicate it; the surfaces share a SCOPE, not a shape. ⚠️ **A BOUND IS TESTED BY VALUE, NOT THE CLAMP COUNT** ([`a-bound-is-tested-by-value`](docs/kb-notes/methodology-a-bound-is-tested-by-value-not-by-the-clamp-count.md)). ⚠️ **Award range is TWO ROWS, never merged**; both bounds count ties. ✅ **Unearned funds are anchored to §78093.2(d)(1)** — a stated PRIORITY, never a mechanism we built. ✅ **FOUR OPEN VERDICTS RULED (Sam, 2026-08-30, the Open Verdicts sheet — `docs/visuals/2026-08-30-open-verdicts.html`):** ① the threshold/floor coupling was **dissolved** into independent dials with a live over/under readout — built since as the mock's balance strip, and largely superseded by one-pool adoption; ② **the Combined award COLUMN is BUILT** (a pair-spanning column, his spec; 19-check `tests/cpl_funding_combined.test.js`; prints the sum that appeared nowhere — NOT the retired NC $ defect); ③ the story-intake question's final wording is **"What changed in your work or career path?"**; ④ the noncredit strategies on the cards are reviewed — **done**. ✅ **The three ruled builds of 2026-08-30 (sticky header + frozen SYSTEM pair · the item-12 goal-card policy · the wired project-pool breakdown) are SHIPPED and test-guarded** — mechanics and catches in [`docs/cpl_funding_lessons.md`](docs/cpl_funding_lessons.md); the guards are `cpl_funding_sticky` 9/9 · `cpl_funding_statutory_goals` 39/39 · `cpl_funding_pool_projects` 10/10. **Folded in from the Register Re-analysis (Sam's ruling, item 20, 2026-08-30):** EC §78093.2(d)(2) requires each campus to demonstrate implementation through the (d)(1) metrics before receiving an allocation, and the statute's three goal words (access · completion · career attainment) are not the funding model's three — a deliberate vocabulary reconciliation is owed in the model's documentation (no register row; the register's gap at n=20 records the fold). ⭐ **THE ONE-POOL EXPLORATION IS LIVE IN THE MOCK (2026-08-31 — Sam's morning direction, verbatim in the vault braindumps).** The Budget Balance mock now carries two modes: the saved two-lane model, and **One pool** — fold the $2M carve-out in, size every institution by credit + noncredit FTES, one window on the combined award. **The cap is PER INSTITUTION (CR+NC ≤ $400K — Mt San Antonio's pair trims $500K→$400K).** **NC-only institutions (NOCE · SDCCE · Calbright): ordinary allocations in the pool, earned by ORIGINATION** — CPL originating from their noncredit programs transcribed at a credit college, scoped to the DISTRICT's campuses for NOCE/SDCCE and STATEWIDE for Calbright, with a deliberate double count (the receiving college earns on the same CPL). No separate Calbright carve-out (his refinement). ⭐ **N1–N3 RULED (Sam, by number, 2026-08-31 — the mock is the stamped scoreboard): N1 a** — exhibits-in-MAP replaces the veteran-JST gate for noncredit-only campuses; **N2 b — NO advance on origination**: a noncredit-only institution draws only as its originated CPL posts, so **the origin feed leads any port** (college NC slices: **F1 RULED 2026-08-31 — listed from day one, reading $0 earned until the NC feeds report**; no hold-vs-label interim, because nothing publishes before the feeds exist); **N3 a** — Calbright's 1,000-FTES stand-in stays for exploration, the model refuses to publish or disburse an allocation built on a placeholder size (the tab's warning promoted to a rule; verified MIS figure is the unlock), and **the double count is confirmed for all three institutions**, not Calbright alone. ⚠️ Origination is a DATA build before a funding rule — no noncredit measure has a feed today; the origin must ride the daily performance pull. ⭐ **THE ADOPTED DIALS (Sam, 2026-08-31): One pool · floor $150,000 · cap $400,000** — floors take 70% of the $25.24M pool, **$7.54M differentiates by size**, 51 at the floor, 7 at the cap. The mock REMEMBERS dials per-browser and carries a "Copy these settings" line. ⭐ **THE WHO-MOVES CARD IS ON THE TAB**: the pools section renders "Who moves" while a what-if overlay changes awards — combined CR+NC vs the saved model, same pipeline with the overlay lifted (`cpl_funding_whomoves`, 13 floored checks). ⚠️ Until the one-pool model is ported as a tab scenario, the card compares dial explorations of the SAVED two-lane model; the one-pool comparison lives in the mock. ⭐ **ONE POOL IS ADOPTED (Sam, 2026-08-31: *"we should hold on 1 until we finalize one-pool, which yes, I want to go with now"*)** — the two-lane model is superseded as the design of record; the tab port is unlocked, ORIGIN FEED FIRST per N2 b (instructions delivered to Malone/Pedro same day). The simplified-tab visual (phases 1–3 under one pool, real solve + real 2026-08-31 MAP actuals: $8.33M earned, $482,669 waiting on origination) is `docs/visuals/2026-08-31-if-tab-simplified.html` (artifact “CPL Implementation Funding”), updated to Sam's reaction round. ⭐ **Vocabulary (Sam, 2026-08-31): the per-institution figure is the MAX AWARD** — *"communicates that awards are based on outcomes, not automatically awarded"* — and the institution list is ALPHABETICAL so colleges don't open on a league table; chips carry plain-language hovers; **funding vocabulary per CCC norms is doctrine (in CLAUDE.md Naming)**. NC differentiation: every award decomposes on its face (CR + NC by FTES share), the NC share restricted to noncredit outcomes. ⭐ **Reaction round ruled (Sam, 2026-08-31): page name “CPL Implementation Funding” · the strip is “Summary” · rows carry CR/NC columns for both FTES and max award (the NC sub-line is gone) · chips read AT BASE / AT CAP · West Hills colleges display as Coalinga and Lemoore.** ⭐ **Standing rule (Sam, verbatim in `cpl_memory`): “you never need to worry about how things look without the needed data feeds. I would never publish anything unless we could measure it with our data” — never design interim policy for a missing feed on an unpublished surface.** **NEXT: ① any further look edits from Sam; ② the port — one-pool solver mode + origination earn-out (origin feed first), the phase 1–3 restructure lands with it; ③ which NC share/factor editors survive is decided in the port (the s202 dials item folded here).** Story: [`docs/cpl_funding_lessons.md`](docs/cpl_funding_lessons.md) ([archive](docs/cpl_funding_lessons_archive.md)).
