---
title: "Implementation Funding tab / the $35M model — lane state"
created: 2026-08-28
updated: 2026-09-13
tags: [reference, roadmap-lane]
kb-status: internal
obsidian-folder: cpl-project-tracker/reference/lanes
related:
  - "[[CLAUDE]]"
---

# Implementation Funding tab / the $35M model

> **Always-current lane state, not an archive** (compacted 2026-09-03 and 2026-09-12; narratives live in the lessons doc).

**What this lane is:** The three priorities, their shares and factors, what each earns against, and the college-by-college allocation.

## Status

✅ **THE ONE-POOL MODEL IS LIVE** (adopted 2026-08-31, S216 PR #1427): `cpl_funding.js` runs one solve over 118 institutions (115 colleges + NOCE / SD Cont. Ed / Calbright as ordinary rows; Mt. SAC NC rides the Mt. San Antonio row) · $25,240,308 · **$150,000 base / $400,000 cap on the COMBINED award** · every award decomposes into credit and noncredit shares by FTES share, the NC share restricted to the noncredit measures · the noncredit trio earn by **ORIGINATION, no advances** (N2 b) · targets ride the pre-bounds entitlement's lane slice · statewide reads the full share (Total Possible = CR+NC). ⭐ **NEVER READ THE CONFIG — CALL `_effective()` / `_alloc()` / `_prios()` / `_ncPrios()` / `_ncAward()`**; dump live dials with `scripts/funding_effective.js --config <file>`. Baked defaults are stale by design; `mirrorYears` makes the stored Year-2 block inert (guarded by `cpl_funding_effective.test.js`).

✅ **THE TAB (S218–S221):** introduction (Summary inside it) leads, then the institution table; every section closed on open except those two, per visit. Three statutory bands — (A) Access · (B)+(C) Success · (D) Opportunities — with **membership DERIVED from the metric's milestone** (`prioGoals()`; an unresolved goal lands in a visible orphan band); the (d)(2) goal spine is a fold one click down. Flat ledger, every dial editable; priority cards a fixed two-column pair; **five prose blocks editable in curate** (`TEXT_BLOCKS`, `text.<key>` overrides). **SECTION ORDER + REPORTED BOXES (S258, PR #1563):** sections build into an id-keyed map joined in a stored order (`secOrder`; the public page's own is `pubSecOrder`), position picker per section, no drag handle. ⭐ **ID-KEYED, NEVER POSITIONAL** (an unknown id is ignored, an unnamed one appends in house order). **A REPORTED BOX** goes to any goal carrying designated activities, derived from `designatedProjects()` and ⚠️ **never an entry in `priorities(slot)`** (share 0 there still enters every sum, line, column, export and memo), so (D) gets one on the same path; the picker is a multi-select over the whole register grouped by Activity, **additive**. Seeded (C): **1.4 · 4.2 · 3.5**. ⚠️ **The register is INLINE in both HTMLs** (32 rows), never loaded from `CPL_Data.js`; the public page has no register, so the designated NAME rides the config and the weekly-changing STATUS deliberately does not. **Rename + Hide (PR #1528)** ride `sectionShell()` — `titles.<id>` / `secHidden.<id>` in the SCENARIO ?? SHARED layers, controls in the section BODY. A paragraph whose EVERY line opens `>` renders as a quotation. `ABOUT_DEFAULT_HTML` is **Sam's own approved introduction**, quoting Ed. Code §78093.2(d) in full.

✅ **THE EXPLAINER IS THE PUBLIC VIEW** (`funding-model/index.html`, S221 PR #1436): it hosts the tab's own college section in embed mode (`window.CPL_FUNDING_EMBED = "college"`) and paints the timing milestones, each priority's strategies, basis and factors from the engine and the payload. ✅ **`cpl_funding_public.html` REDIRECTS** to `funding-model/` (Sam, 2026-09-09). Its **seven** sections (`lede` `institutions` `allocation` `qualify` `earning` `timing` `choices`; `noncredit` sits inside `allocation`, `sources` is the footer and carries the draft disclaimer) read `T.sectionCuration()`. ⚠️ **Its ids meet the tab's only at `timing`** and are **deliberately NOT aliased** (`lede` and `choices` have no twin; `allocation` spans two): `PUBLIC_SECTIONS` in `cpl_funding.js` DECLARES the seven, the public preview curates them under their own ids with exclude chips (not on the live explainer — a new public write surface, Rule 10 a3), and the suite asserts declaration == markup in membership AND order; `timing` stays one switch for both. Verification: the `cpl_funding_*` family (~2,000 checks; anchor `cpl_funding_one_pool`; retired mechanisms are absence guards naming their R-ruling). ✅ **A11Y TARGET SIZE IS CLEAR ON THE EXPLAINER (S260, 2026-09-13):** 245 findings → **0**, collapsing to SIX causes (the triage is the step that matters — 118 of the 245 were one selector). Fixes are in `cpl_funding.js`'s shared section, so they carry to the tab: `.cplfund-caret` `padding: 3px 2px` · `min-height: 24px` on `.cplfund-optbtn` / `.cplfund-optin-jump` / `.cplfund-optin-grid input` · `.cplfund-colmenu-item` · `.cplfund-srclink`; the explainer's two `.tablebox` regions got `role=region` + `tabindex` + a name. ⚠️ **EVERY NUMBER THERE WAS MEASURED, NOT CHOSEN** — 1px cleared 1440px and left 118 targets at 21.6 ≤561px where the font is smaller, and **Taft** (four characters, the shortest name in the state) fails on WIDTH alone, which is the only reason the horizontal value is 2. ⚠️ **The floor goes on whichever box the engine MEASURES, and that differs per control**: a wrapping `<label>` REPLACES its checkbox's box (so `.cplfund-colmenu-item` takes it), while the opt-in label is a flex COLUMN that already clears it (so its input takes it). ⚠️ **`--gold-accent` IS UNDEFINED ON THE EXPLAINER** — a `.tablebox:focus-visible` rule naming it out-specified the page's global `:focus-visible` and REMOVED the ring (137→139); the page's token is `--focus-ring`, and the fix was deleting the rule. ✅ **THE COBI TAB IS CLEAR TOO (verified on `npm run a11y cobi`, 38 routes):** every `cplfund-*` target passes. ⚠️ **BUT THE TAB HAD FAILURES THE EXPLAINER SWEEP STRUCTURALLY CANNOT SEE** — the curate-only controls render only for a signed-in curator, so the public sweep reported ZERO for them while the tab failed. **A surface the public sweep clears is not a surface that passes**, and the old "the tab fails on 317 of the SAME" was wrong on that word. Fixing the four the first cobi sweep named surfaced two more on the next (each run reports only what it happened to render), so the rule now floors **all 26 interactive classes in the tab's markup as a family** — do not trim it back to the ones a sweep last named. **STILL OPEN:** 137 focusable-with-no-ring findings on the explainer, PRE-EXISTING and unchanged; and COBI-wide **CONTRAST** — 17 of 38 routes, 105 findings, worst `#FFFFFF on #FFFFFF` at **1:1** (invisible text), `#E6BB54 on #FFFFFF` 1.82:1, `#9CA3AF on #F4F2ED` 2.27:1. Grouped by COLOR PAIR that is a short list of color decisions, not 105 problems — a separate pass, not this lane's.

✅ **THE MEASURES.** Credit: `pe` eligible units · `pa` applied units · `p3` transcribed units (the documented cohort, `and not is_potential`). Access: `ppe`/`ppa` (+`_u`) among portal-origin students, DISJOINT from `pe`/`pa`, riding `Potential Student` until CollegeID2 lands. Accepted: **`pac`/`pac_u` — OMITTED since 2026-09-08: MAP removed `Counselor_Verified`** (restoring it needs no consumer edit — ask: `map-custom-reports` ⓪). ⭐ **The counselor step is a policy attestation, not a technical guarantee** (Sam, 2026-09-01); no surface may claim otherwise. Noncredit: F1 feeder eligible headcount listed from day one, $0 earned until the origination feed reports (`srcDelivered()` reads an undelivered key as $0, never a cap); the builder emits `payload["origination"]` (guarded by `funding_origination_test.py`) and the tab needs no consumer edit when it lands. **The earn diagnostic** (2026-09-01: the credit slice paid 34.0% of its cap; 84% came from Access: Outreach) is the lane's best health check — **re-run it after the dials move**.

✅ **COUNTS MASK UNDER 10; UNITS CARRY THE MONEY; PUBLIC DOLLARS COARSEN** (Sam, 2026-09-03, PR #1439 — [`adr-funding-counts-mask-under-10-units-carry-the-money`](../../kb-notes/adr-funding-counts-mask-under-10-units-carry-the-money.md)). Floor 5 → 10, every metric alike (raw carve-outs retired); per-college UNIT sums never masked (`earnFraction()` prices true numbers); a lone masked college gets a complementary mask; public EARNED figures read "<$1,000" or the nearest $1,000 (`earnedMoney` / `earnedCsv`, labels from `PUBLIC_MONEY_FLOOR`), caps and dials exact, the curator view exact. ⚠️ "<10" shows on the internal tab too until the split lands a gated copy.

**Durable rulings:** ⭐ **SUNSHINE RULE (Sam, 2026-09-01)** — outward materials carry the model's SHAPE only, no dollar figures, weights, institution counts or the explainer link, until he confirms with CO leadership (verbatim in `CLAUDE.md` Naming and `cpl_memory`). **FUNDED and MEASURED are two axes** — goal (C) is funded with no campus measure, honestly; a priority's goal derives from its MEASURE's milestone, never its title. Sam, verbatim in `cpl_memory`: *"you never need to worry about how things look without the needed data feeds. I would never publish anything unless we could measure it with our data."* Unearned funds are anchored to §78093.2(d)(1), never an invented mechanism. **N3 a:** Calbright's 1,000-FTES stand-in never publishes or disburses. Curating requires a magic-link reviewer (#1372). Shares, factors, titles, pins and text are curator edits through the tab, never SQL. **The explainer video** (Sam, 2026-09-12) **frames** the institution table, never blurs, and shows only after the model is final.

✅ **POSITIVE-FIRST PROSE (Sam, 2026-09-13; S259):** every rendered sentence opens with what is true, never with No / Nothing / Not / Never / Cannot / None (`cpl_funding_calm` §5 checks sentence starts; *Not Applicable* is a MAP status, exempt); `baked` · `scored` · `falling back` · `pin it` · `no data yet` are banned stems; an unmeasured metric reads **awaiting measurement**, a bad `metric_src` **awaiting a known measure**, and the (C) box reads his sentence verbatim (*Funded through the statewide project allocation and reported based on the aligned activities.*). ✅ **"EARN" IS RETIRED (Sam, 2026-09-13; S260, map confirmed in session):** *earns against* → **counts toward** (the MEASURE is the actor) · *a college earns* → **qualifies for** · *earned* (result) → **demonstrated**, the statute's own verb in (d)(2) · *unearned* → **remaining**. 52 sites in `cpl_funding.js`, 2 in the CSV export, 15 in the public explainer (which the queue had scoped to its label alone, and which also still opened a sentence with *No* and used *scores*). ⚠️ **THE CELL LABELS KEEP THE PRESENT PARTICIPLE** — his 2026-08-27 ruling (*"'earning', not 'earned'"*; the past tense read like a settled award) survives as **qualifying**; flattening it to *demonstrated* reverses him. ⚠️ **"Students earn credit" STAYS** — the subject is a STUDENT, the ordinary academic sense, exempt BY NAME so rewording that sentence fails the guard; same subject test as expended-vs-allocated. Identifiers stay, the explainer's `earning` **section id** included (a stored curation key: renaming it orphans a saved `titles.earning`). ⭐ **TWO GUARDS, because one was provably not enough:** `cpl_funding_calm` §5 bans the stems in RENDERED text, but a mutation left it 57/57 green on the orphan-band note the fixture never paints — so `cpl_funding_earn_retired.test.js` reads every quoted string in the SOURCE, and that is what caught `"Earned <window>"` still in the CSV column header. Both mutation-verified; the `\b` is load-bearing (without it the ban forbids *credit for prior learning*). ⭐ **RULED 2026-09-13 — NEXT ⑫/⑬ (⑪ landed S260):** four bands (A)(B)(C)(D) with the outcome chosen ON THE CARD (ask 1) and REPORTED CARDS for outcomes without a measure, activities picked under each card's Recommended Strategies (ask 2). Recommended, unruled: the measure-derived outcome stays the default and a curator's assignment shows in words as the CPL team's; a reported card never enters `priorities(slot)`.

**HELD (Sam, 2026-09-12: "Hold, no action"):** the **LWDA partnership** stays undesignated on the (C) card — no register project names it and he chose to leave it; do **not** designate it on inference.

**NEEDS SAM:** ⓪ **SET THE DIALS THROUGH THE TAB** (ruled 2026-09-01: *"I don't want you to fix it; I want the tab to save it"*): **Accepted at 25% share / factor 1.0**, starting set **Eligible 40% · Accepted 25% · Transcribed 35%**; `prioTarget()` makes factor the price premium. No per-year ramp in this window (`mirrorYears`); expect a large uncommitted balance until the feeds land. ① **Annual-view earning percent** — a cell can read "earning $140,476 · 191%" (window earning against a per-year figure); keep, or compute against the window figure. ② **When does `CollegeID2` land, and on both views?** (Pedro's spelling for the origination key, Sam 2026-09-02); until then origin is `Potential Student`. It unlocks the `ppa` cutover and the noncredit trio's first real earn-out. ③ **React to the calm pass's calls by letter** (S220): (a) subtitle gone; (b) the name is the row toggle; (c) the sort mark kept, ghosted; (d) the CPL area word muted beside the picker; (e) an edited scenario reads "(edited)"; (f) the held figure inside the gate sentence and ahead of Total Possible; (g) the eligibility introduction is prose with an Edit word. ⑤ **Whether COBI should keep showing "<10"** until the split gives the internal view its own ungated artifact.

**NEXT:** ⓪ **ONE REQUEST TO PEDRO** for the remaining feed additions: Origin / CollegeID2 (on none of the four views as of 2026-09-03) and **completions** (a NEW MAP view — the aggregated view has no award column); the lifecycle booleans are delivered (#1437). ① **The `ppa` cutover** the day CollegeID2 lands: re-run the probe, confirm the spelling, switch the source, re-run the diagnostic. ② Sweep the college briefing's display names (college-district-identity). ③ Cleanup: ~15 dead CSS rules for retired row shapes (`.cplfund-ncrow` et al.), `pinFrozenRows`'s `:not(.cplfund-ncsysrow)`, the who-moves caveat. ④ Extend the editable prose to any block Sam names (one `TEXT_BLOCKS` entry + one `proseBlockHtml()` call; the memo builder does not read the blocks). ⑥ Add the explainer to `scripts/check_public_page_layout.js` (one PAGES block). ⑦ `prototype/check_funding_explainer.js` is dead; the snapshot twin rebuilds only via `build_funding_model_explainer.js`. ⑧ The explainer's footer split (sources vs the draft disclaimer) is unruled. ⑨ Sam's **expended vs allocated** rule (2026-09-09) is applied at the one memo site found; sweep the rest of the memo/report builder when it next moves. ⑩ **`/a11y-pass`** on the tab + explainer (numbers above). Story: [`docs/cpl_funding_lessons.md`](../../cpl_funding_lessons.md) ([archive](../../cpl_funding_lessons_archive.md)).
