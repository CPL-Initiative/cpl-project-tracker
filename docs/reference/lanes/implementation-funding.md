---
title: "Implementation Funding tab / the $35M model — lane state"
created: 2026-08-28
updated: 2026-09-13
tags: [reference, roadmap-lane]
kb-status: internal
obsidian-folder: cpl-project-tracker/reference/lanes
related:
  - "[[CLAUDE]]"
---

# Implementation Funding tab / the $35M model

> **Always-current lane state, not an archive** (compacted 2026-09-03, 09-12, 09-13; narratives live in the lessons doc).

**What this lane is:** The three priorities, their shares and factors, what each earns against, and the college-by-college allocation.

## Status

✅ **THE ONE-POOL MODEL IS LIVE** (adopted 2026-08-31, S216 PR #1427): `cpl_funding.js` runs one solve over 118 institutions (115 colleges + NOCE / SD Cont. Ed / Calbright as ordinary rows; Mt. SAC NC rides the Mt. San Antonio row) · $25,240,308 · **$150,000 base / $400,000 cap on the COMBINED award** · every award decomposes into credit and noncredit shares by FTES share, the NC share restricted to the noncredit measures · the noncredit trio earn by **ORIGINATION, no advances** (N2 b) · targets ride the pre-bounds entitlement's lane slice · statewide reads the full share (Total Possible = CR+NC). ⭐ **NEVER READ THE CONFIG — CALL `_effective()` / `_alloc()` / `_prios()` / `_ncPrios()` / `_ncAward()`**; dump live dials with `scripts/funding_effective.js --config <file>`. Baked defaults are stale by design; `mirrorYears` makes the stored Year-2 block inert (guarded by `cpl_funding_effective.test.js`).

✅ **THE TAB:** introduction leads, then the institution table; sections build into an id-keyed map joined in a stored order (`secOrder`; the public page's own is `pubSecOrder`), with a position picker per section. ⭐ **ID-KEYED, NEVER POSITIONAL** (an unknown id is ignored, an unnamed one appends in house order). Outcome cards with **the goal DERIVED from the metric's milestone by default** (`prioGoals()`; a card whose goal does not resolve says so on its own face). Five prose blocks editable in curate (`TEXT_BLOCKS`); Rename + Hide ride `sectionShell()`. ⚠️ **A REPORTED CARD IS NEVER AN ENTRY IN `priorities(slot)`** — share 0 there still enters every sum, line, column, export and memo. ⚠️ **The register is INLINE in both HTMLs** (32 rows), never loaded from `CPL_Data.js`; the public page has no register, so the designated NAME rides the config and the weekly-changing STATUS deliberately does not. `ABOUT_DEFAULT_HTML` is **Sam's own approved introduction**, quoting Ed. Code §78093.2(d) in full.

✅ **THE EXPLAINER IS THE PUBLIC VIEW** (`funding-model/index.html`, S221 PR #1436) — it hosts the tab's own college section in embed mode (`CPL_FUNDING_EMBED = "college"`), so the outcome section is TAB-SIDE and every curator control is `publicMode()`-gated; `cpl_funding_public.html` REDIRECTS here (Sam, 2026-09-09). ⚠️ **Its seven section ids meet the tab's only at `timing` and are deliberately NOT aliased**; the suite asserts declaration == markup in membership AND order. The public preview curates them under their own ids — never the live explainer, which would be a new public write surface (Rule 10 a3). Verification: the `cpl_funding_*` family, ~2,000 checks, anchor `cpl_funding_one_pool`; retired mechanisms are absence guards naming their ruling.

✅ **THE MEASURES.** Credit: `pe` eligible units · `pa` applied units · `p3` transcribed units (the documented cohort, `and not is_potential`). Access: `ppe`/`ppa` (+`_u`) among portal-origin students, DISJOINT from `pe`/`pa`, riding `Potential Student` until CollegeID2 lands. Accepted: ✅ **`pac`/`pac_u` ARE LIVE AND AWARDS IS PINNED TO THEM** (Sam, 2026-09-15). Measured 2026-09-15: `pac_u` **24,804.45** units statewide across **23 college rows carrying a value** — ⚠️ an earlier entry read "110 college rows", which counted rows carrying the KEY, not rows with a number; the distinction is the whole point of the measure. ⭐ **THIS IS THE ONLY PATH TO A CAMPUS MEASURE FOR GOAL (C)**: `prioGoals()` resolves the `accepted` milestone to **(B) AND (C)** (Sam, 2026-09-01 — the advising step is the part of career attainment a campus controls), so pinning it moved P2's outcome row off (A) and gave (C) its first campus measure. ⭐ **The counselor step is a policy attestation, not a technical guarantee** (Sam, 2026-09-01); no surface may claim otherwise. Noncredit: F1 feeder eligible headcount listed from day one, $0 until the origination feed reports — ⚠️ `srcDelivered()` reads an undelivered key as **$0, never a cap**, so pinning a priority to an undelivered measure strands its whole share. **The earn diagnostic** is the lane's best health check — **re-run it after the dials move**.

✅ **COUNTS MASK UNDER 10; UNITS CARRY THE FUNDING; PUBLIC DOLLARS COARSEN** (Sam, 2026-09-03, PR #1439 — [the ADR](../../kb-notes/adr-funding-counts-mask-under-10-units-carry-the-money.md) holds the derivation). Floor 10; per-college UNIT sums never masked; a lone masked college gets a complementary mask; public demonstrated figures coarsen to the nearest $1,000, caps and dials exact. ⚠️ "<10" shows on the internal tab too until the split lands a gated copy.

**Durable rulings:** ⭐ **SUNSHINE RULE (Sam, 2026-09-01)** — outward materials carry the model's SHAPE only, no dollar figures, weights, institution counts or the explainer link, until he confirms with CO leadership (verbatim in `CLAUDE.md` Naming and `cpl_memory`). **FUNDED and MEASURED are two axes** — goal (C) is funded with no campus measure, honestly; a priority's goal derives from its MEASURE's milestone, never its title. Sam, verbatim in `cpl_memory`: *"you never need to worry about how things look without the needed data feeds. I would never publish anything unless we could measure it with our data."* Unearned funds are anchored to §78093.2(d)(1), never an invented mechanism. **N3 a:** Calbright's 1,000-FTES stand-in never publishes or disburses. Curating requires a magic-link reviewer (#1372). Shares, factors, titles, pins and text are curator edits through the tab, never SQL. **The explainer video** (Sam, 2026-09-12) **frames** the institution table, never blurs, and shows only after the model is final.

✅ **POSITIVE-FIRST PROSE (Sam, 2026-09-13):** a rendered sentence never opens with No / Nothing / Not / Never / Cannot / None (`cpl_funding_calm` §5; *Not Applicable* is a MAP status, exempt); `baked` · `scored` · `falling back` · `pin it` · `no data yet` are banned stems; an unmeasured metric reads **awaiting measurement**. ✅ **"EARN" IS RETIRED (Sam, 2026-09-13; PR #1570):** *counts toward* · *qualifies for* · *demonstrated* · *remaining*. ⚠️ Award cells keep the present participle ***qualifying***, and **"students earn credit" stays** — check who the SUBJECT is. **TWO guards**, because a mutation proved the rendered-text ban green on a branch the fixture never paints: `cpl_funding_calm` reads the DOM, `cpl_funding_earn_retired` reads the SOURCE.

⭐ **THE OUTCOME SECTION IS ONE FLAT CARD FAMILY (Sam, 2026-09-14; S262, PR #1574).** The band WRAPPER is retired; each card carries key, name, citation, the statute's sentence and a picker, with the measure-derived goal as DEFAULT. ⚠️ **Clearing stores the sentinel `"derived"`, never a deleted key** (a deleted key lets a SHARED value resurface). Every card renders always; the **totals row** names every statutory goal, served or not. ⭐ **The project allocation SPLITS across (C)/(D)**, fixing a DOUBLE CLAIM — [note](../../kb-notes/methodology-a-figure-tagged-to-two-owners-is-claimed-twice.md). Guard: `cpl_funding_outcome_cards.test.js` (77).

⭐ **THE DRILL-IN IS ONE RENDERER.** `prioDetailTableHtml(scope)` serves BOTH the college expand and the statewide row —
a copy is how a statewide surface once disagreed with the per-college cells by 30x. ⚠️ **The scope supplies `earned`; the
renderer never derives it.** ⚠️ **The statewide row must NOT carry `.cplfund-row`** — that class means "is an INSTITUTION
row" to eleven selectors. Guard: `cpl_funding_statewide_expand.test.js` (37).

⚠️ **EVERY COMBINATOR IN `colHideStyleHtml()` IS A CHILD COMBINATOR — keep it that way.** A descendant combinator reached
through the detail row into the nested drill-in table and hid its third cell on the SHIPPED default, for every reader.
⚠️ **No DOM-reading test could catch it** (the markup was always right, the damage was at paint); `cpl_funding_col_hide_scope.test.js` (14)
asserts on `Element.matches()`. Story: the lessons doc.

⚠️ **`target_rate` IS DEAD ON BOTH ENDS — LEAVE IT THAT WAY.** `prioTarget()` reads it only on the students path, so on an
all-FTES config it moves nothing; no control renders it. S263 wired it (#1579) and Sam had it REVERTED (#1580) as redundant
with `factor` — `rate = k/factor` reproduces every target. One degree of freedom, two parameterizations. **Do not rebuild it.**

**HELD (Sam, 2026-09-12: "Hold, no action"):** the **LWDA partnership** stays undesignated on the (C) card — no register project names it and he chose to leave it; do **not** designate it on inference.

⭐ **THE DIALS ARE SET, AND SAM SET THEM (2026-09-15).** ⚠️ **LIVE IS 33 / 33 / 34 AT FACTOR 0.5**, displayed `[0, 2, 1]`, titles **Outreach · Completion · Awards**. **P1 = `ppa_u`** with origin wording ("originating from either CPL Portal, College CPL Landing Page, or batch upload"); **P2 = `pac_u`** with the counselor wording ("for students with Counselor step checked"); P3 prose-resolves to `p3_u`. ⭐ **HE SPLIT THE ELEMENTS ACROSS TWO PRIORITIES rather than combining them into one measure** — asked for a single measure carrying counselor AND origin, he put origin on P1 and the counselor step on P2. P2's text and measure now match exactly (zero milestone warnings). That also dissolved the old "DO NOT BUILD A COMBINED SOURCE" caution: it existed because Awards held `ppa_u`, and Awards no longer does. Do not build one anyway — the case was never there (`pa_u` + `ppa_u` buys 0.3% over `pa_u`).

⚠️ **P1 MEASURES 666.5 UNITS AGAINST AN ~88,000-UNIT TARGET, AND THAT IS ACCEPTED.** Sam, 2026-09-15: *"don't worry about measurable but for the moment stranded funding. We are working on adding the origination element, including the NC location origination and the landing page, portal, batch upload element."* Measured that day: his configuration demonstrates $2,167,955 (8.6%) against $6,918,139 (27.4%) with P1 on `pa_u`, and **0 of 118** institutions reach the P1 target. ⭐ **His pin is FORWARD-CORRECT** — `ppa_u` is the key the origin cut arrives on, so P1 needs no change at the cutover. **Do not re-advise moving P1.**

⚠️ **THE ORIGINATION CUTOVER IS ASYMMETRIC — half of it lands on its own and half waits on a person.** The **NC side** (`LocID2`) is automatic: the builder emits `nc_pe`/`nc_pa`/`nc_pt` and the origination block the day the column appears, consumers already wired. The **credit side is NOT**: `funding/_build_funding_performance.py` prints *"the ppa cutover from 'Potential Student' to named origins stays PENDING"* and emits an `origin_values` histogram so the change is made on CONFIRMED spellings, never guessed. Only that cutover makes `ppa_u` cover batch upload — and its only signal is one line in a daily run log. ⚠️ **A LANE FILE IS A SUMMARY OF A MEASUREMENT, NOT THE MEASUREMENT** — dump live dials with `scripts/funding_effective.js --config <file>`, which refuses to run on baked defaults for exactly this reason ([note](../../kb-notes/methodology-a-lane-file-is-a-summary-of-a-measurement.md)). `prioTarget()` makes factor the price premium; no per-year ramp in this window (`mirrorYears`).

**NEEDS SAM** — the [2026-09-15 decision sheet](../../visuals/2026-09-15-counselor-measure-and-carried-dials.html) carries seven items and **ALL SEVEN ARE ANSWERED**; 1 and 3 came back `edit`, the rest `yes`. Executed 2026-09-15: item 1 drove the route-named picker labels (#1585); 3 resolved to `ppa_u`, not the `pa_u` proposed; 4 removed the Headcount basis; 5 added the per-CPL-type unit portion to ⓪ below; 6 is drafted at [`docs/map_eligible_label_note_draft.md`](../../map_eligible_label_note_draft.md) for SAM to send (the dashboard's "Eligible" reconciles exactly to APPLIED: 220,371 = `pa_u` + `ppa_u`, against our eligible rung of 1,410,529 — re-measure before sending); 7 merged CPLBrain PR #148. ✅ **PINS VERIFIED FROM THE CONFIG TABLE ITSELF, 2026-09-15 — the screen and the stored blob agree.** Scenario 1 / year 1: slot 0 Outreach `ppa_u`, slot 1 Completion **UNPINNED** (its wording "Transcribed CPL Units measured in FTES" prose-derives `p3_u`, transcribed — Sam's "just transcribed units"), slot 2 Awards `pac_u`. ⚠️ **THE PATH IS NOT `config->'priorities'`** — that returns empty and reads like an empty table. It is `config -> projects -> <project> -> scenarios -> <scenario> -> yearPriorities -> <year> -> <slot>`, where the year and slot levels are OBJECTS keyed "0"/"1"/"2", not arrays, so `jsonb_array_elements` errors with *cannot extract elements from an object*. Use `jsonb_each`. ⛔ **P1'S PIN IS THE ONLY THING HOLDING IT, AND SAM RULED LEAVE IT (2026-09-15, "1 leave it"). DO NOT "FIX" THIS.** Measured through the real predicates: P1's wording ("Applied CPL Units (FTES) originating from either CPL Portal...") matches `pp_u` (portal TRANSCRIBED, ~25 units statewide) BEFORE `pa_u`, because the portal rule sits ahead of the applied rule in `MEASURES`; and **no wording derives `ppa_u` at all** — it is reachable only by the pin. So un-pinning P1 silently moves it to transcribed. The offered fix (rung-aware portal rules + a `ppa_u` prose entry) was DECLINED; the pin holds and nothing is wrong on screen. P2 is safe either way — un-pinned it still lands on `pac_u`. ✅ **ITEM 2 — SAM TRIMMED IT HIMSELF**; verified 2026-09-15 against his screenshot of the live tab, P2 read "Applied CPL units (FTES) for students with Counselor step checked" with no origin clause, so all seven items are carried out. ⚠️ **He then said he was STILL CORRECTING THE TEXT — re-read `cpl_funding_config` and re-run the curator diagnostic before trusting any of this**; a reworded metric can outrun its pin, which is the whole defect #1582 fixed. ⚠️ **S264 recorded it as P1's clause; it was always P2's.** ⚠️ **A SHEET'S STORE IS LIVE — RE-READ IT AT EXECUTION.** This run reported five items unanswered because it read `replies` once, before he wrote them at 20:02–20:03Z. ① **Annual-view earning percent** — a cell can read "earning $140,476 · 191%" (window earning against a per-year figure); keep, or compute against the window figure. ② **When does `CollegeID2` land, and on both views?** Until then origin is `Potential Student`; it unlocks the `ppa` cutover and the noncredit trio's first real earn-out. ③ **The calm pass's lettered calls (S220)** — seven, listed in the lessons doc. ⑤ Whether COBI should keep showing "<10" until the split gives the internal view its own ungated artifact. ⑥ **Position + drag on the (C)/(D) cards** — one id-keyed `cardOrder` covering both card kinds (writing `priorityOrder` in the same action so the two cannot disagree), or reported cards ordering among themselves after the priorities. The approved mockup showed the first.

**NEXT:** ⓪ **ONE REQUEST TO PEDRO** for the remaining feed additions: Origin / CollegeID2 (on none of the four views as of 2026-09-03) and **completions** (a NEW MAP view — the aggregated view has no award column) and a **per-CPL-type unit portion** on the aggregated view (added 2026-09-15, Sam's sheet item 5). ⚠️ **That third ask is why `_build_funding_performance.py` refuses per-type unit sums**: a source row carries the STUDENT's total credits, not that type's share, so summing by type credits every type with the whole total. Type conversion is available at STUDENT grain only, and the model reasons in units. The lifecycle booleans are delivered (#1437). ① **The `ppa` cutover** the day CollegeID2 lands: re-run the probe, confirm the spelling, switch the source, re-run the diagnostic. ② Sweep the college briefing's display names (college-district-identity). ③ Cleanup, all one-liners: ~15 dead CSS rules for retired row shapes (`.cplfund-ncrow` et al.), `pinFrozenRows`'s `:not(.cplfund-ncsysrow)`, the who-moves caveat, the explainer into `scripts/check_public_page_layout.js`, and the dead `prototype/check_funding_explainer.js` (the snapshot twin rebuilds only via `build_funding_model_explainer.js`). ④ Extend the editable prose to any block Sam names (one `TEXT_BLOCKS` entry + one `proseBlockHtml()` call; the memo builder does not read the blocks). ⑧ The explainer's footer split (sources vs the draft disclaimer) is unruled. ⑨ Sam's **expended vs allocated** rule (2026-09-09) is applied at the one memo site found; sweep the rest of the memo/report builder when it next moves. Story: [`docs/cpl_funding_lessons.md`](../../cpl_funding_lessons.md) ([archive](../../cpl_funding_lessons_archive.md)).
