---
title: "Implementation Funding tab / the $35M model — lane state"
created: 2026-08-28
updated: 2026-09-23
tags: [reference, roadmap-lane]
kb-status: internal
obsidian-folder: cpl-project-tracker/reference/lanes
related:
  - "[[CLAUDE]]"
---

# Implementation Funding tab / the $35M model

> **Always-current lane state, not an archive** (compacted 2026-09-03, 09-12, 09-13; narratives live in the lessons doc).

**What this lane is:** The four priorities, their shares and factors, what each counts toward, and the college-by-college allocation.

## Status

✅ **THE ONE-POOL MODEL IS LIVE** (adopted 2026-08-31, S216 PR #1427): `cpl_funding.js` runs one solve over 118 institutions (115 colleges + NOCE / SD Cont. Ed / Calbright as ordinary rows; Mt. SAC NC rides the Mt. San Antonio row) · $25,240,308 · **$150,000 base / $400,000 cap on the COMBINED award** · every award decomposes into credit and noncredit shares by FTES share, the NC share restricted to the noncredit measures · the noncredit trio earn by **ORIGINATION, no advances** (N2 b) · targets ride the pre-bounds entitlement's lane slice · statewide reads the full share (Total Possible = CR+NC). ⭐ **NEVER READ THE CONFIG — CALL `_effective()` / `_alloc()` / `_prios()` / `_ncPrios()` / `_ncAward()`**; dump live dials with `scripts/funding_effective.js --config <file>`. Baked defaults are stale by design; `mirrorYears` makes the stored Year-2 block inert (guarded by `cpl_funding_effective.test.js`).

✅ **THE TAB:** introduction leads, then the institution table; sections build into an id-keyed map joined in a stored order (`secOrder`; the public page's own is `pubSecOrder`), with a position picker per section. ⭐ **ID-KEYED, NEVER POSITIONAL** (an unknown id is ignored, an unnamed one appends in house order). Outcome cards with **the goal DERIVED from the metric's milestone by default** (`prioGoals()`; a card whose goal does not resolve says so on its own face). Five prose blocks editable in curate (`TEXT_BLOCKS`); Rename + Hide ride `sectionShell()`. ⚠️ **A REPORTED CARD IS NEVER AN ENTRY IN `priorities(slot)`** — share 0 there still enters every sum, line, column, export and memo. ⚠️ **The register is INLINE in both HTMLs** (32 rows), never loaded from `CPL_Data.js`; the public page has no register, so the designated NAME rides the config and the weekly-changing STATUS deliberately does not. `ABOUT_DEFAULT_HTML` is **Sam's own approved introduction**, quoting Ed. Code §78093.2(d) in full.

✅ **THE EXPLAINER IS THE PUBLIC VIEW** (`funding-model/index.html`; `cpl_funding_public.html` REDIRECTS here). It hosts the tab's own college section in embed mode (`CPL_FUNDING_EMBED = "college"`), so the outcome section is TAB-SIDE and every curator control is `publicMode()`-gated. ⭐ **ONE PUBLIC LINK IS A CORRECTNESS REQUIREMENT, NOT A PREFERENCE** — per-section "Hide on the public page" rides `sectionShell()`, which only the retired page's rendering passed through, so a second public rendering shows colleges what the CO decided to withhold. **Do not re-split it.** ⛔ **A SHARED SECTION ID IS A SHARED CONTROL:** its EIGHT ids meet the tab's only at `timing`, deliberately un-aliased — a section shipped as `priorities` inherited the tab's live `titles.priorities` override and its hide state before being renamed `outcomes`. `funding_model_page.test.js` reads `SECTION_HOUSE_ORDER` out of the source and fails any collision past `timing`; it also asserts declaration == markup in membership AND order.

⛔ **NEVER PIN A FIGURE THE DAILY CRON REGENERATES — it turned `main` RED on 2026-09-16.** Three assertions hard-coded numbers out of `cpl_funding_performance.js` (`measure_picker` 4c, `metric_pin` 7b/7b2); the cron moved `pac_u` and `pp_u` by ordinary amounts and the tests went red with **no code change behind them**, taxing an unrelated PR that had to bisect to prove it was not theirs. **Every expectation is DERIVED from the artifact now**, keyed to a specific measure, the rival asserted ABSENT, and `chosen !== rival` asserted outright so a check that cannot fail says so. ⚠️ **A mutation that changes nothing proves nothing** — verify the mutation moved the output before concluding a guard is weak. [note](../../kb-notes/methodology-a-test-that-pins-a-generated-figure-fails-on-a-data-refresh.md)

⭐ **THE EXPLAINER READS THE MODEL, NOT COPIES OF IT (S265).** Priority card sentence = the config's `description`; baseline requirements and the participation deadline = `_requirements()`. A card gloss keyed on the priority TITLE had survived a rename and was describing a measure the model no longer used. ⚠️ **THE TWO VOCABULARY GUARDS CANNOT SEE THIS PAGE** — `cpl_funding_calm` reads the TAB's mount and `cpl_funding_earn_retired` reads `cpl_funding.js`; the explainer is a THIRD file, and `funding_model_page.test.js` scans its prose. ⚠️ **THE PRINT STYLESHEET IS THE PDF'S DESIGN** (the page prints itself — no built file) and **must never hide `.cplfund-caret`, which IS the institution's name**. ⚠️ **`--gold-accent` / `--navy-secondary` are COBI tokens the page never defined**, so 138 focus rings drew nothing; mapped to its own `--focus-ring` (the gold is 1.74:1 on paper). `npm run a11y funding-model` passes clean.

✅ **THE MEASURES.** Credit: `pe` eligible units · `pa` applied units · `p3` transcribed units (the documented cohort, `and not is_potential`). Access: `ppe`/`ppa` (+`_u`) among portal-origin students, DISJOINT from `pe`/`pa`, riding `Potential Student` until CollegeID2 lands. Accepted: ✅ **`pac`/`pac_u` ARE LIVE AND "COMPLETION WITH COUNSELING" IS PINNED TO THEM** (Sam, 2026-09-15). Measured 2026-09-15: `pac_u` **24,804.45** units statewide across **23 college rows carrying a value**. ⭐ **The `accepted` milestone serves (B) ALONE** (Sam, 2026-09-22: *"Change P2 B&C Completion to B Completion with Counseling"*, superseding his 2026-09-01 (B)+(C)). ⭐ **The counselor step is a policy attestation, not a technical guarantee** (Sam, 2026-09-01); no surface may claim otherwise. Career: ✅ **`ca_u` / `nc_ca_u`** (milestone `career`, `origin: "co"`) — CPL units for students with an EDD career outcome, which the CO measures and imports; `srcByCo()` keeps every surface from saying "MAP" or "daily feed" about them. Noncredit: F1 feeder eligible headcount listed from day one, $0 until the origination feed reports — ⚠️ `srcDelivered()` reads an undelivered key as **$0, never a cap**, so pinning a priority to an undelivered measure strands its whole share. **The earn diagnostic** is the lane's best health check — **re-run it after the dials move**.

✅ **COUNTS MASK UNDER 10; UNITS CARRY THE FUNDING; PUBLIC DOLLARS COARSEN** (Sam, 2026-09-03, PR #1439 — [the ADR](../../kb-notes/adr-funding-counts-mask-under-10-units-carry-the-money.md) holds the derivation). Floor 10; per-college UNIT sums never masked; a lone masked college gets a complementary mask; public demonstrated figures coarsen to the nearest $1,000, caps and dials exact. ⚠️ "<10" shows on the internal tab too until the split lands a gated copy.

**Durable rulings:** ⭐ **SUNSHINE RULE (Sam, 2026-09-01)** — outward materials carry the model's SHAPE only, no dollar figures, weights, institution counts or the explainer link, until he confirms with CO leadership (verbatim in `CLAUDE.md` Naming and `cpl_memory`). **FUNDED and MEASURED are two axes** — goal (C) is carried by Priority 4 at 0% and measured by the CO, awaiting its first import; a priority's goal derives from its MEASURE's milestone, never its title. Sam, verbatim in `cpl_memory`: *"you never need to worry about how things look without the needed data feeds. I would never publish anything unless we could measure it with our data."* Unearned funds are anchored to §78093.2(d)(1), never an invented mechanism. **N3 a:** Calbright's 1,000-FTES stand-in never publishes or disburses. Curating requires a magic-link reviewer (#1372). Shares, factors, titles, pins and text are curator edits through the tab, never SQL. **The explainer video** (Sam, 2026-09-12) **frames** the institution table, never blurs, and shows only after the model is final.

✅ **THE VOCABULARY RULES LIVE IN `CLAUDE.md` (Naming & terminology), NOT HERE** — positive-first prose, the retired *earn* / *draw* / *unspent* stems, *qualifying* in award cells, the student-subject and college-subject exemptions. **TWO guards, because a rendered-text ban covers only the branches a fixture paints:** `cpl_funding_calm` reads the DOM, `cpl_funding_earn_retired` reads the SOURCE — and neither opens the public explainer, which `funding_model_page.test.js` scans.

⭐ **THE OUTCOME SECTION IS ONE FLAT CARD FAMILY** (Sam, 2026-09-14; PR #1574). Each card carries key, name, citation, the statute's sentence and a picker, with the measure-derived goal as DEFAULT. ⚠️ **Clearing stores the sentinel `"derived"`, never a deleted key** (a deleted key lets a SHARED value resurface). Every card renders always; the **totals row** names every statutory goal, served or not. ⭐ **The project allocation SPLITS across (C)/(D)**, fixing a double claim — [note](../../kb-notes/methodology-a-figure-tagged-to-two-owners-is-claimed-twice.md). Guard: `cpl_funding_outcome_cards.test.js` (77).

⭐ **THE DRILL-IN IS ONE RENDERER.** `prioDetailTableHtml(scope)` serves BOTH the college expand and the statewide row —
a copy is how a statewide surface once disagreed with the per-college cells by 30x. ⚠️ **The scope supplies `earned`; the
renderer never derives it.** ⚠️ **The statewide row must NOT carry `.cplfund-row`** — that class means "is an INSTITUTION
row" to eleven selectors. Guard: `cpl_funding_statewide_expand.test.js` (37).

⚠️ **EVERY COMBINATOR IN `colHideStyleHtml()` IS A CHILD COMBINATOR — keep it that way.** A descendant combinator reached
through the detail row into the nested drill-in table and hid its third cell on the SHIPPED default, for every reader.
⚠️ **No DOM-reading test could catch it** (the markup was always right, the damage was at paint); `cpl_funding_col_hide_scope.test.js` (14)
asserts on `Element.matches()`. Story: the lessons doc.

⚠️ **`target_rate` IS DEAD ON BOTH ENDS — LEAVE IT THAT WAY.** `prioTarget()` reads it only on the students path, so on an
all-FTES config it moves nothing; no control renders it. S263 wired it (#1579) and Sam had it REVERTED (#1580) as redundant
with `factor` — `rate = k/factor` reproduces every target. One degree of freedom, two parameterizations. **Do not rebuild it.**

**HELD (Sam, 2026-09-12: "Hold, no action"):** the **LWDA partnership** stays undesignated on the (C) card — no register project names it and he chose to leave it; do **not** designate it on inference.

⭐ **THE DIALS ARE SET, AND SAM SET THEM (2026-09-15; read from the config 2026-09-23).** ⚠️ **LIVE IS 33 / 33 / 34 AT FACTOR 0.5 PLUS PRIORITY 4 AT 0% AND FACTOR 1.0**, displayed `[0, 2, 1]` and extended to `[0, 2, 1, 3]` (`priorityOrder()` keeps a stored order a newer priority postdates). Slots: **0 Outreach `ppa_u`** (origin wording) · **1 Completion with Transcription `p3_u`** (now pinned) · **2 Completion with Counseling `pac_u`** (counselor wording) · **3 Career attainment `ca_u`**, which has NO live entry and inherits the bake until Sam edits it. Card headers read each title ("(B) Completion with Counseling"). ⭐ **HE SPLIT THE ELEMENTS ACROSS TWO PRIORITIES rather than combining them into one measure** — asked for a single measure carrying counselor AND origin, he put origin on P1 and the counselor step on P2. P2's text and measure now match exactly (zero milestone warnings). That also dissolved the old "DO NOT BUILD A COMBINED SOURCE" caution: it existed because Awards held `ppa_u`, and Awards no longer does. Do not build one anyway — the case was never there (`pa_u` + `ppa_u` buys 0.3% over `pa_u`).

⛔ **P1 MEASURES 666.5 UNITS AGAINST AN ~88,000-UNIT TARGET, AND SAM ACCEPTS IT** (2026-09-15: *"don't worry about measurable but for the moment stranded funding"*). 0 of 118 institutions reach the P1 target. ⭐ **His pin is FORWARD-CORRECT** — `ppa_u` is the key the origin cut lands on, so P1 needs no change at the cutover. ⛔ **P1'S PIN IS THE ONLY THING HOLDING IT and he ruled "1 leave it":** its wording matches `pp_u` (portal TRANSCRIBED) before `pa_u`, and **no wording derives `ppa_u` at all**, so un-pinning silently moves P1 to transcribed. The offered fix was DECLINED. **Do not re-advise either.**

⚠️ **THE ORIGINATION CUTOVER IS ASYMMETRIC — half of it lands on its own and half waits on a person.** The **NC side** (`LocID2`) is automatic: the builder emits `nc_pe`/`nc_pa`/`nc_pt` and the origination block the day the column appears, consumers already wired. The **credit side is NOT**: `funding/_build_funding_performance.py` prints *"the ppa cutover from 'Potential Student' to named origins stays PENDING"* and emits an `origin_values` histogram so the change is made on CONFIRMED spellings, never guessed. Only that cutover makes `ppa_u` cover batch upload — and its only signal is one line in a daily run log. ⚠️ **A LANE FILE IS A SUMMARY OF A MEASUREMENT, NOT THE MEASUREMENT** — dump live dials with `scripts/funding_effective.js --config <file>`, which refuses to run on baked defaults for exactly this reason ([note](../../kb-notes/methodology-a-lane-file-is-a-summary-of-a-measurement.md)). `prioTarget()` makes factor the price premium; no per-year ramp in this window (`mirrorYears`).

**NEEDS SAM** — **three items on the [funding review sheet](https://claude.ai/artifact/9MfbN6jqio8as9mY4LwPB2)** (`kb/_build_funding_review_decision_sheet.py`, rebuilt 2026-09-23 on an empty store): when Priority 4 takes a share and at what factor, whose wage definition, and the drill-in consolidation. Sam's chat ruling settled the old item 3 (P2 is (B) only). Read its `replies` before executing. ⚠️ **Designate on the (D) card:** the edge logs for 2026-09-22 show nine 200 PATCHes and **none after his four releases** (1.4, 3.5, 4.2, 4.3 → `[]`), so his click never saved; #1662 makes an empty selection say so and fixes two real misses (a page-wide select lookup, the browser-layer shadow). **Ask him to retry before building more.** The 2026-09-15 decision sheet's seven items are answered AND executed. Its one live residue: [`docs/map_eligible_label_note_draft.md`](../../map_eligible_label_note_draft.md), which **SAM sends, never a session** — re-measure the reconciliation first, because the numbers ARE the argument. ⚠️ **A SHEET'S STORE IS LIVE — RE-READ IT AT EXECUTION**, not once at the top of a run. ✅ **PINS VERIFIED FROM THE CONFIG TABLE (2026-09-23):** see the dials line above; `projectGoals` holds only the four releases and `reportedCards` is unset (derived). ⚠️ **THE QUERY PATH IS NOT `config->'priorities'`** (that returns empty and reads like an empty table): it is `config -> projects -> <project> -> scenarios -> <scenario> -> yearPriorities -> <year> -> <slot>`, where year and slot are OBJECTS keyed "0"/"1"/"2" — `jsonb_array_elements` errors there, use `jsonb_each`. **Unruled, his call when he wants it:** the annual-view earning percent (a cell can read "191%"); when `CollegeID2` lands; the calm pass's seven lettered calls; whether COBI keeps "<10" until the split; position + drag on the (C)/(D) cards.

✅ **THE LEADERSHIP-REVIEW PASS (Sam, 2026-09-22, S283).** Before the CO leadership review: the Summary opens on the **total allocated** and ends on **local confirmation**, with the reserve folded into the demonstrated figure (`winEarned + winHeld`) and a new **base-and-cap bullet** (`floorCount`/`floorCost`, `cappedCount`/`capReleased`, all from `allocModel()`); the noncredit bullet names **every** noncredit program. The institution table's notes are **one sources line** (the unmatched-name check moved to the curator-only Metric wiring panel; the <10 floor rides the MAP citation because the public page must name it). Statewide ledger lines read in **black ink**, never red; "deducted" is gone. Designated projects **fold their register update under the title, closed**. The baseline block counts over **116 colleges — the credit colleges plus Calbright** (`eligColleges()`; NOCE and SD Cont. Ed are institutions, not colleges), and MAP's "Calbright College Credit/Non-Credit" contact rows now match the Calbright row. The explainer's folds are headed by **text, never "Step one"**. Guard: `tests/cpl_funding_leadership_review.test.js`.

⭐ **GOAL (C) IS MEASURABLE, BY THE CO, FROM EDD WAGE DATA (Sam, 2026-09-22).** Verbatim: *"we can use EDD wage data to measure this... This would not be reported by the colleges but instead measured by the CO and reflected on our funding model with periodic updates (imports) of the data. The Opportunities section would stay as is."* ✅ **PRIORITY 4 CARRIES IT (#1662, Sam's "Add P4 with the same ability to edit as other P cards"):** every control the other cards have, **0% share, factor 1.0, `ca_u` in CPL units** (his ruling that headcount is not a metric in this tab), (C) derived from the `career` milestone, and the (C) account note states his EDD ruling in place of "not directly measured". ⚠️ `srcDelivered()` reads an undelivered key as **$0**, so a share set before the first import sits at $0 until it lands. **The import is the next build:** a committed aggregate that adds `ca_u` / `nc_ca_u` (statewide + per college, masked under 10) to `cpl_funding_performance.js`; the model needs no edit the day it lands. Braindump: `CPLBrain/03-professional/braindumps/braindump-2026-09-22-2030-career-attainment-measured-by-edd-wage-data.md`.

**NEXT:** ⓪ **The EDD import** (above), once Sam rules on the definition. ⓪b **The college briefing's funding box** still says *earned*, *drawable* and *the dollars*; sweep it to the funding vocabulary. ⓪c `college_briefing.js` `applyPriorityOrder()` repeats `priorityOrder()`'s extension rule; keep the two in step or share one. ⓪d **ONE REQUEST TO PEDRO** for the remaining feed additions: Origin / CollegeID2 (on none of the four views as of 2026-09-03) and **completions** (a NEW MAP view — the aggregated view has no award column) and a **per-CPL-type unit portion** on the aggregated view (added 2026-09-15, Sam's sheet item 5). ⚠️ **That third ask is why `_build_funding_performance.py` refuses per-type unit sums**: a source row carries the STUDENT's total credits, not that type's share, so summing by type credits every type with the whole total. Type conversion is available at STUDENT grain only, and the model reasons in units. The lifecycle booleans are delivered (#1437). ① **The `ppa` cutover** the day CollegeID2 lands: re-run the probe, confirm the spelling, switch the source, re-run the diagnostic. ② Sweep the college briefing's display names (college-district-identity). ③ Cleanup, all one-liners: ~15 dead CSS rules for retired row shapes (`.cplfund-ncrow` et al.), `pinFrozenRows`'s `:not(.cplfund-ncsysrow)`, the who-moves caveat, the explainer into `scripts/check_public_page_layout.js`, and the dead `prototype/check_funding_explainer.js` (the snapshot twin rebuilds only via `build_funding_model_explainer.js`). ④ Extend the editable prose to any block Sam names (one `TEXT_BLOCKS` entry + one `proseBlockHtml()` call; the memo builder does not read the blocks). ⑧ The explainer's footer split (sources vs the draft disclaimer) is unruled. ⑨ Sam's **expended vs allocated** rule (2026-09-09) is applied at the one memo site found; sweep the rest of the memo/report builder when it next moves. Story: [`docs/cpl_funding_lessons.md`](../../cpl_funding_lessons.md) ([archive](../../cpl_funding_lessons_archive.md)).
