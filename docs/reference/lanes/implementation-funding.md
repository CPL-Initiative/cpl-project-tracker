---
title: "Implementation Funding tab / the $35M model — lane state"
created: 2026-08-28
updated: 2026-09-13
tags: [reference, roadmap-lane]
kb-status: internal
obsidian-folder: cpl-project-tracker/reference/lanes
related:
  - "[[CLAUDE]]"
---

# Implementation Funding tab / the $35M model

> **Always-current lane state, not an archive** (compacted 2026-09-03, 09-12, 09-13; narratives live in the lessons doc).

**What this lane is:** The three priorities, their shares and factors, what each earns against, and the college-by-college allocation.

## Status

✅ **THE ONE-POOL MODEL IS LIVE** (adopted 2026-08-31, S216 PR #1427): `cpl_funding.js` runs one solve over 118 institutions (115 colleges + NOCE / SD Cont. Ed / Calbright as ordinary rows; Mt. SAC NC rides the Mt. San Antonio row) · $25,240,308 · **$150,000 base / $400,000 cap on the COMBINED award** · every award decomposes into credit and noncredit shares by FTES share, the NC share restricted to the noncredit measures · the noncredit trio earn by **ORIGINATION, no advances** (N2 b) · targets ride the pre-bounds entitlement's lane slice · statewide reads the full share (Total Possible = CR+NC). ⭐ **NEVER READ THE CONFIG — CALL `_effective()` / `_alloc()` / `_prios()` / `_ncPrios()` / `_ncAward()`**; dump live dials with `scripts/funding_effective.js --config <file>`. Baked defaults are stale by design; `mirrorYears` makes the stored Year-2 block inert (guarded by `cpl_funding_effective.test.js`).

✅ **THE TAB (S218–S221):** introduction leads, then the institution table; sections build into an id-keyed map joined in a stored order (`secOrder`; the public page's own is `pubSecOrder`), with a position picker per section. ⭐ **ID-KEYED, NEVER POSITIONAL** (an unknown id is ignored, an unnamed one appends in house order). Statutory bands with **membership DERIVED from the metric's milestone** (`prioGoals()`; an unresolved goal lands in a visible orphan band). Five prose blocks editable in curate (`TEXT_BLOCKS`); Rename + Hide ride `sectionShell()`. ⚠️ **A REPORTED CARD IS NEVER AN ENTRY IN `priorities(slot)`** — share 0 there still enters every sum, line, column, export and memo. ⚠️ **The register is INLINE in both HTMLs** (32 rows), never loaded from `CPL_Data.js`; the public page has no register, so the designated NAME rides the config and the weekly-changing STATUS deliberately does not. `ABOUT_DEFAULT_HTML` is **Sam's own approved introduction**, quoting Ed. Code §78093.2(d) in full.

✅ **THE EXPLAINER IS THE PUBLIC VIEW** (`funding-model/index.html`, S221 PR #1436) — it hosts the tab's own college section in embed mode (`CPL_FUNDING_EMBED = "college"`); `cpl_funding_public.html` REDIRECTS to it (Sam, 2026-09-09). ⚠️ **Its seven section ids (`lede` `institutions` `allocation` `qualify` `earning` `timing` `choices`) meet the tab's only at `timing` and are deliberately NOT aliased**; `PUBLIC_SECTIONS` declares them and the suite asserts declaration == markup in membership AND order. The public preview curates them under their own ids — not the live explainer, which would be a new public write surface (Rule 10 a3). Verification: the `cpl_funding_*` family, ~2,000 checks, anchor `cpl_funding_one_pool`; retired mechanisms are absence guards naming their R-ruling.

✅ **THE MEASURES.** Credit: `pe` eligible units · `pa` applied units · `p3` transcribed units (the documented cohort, `and not is_potential`). Access: `ppe`/`ppa` (+`_u`) among portal-origin students, DISJOINT from `pe`/`pa`, riding `Potential Student` until CollegeID2 lands. Accepted: ✅ **`pac`/`pac_u` ARE BACK IN THE BAKE** — measured 2026-09-13 on the artifact's own `as_of`: `pac_u` 24,777.95 statewide across **110 college rows**, `pac` 2,830. Pedro restored the six lifecycle booleans on 2026-09-10 and the daily bake picked them up; this line read "OMITTED since 2026-09-08" for three days after it stopped being true. ⭐ **THIS IS THE ONLY PATH TO A CAMPUS MEASURE FOR GOAL (C)**: `prioGoals()` resolves the `accepted` milestone to **(B) AND (C)** (Sam, 2026-09-01 — the advising step is the part of career attainment a campus controls). The live **Awards** priority is pinned to `ppa_u`, whose `applied` milestone resolves to (A) alone, so the Counselor step sits in that priority's STRATEGIES as prose while its METRIC points elsewhere. Pointing Awards at the accepted measure is a DIAL — set through the tab, never by a session (his 2026-09-01 ruling). ⭐ **The counselor step is a policy attestation, not a technical guarantee** (Sam, 2026-09-01); no surface may claim otherwise. Noncredit: F1 feeder eligible headcount listed from day one, $0 earned until the origination feed reports (`srcDelivered()` reads an undelivered key as $0, never a cap); the builder emits `payload["origination"]` (guarded by `funding_origination_test.py`) and the tab needs no consumer edit when it lands. **The earn diagnostic** (2026-09-01: the credit slice paid 34.0% of its cap; 84% came from Access: Outreach) is the lane's best health check — **re-run it after the dials move**.

✅ **COUNTS MASK UNDER 10; UNITS CARRY THE MONEY; PUBLIC DOLLARS COARSEN** (Sam, 2026-09-03, PR #1439 — [`adr-funding-counts-mask-under-10-units-carry-the-money`](../../kb-notes/adr-funding-counts-mask-under-10-units-carry-the-money.md)). Floor 5 → 10, every metric alike (raw carve-outs retired); per-college UNIT sums never masked (`earnFraction()` prices true numbers); a lone masked college gets a complementary mask; public EARNED figures read "<$1,000" or the nearest $1,000 (`earnedMoney` / `earnedCsv`, labels from `PUBLIC_MONEY_FLOOR`), caps and dials exact, the curator view exact. ⚠️ "<10" shows on the internal tab too until the split lands a gated copy.

**Durable rulings:** ⭐ **SUNSHINE RULE (Sam, 2026-09-01)** — outward materials carry the model's SHAPE only, no dollar figures, weights, institution counts or the explainer link, until he confirms with CO leadership (verbatim in `CLAUDE.md` Naming and `cpl_memory`). **FUNDED and MEASURED are two axes** — goal (C) is funded with no campus measure, honestly; a priority's goal derives from its MEASURE's milestone, never its title. Sam, verbatim in `cpl_memory`: *"you never need to worry about how things look without the needed data feeds. I would never publish anything unless we could measure it with our data."* Unearned funds are anchored to §78093.2(d)(1), never an invented mechanism. **N3 a:** Calbright's 1,000-FTES stand-in never publishes or disburses. Curating requires a magic-link reviewer (#1372). Shares, factors, titles, pins and text are curator edits through the tab, never SQL. **The explainer video** (Sam, 2026-09-12) **frames** the institution table, never blurs, and shows only after the model is final.

✅ **POSITIVE-FIRST PROSE (Sam, 2026-09-13; S259):** every rendered sentence opens with what is true, never with No / Nothing / Not / Never / Cannot / None (`cpl_funding_calm` §5 checks sentence starts; *Not Applicable* is a MAP status, exempt); `baked` · `scored` · `falling back` · `pin it` · `no data yet` are banned stems; an unmeasured metric reads **awaiting measurement**, a bad `metric_src` **awaiting a known measure**, and the (C) box reads his sentence verbatim (*Funded through the statewide project allocation and reported based on the aligned activities.*). ✅ **"EARN" IS RETIRED (Sam, 2026-09-13; PR #1570):** *earns against* → **counts toward** · *a college earns* → **qualifies for** · *earned* (result) → **demonstrated** (the statute's verb, (d)(2)) · *unearned* → **remaining**. 69 sites across the tab, the CSV export and the explainer. ⚠️ **The award cells keep the PRESENT PARTICIPLE — *qualifying*** (his 2026-08-27 ruling that a past tense reads like a settled award survives the sweep). ⚠️ **"Students earn credit" STAYS** — student subject, exempt by name. Identifiers stay, the explainer's `earning` section id included (a stored curation key). **TWO guards:** `cpl_funding_calm` §5 bans the stems in RENDERED text and `cpl_funding_earn_retired` reads the SOURCE — the second exists because a mutation proved the first green on a branch the fixture never paints, and it is what caught the CSV header. ⭐ **THE OUTCOME SECTION IS ONE FLAT CARD FAMILY (Sam, 2026-09-14; S262).** The BAND WRAPPER IS RETIRED — his ruling, from a marked-up screenshot: *"Seems we can eliminate the outcomes header/layer if we can designate those at the Priority Level."* What the band head carried now rides the card, above the title: key, name, citation and the statute's own sentence, with a picker to change it. `setPrioGoal()` writes the assignment and the measure-derived goal stays the DEFAULT; **clearing stores the sentinel `"derived"`, never deletes the key** (a deleted key lets a SHARED value resurface and the reset undoes itself). The picker offers *Derived from the metric* first because an `accepted` milestone resolves to (B) AND (C) and no single-choice list can express a pair. ⚠️ **The orphan band went too, and the guarantee got STRONGER:** a flat grid filters nothing, so every card renders always and an unresolvable one says *Awaiting a statutory outcome* on its own face. The per-outcome Total Possible is the one figure no card can state, so it survives as the totals row — which **names every statutory goal, served or not**, and is what stops a goal going quiet now that cards can be re-pointed. **Every section below the Metric collapses**, each summary carrying its own figure (a closed *Progress* still reads `$6,694,430 · 80.37%`); Designated activities stays OPEN, since his 2026-09-13 always-visible ruling survives — collapsible and collapsed are different asks. **REPORTED CARDS ARE STORED WITH THEIR OWN IDENTITY**, defaulting to the derived set, one per goal, with their own strategies store (`reportedStrategies`, goal-keyed — a reported card is never an entry in `priorities(slot)`). ⭐ **THE PROJECT ALLOCATION SPLITS ACROSS (C)/(D) — AND THAT FIXED A DOUBLE CLAIM:** the pool is tagged to BOTH, and `goalFunding()` pushed its full amount into each, so the same $8,959,692 reported under two statutory goals at once and nothing summed them, so nothing caught it. `poolGoalAmount()`/`setPoolGoalAmount()`; two goals means the other takes the remainder, more than two states the shortfall; the default puts the whole allocation on the last goal in statute order. **No college award moves** — pool line items come off the top. **Card size is a per-BROWSER dial** (width + a MINIMUM height; an unset width keeps the two-column layout exactly). Guard: **`cpl_funding_outcome_cards.test.js` (77 assertions) REPLACES `cpl_funding_statutory_bands.test.js`** — §1 is an absence guard so the wrapper cannot return, §5 holds the no-card-goes-missing invariant the bands existed for, §11 fails if anyone restores the double claim.

**HELD (Sam, 2026-09-12: "Hold, no action"):** the **LWDA partnership** stays undesignated on the (C) card — no register project names it and he chose to leave it; do **not** designate it on inference.

**NEEDS SAM:** ⓪ **SET THE DIALS THROUGH THE TAB** (ruled 2026-09-01: *"I don't want you to fix it; I want the tab to save it"*). ⚠️ **LIVE IS 33 / 33 / 34 AT FACTOR 0.5** — titles **Outreach · Completion · Awards**, displayed `[0, 2, 1]`, Awards pinned to `ppa_u` (read from `cpl_funding_config` 2026-09-13). The **Eligible 40 · Accepted 25 · Transcribed 35** that stood here was a PROPOSAL he never applied, and a session read it as state and published it (S260). **A LANE FILE IS A SUMMARY OF A MEASUREMENT, NOT THE MEASUREMENT** — dump live dials with `scripts/funding_effective.js --config <live.json>`, which refuses to run on baked defaults for this reason. `prioTarget()` makes factor the price premium. No per-year ramp in this window (`mirrorYears`); expect a large uncommitted balance until the feeds land. ① **Annual-view earning percent** — a cell can read "earning $140,476 · 191%" (window earning against a per-year figure); keep, or compute against the window figure. ② **When does `CollegeID2` land, and on both views?** (Pedro's spelling for the origination key, Sam 2026-09-02); until then origin is `Potential Student`. It unlocks the `ppa` cutover and the noncredit trio's first real earn-out. ③ **React to the calm pass's calls by letter** (S220): (a) subtitle gone; (b) the name is the row toggle; (c) the sort mark kept, ghosted; (d) the CPL area word muted beside the picker; (e) an edited scenario reads "(edited)"; (f) the held figure inside the gate sentence and ahead of Total Possible; (g) the eligibility introduction is prose with an Edit word. ⑤ **Whether COBI should keep showing "<10"** until the split gives the internal view its own ungated artifact.

**NEXT:** ⓪ **ONE REQUEST TO PEDRO** for the remaining feed additions: Origin / CollegeID2 (on none of the four views as of 2026-09-03) and **completions** (a NEW MAP view — the aggregated view has no award column); the lifecycle booleans are delivered (#1437). ① **The `ppa` cutover** the day CollegeID2 lands: re-run the probe, confirm the spelling, switch the source, re-run the diagnostic. ② Sweep the college briefing's display names (college-district-identity). ③ Cleanup: ~15 dead CSS rules for retired row shapes (`.cplfund-ncrow` et al.), `pinFrozenRows`'s `:not(.cplfund-ncsysrow)`, the who-moves caveat. ④ Extend the editable prose to any block Sam names (one `TEXT_BLOCKS` entry + one `proseBlockHtml()` call; the memo builder does not read the blocks). ⑥ Add the explainer to `scripts/check_public_page_layout.js` (one PAGES block). ⑦ `prototype/check_funding_explainer.js` is dead; the snapshot twin rebuilds only via `build_funding_model_explainer.js`. ⑧ The explainer's footer split (sources vs the draft disclaimer) is unruled. ⑨ Sam's **expended vs allocated** rule (2026-09-09) is applied at the one memo site found; sweep the rest of the memo/report builder when it next moves. ⑩ **`/a11y-pass`** on the tab + explainer (numbers above). Story: [`docs/cpl_funding_lessons.md`](../../cpl_funding_lessons.md) ([archive](../../cpl_funding_lessons_archive.md)).
