---
title: "Implementation Funding tab / the $35M model — lane state"
created: 2026-08-28
updated: 2026-09-12
tags: [reference, roadmap-lane]
kb-status: internal
obsidian-folder: cpl-project-tracker/reference/lanes
related:
  - "[[CLAUDE]]"
---

# Implementation Funding tab / the $35M model

> **Always-current lane state, not an archive** (compacted 2026-09-03 and 2026-09-12; narratives live in the lessons doc).

**What this lane is:** The three priorities, their shares and factors, what each earns against, and the college-by-college allocation.

## Status

✅ **THE ONE-POOL MODEL IS LIVE** (adopted 2026-08-31, S216 PR #1427): `cpl_funding.js` runs one solve over 118 institutions (115 colleges + NOCE / SD Cont. Ed / Calbright as ordinary rows; Mt. SAC NC rides the Mt. San Antonio row) · $25,240,308 · **$150,000 base / $400,000 cap on the COMBINED award** · every award decomposes into credit and noncredit shares by FTES share, the NC share restricted to the noncredit measures · the noncredit trio earn by **ORIGINATION, no advances** (N2 b) · targets ride the pre-bounds entitlement's lane slice · statewide reads the full share (Total Possible = CR+NC). ⭐ **NEVER READ THE CONFIG — CALL `_effective()` / `_alloc()` / `_prios()` / `_ncPrios()` / `_ncAward()`**; dump live dials with `scripts/funding_effective.js --config <file>`. Baked defaults are stale by design; `mirrorYears` makes the stored Year-2 block inert (`cpl_funding_effective.test.js` guards the retired fields).

✅ **THE TAB (S218–S221):** introduction (Summary inside it) leads, then the institution table; every section closed on open except those two, per visit. Three statutory bands — (A) Access · (B)+(C) Success · (D) Opportunities — with **membership DERIVED from the metric's milestone** (`prioGoals()`; an unresolved goal lands in a visible orphan band); the (d)(2) goal spine is a fold one click down, evidence lines from ONE function. Flat ledger, every dial editable; priority cards a fixed two-column pair; the calm pass (every glyph a word or gone); **five prose blocks editable in curate** (`TEXT_BLOCKS`, `text.<key>` overrides). Vocabulary doctrine: `CLAUDE.md` (Naming). **SECTION ORDER + REPORTED BOXES (S258, PR #1563):** sections build into an id-keyed map joined in a stored order (`secOrder`; the public page's own is `pubSecOrder`), position picker per section, no drag handle. ⭐ **ID-KEYED, NEVER POSITIONAL** — the set grows whenever a section ships; an unknown id is ignored, an unnamed one appends in house order, and those two rules let a stored order survive our edits. **A REPORTED BOX** goes to any goal carrying designated activities, derived from `designatedProjects()` and ⚠️ **never an entry in `priorities(slot)`** (share 0 there still enters every sum, line, column, export and memo), so (D) gets one on the same path; the picker is a multi-select over the whole register grouped by Activity, **additive**. Seeded (C): **1.4 · 4.2 · 3.5**. ⚠️ **The register is INLINE in both HTMLs** (32 rows), never loaded from `CPL_Data.js`; the public page has no register, so the designated NAME rides the config and the weekly-changing STATUS deliberately does not. **Rename + Hide (PR #1528)** ride `sectionShell()`, the one function every section passes through — `titles.<id>` / `secHidden.<id>` in the same SCENARIO ?? SHARED layers, controls in the section BODY (a button inside a `<summary>` is nested interactive content). A prose paragraph whose EVERY line opens `>` renders as a quotation. `ABOUT_DEFAULT_HTML` is **Sam's own approved introduction**, quoting Ed. Code §78093.2(d) in full.

✅ **THE EXPLAINER IS THE PUBLIC VIEW** (`funding-model/index.html`, S221 PR #1436): it hosts the tab's own college section in embed mode (`window.CPL_FUNDING_EMBED = "college"`) and paints the timing milestones, each priority's strategies, basis and factors from the engine and the payload. ✅ **`cpl_funding_public.html` REDIRECTS** to `funding-model/` (Sam, 2026-09-09; Pages cannot 301). Its **seven** sections (`lede` `institutions` `allocation` `qualify` `earning` `timing` `choices`; `noncredit` sits inside `allocation`, `sources` is the footer and carries the draft disclaimer) read `T.sectionCuration()`. ⚠️ **Its ids meet the tab's only at `timing`, by coincidence** — until S258 (PR #1563) six of the seven could not be curated (the test had written the id it then read). **Deliberately NOT aliased onto the tab's sections** (`lede` and `choices` have no twin; `allocation` spans two): `PUBLIC_SECTIONS` in `cpl_funding.js` DECLARES the seven, the public preview curates them under their own ids with exclude chips (not on the live explainer — a new public write surface, Rule 10 a3), and the suite asserts declaration == markup in membership AND order; `timing` stays one switch for both. Verification: the `cpl_funding_*` family (~2,000 checks; anchor `tests/cpl_funding_one_pool.test.js`; `funding_model_page` boots the engine and pins every `P.<key>` the painter reads; `cpl_funding_section_order` 63; retired mechanisms are absence guards naming their R-ruling, R1–R11). ⚠️ **A11Y, MEASURED 2026-09-12 (target added, PR #1567):** the explainer fails on **245 targets under 24×24 at every width** (`cplfund-optin-jump` ×118, `cplfund-caret` 19–22px, `cplfund-optbtn`, eight inputs) and **two `.tablebox` regions not keyboard reachable ≤430px**; the tab fails on 317 of the same classes plus 18 `--text-faint` contrast pairs. All of it is the shared section in `cpl_funding.js` — one `/a11y-pass` fixes both.

✅ **THE MEASURES.** Credit: `pe` eligible units · `pa` applied units · `p3` transcribed units (the documented cohort, `and not is_potential`). Access: `ppe`/`ppa` (+`_u`) among portal-origin students, DISJOINT from `pe`/`pa`, riding `Potential Student` until CollegeID2 lands. Accepted: **`pac`/`pac_u` — OMITTED since 2026-09-08: MAP removed `Counselor_Verified`** (applied units on a counselor-verified plan; restoring it needs no consumer edit — ask: `map-custom-reports` ⓪). ⭐ **The counselor step is a policy attestation, not a technical guarantee** (Sam, 2026-09-01) — it can be batch-loaded legitimately; no surface may claim otherwise. Noncredit: F1 feeder eligible headcount listed from day one, $0 earned until the origination feed reports (`srcDelivered()` reads an undelivered key as $0, never a cap); the builder emits `payload["origination"]` (district scope for NOCE/SDCCE, statewide for Calbright; absent-not-zeroed; guarded by `funding_origination_test.py`) and the tab needs no consumer edit when it lands. **The earn diagnostic** (2026-09-01: the credit slice paid 34.0% of its cap; 84% of everything earned came from Access: Outreach, which 97 of 115 colleges max out) is the lane's best health check — **re-run it after the dials move**.

✅ **COUNTS MASK UNDER 10; UNITS CARRY THE MONEY; PUBLIC DOLLARS COARSEN** (Sam, 2026-09-03, PR #1439 — [`adr-funding-counts-mask-under-10-units-carry-the-money`](../../kb-notes/adr-funding-counts-mask-under-10-units-carry-the-money.md)). Floor 5 → 10, every metric alike (the `pp`/`ppa`/`ppe` raw carve-outs retired); per-college UNIT sums never masked, so `earnFraction()` prices the true numbers; a lone masked college gets a complementary mask; public EARNED figures read "<$1,000" or the nearest $1,000 (`earnedMoney` / `earnedCsv`, labels from `PUBLIC_MONEY_FLOOR`), caps and dials exact, the curator view exact. ⚠️ The artifact is public, so "<10" shows on the internal tab too until the split lands a gated copy.

**Durable rulings:** ⭐ **SUNSHINE RULE (Sam, 2026-09-01)** — outward materials carry the model's SHAPE only, no dollar figures, weights, institution counts or the explainer link, until he confirms with CO leadership (verbatim in `CLAUDE.md` Naming and `cpl_memory`). **FUNDED and MEASURED are two axes** — goal (C) is funded with no campus measure, honestly; a priority's goal derives from its MEASURE's milestone, never its title. Sam, verbatim in `cpl_memory`: *"you never need to worry about how things look without the needed data feeds. I would never publish anything unless we could measure it with our data."* Unearned funds are anchored to §78093.2(d)(1), never an invented mechanism. **N3 a:** Calbright's 1,000-FTES stand-in never publishes or disburses. Curating requires a magic-link reviewer (#1372). Shares, factors, titles, pins and text are curator edits through the tab, never SQL. **The explainer video** (Sam, 2026-09-12) **frames** the institution table (search to one college, then its drill-in), never blurs, may carry details, and shows only after the model is final.

**HELD (Sam, 2026-09-12: "Hold, no action"):** the **LWDA partnership** stays undesignated on the (C) card — no register project names it (only 1.4's update text; 4.3 names no partner) and he chose to leave it. The multi-select is built and waiting; do **not** designate it on inference.

**NEEDS SAM:** ⓪ **SET THE DIALS THROUGH THE TAB** (ruled 2026-09-01: *"I don't want you to fix it; I want the tab to save it"*): **Accepted at 25% share / factor 1.0**, starting set **Eligible 40% · Accepted 25% · Transcribed 35%**; `prioTarget()` makes factor the price premium. No per-year ramp in this window (`mirrorYears`); expect a large uncommitted balance until the feeds land. ① **Annual-view earning percent** — a cell can read "earning $140,476 · 191%" (window earning against a per-year figure); keep, or compute against the window figure. ② **When does `CollegeID2` land, and on both views?** (Pedro's spelling for the origination key, Sam 2026-09-02); until then origin is `Potential Student`. It unlocks the `ppa` cutover and the noncredit trio's first real earn-out. ③ **React to the calm pass's calls by letter** (S220): (a) subtitle gone; (b) the name is the row toggle; (c) the sort mark kept, ghosted; (d) the CPL area word muted beside the picker; (e) an edited scenario reads "(edited)"; (f) the held figure inside the gate sentence and ahead of Total Possible; (g) the eligibility introduction is prose with an Edit word. ⑤ **Whether COBI should keep showing "<10"** until the split gives the internal view its own ungated artifact.

**NEXT:** ⓪ **ONE REQUEST TO PEDRO** for the remaining feed additions: Origin / CollegeID2 (on none of the four views as of 2026-09-03, probe 33767273456) and **completions** (a NEW MAP view — the aggregated view has no award column); the lifecycle booleans are delivered (#1437). ① **The `ppa` cutover** the day CollegeID2 lands: re-run the probe (Actions → Discover MAP datasets), confirm the spelling, switch the source, re-run the diagnostic. ② Sweep the college briefing's display names (belongs with college-district-identity). ③ Cleanup: dead CSS for retired row shapes (~15 rules: `.cplfund-ncrow`, `.cplfund-ncsysrow`, `.cf-lanechip`, `.cplfund-awardrow`, `.cf-prio`), `pinFrozenRows`'s `:not(.cplfund-ncsysrow)`, the who-moves caveat if it still names two lanes. ④ Extend the editable prose to any block Sam names (one `TEXT_BLOCKS` entry + one `proseBlockHtml()` call; the memo builder does not read the blocks). ⑥ Add the explainer to `scripts/check_public_page_layout.js` (one PAGES block). ⑦ Still dead: `prototype/check_funding_explainer.js`; the snapshot twin `prototype/funding_model_explainer.html` rebuilds only via `build_funding_model_explainer.js` with an md5-verified config. ⑧ **The explainer's footer** — whether the *sources* paragraph splits from the draft disclaimer so it becomes hideable (unruled; the whole footer stays out of curation). ⑨ Sam's **expended vs allocated** rule (2026-09-09) is applied at the one memo site found; sweep the rest of the memo/report builder when it next moves. ⑩ **`/a11y-pass`** on the tab + explainer (numbers above). Story: [`docs/cpl_funding_lessons.md`](../../cpl_funding_lessons.md) ([archive](../../cpl_funding_lessons_archive.md)).
