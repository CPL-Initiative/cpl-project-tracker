---
title: "Implementation Funding tab / the $35M model — lane state"
created: 2026-08-28
updated: 2026-09-02
tags: [reference, roadmap-lane]
kb-status: internal
obsidian-folder: cpl-project-tracker/reference/lanes
related:
  - "[[CLAUDE]]"
---

# Implementation Funding tab / the $35M model

> **Relocated verbatim from `CLAUDE.md` §11 on 2026-08-28** (Session 206, the
> consolidation). This is **always-current lane state, not an archive** —
> update it at every checkpoint that moves this lane, exactly as you used to
> update the §11 cell. `CLAUDE.md` keeps the one-line pointer; the detail is
> here.

**What this lane is:** The three priorities, their shares and factors, what each earns against, and the college-by-college allocation.

## Status

✅ **LEAD WITH THE TABLE, AND THE EXPLAINER IS THE PUBLIC VIEW (S221 SkyLead, 2026-09-02 — PR #1436).** Sam's seven asks, all shipped on one branch. **The institution table is the first section after the introduction** on both surfaces (*"so folks don't have to scroll down through the steps to see it — most won't care about the details, just their funding"*); the model's mechanics (window · breakdown · formula · eligibility · outcomes · timing) follow it, and the table's footnote moved with the table (`footHtml()`, one function for both hosts). **Every section is closed on open except the introduction and the table — and that is PER VISIT now:** the v2 per-browser store (`cplfund_sections_v2`) is retired, because a section opened once stayed open on every later visit, which is what his ask was about — a fresh browser already showed the collapsed default, so the author was the one reader who could not see it. **The Summary lives inside the introduction's box** (a ruled-off list under the prose, not a box in a box). R11 is re-aimed a second time: its requirement is that the readout is never hidden on open, and the introduction is the section open on every visit (`cpl_funding_render.test.js`). **The priority cards are a fixed two-column pair** (`repeat(2, minmax(0, 1fr))`, one column below 560px) — auto-fit had stretched the Success band's lone card to twice the width of the two Access cards. **The card's "Combined funding" line is retired:** it restated the band head's Total Possible, the Target line and the price line (under front-load the effective rate IS the price, because the target is the window figure divided by it), and the Current Total line still reads "of $W full-window Total Possible", so nothing moved and nothing was lost; only the carryover year keeps its explanatory line. ⭐ **THE EXPLAINER HOSTS THE TAB'S OWN COLLEGE SECTION.** `funding-model/index.html` already booted the whole tab into a hidden mount to compute its payload, then painted a second, simpler table from `rows` — the copy that had drifted twice (sort order, a mislabeled header). The hidden host is now the visible mount: `window.CPL_FUNDING_EMBED = "college"` makes `render()` emit only the college section body and its footnote (`embedMode()`, public rendering implied), the page's own table CSS is scoped to `.tablebox`, and the sidecars load through a `../`-prefixed `CPL_TABS.loadScript` shim so the actuals fill in. Same rows, same drill-in on the institution's name, same editable introduction (Publish reaches it), search · grouping · columns · Excel export included. The explainer's steps are folds closed on open (a Show / Hide word), the **timing milestones** paint in Step four and the **recommended strategies** under each priority card, both from the payload (`_timing()`; `strategies` on `_prios()`), so a curator's edit reaches the page and a typed copy cannot go stale. Guards: `tests/cpl_funding_lead_with_the_table.test.js` (29 checks) and `funding_model_page.test.js` (37), twelve mutations caught by name — the first draft of the new suite DIED under one of them instead of failing, the third recurrence of S219's lesson, and it fails by name now. ⚠️ `cpl_funding_public.html` is still live and still the tab in public mode; the recommendation (NEEDS SAM ④) is to make it a redirect to `funding-model/` so one public link exists.

✅ **THE CALM PASS (S220 SkyCalm, 2026-09-02).** Sam's brief, verbatim: *"get rid of any cheesy glyphs (per our rules) and preserve all needed functionality while eliminating any visual noise possible. I want folks to feel calm when they open this model."* Every rendered glyph on the tab is now a word or gone: the section chevron is a **Show / Hide** word at the right of each heading, the row caret is retired and **the institution's name is the toggle button** (a real `<button>` with `aria-expanded`), the controls read Add project · New scenario · Delete scenario · Remove · Hide · Restore: … · Hide from public · Show to public · Download as Excel · Columns · Copy requirements · Generate brief · Regenerate · Copy text · Save as PDF · Download as Word, and the $50K view's outcome marks are the words met / partial / not yet / n/a / pending. The one glyph kept is the **sort mark** — ghosted CO blue on white, aria-hidden, beside the `aria-sort` that carries the state. The chrome is quiet: no fill or stripe on the project strip or the sign-in line, Reset an outlined word and Publish the one filled control, the Draft chip an outlined word rather than a gold pill, the explainer link a plain link, the colored left borders off the basis note, eligibility, priority cards, timing, goal quotes and the CO lanes, and the balanced Summary in ink (green and red are for a state to act on). The static subtitle under the title — which restated the introduction in retired vocabulary ("pools") — is gone from both HTMLs. ⭐ **EDITABLE PROSE IN CURATE MODE** (his ask, verbatim: *"It would be nice to be able to edit while in curate, any of the text sections"*): five blocks — the introduction, Reading the funding, the eligibility introduction, the earning rules for noncredit, the institution table introduction — render from ONE registry (`TEXT_BLOCKS`): a house default plus an optional override stored as plain text under `text.<key>` in the config layers (SCENARIO ?? SHARED ?? default), so Reset returns the words with the numbers and Publish carries them to everyone, the public page included. A signed-in reviewer sees the word **Edit** beneath a block; then a textarea with Save · Cancel · Restore the default text. Overrides are escaped on render (a blank line starts a paragraph), a re-render mid-edit keeps the draft, and the `CURATE_ATTRS` sweep is the public-mode backstop. ⚠️ **Prose is not a dial:** a signed-out visitor gets no Edit control — the eligibility introduction used to be an always-open textarea for everyone, and its legacy `eligIntro` key still resolves (a save migrates it to `text.elig_intro`). The explainer's own prose stays its own; the tab's blocks reach the public page because the public page IS the tab. **The held-in-reserve figure** no longer floats as its own item under the NC column (Sam, on a drill-in reading "$400,000 … $132,000 held in reserve": *"isn't clear when compared to 400k available"*, then *"put it before the $400k CR total and not on the NC total"*): it is named inside the gate sentence as a part of the max award, held not lost, ahead of the numbered requirements, and again in the priority caption ahead of Total Possible; the drill-in's size line reads "FTES share" (one-pool sizes on combined FTES). **The bound word moved into the award cells** on his follow-up the same day (*"move the at cap and at base notes next to the CR and NC total funding on main rows and put the note in parens (at cap), (at base)"*): `boundWordHtml()` renders it after the figure in both the CR and NC cells (the NC cell only when it holds a share); `rowChips()` keeps NC only and the Confirm Participation control by the name. Vocabulary swept on rendered text (pool → funding, "apportioned" → allocated, "cannot be spent" → cannot be allocated, the explainer's "% of the money" → "% of the funding"), and the explainer's masthead **Scenario** and **Version as of** tags are painted from the payload (`model_version`, `scenario`) rather than typed. Guard: `tests/cpl_funding_calm.test.js` (56 checks, mutation-verified seven ways; a control a regression removed FAILS BY NAME rather than crashing the run). ⚠️ **A rendered-text sweep must read words with a seam at every tag:** `textContent` has none, so "poolRemove" hid a banned word from `\bpool\b`, and two arrows the eye missed sat in tooltip `title`s and an `&rarr;` entity — KB note `methodology-the-text-a-reader-sees-is-not-the-text-a-test-reads`.

✅ **THE THREE STATUTORY BANDS ARE SHIPPED (S218, PR #1429 — 2026-09-01).** The tab carried **two sections describing one allocation** — the priorities and the §78093.2(d)(1) goals, stitched by a superscript letter — and they are now ONE. Sam's fold: **(A) Access · (B)+(C) Success · (D) Opportunities**, where Success pairs completion with career attainment *"the same way we combine two aspects of Access"* and (D) is named for the statute's own object (*"credit for prior learning opportunities"*) rather than the pilot projects that are its means. ⭐ **Band membership is DERIVED, never typed** — a card lands by the goal `prioGoals()` resolves from its metric's MILESTONE, the same resolver the earning math uses, so the band a college reads and the dollars it earns cannot disagree; a priority whose goal does not resolve lands in a visible **orphan band** rather than vanishing. The goal spine is **not retired** — it is the §78093.2(d)(2) reporting artifact and the only place goal (C) reads honestly funded-and-unmeasured, so it sits one click down as a fold. New milestone **`accepted`** → (B)+(C), which is what puts the counselor-gated measure in Success. ⚠️ **Reordering now orders cards WITHIN a band** (bands render in statute order), so the card at display position N is Nth within its band, not Nth in the document; identity, funding and editing are unchanged.

✅ **THE TAB IS CONSOLIDATED AND THE LEDGER IS FLAT (S219, PR #1433).** Sam ruled both sheet
decisions the day they were put to him. **(1) The goal spine folded into the bands.** Half the fold
was a second printing of the band above it — key, name, citation, statute quote, and the
per-priority funding the cards already print — and that half is gone. The half with no other home
moved UP: each band now carries a **per-goal evidence line**, with the (A) equity limit and the (C)
demonstrated-not-measured note behind a fold on the line they belong to. ⚠️ **Per GOAL, not per
band, and that is the design:** Success is (B)+(C) and they differ on precisely the axis
§78093.2(d)(2) asks about, so one band-level sentence would have to be wrong about one of them. The
four cards became a **four-row (d)(2) table** one click down — not decoration: `goalSpineHtml()`
renders in exactly ONE place and the Report sub-view does not carry it, so that fold IS the
demonstration, and the only place all four goals read together in statute order. ⭐ **The evidence
state and the limits come from ONE function both surfaces call** (`goalEvidence` /
`goalLimitHtml` / `goalFundsHtml`) — two copies of "is this goal measured?" is the drift the
consolidation exists to remove, and a check asserts the band and the table agree.

⭐ **(2) THE FLAT LEDGER SHIPPED, WITH EVERY DIAL STILL EDITABLE.** Three money lines reading down
to the total, **base and cap on their own line** beneath, and the two CONTEXT figures (the
1,151,171-FTES basis and the $5,649.63 rate) moved into *How an allocation is computed* — neither
is a sum that nets down to the total, and standing in a money ledger they read as though they were.
⚠️ **The class vocabulary is deliberately unchanged**: `.cplfund-card` names a ROLE (a labeled
figure), not a shape, so every editor, control, fold and absence guard kept working and only the
presentation moved; `card()` is hoisted to module scope because the context figures render in
another section now. ⭐ **Sam's condition, verbatim (2026-09-01): "I don't want to lose editability
of variables we have in the model through the simplifying and consolidation process."** That was
the real risk — the seven boxes were his EDITING SURFACE, not a display, and the natural way to
build a flat ledger is to print the values. It is a **guard, not a promise**:
`tests/cpl_funding_ledger_editable.test.js` (26 checks) pins every amount and label as an input,
the bounds pair's distinct accessible names, the rate keeping its editor through the move, the
remove / visibility / Add controls, a commit reaching the model and repainting the total, and the
public view rendering figures with no dial. Mutation-verified both ways. Both **folds** survived —
the named-projects list and the base/cap institution list — which is the other thing a flatten
silently drops. The **print CSS** learned the ledger too (the print window clones the live tab).

✅ **An INTRODUCTION opens the tab** (Sam's ask, collapsible on his follow-up, open by default —
`SECTION_DEFAULT_OPEN.about`). House voice, and **no figures in it**, so it cannot go stale against
a live solve. ⚠️ **R11 was re-aimed, not weakened:** its guard read "the Summary sits above the
first section", which the intro violates; R11's requirement is that the Summary is never inside a
fold, so the check now asserts both halves — no enclosing `<details>`, and only the intro may
precede it.

✅ **THE DRILL-IN IS TRIMMED, AND ITS TARGETS ARE READABLE (S219, PR #1432).** Sam struck four
restatements from the college expand — the headcount aside, the **base and cap tails** (the bound
rule is stated in full by the formula box; the drill-in is for THIS college's figures), the
`(proposed — the gate to participate)` parenthetical, and the reserve sentence's second half, which
repeated the gate text one column to its left. The base tail came off with the cap's: they were a
mirrored pair. On the row, the **⛔ gate chip is retired** (it duplicated the Elig pie three cells
away; its guard is re-aimed to the pie + the award cell's words, never dropped) and both confirm
controls read **Confirm Participation**, no pencil.

⭐ **THE PER-PRIORITY TARGETS WERE NEVER MISSING — THEY WERE LAID OUT INTO A 240px COLUMN.** Sam read
the expand as carrying no targets. It rendered Target and Actual all along, but
`.cplfund-dtl-tscroll` is a CHILD of `.cplfund-detail-grid`, whose
`repeat(auto-fit, minmax(240px, 1fr))` gave it one column — so a 620px-min table scrolled sideways
inside ~240px with three of its eight columns out of view. **A `grid-column: 1 / -1` span is the
whole fix**, and it now also covers the attestation form and the CO note, which were squeezed the
same way. ⚠️ The span is invisible in a screenshot, so `cpl_funding_detail_trim.test.js` asserts
both the DOM parentage and the rule's presence in the injected CSS. New column **To go** —
the distance to target plus the funding it would earn — renders ONLY for a measured state:
a privacy-suppressed actual gets no distance (mask + gap = the value, by subtraction) and an
unmeasured one has nothing to subtract; both read the plain absence, because *"we cannot see"* and
*"you are done"* are different claims about a college.

⭐ **THE EARN DIAGNOSTIC — the finding that justified the restructure (measured 2026-09-01, live model × live feed).** The credit slice pays **34.0% of its cap** ($7,969,414 of $23,456,909), and **84% of everything earned comes from Access: Outreach, which 97 of 115 colleges already max out** (86.0% earned). Completion earns 16.1% (13 full · 9 partial · 93 zero) and Access: Statewide 0.8% (0 at full, `ppa_u` = 649.5 units). `earnFraction()` caps at `min(1, actual/target)`, so an over-target measure is an automatic payment — the same "earns nothing and incentivises nothing" the metric diagnostic warns about for an unmeasurable metric, reached from the other direction. **Re-run this after the dials move**; it is the lane's best single health check.

**The two new measure sources (declared-before-delivered, the noncredit lane's proven pattern — `srcDelivered()` reads an undelivered key as $0, never a full cap):** `ppe`/`ppe_u` = ELIGIBLE units among portal-origin students (Sam ruled "filter now"; emits from today's feed since Potential Student is already in the pull) and `pac`/`pac_u` = APPLIED units on an accepted Student CPL Plan via the MAP lifecycle attestation (OMITTED, not zeroed, until the builder's sweep names `Counselor_Verified` — fetched daily since 2026-09-02 / #1437; its meaning waits on Sam, NEEDS SAM ⑤). ⚠️ **`ppe` is `pe`'s COMPLEMENT, not `pe` filtered** — every `pe`/`pa`/`p2`/`p3` hit carries `and not is_potential`, exactly as `ppa` is `pa`'s. ⚠️ **`pac` carries NO `is_potential` condition** (the attestation is done for one student regardless of how they arrived) and is named for the credit's STATE, not who attested.

⭐ **THE COUNSELOR STEP IS A POLICY ATTESTATION, NOT A TECHNICAL GUARANTEE (Sam, 2026-09-01 — corrects a claim this lane briefly carried).** It CAN be batch-loaded, legitimately: *"we ask colleges to batch upload previously transcribed CPL from their SIS, with the assumption that they went through the counseling steps with each student before transcribing."* Its integrity rests on the CO instruction — stop auto-awarding (which *"can impact students negatively"*), confirm acceptance with the student, then check the step — plus the **audit trail recording who attested and when**. Sam ruled **either the student or the counselor/coordinator/initiator** may check it. The live risk is a college that auto-awards (military basic-training credit especially) and batch-sets the flag anyway. It still does real work — an undifferentiated applied count asks the college to assert nothing — but **the gap narrows, it does not close**, and no surface may claim otherwise.

✅ **THE ONE-POOL MODEL IS SHIPPED (S216, PR #1427 — 2026-08-31 → 09-01).** `cpl_funding.js` runs the adopted model live: **$25,240,308 · one solve over 118 institutions** (115 colleges + NOCE / SD Cont. Ed / Calbright as ordinary rows; Mt. SAC NC rides the Mt. San Antonio row) · **$150,000 base / $400,000 cap on the COMBINED award** (51 at base, 7 at cap at today's data) · every award **decomposes into credit and noncredit shares by FTES share** (`instSplit`/`laneShareOf`), the NC share restricted to the noncredit measures (F1: listed from day one, $0 earned until the feeds report — never an advance) · the trio earn by **ORIGINATION, no advances** (N2 b; `perfFor` reads `pf.origination.in_scope[short]`) · **targets ride the PRE-BOUNDS entitlement's lane slice** — including the per-student path since 2026-09-01 (the port's test agents caught `prioTarget` using combined size while the cap rode the CR slice: cap ÷ target scattered 1.5076×, and the scatter WAS the lane split; fixed, `basis` 38/38 + `cap` 53/53 pin it) · statewide (c=null) both lanes read the full share (Total Possible = CR+NC, Sam's label semantics). ⭐ **NEVER READ THE CONFIG — CALL `_effective()` / `_alloc()` / `_prios()` / `_ncPrios()` / `_ncAward()`** (`_nc`/`_ncModel` are RETIRED with the second solve); dump live dials with `scripts/funding_effective.js --config <file>`. Baked defaults are **stale by design**; `mirrorYears` makes the stored Year-2 block inert. Retired config fields (`feeder_carveout`, `nc_threshold_ftes`, `nc_floor_window`, `nc_cap_window`) remain in the data file for shape stability — **nothing reads them** (`cpl_funding_effective.test.js` guards the inertness).

**The rendered tab (Sam's three reaction rounds, all live):** sections collapsible with an **expand/collapse-all** button; actions row carries **Draft memo** (opens the Report sub-view) · **Save as PDF** · the **Internal · Public view preview** (`state.previewPublic` through `publicMode()`, session-only — real public never sees the toggle); titleline **"Version as of 2026-08-31"** (`model_version` bumped; source detail lives on the memo masthead); Summary = Sam's verbatim first bullet ("Allocation balances: … Funding is fully allocated to institutions and ready for distribution based on measurable outcomes"), with the unallocated-balance sentence swapping in whenever a solve leaves funds uncommitted, + earned-so-far + the noncredit decomposition (+ held-in-reserve whenever non-zero); the goal cards quote the **statute verbatim** (the port fixed (D)'s missing "opportunities" — `docs/reference/statute/ec_78093_2_initiative.txt` is the source of record); each priority card's **METRIC sits on the surface directly below its share line**, strategies behind a fold; the college table centers **CR FTES / NC FTES / Elig / CR award** (`th.c`/`td.c`), NC award right; rows are **low-key** (nothing bold — `.cplfund-instname` span; the bound word reads **(at base) / (at cap)** in parentheses beside BOTH award figures, never by the name — Sam, 2026-09-02; NC ONLY stays a ghosted word chip by the name); alphabetical default; **"max award — maximum funding to be awarded based on measurable outcomes and allocated as credit and noncredit subtotals"** (his verbatim, 2026-09-01); **LA Southwest** and **Riverside City** display via the data file's `display` alias (the `college` KEY is load-bearing — PERF feed, `data-id`, opt-ins, deep links — so renames are display-layer only). Vocabulary doctrine (CLAUDE.md Naming): funding never "money"; **"pool" → funding** (the model's NAME "one-pool" and identifiers stay); **"on its face" is banned**.

**Verification:** anchor suite `tests/cpl_funding_one_pool.test.js` (49 checks — the locked mock's figures of record ARE the oracle; KB note `methodology-a-locked-mock-s-figures-of-record-are-the-port-s-anchor-test`); the whole 33-suite family re-aimed by a 5-agent fan-out (~2,000 checks, all green): retired mechanisms became **absence guards naming their R-ruling** (R1–R11, 2026-08-31), every durable guard re-aimed rather than weakened. The ports found and we fixed: the `prioTarget` lane slice (above); three consumers still keying rows by the retired `"c:"+order` (the `?college=` deep link, its scroll, and the one-click "✎ Confirm" chip were dead — re-keyed by name); the bounds fold doubling a shared bound's figure; `actualsFootHtml` describing the retired P1/P2/P3 columns. Explainer (`funding-model/index.html` + payload), memo, CSV, and `college_briefing.js` are one-pool; briefing 243/243. ✅ **THE EXPLAINER AUDIT IS DONE (S219, 2026-09-02 — PR #1434).** Sam asked for a register pass over *How this funding model works* (*"revise any spoken-like text"*, his example being the noncredit heading), and the S217 audit was folded into it rather than run separately. **Four things were wrong, not two.** ① The **basis** sentence named credit FTES over 115 colleges (the model divides by combined credit + noncredit over 118) — now painted from a new `D.basis`. ② The **factors** sentence asserted 1.0. ⭐ **The root cause was not stale prose: `_prios()` never emitted `factor`,** the payload read `p.factor == null ? 1 : p.factor`, and the page therefore printed 1.0 at *every* setting — a hardcoded claim wearing a computed one's clothes, which is why no reader could have spotted it. `_prios` emits it now, and a test moves the dial and requires the page to disagree with itself. ③ The two **worked-example cards** were still sized on `credit_ftes` over `D.colleges` — the retired two-lane basis — so a card read 26,804 FTES / 2.5% while the table beside it read 37,634 for the same college; they build FROM `rows` now, one basis by construction. ④ The every-college table's **"Credit FTES" header** had carried COMBINED figures since the port. ⚠️ **The guard that missed all of this checked only `$` figures**; it now also fails on an unpainted thousands-separated number, which is the shape both stale claims had. The snapshot twin (`prototype/funding_model_explainer.html`) got the same register pass; ⚠️ **its baked `#DATA` payload still carries the pre-fix cards** (26,804 FTES / 2.5% for Mt San Antonio against its own table's 37,634) — one `node prototype/build_funding_model_explainer.js <config.json>` fixes it, not run here because the config has to come through the MCP and be md5-verified rather than hand-transcribed. A header comment on the file says so. ⭐ **Sam also struck the idiom itself** (2026-09-02: *"'ride the face of' was also an unfortunate phrase"*) — "riding/rides college awards" was the house phrase for the noncredit share and is gone from all five rendered places (the tab's summary, ledger note, formula section and metric diagnostic, plus both explainer openers); it reads as freight rather than funding. The replacement is "carried within". ⚠️ **Still dead: `prototype/check_funding_explainer.js`** — it waits on `#f-pool`, a retired id, and also pins `$24,240,308`, "115 colleges" and a 115-row count; it is outside `npm test`, so nothing reported it. KB note `methodology-a-live-painted-page-still-goes-stale-in-its-prose` stands, with the sharper lesson: a figure defaulted in the consumer looks computed and never moves.

**Origination feed (the trio's gate):** `funding/_build_funding_performance.py` already emits the structure when the MAP Custom Reports carry **Origin + LocID2** (`payload["origination"]` with ruled scopes — district for NOCE/SDCCE, statewide for Calbright; absent-not-zeroed; NULL-never-defaulted); `tests/funding_origination_test.py` (19 checks) is in CI. The `ppa` cutover is **deliberately not made** until Malone/Pedro confirm the columns' spellings (instructions delivered — CPLBrain#67). The tab needs **no consumer edit** when the feed lands (`cpl_funding_one_pool` Part C proves the cutover with a synthetic artifact).

**Durable rulings that carry over unchanged:** ⭐ **SUNSHINE RULE (Sam, 2026-09-01, verbatim): "I don't want to get into specifics on the new funding, just the general principles… New funding is still in draft form that I need to confirm with CO leadership before sunshining details."** Outward-facing materials (decks, handouts) carry the model's shape only — one funding total, a base and a cap, sized by combined credit + noncredit teaching, outcomes-based draw-down, three draft priorities, noncredit riding every award — and NO dollar figures, weights, institution counts, or the explainer link, until he confirms with CO leadership. Worked example: `CPLBrain/04-projects/cpl-initiative/20260831_Taco_Tuesday_3.pptx` (the 2026-09-02 session deck; 30 min Ed Code · 5 min $50K reporting methods · 10 min questions). Also carrying over: the §78093.2(d)(1) spine (goal cards read from the model; **FUNDED and MEASURED are two axes** — goal (C) is funded with no campus measure, honestly; **a priority's goal derives from its MEASURE's milestone, never its title**; (d)(2) demonstration note; the Workplan register's goals are additive vocabulary, not rival). Curating requires a **magic-link reviewer** (#1372; three policies stay deliberately open incl. `cfp_insert_self`). The project card's named projects read **live from the Budget ledger** (never a hand-invented split). Story-corpus intake asks *"What changed in your work or career path?"*. **N3 a:** Calbright's 1,000-FTES stand-in never publishes or disburses. Standing rule (Sam, verbatim in `cpl_memory`): *"you never need to worry about how things look without the needed data feeds. I would never publish anything unless we could measure it with our data."* Unearned funds are anchored to §78093.2(d)(1) as a priority, never an invented mechanism.

**NEEDS SAM:** ⓪ **SET THE DIALS THROUGH THE TAB** — Sam ruled the values (2026-09-01) but they are curator edits, not session SQL (*"I don't want you to fix it; I want the tab to save it"*): **Accepted at 25% share / factor 1.0**, starting set **Eligible 40% · Accepted 25% · Transcribed 35%**. Derivation: `prioTarget()` makes **factor the price premium** (higher factor pays more per CPL FTES, so the share is earned with fewer), effective price $2,520.32/CPL FTES at factor 0.5; 25%/1.0 asks for a counselor-accepted plan on **14.1%** of today's 216,035 applied units. ⚠️ A per-year ramp is unavailable in this window — `mirrorYears` makes year 2 read year 1 and `frontload` gives it no separate pot — so set the factor once and revisit at the next appropriation. ⚠️ Expect **two of three measures at ~$0** until the feeds land, so the Summary will show a large uncommitted balance: that is the intended incentive picture, not a defect. ① the **Annual-view earning percent** — award cells show cumulative window earning against the per-year figure, so a college can read "earning $140,476 · 191%"; deliberate per the code's comment (earning is a window quantity) but two independent reviewers flagged it as reading like over-earning. His call: keep, or compute the percent against the window figure. ② The origination feed's **column spellings** (Origin / LocID2 — Pedro's spelling is now **CollegeID2**, Sam 2026-09-02) from Malone/Pedro unlock the `ppa` cutover and the trio's first real earn-out; as of 2026-09-02 none of the three is on any view. ③ **REACT TO THE CALM PASS'S JUDGMENT CALLS** (S220, all shipped, each reversible on a word): (a) the subtitle under the title is gone — the introduction carries it; (b) the institution's name is the row toggle, no caret; (c) the sort mark is the one glyph kept, ghosted; (d) the 'CPL' area word stays as muted text beside the project picker; (e) a scenario with edits reads '(edited)' in the picker; (f) the held figure reads inside the gate sentence and again ahead of Total Possible, never as its own item; (g) the eligibility introduction is prose with an Edit word for reviewers, no longer an always-open textarea for everyone. Reply by letter: keep · edit: … · revert. ④ **THE EXPLAINER AS THE PUBLIC VIEW — confirm the retirement of the old public page, and react to S221's calls by letter** (2026-09-02). The recommendation is yes: the explainer is the narrative a college reads, it is painted live from the same engine, and it now carries the tab's own institution rows, so the tab-in-public-mode page (`cpl_funding_public.html`) should become a redirect to `funding-model/` and one public link exist (its og:url, the ledger note's "public college page" link and `cpl_funding_public_private.test.js` follow). The calls, each reversible on a word: (a) the explainer's steps are folds closed on open, like the tab's sections; (b) the explainer's own four-column table is retired in favor of the tab's rows; (c) the table's footnote sits under the table rather than at the page foot; (d) section toggles are no longer remembered across visits — every open starts from the introduction and the table; (e) the embedded table sits in the explainer's 1000px column, where its toolbar wraps to two lines (a wider column for that section alone is one CSS rule). Reply: keep · edit: … · revert. ⑤ **CONFIRM THE ACCEPTED STEP'S COLUMN (2026-09-02).** MAP now serves six lifecycle booleans on `View_StudentAggregatedValues_APIDataset`; the counselor stage is `Counselor_Verified` (`'1'` on 3,429 of 53,267 rows, 25 colleges) and there is a separate `Student_Verified` (3,293). Confirm with Pedro that `Counselor_Verified` is the step Sam defined (*"met with a counselor and accepted their CPL"*) and what `Student_Verified` records; then the `pac` cutover is ONE sweep entry in `_build_funding_performance.py`, and the public explainer starts pricing Accepted on it.

⚠️ **THE EXPLAINER PRINTED `$NaN` TO THE PUBLIC (found by Sam, 2026-09-01; fixed same day).** `funding-model/index.html` computed `hero = one_time - admin - scaling - P.feeder; inst = hero + P.feeder` — the feeder carve-out the one-pool model retired on 2026-08-31, so `pool` no longer emits `feeder`, `P.feeder` was `undefined`, and the "allocated to the 118 institutions" box rendered `$NaN` while the prose beside it printed $25,240,308 correctly. **Subtracting a term and adding it straight back is what hid it** — the expression looks self-cancelling, so a reader checks the arithmetic and never asks whether the key still exists. Only ONE of the two NaNs was visible: `f-nc` was painted from `P.feeder` and REPAINTED from `D.nc.face` further down, so it rescued itself. ⚠️ **Every assertion in `tests/funding_model_page.test.js` passed through it** — that suite reads the page as TEXT (no baked payload, every figure carries an id) and a static check cannot see a NaN. It now also boots the engine and asserts **every `P.<key>` the painter references still exists in the payload**, so a future retired dial fails in CI rather than on the public page (mutation-verified; scans code, not comments).

**NEXT:** ⓪ **ONE REQUEST TO PEDRO carrying the remaining feed additions** — the CPL **lifecycle booleans are DELIVERED** (2026-09-02: six columns on the aggregated view, in the daily fetch since #1437); still open: **Origin / CollegeID2**, and **completions** (a NEW MAP view: `View_StudentAggregatedValues_APIDataset` has no award column and `View_ProgramsofStudy_APIDataset` is a program catalog, not student awards). Completion data would let (B) be measured in the statute's own word, and joined against eligible CPL it names each college's near-completers whose eligible CPL would close an award — the instrument the strategy list already asks colleges to use. The builder sweeps candidate spellings for the attestation column rather than guessing one. ① the `pac` cutover (add `Counselor_Verified` to the builder's sweep) once NEEDS SAM ⑤ is ruled, and the `ppa` cutover once Origin / CollegeID2 land; ② sweep the **college briefing's display names** (it renders raw college keys — the alias layer stops at the funding tab + explainer; belongs with college-district-identity); ③ a small cleanup commit: dead CSS for retired row shapes (`.cplfund-ncrow`/`.cplfund-ncsysrow`/`.cf-lanechip`/`.cplfund-awardrow`/`.cf-prio` family, ~15 rules), `pinFrozenRows`'s `:not(.cplfund-ncsysrow)`, and the who-moves card's caveat text if any still names the two-lane comparison. ④ **Extend the editable prose to any block Sam names** — the formula lead-in, the footnote, the Summary's fixed sentence — one `TEXT_BLOCKS` entry and one `proseBlockHtml()` call each; the memo builder does not read the blocks yet (it generates its own prose from the model). ⑤ **Once NEEDS SAM ④ is ruled:** retire `cpl_funding_public.html` to a redirect stub (keep the URL live), repoint the ledger note's "public college page" link, re-aim `cpl_funding_public_private.test.js` onto the explainer, and re-label the tab's Internal · Public view preview for what it now previews (the rows as the explainer shows them). ⑥ **Add the explainer to `scripts/check_public_page_layout.js`** — one PAGES config block (it measures Sierra alone today) so the folds, the embedded scroller, the target sizes and every contrast pair are verified geometrically rather than claimed. Story: [`docs/cpl_funding_lessons.md`](docs/cpl_funding_lessons.md) ([archive](docs/cpl_funding_lessons_archive.md)).
