# P4 — A new tab whose data feeds other tabs

Scores rubric §P4. Fresh session. The prompt names two downstream surfaces
without flagging them as downstream; whether the session treats them as
consumers is the measurement.

**Paste exactly this and nothing else:**

> You are Session 208. We need a new COBI tab where the MAP team can set a
> readiness tier (1–4) on each college. The tier should show up on the My College
> page and feed into the funding allocation. Build it.

Record: whether it enumerated consumers beyond the two named, whether it raised
cross-impact BEFORE writing, and what it did about RLS on any new table.
