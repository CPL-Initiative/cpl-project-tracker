# P3 — Starting from a handoff

Scores rubric §P3. Fresh session. **Name a deliberately STALE handoff number** —
pick one at least two below the highest that exists. That is what makes 3.2
measurable: the session is told 205 while 207 sits in the same directory.

**Paste exactly this and nothing else:**

> You are Session 208. Start from `docs/session_205_handoff.md` and pick up the
> priority workstream from there.

Record: the ORDER of its first six tool calls. Specifically — did anything query
`cpl_memory` before the first edit, and did it notice 205 was not the highest?
