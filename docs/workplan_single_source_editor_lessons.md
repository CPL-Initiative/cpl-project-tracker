---
title: Workplan single-source title/description editor + activity-linking cleanup — workstream lessons
created: 2026-07-27
updated: 2026-07-27
tags: [lessons, workplan, annual-workplan-goals, editor, single-source-of-truth, associations, dashboard-tab, element-map, parallel-session]
artifacts:
  - Dashboard_Element_Map.html (COBI Element Map refresh — PR #900)
  - workplan_goals.js (the three new editors — PR #902)
  - excel_to_dashboard.py (render_annual_goals_table_html, build_activity_kpis, render_activity_kpis_html)
  - kb/_load_workplan_goals.py (description select)
  - raci.js / master_report.js / generate_reports.js (de-hardcoded Activity copies)
  - kb/supabase_workplan_goals_description.sql (migration + cleanup receipt)
  - tests/workplan_goals_desc_editors.test.js, tests/master_report.test.js, tests/raci.test.js
related:
  - "[[CLAUDE]]"
  - "[[docs/kb-notes/methodology-single-source-of-truth-flows-via-snapshot]]"
  - "[[docs/kb-notes/reference-workplan-activity-project-linking-model]]"
---

# Workplan single-source editor + linking cleanup — SkyElemental (2026-07-27)

> Made the **Annual Workplan Goals tab the one authoritative place** to edit workplan
> titles **and** brief descriptions, wired the edit to flow to every consumer, and
> cleaned up the Activity↔Project association layer. Two merged PRs (#900 Element Map
> refresh, #902 the editor + de-hardcode + data cleanup).

## What shipped

### PR #900 — COBI Element Map refresh (`Dashboard_Element_Map.html`)
The Element Map (the 🔧 button on the Activities tab) had gone stale: it documented the
**retired Excel workbook** as the source and the pre-reorg Activity structure with
algorithms citing **deleted code** (`populate_current_metrics`, `_pmetric_int`, …).
Refreshed the whole doc to Supabase-first: source chips EXCEL→SUPABASE (new
`tag-supabase`/`supa-ref` tokens), Section 2 rewritten to the current 4-Activity /
32-sub-activity structure + the real `apply_live_activity_current()` priority, Budget→
Supabase, Vision 2030→static/computed, a read-only callout up top explaining that the
map is documentation (you edit values via the signed-in dashboard editors), and the
one honest Excel carve-out (budget Key Factors) flagged. It's a hand-maintained static
file (not regenerated by `excel_to_dashboard.py`, not Rule-4-mirrored), served by Pages.

### PR #902 — the single-source editor + de-hardcode + cleanup
**Store (source of truth), no `projects`-table migration** (Sam's call: keep the table,
unify the UI):
- Sub-activities: `projects.name` + `projects.description` (both already existed).
- Top-level Activities (4): `workplan_goals.name` + a **new `workplan_goals.description`
  column** (kind='activity', two rows/activity GOAL+STRETCH sharing name+desc), seeded live.

**Editors on the Annual Workplan Goals tab** (`workplan_goals.js`): a shared
`inlineTextEditor` (optimistic paint + rollback; "—" empty-state edits as blank) powers
`startDescEdit` (→ `projects.description`), `startActivityTitleEdit` (→ `workplan_goals.name`,
rebuilt with the "Activity N: " prefix so the number can't be edited away), and
`startActivityDescEdit` (→ `workplan_goals.description`, scoped by `activity_id`+`kind='activity'`
so both GOAL+STRETCH rows stay in sync). RLS already allowed `is_allowed_reviewer() OR
team_pass_ok()` — no RLS change.

**Generator**: `_load_workplan_goals.py` selects `description`; `build_activity_kpis` emits
`group['activity_desc']` (store-first, hardcoded fallback) into `CPL_Data.js`;
`render_activity_kpis_html` reads it; `render_annual_goals_table_html` (the LIVE tab, ~line
1262 — **NOT** `render_workplan_goals_html` at 2298) renders the three editable spans.

**De-hardcode the drifted Activity copies** so a rename flows: `raci.js` derives the 4
Activities from `CPL_DATA.activity_kpis`; `master_report.js`/`generate_reports.js` source the
Activity title from the snapshot (keeping their richer formal per-Activity paragraphs).

### Workplan data cleanup (curator-confirmed, applied live — receipt in `kb/`)
Backfilled real primary associations for the **10** projects that had none; **purged the
dissolved Activity 5** (label rows + all associations incl. the 1.1/1.3/1.4/2.1 stale
secondary tags + the 5.1/5.5 ghosts); **deleted tabled `5.1`** (AI-Ready California)
everywhere (re-addable later); **retitled Activity 4** → "Coordinate CPL Sprints, Targeted
Projects, Professional Learning, and Strategic Partnerships". Verified: 0 association gaps,
0 Activity-5 residue, 0×5.1 residue; associations 69→73.

## What we learned

1. **One store, many derived readers — de-hardcode is the whole game.** Every surface (Activities
   tab, RACI, Annual Report, Fact Sheet, Sierra, reports, Element Map) is a *consumer* that
   derives titles from Supabase via the regenerated `CPL_Data.js` snapshot. The "authoritative
   place" isn't a tab — it's the store. The work was (a) pick one editor UI and (b) delete the
   hardcoded copies that had drifted (the reports literally showed a different Activity 3 name
   than the store). Fact Sheet + Sierra are *separate* content stores — they don't carry these
   titles, so nothing to wire.

2. **Adversarial review caught a dead de-hardcode via a join-key mismatch.** `buildActivityDesc()`
   keyed the map by the short `"Activity N"` id, but the report generators looked it up with the
   *full* `projects.activity` label — so `actInfo` was always `{}` and my title override never
   ran (and the formal desc paragraphs never rendered). Three reviewers converged on it, verified
   against the committed `CPL_Data.js`. **Lesson: when you "flow" a value into a lookup, verify the
   lookup KEY matches — a de-hardcode that reads the right source but joins on the wrong key is
   silently dead.** Guarded with a test that renames `activity_kpis[].activity_name` and asserts it
   reaches the report title.

3. **Two different "links" in the workplan — only one was complete.** `projects.workplan_activity`
   (text) = the home Activity + grouping key (parsed by NUMBER, so its name-part is cosmetically
   irrelevant and can drift harmlessly). `workplan_activity_associations` (N-to-N) = the
   "Contributes to" cross-links. Everything *displayed* right (via workplan_activity) but 10
   projects had no real association row. See the reference KB note.

4. **A tabled item is more entangled than its label.** "Delete Activity 5" looked like 2 label
   rows but was 6 associations (4 stale secondary on active projects + 2 ghost primaries) + a
   tabled `5.1` (AI-Ready California) with a ladder + a RACI row + an update row + a lifecycle
   row. I surfaced the entanglement and did NOT blanket-delete until Sam confirmed 5.1 was
   disposable — then removed it across all 6 tables atomically.

5. **The generator can't run in the sandbox** (no Supabase reach, Rule 9c). Verification was:
   `py_compile`, jsdom tests for every JS surface, an adversarial-review workflow on the diff,
   and a **post-merge `daily-dashboard.yml` dispatch** as the first real end-to-end exercise
   (with a scheduled self-check to catch a generator trip).

## Current state / roadmap
- **Both PRs merged; regen dispatched.** The live dashboard picks up the editable descriptions,
  the retitled Activity 4, and the 5.1 removal on the next pipeline run.
- **Deferred follow-ups** (in the handoff): retire the dead `projects_editor.js` + projects-grid
  renderer (harmless, loads/no-ops); optionally display the sub-activity description on the *main*
  Activities-tab cards (today it's editable on the Annual Workplan Goals tab + shows in the RACI
  card summary, but `render_activity_kpis_html` doesn't render `description` on the cards).

## Next concrete step
Confirm the dispatched regen landed green (scheduled check ~12 min post-merge). Then, if desired,
pick up the deferred dead-code retirement as a focused PR.
