---
title: Session 60 Hand-off Prompt
date: 2026-06-16
session: 59 → 60 hand-off (written at the Session-59 checkpoint — Bruh Star Navicus)
status: hand-off — paste the fenced block into the next session's first message
tags: [handoff, session-prompt, tmc, adt, tmc-builder, design-lane]
related:
  - docs/tmc_builder_lessons.md (the full TMC Builder story + next steps)
  - docs/kb-notes/reference-tmc-adt-data-model.md (the data model)
  - docs/session_59_handoff.md (the DATA lane — Jaccard + Suggested-merges, paused)
moniker_suggestion: Session 59 was "Bruh Star Navicus"; claim your own
---

# Session 60 Hand-off Prompt

Session 59 (Bruh Star Navicus) took a design-lane detour: shipped a NEW **TMC
Builder** tab — an interactive Transfer Model Curriculum (ADT) submission helper.
The data lane (Suggested-merges + the member-join Jaccard) is paused but fully
intact in `docs/session_59_handoff.md`. Paste the block below.

```
You are Session 60 on the CPL Project Tracker. Two open lanes — pick with Sam:
  • DESIGN lane: continue the new TMC Builder tab (this handoff).
  • DATA lane: resume Suggested-merges / the member-join Jaccard (read
    docs/session_59_handoff.md). Coordinate via kb/cpl_todos.json.

Read first, in order:
  1. CLAUDE.md — all of it. Rules 1/4/5/7/8; merge-on-green = clean OR unstable;
     §7d (the TMC Builder tab); §11 terminology landmine (MC ≠ TMC — the TMC
     Builder is the REAL transfer process, a separate thing from the M-ID/MC lane).
  2. docs/tmc_builder_lessons.md — the full TMC Builder story + next steps.
  3. docs/kb-notes/reference-tmc-adt-data-model.md — the data model + auto-match.

WHAT SHIPPED IN SESSION 59 (TMC Builder — one PR, draft):
  A new top-level tab #tmc-builder ("TMC Builder"). Pick a College + a Transfer
  Model Curriculum at the top → LEFT column = the fixed ASCCC-defined C-ID course
  list (Required Core / List A/B/C); RIGHT column = a searchable picker of THAT
  college's own COCI courses, AUTO-FILLING the local course that already carries
  each slot's C-ID. Shows Total Units (header meter + per-section subtotal +
  grand-total footer). Saves/Resumes to Supabase public.tmc_submissions (anon,
  no PII) and exports .docx (via docx.min.js) / print / JSON.
  Files (all STATIC, all lazy, NOT cron artifacts, NOT regenerated by the .py):
   - tmc_builder.js — renderer/interaction (injects own CSS).
   - tmc_templates.js — 8 DRAFT TMCs (Psychology, Sociology, Anthropology,
     Communication Studies 2.0, History, Business Admin 2.0, Math, Biology). Slot
     C-IDs are REAL (from kb/reference/cid_descriptors.json); section/unit/select
     details are seed — FACULTY MUST VERIFY. Add the rest by appending templates[].
   - tmc_college_courses.js — per-college COCI index (120 colleges, 141,699
     courses, 7.5 MB). Built one-shot: `python3 tmc/_build_college_courses.py`
     from kb/reference/coci_course_list.xlsx. Rebuild only on a fresh extract.
   - tmc/supabase_tmc_submissions.sql — schema of record (anon RLS, applied live).
   - tests/tmc_builder.test.js — jsdom (auto-match, Total Units, Rule 4, null-safety).
  Nav button + pane + boot wiring mirrored in BOTH HTMLs (Rule 4); `cp
  CPL_Dashboard.html index.html` keeps them in sync.

KEY DESIGN FACTS:
  - Auto-match is C-ID-EXACT ONLY (COCI CIDNumber == descriptor key, 12,738/12,792
    exact). Never title-guess an auto-fill — title/subject similarity only RANKS
    the manual picker, the human confirms.
  - ~1/4 of colleges (incl. Riverside 25, Norco 0) report few C-IDs in the COCI
    extract → sparse auto-match; they pick from the dropdown. A *reporting* gap,
    not the tool. (American River 266, Glendale 249, Moreno Valley 140 are rich.)
  - No contact hours in COCI → legitimacy check = units-in-range + C-ID for now.

YOUR PRIORITY QUEUE (design lane):
  1. ADD THE REMAINING ~37 TMCs once Sam supplies the official templates (the C-ID
     TMC site 403s automated fetch). Same shape; append to tmc_templates.js.
  2. SPARSE-COVERAGE UX: a "N of your courses carry a C-ID in COCI" hint + an
     OPTIONAL title-similarity "suggested (verify)" secondary fill, visually
     DISTINCT from C-ID auto-fill (never silently). Measure false-fill risk first.
  3. Capture contact-name/notes + a "mark submitted" status (columns already exist).
  4. Optional statewide adoption view off tmc_submissions (no PII — trivial vs EACR).
  5. Faculty-verify the 8 seed TMCs' groupings/units against the official template.

PATTERNS THAT WORKED:
  - The repo already had both data sources (cid_descriptors.json + coci_course_list
    .xlsx); the join is an exact string compare because COCI's CIDNumber IS the
    descriptor key. Always check what's already in kb/reference before inventing.
  - Sam drives interactively — he sent 5 mid-build clarifications; fold each in
    immediately, keep the build moving, address every one.
  - Commit the test (jsdom, guard the failure mode); validate Supabase RLS by
    simulating `set local role anon` in a ROLLED-BACK execute_sql tx (sandbox
    blocks egress to supabase.co, so curl from Bash won't reach it).

SAFETY:
  - merge-on-green = clean OR unstable. Watch the PR; drive it to merged. Never
    force-push main (Rule 5). Supabase: tmc_submissions is additive + anon-RLS;
    never touch auth/Redirect-URL or projects/budget/personnel.
  - tmc_college_courses.js (7.5 MB) is committed (static, like cpl_funding_data.js)
    — NOT in the daily git-add list; the cron won't rebuild it.
  - To-Do feed (kb/cpl_todos.json): bump as needed, DELETE done items, ≤12.
```

Good hunting — the TMC Builder is a clean, self-contained college-facing tool;
the biggest lever is real TMC data (Sam's templates) + the sparse-coverage UX. 🎓
